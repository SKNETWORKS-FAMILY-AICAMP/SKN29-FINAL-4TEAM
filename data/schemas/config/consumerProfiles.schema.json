{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ConsumerHandoffProfiles",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "config_version",
    "dataset_scope",
    "service_contracts_used",
    "profiles"
  ],
  "properties": {
    "config_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "dataset_scope": {
      "const": "consumer_handoff_only"
    },
    "service_contracts_used": {
      "const": false
    },
    "profiles": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rag", "db-smoke", "db-full", "qa"],
      "properties": {
        "rag": {"$ref": "#/$defs/profile"},
        "db-smoke": {"$ref": "#/$defs/profile"},
        "db-full": {"$ref": "#/$defs/profile"},
        "qa": {"$ref": "#/$defs/profile"}
      }
    }
  },
  "$defs": {
    "profile": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "purpose",
        "readiness",
        "contract_dependency",
        "items",
        "selection",
        "excluded",
        "commands"
      ],
      "properties": {
        "purpose": {"type": "string", "minLength": 1},
        "readiness": {"type": "string", "minLength": 1},
        "contract_dependency": {
          "enum": ["NONE", "SERVICE_MAPPING_PENDING"]
        },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["path", "role"],
            "properties": {
              "path": {"type": "string", "minLength": 1},
              "role": {"type": "string", "minLength": 1}
            }
          }
        },
        "selection": {"type": "object"},
        "excluded": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        },
        "commands": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
