# 이동윤 8월 13~14일 E2E 집중 작업 진행도

> 작성 일시: **2026-08-14 13:53 KST**
> 기준 Commit: `a29ec0f966bdefb309ee925503b0302f9e0402e8`
> 기준 문서: [팀원별 관할 영역 v2](../planning/md/팀원별%20관할%20영역%20v2.md), [이동윤 E2E 집중 업무 지침서](../weekly-task/이동윤_5주차_8월13-14일_E2E_집중_업무_지침서.md)
> 종합 판정: **진행 중 — Canonical 7×1024 Fixture Exporter는 병합됐지만 실제 LLM·팀 pgvector·Backend Import 수직 흐름은 여전히 HOLD**

## 1. 절별 완료도

| 지침서 절 | 완료도 | 현재 판정 | 남은 핵심 항목 |
|---|---:|---|---|
| 2 E2E용 AI Baseline 고정 | 97% | Canonical Exporter 병합 | 실제 Fixture 생성·Hash·Importer 합의 |
| 3.1 생성형 LLM 실제 연결 | 75% | 안전한 Provider 코드 완료 | 실제 OpenAI 호출·Token·Latency·응답 증거 |
| 3.2 RAG Baseline 실제 실행 | 78% | Exporter·Git LF 계약 완료 | Fixture 생성·Import·팀 pgvector 재실행 |
| 3.3 Backend↔AI 수직 연결 | 70% | 위험 경로 실제 PASS | LLM·RAG 일반 경로의 최신 HTTP·DB·Correlation |
| 4.1 정상 서비스 E2E | 65% | 위험 문의 Workflow 완주 | 최신 SHA·일반 RAG 경로 완주 |
| 4.2 최소 Fallback | 85% | 위험 경로 실증·단위 PASS | 실제 Provider/Vector 장애 공동 HTTP |
| 5 실험 업무 중단 규칙 | — | 범위 준수 | E2E 전 새 실험 운영 반영 금지 |
| 6 Blocker 처리 | 95% | 대부분 완료 | Timeout Event PM 결정과 구현 결과 회신 |

## 2. 절별 평가와 완료 방법

### 2.1 2 E2E용 AI Baseline 고정 — 95%

- [Runtime Identity](../../ai/configs/runtime_identity.json)에 Single RAG, `gpt-4.1-mini`, BAAI/bge-m3 1024차원, pgvector, 승인 청크 7건 기준이 기록됐다.
- 새로운 Multi-Agent 전환 없이 현재 Baseline을 우선한다.
- [Backend 통합 환경 설정](../../ai/configs/backend_integration_environment.json)에 필수 환경변수명, 실제 Local Smoke·Runtime Verifier 명령, Backend View `backend_ai_rag_chunks_v1` 경계가 고정됐다.
- 최신 `626a7a4`에서 [Canonical Embedding Fixture Exporter](../../ai/scripts/export_canonical_embedding_fixture.py)와 계약 Test가 병합됐다. 승인 7청크를 `chunk_id` 순으로 BGE-M3 1024차원 `FLOAT32` Fixture로 생성하고 `.runtime/backend-ai/` 밖 출력을 거부한다.
- PM 확인으로 소스 SHA는 전원 일치하지만 실제 공용 프로세스가 이 설정으로 기동됐는지는 확인되지 않았다.

완료하려면 기준 SHA와 모델·Prompt·Embedding Revision·Chunk Set Hash·Top-K·Threshold를 실제 실행 로그와 AIRun에 동일하게 기록한다.

### 2.2 3.1 생성형 LLM 실제 연결 — 75%

- [OpenAI LLM Client](../../ai/app/integrations/llm/llm_client.py)가 Responses API, `gpt-4.1-mini`, Strict Structured Output, Timeout·일시 오류·Token Usage를 구현한다.
- Guidance Generator는 일반·주의와 공식 Evidence가 있을 때만 LLM을 호출하고 위험·No-Evidence는 결정론적 경계를 유지한다.
- Prompt v2, `store=false`, 승인된 OpenAI Base URL, PII 최소화, Evidence 문장·행동 Allowlist·상태 의미 검증이 추가됐다.
- Fake Provider 기반 작성자 검증은 AI Unit **219 passed / 7 subtests passed**이고 Uvicorn Mock 실호출도 HTTP 200·`SUCCEEDED`·Correlation Echo를 확인했다.
- `OPENAI_API_KEY`와 팀 DB 설정이 없어 실제 Provider 호출은 `NOT_RUN`이다.

완료하려면 승인된 Secret 환경에서 정상 1건을 호출하고 모델명·Prompt Version·Token·Latency·Retry 0/1·Schema PASS를 PII 없이 기록한다.

별도 확인: OpenAI 사용 승인, 비용 한도, 고객 입력 외부 전송 범위가 필요하다.

### 2.3 3.2 현재 RAG Baseline 실제 실행 — 75%

- 과거 격리 PostgreSQL/pgvector에서 승인 7청크·1024차원·Exact Search 12/12 PASS와 실제 Local HTTP PASS 증거가 있다.
- AI가 Backend 읽기 전용 View를 선택하고 Read-only Transaction·Schema CREATE 금지·원본 Table 접근 금지를 Fail-closed로 확인하도록 구현됐다.
- 실제 Embedding Fixture 생성 코드는 병합됐으나 현재 검증 환경에는 `numpy`가 없어 신규 Exporter Test가 Collection 단계에서 중단됐다. 이는 코드 실패 판정은 아니지만 최신 독립 PASS 증거도 아니다.
- 현재 공용 팀 DB에는 공식 Evidence 적재·Crosswalk Apply·최소 권한 DSN 완료 증거가 없다. Backend Audit 로컬 실측도 Crosswalk·View 행 `0/7`로 `BLOCKED`다.
- `ccf30f2`의 `.gitattributes` LF 계약으로 Git 원본 Identity SHA는 Backend Import Package 기대값과 일치한다. 현재 PC의 기존 CRLF 작업 파일만 재정규화가 필요하다.
- 재정규화 전 로컬 표적 검증은 **46 passed / 19 failed**이므로 새 Checkout 또는 정규화 후 Import 회귀를 다시 실행해야 한다.

완료하려면 현재 SHA에서 팀 pgvector에 7청크를 멱등 적재하고 `WPUJAC104DWH` 출수량 저하 질의를 Top-K 5, Threshold 0.4로 실행해 검색 Evidence와 Hash를 저장한다.

별도 확인: 공식 Evidence 7건과 Backend Crosswalk를 김은진·최지용이 승인해야 한다.

### 2.4 3.3 Backend↔AI 수직 연결 — 70%

- `1cd1905`에서 Backend→실제 FastAPI AI 위험 판정→Guidance 저장→Mobile·Web 소비가 동일 Inquiry로 실행됐다.
- 위험 규칙 경로는 LLM과 pgvector를 호출하지 않으므로 생성형 LLM·일반 RAG 수직 PASS로 확대하지 않는다.
- 최신 `a29ec0f`에서는 상담사 상세의 검증된 AI Guidance·제한 기능 Projection이 구현됐지만, 고객 Guidance 조회와 전체 Cross-client 흐름은 재실행되지 않았다.

완료하려면 Backend 요청으로 Local AI를 호출해 AI Response→AIRun·Guidance·EvidenceLink→`AI_GUIDANCE` 상태 적용과 같은 `correlation_id`를 확인한다.

### 2.5 4.1 정상 시나리오 E2E — 65%

- 위험 문의 기준 Mobile→AI→상담→Web→Mobile 흐름은 `1cd1905`에서 완주됐다.
- 최신 SHA와 일반 RAG·LLM 경로는 아직 미완료다.

### 2.6 4.2 최소 Fallback — 85%

- 위험·No-Evidence는 LLM 호출 0회, Provider Timeout·Schema·거부·안전 문장 위반은 실패 폐쇄하도록 구현됐다.
- 408·504 Timeout과 409·429·5xx를 구분하고 LLM 자유 문장이 제한 행동이나 근거 없는 안전 주장을 만들지 못하도록 최종 Guard가 추가됐다.
- AI Timeout의 Backend 상담 자동 전환은 아직 `OPEN`, 실제 지연 주입 HTTP는 `NOT_RUN`이다.

완료하려면 정상 성공 후 No-Evidence·위험·LLM 실패 각각 한 건을 공용 Runtime에서 실행하고 안전 안내·상담 전환·중복 0을 검증한다.

### 2.7 5 실험 업무 중단 규칙 확인

- 오늘 핵심 변경은 E2E에 필요한 LLM Runtime 구현이며 새 검색 실험을 운영 Pipeline에 반영하지 않았다.
- 기존 D04·Alias 결과는 보존하되 E2E 완료 전 추가 비교나 기준선 교체를 하지 않는다.
- **범위 준수 알림: 없음.** 이 항목은 완료도 계산에 포함하지 않는다.

### 2.8 6 Blocker 처리 — 95%

- [Timeout 상담 전환 Gate 회신](../individual/dongyoon/인계/20260813_이동윤_to_최지용_Backend_AI_Timeout_상담전환_Gate_회신_v0.1.md)에 현재 Gap, 담당자, 재현 Test, 완료 조건이 구체적으로 기록됐다.
- PM이 Timeout 전용 SYSTEM Event 또는 명시적 매핑을 결정하고 최지용이 구현·실제 HTTP 검증해야 한다.

## 3. 코드로 확인할 수 없는 사항

- 실제 OpenAI 호출 성공과 비용·Token 사용량
- 팀 pgvector DSN·Extension·공식 7청크 적재 상태
- 최신 AI Exporter 의존성이 설치된 환경의 Test·Fixture SHA
- Canonical LF 원본 기반 Exporter→Importer 재실행과 팀 pgvector 실제 결과
- 공용 Backend에서 실제 AI Local Mode와 Prompt v2를 호출했는지
- Mobile·Web 사용자가 같은 AI 결과를 실제로 확인했는지

## 4. 최종 체크리스트 판정

- [ ] `gpt-4.1-mini` 실제 호출이 동작한다. — Adapter만 구현, 실제 호출 `NOT_RUN`.
- [x] `BAAI/bge-m3` 1024차원 기존 Embedding 설정과 구현을 유지한다.
- [ ] 승인 7청크·Cosine Exact Search·Top-K 5·Threshold 0.4 Baseline이 실제 팀 pgvector에서 동작한다.
- [ ] Backend → FastAPI → RAG → LLM → Backend 수직 흐름이 PASS한다.
- [ ] AI Guidance와 Public Evidence가 최신 Backend에 실제 결과로 저장된다. — `1cd1905` 위험 경로에서는 Guidance 저장·Evidence 0건을 확인했다.
- [ ] 같은 결과를 최신 고객 Mobile에서 확인할 수 있다. — `1cd1905`에서는 PASS.
- [ ] 상담 요청 이후 상담사 Web까지 최신 대표 E2E가 이어진다. — `1cd1905`에서는 PASS.
- [x] No-Evidence·위험·LLM 실패 최소 Fallback이 코드와 단위 Test에 구현돼 있다. — 실제 공동 HTTP는 남아 있다.
- [ ] `correlation_id`로 최신 실제 AI 실행을 추적할 수 있다. — 과거 P0 요청·Backend 로그 대조 증거는 있음.
- [x] 오늘 AI 작업은 비교 실험보다 E2E Runtime 구현을 우선했다.
