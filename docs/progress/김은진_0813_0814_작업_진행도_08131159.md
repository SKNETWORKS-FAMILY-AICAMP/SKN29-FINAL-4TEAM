# 김은진 8월 13~14일 E2E 집중 작업 진행도

> 작성 일시: **2026-08-14 13:53 KST**
> 기준 Commit: `a29ec0f966bdefb309ee925503b0302f9e0402e8`
> 기준 문서: [팀원별 관할 영역 v2](../planning/md/팀원별%20관할%20영역%20v2.md), [김은진 E2E 집중 업무 지침서](../weekly-task/김은진_5주차_8월13-14일_E2E_집중_업무_지침서.md)
> 종합 판정: **진행 중 — G1-B LF 계약과 Git 원본 Hash는 정합화됐지만 현재 PC 작업 트리 재정규화와 팀 DB·공동 Runtime Gate가 남아 HOLD**

## 1. 절별 완료도

| 지침서 절 | 완료도 | 현재 판정 | 남은 핵심 항목 |
|---|---:|---|---|
| 3.1 공용 통합 실행 환경 | 70% | Backend Check PASS·팀 환경 차단 | AI 의존성·팀 PostgreSQL 접속과 공동 Health |
| 3.2 Backend↔AI 실제 Baseline 검증 | 75% | Git LF·Hash 계약 정합 | 작업 트리 재정규화 후 Import·팀 DB·RAG·LLM 검증 |
| 3.3 8월 13일 종료 Gate | 80% | 실행·차단 증거 확보 | 최신 SHA 공동 결과와 Gate 문서 최신화 |
| 4.1 대표 P0 E2E | 70% | `1cd1905` 완주·최신 HOLD | 최신 SHA 동일 Inquiry 재실행 |
| 4.2 E2E 진행·최소 오류 | 60% | 주요 경계 실증 | 최신 PostgreSQL 409·동시성·AI 실패 재검증 |
| 4.3 전화 문의 최소 Smoke | 90% | 기존 증거 있음 | 현재 최종 SHA에서 최소 회귀 확인 |
| 5 Blocker 처리 | 95% | LF 원인·수정 Commit 확인 | 재정규화 표적 회귀·공동 Runtime 결과 추가 |
| 6 보류 업무 | — | 범위 준수·PM 승인 협업 | 허가된 Import Package·자동 Manifest 정합화 유지 |
| 8 PM 최종 결과 전달 | 60% | 실패 근거 포함 보고 가능 | 최종 PASS/HOLD·담당자·재검증 결과 전달 |

## 2. 절별 평가와 완료 방법

### 2.1 3.1 공용 통합 실행 환경 확정 — 65%

- [E2E 실행 계획](../testing/week5/20260813-eunjin-p0-e2e-execution-plan.md)에 격리 PostgreSQL 16.14, pgvector 승인 7청크, bge-m3 고정 Revision, Web 144 tests·Build PASS가 기록됐다.
- `1cd1905`에서는 Android·Backend·FastAPI AI·Web을 연결한 위험 문의 P0가 실제 완주됐다.
- 최신 `a29ec0f`에서는 Backend `.venv`로 Django Check와 Migration Drift 검사가 PASS해 실행 진입점은 복구됐다. 다만 AI 실행 의존성과 팀 PostgreSQL 접속·공동 Health는 확인되지 않았다.

완료하려면 `docs/testing/e2e/`에 확정 SHA, Backend/AI Base URL, DB·Seed 상태, 필요한 환경변수명, Health 결과를 기록한 짧은 실행 결과서를 작성해야 한다. Secret 값은 기록하지 않는다.

별도 확인: 팀원 PC 또는 공용 서버에서 Mobile과 Web이 같은 Backend 주소를 실제로 호출하는지 확인해야 한다.

### 2.2 3.2 Backend↔AI 실제 Baseline 연결 검증 — 75%

- Backend Evidence Verifier·Safety Rule·Crosswalk·읽기 전용 View 사전점검과 AI Prompt v2 안전 Guard가 구현돼 있다.
- [G1-B 독립 QA](../individual/eunjin/20260813_Backend_AI_G1B_독립_QA_결과.md)는 단위 Test `25 passed`지만 팀 DB 연결 Timeout과 Role Credential 부재로 `ENVIRONMENT_BLOCKED`다.
- `1cd1905` P0는 실제 FastAPI 위험 규칙 경로를 사용했으나 생성형 LLM·일반 pgvector 검색 경로 검증은 아니다.
- `ccf30f2`에서 Canonical 설정의 LF 계약과 회귀 Test가 추가됐다. Git 원본 Identity SHA는 Import Package 기대값 `925088...`와 일치한다.
- 현재 PC에는 기존 CRLF 작업 파일이 남아 실제 SHA가 `16514d...`이므로 표적 재실행은 **46 passed / 19 failed**다. 이는 Git 원본 계약 실패가 아니라 기존 작업 트리 미정규화 문제이며, 재정규화 또는 새 Checkout 후 다시 검증해야 한다.

완료하려면 실제 Local Mode에서 Backend→FastAPI→pgvector→LLM→Backend를 한 번 실행하고 AIRun·Guidance·EvidenceLink·State·`correlation_id`를 `tests/integration/backend-ai/**` 또는 실행 결과서에 연결해야 한다.

별도 확인: `OPENAI_API_KEY`, 팀 DB 최소 권한 DSN, 공식 Evidence 7건 적재·Crosswalk Apply 여부가 필요하다.

### 2.3 3.3 8월 13일 종료 Gate — 80%

- 기준 Scenario·Cut-off·PASS/HOLD 원칙과 차단 항목은 실행 계획에 기록됐다.
- 실제 P0 실행 결과와 Inquiry·상태 v1→v8·Correlation 증거가 추가됐지만 최신 SHA 공동 결과는 아니다.

완료하려면 `baseline_commit`, 실행 명령, Health, Inquiry ID, Correlation ID, DB 저장 건수, PASS/HOLD, 남은 Blocker를 한 페이지 결과서로 남긴다.

### 2.4 4.1 대표 P0 E2E — 70%

- [E2E Acceptance](../testing/e2e/week5-e2e-acceptance.md)에 기대값이 정의됐고, [P0 실행 결과](../handoffs/week5/20260813_한예나_E2E_P0_실행결과.md)에서 동일 Inquiry가 `COMPLETION_PENDING` v8까지 완주됐다.
- 다만 해당 결과는 `1cd1905` 통합 작업 기준이며 최신 `a29ec0f`에서는 동일 PostgreSQL Inquiry로 재실행되지 않았다.

완료하려면 방문기사 단계를 제외하고 동일 Inquiry를 사용한 정상 흐름을 실기기·브라우저·PostgreSQL에서 완주하고 화면·DB·로그 증거를 연결한다.

별도 확인: 실제 기기·브라우저 접근과 합성 CUSTOMER/CONSULTANT 계정이 필요하다.

### 2.5 4.2 Happy Path 후 최소 오류 — 60%

- 409·멱등 Replay·타 상담사 404·원자 선점·가짜 Evidence 0건은 과거 Runtime과 Test에서 확인됐다.
- 최신 공동 Runtime의 AI 503/Timeout과 PostgreSQL 동시성 재검증은 남아 있다.

완료하려면 Happy Path 성공 후 실패 구간만 재현하여 추가 Business Record·Version·History가 생기지 않는지 기록한다.

### 2.6 4.3 전화 문의 등록 최소 Smoke — 90%

- 8월 12일 실제 PostgreSQL·브라우저에서 전화 문의 등록→목록→상세→상담 시작 지속성 PASS 증거가 [Web PM 승인 반영 문서](../handoffs/week5/20260813_한예나_5주차_8월13-14일_E2E_집중_업무_지침서_PM승인반영.md)에 있다.
- 현재 최종 SHA에서 등록→목록·상세의 최소 회귀만 다시 확인하면 된다.

### 2.7 5 Blocker 처리 — 95%

- 11:37 기준 연결 구간별 Blocker·담당자·해제 조건이 중앙 등록됐다.
- 상담사 합성 배정은 구현·현재 HEAD 표적 Test와 PostgreSQL 반복검증까지 통과했다.
- 중앙 Register는 여전히 해당 항목을 구현 대기로 표시하므로 최신 판정 반영과 실제 Mobile→Web 재검증이 남았다.

완료하려면 각 Blocker에 `재현 명령 → 수정 Commit → 동일 구간 재검증 → PASS/HOLD`를 추가하고 E2E 비차단 항목은 Backlog로 이동한다.

### 2.8 6 보류 업무 범위 확인

- 김은진 담당 범위에서는 오늘 추가 비교 실험을 운영 기준선으로 승격하지 않았다.
- 기존 실험·QA 자산은 보존하되 공용 E2E 검증보다 앞세우지 않으면 된다.
- 최지용 작성 커밋 `2e5b1f0`의 `data/config/**`, `data/processed/**` 변경은 PM 허가를 받은 협업 변경이므로 관할 침범이 아니다.
- `final_dataset_manifest.json`은 SHA 불일치에 따른 CI 실패를 막기 위해 커밋 시 자동 갱신되는 파일이므로 해당 변경을 정상 결과로 유지한다. 이 항목은 완료도 계산에 포함하지 않는다.

### 2.9 8 PM 최종 결과 전달 — 50%

- 과거 P0 PASS와 최신 SHA HOLD를 구분한 중간 보고가 가능하다.
- 8월 14일 종료 시 성공 여부, 실제 성공 구간, 기준 SHA, 남은 Blocker·담당자, 후속 Backlog 다섯 항목을 짧은 최종 회신으로 전달해야 한다.

## 3. 코드로 확인할 수 없는 사항

- 모든 팀원이 같은 소스 SHA를 사용하더라도 같은 Backend·AI·DB Runtime을 실제로 사용했는지
- 실제 OpenAI Provider·팀 pgvector 호출 성공 여부
- Canonical LF 원본을 반영한 새 Checkout 또는 재정규화 표적 회귀가 PASS하는지
- 실기기 Mobile과 실제 브라우저 Web의 동일 Inquiry 확인
- 최종 시연 화면·DB Delta·Correlation 로그의 사람 검수

## 4. 최종 체크리스트 판정

- [ ] 같은 기준 Commit에서 Backend·AI·Web·Mobile이 통합 환경에 연결된다. — 소스 SHA는 일치하지만 공용 Runtime은 미확인이다.
- [ ] 고객 Mobile에서 최신 Backend로 문의를 생성할 수 있다. — `1cd1905`에서는 PASS.
- [ ] Backend가 최신 Baseline AI를 호출하고 응답을 저장한다. — 과거 위험 규칙 경로 PASS, 일반 RAG·LLM 미확인.
- [ ] 고객이 최신 AI 처리 결과를 Mobile에서 확인할 수 있다. — `1cd1905`에서는 PASS.
- [ ] 미해결/상담 필요 문의가 최신 상담사 Workflow로 전환된다. — `1cd1905`에서는 PASS.
- [ ] 상담사 Web에서 최신 SHA의 같은 문의를 조회하고 처리할 수 있다. — `1cd1905`에서는 PASS.
- [ ] 상담 완료 결과가 최신 Backend와 DB에 저장된다. — 과거 v8 PASS.
- [ ] 고객이 최신 상태를 다시 확인할 수 있다. — 과거 Mobile v8 재조회 PASS.
- [ ] 핵심 흐름의 `correlation_id` 또는 이에 준하는 최신 추적 근거가 남는다. — 과거 실행 로그 대조 있음.
- [ ] 실패했던 E2E Blocker와 수정 Commit·재검증 결과가 기록된다.
- [ ] 방문기사 기능이 없어도 최신 P0 E2E가 완료된다. — `1cd1905`에서는 방문 없이 완주.
- [x] 전화 문의 등록 최소 Smoke 결과가 기록된다. 단, 현재 최종 SHA 회귀는 남아 있다.
