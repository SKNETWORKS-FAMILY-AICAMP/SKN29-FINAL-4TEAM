# 최지용 3주차 작업 진행도

- 작성 일시: **2026-07-29 16:35:04 +09:00 (Asia/Seoul)**
- 기준 문서: `docs/planning/md/팀원별 관할 영역 v2.md`, `docs/weekly-task/최지용_3주차_업무_지침서.md`
- 점검 범위: 최지용 주관할인 `backend/**`, `contracts/api/**`, `contracts/codes/**`, `contracts/error-codes/**`와 관련 공동 문서
- 점검 방식: 담당 영역을 수정하지 않고 코드·Migration·계약·테스트·검증 보고서를 읽기 전용으로 대조했다. 이 보고서 외의 Backend·Contracts·Data·Tests 파일은 변경하지 않았다.
- 실행 검증 제한: 현재 작업 환경에는 `backend/.venv`가 없고 시스템 Python은 프로젝트 고정 버전 `3.13.13`이 아닌 `3.13.12`이며 Django·PyYAML도 설치되어 있지 않았다. 따라서 Backend 테스트와 T-005 검사기를 이번 점검에서 재실행하지 못했다. 2026-07-29 검증 보고서에 기록된 `353 passed`, PostgreSQL 16.14 Migration·Seed 결과는 저장소 증거로 인정하되, 현재 Commit의 독립 재현 여부는 별도 확인 대상으로 구분했다.

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 핵심 사유 |
| --- | ---: | --- | --- |
| 3.1 ERD·Model·Migration·DB 설계 문서 | 45% | 부분 완료 | ERD·테이블 계약과 1~2차 Runtime Wave는 있으나 현행 인계 기준 32개 중 7개 테이블만 구현됐고 공식 제출용 DB·저장소 설계 문서가 없음 |
| 3.2 Django·DRF 공통 구조와 실행 환경 | 85% | 대부분 완료 | 환경별 설정·공통 응답·오류·추적·로그·Health·README와 7월 29일 실행 증거가 있으나 현재 환경 재실행과 시간대·실로그 검증이 남음 |
| 3.3 가상 로그인·역할 기반 접근 제어 | 75% | 부분 완료 | 로그인·재발급·로그아웃·현재 사용자·역할 Seed·권한 프리미티브는 구현됐으나 기사 배정·상담사·운영자 객체 권한의 실제 Runtime 수직 검증이 부족함 |
| 3.4 문의 생성·증상 제출·입력 누적 API | 40% | 부분 완료 | 문의 생성은 구현됐지만 문진 누적·자가조치 결과 API는 OpenAPI 전용이며 복수 증상·추가 답변·AI 실패 보존 흐름이 미구현 |
| 3.5 State Machine 행동 API와 상태 이력 | 45% | 부분 완료 | 계약 Loader·Engine·Guard·허용 행동과 생성·취소 이력·멱등성·409는 있으나 취소 외 행동 API와 범용 Service 연결·PostgreSQL 동시성 검증이 없음 |
| 3.6 API 계약·테스트 자료·산출물 | 55% | 부분 완료 | Runtime 7개 계약·예시 22개·오류 Registry·README·인계 문서는 충실하지만 빈 Workflow Schema, 공식 제출 문서, 팀 리뷰·PR 증빙과 미구현 Runtime이 남음 |

단순 평균 기준 종합 완료도는 **약 58%**이다. 공통 Backend, 인증, 문의 생성, 문의 취소의 최소 수직 흐름과 계약 검증은 실제 코드로 확인된다. 그러나 지침서의 핵심인 전체 DB 구조, 문의 입력 누적, 최소 State Machine 행동군, 실제 PostgreSQL 동시성 검증과 공식 제출 문서는 아직 완료 기준에 도달하지 못했다.

## 2. 현재 구현 경계

### 확인된 구현·문서 증거

- `GET /health`, Demo 로그인, Token 재발급·폐기, `/me`, 문의 생성, 문의 취소까지 **Runtime 7개**가 등록되어 있다.
- OpenAPI Operation은 9개이며, 문진 누적과 자가조치 결과 2개는 `OPENAPI_ONLY`로 명시되어 있다.
- `accounts`, `products`, `subscriptions`, `care`, `inquiries`, `workflow`에 번호 Migration이 있다.
- 문의 생성은 공개 UUID, 별도 `inquiry_code`, `state_version`, `allowed_actions`, `Idempotency-Key`를 사용한다.
- 문의 취소는 소유권·현재 상태·버전을 검사하고, 같은 Key 재전송과 다른 Payload Key 재사용을 구분한다.
- 공통 응답·오류·페이지네이션, `correlation_id`, CORS, 구조화 로그 기반과 테스트가 있다.
- 인증·오류·문의·Workflow 예시 JSON 22개와 OpenAPI 상대 참조 검증이 있다.
- 2026-07-29 검증 보고서에는 Django System Check 성공과 Backend 전체 `353 passed`가 기록되어 있다.
- 공유 인계서에는 PostgreSQL 16.14 Migration 검사와 Accounts→Products→Subscriptions→Care Seed 2회 실행 성공이 기록되어 있다.

### 완료 판정을 제한하는 현재 경계

- 현행 팀 인계 문서는 T-005를 **계약 테이블 32개 중 7개 구현, 25개 후속**으로 명시한다.
- `questionnaires`, `consultations`, `visits`, `evidence`, `audit`의 주요 Model 파일은 Docstring만 있는 골격이고 번호 Migration이 없다.
- `FollowupAnswer`, `CustomerActionResult`도 실질 Model이 아니라 Docstring 골격이다.
- `PATCH /inquiries/{id}/questionnaire`, `POST /inquiries/{id}/action-results`는 Runtime Route·View가 없다.
- 실제 Workflow Runtime은 `START_INQUIRY`와 `CANCEL_INQUIRY` 대표 흐름에 한정된다.
- `AllowedAction.yaml`, `StateTransitionResult.yaml`의 `properties`가 비어 있어 범용 상태 전환 응답 계약이 완성되지 않았다.
- `docs/submission/database-storage-design.md`와 지침서가 요구한 제출용 DB·저장소 설계 문서가 없다.
- `tests/contract/api/**`, `tests/integration/backend-database/**`, `tests/integration/workflow/**`에는 현재 파일이 없다.
- 완료 증거 파일은 `*.example.json`만 있고 작성자 외 재현·리뷰가 완료된 실제 증거 JSON은 없다.

## 3. 필수 업무별 상세 평가와 완료 방법

## 3.1 ERD·Django Model·Migration과 데이터베이스 설계 문서 마감 — 45%

### 확인된 완료 내용

- `docs/database/`에 32개 테이블·526개 컬럼 기준의 ERD, 테이블 사전, 논리·물리 계약과 결정 등록부가 있다.
- 내부 PK, 외부 `public_id`, 업무 코드의 3계층 식별자 원칙과 상태 이력·멱등성 범위가 ADR과 물리 계약 v1.2에 기록되어 있다.
- `accounts`, `products`, `subscriptions`, `care`, `inquiries`, `workflow`에 실질 Model과 번호 Migration이 존재한다.
- 현재 구현된 문의·Workflow 테이블에는 UUID, FK, UNIQUE, CHECK, Index, `state_version`, 이력·멱등성 제약 일부가 Migration으로 반영되어 있다.
- Accounts·Products·Subscriptions·Care Seed는 `update_or_create` 방식으로 작성되어 있다.
- 2026-07-29 인계 문서에는 PostgreSQL 16.14에서 Migration 확인과 네 Seed의 2회 실행 성공이 기록되어 있다.

### 완료되지 않은 항목

- 전체 32개 계약 테이블 중 현행 인계 기준 구현은 7개뿐이며 25개가 후속이다.
- 문진 세션·답변, 고객 보충 답변, 자가조치 결과, 상담·확정 요약, 방문·결과, 공식 근거, AI 실행·감사 구조가 실질 Model·Migration으로 완성되지 않았다.
- `questionnaire_session_public_id`는 문의에 UUID 값으로만 보관되고 실제 문진 세션 FK 관계가 없다.
- Evidence·문서·페이지·Chunk·Embedding 연결과 pgvector 1024차원 구조가 Runtime Migration으로 구현되지 않았다.
- Accounts의 기존 문자열 PK를 내부 BigInt PK로 전환하는 3계층 식별자 작업은 전환 브리지 상태다.
- 지침서가 요구한 `docs/submission/database-storage-design.md`가 없고, `docs/database/`의 설계 패키지가 공식 제출 양식으로 정리되지 않았다.
- 데이터 전처리 결과서의 DB 적재·Vector Store·근거 레지스트리 검수 의견을 확인할 수 없다.
- 현재 Commit에서 빈 PostgreSQL Migration과 전체 Seed를 독립 재실행하지 못했다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. T-005 Wave 3~5 순서로 `questionnaires`, 입력 누적, 상담, 방문, Evidence, Audit·AI 실행 Model을 작성하고 각 App에 번호 Migration을 만든다.
2. 문진 세션·추가 답변·AI 실행·근거 참조를 문자열 UUID가 아닌 명시적 FK와 공개 UUID 조합으로 연결하고 삭제 정책·UNIQUE·CHECK·Index를 Migration에 둔다.
3. `FollowupAnswer`, `CustomerActionResult`와 문진 답변 Model을 구현해 최초 원문과 후속 입력이 서로 덮어써지지 않게 한다.
4. 이동윤과 합의한 문서·페이지·Chunk·Embedding 버전 키를 Evidence Model에 반영하고, pgvector 1024차원·Exact Search 기준을 Migration과 문서에 기록한다.
5. 모든 Wave에 대해 빈 PostgreSQL Migration, `makemigrations --check --dry-run`, `migrate --check`, 제약 검사를 실행한다.
6. Seed Command 또는 검증된 Importer를 추가해 역할 계정뿐 아니라 문의·문진·상담·방문·근거 관계까지 반복 적재하고 2회 실행 결과를 남긴다.
7. `docs/submission/database-storage-design.md`에 실제 테이블명·경로·저장소·보안·백업·복구·보존·Seed·검증 수치를 현재 Runtime 기준으로 작성한다.
8. 데이터 전처리 결과서에 관계형 DB·Vector Store·Evidence Registry별 적재 대상과 아직 미구현인 경계를 검수 의견으로 추가한다.

### 코드 외 별도 확인

- 김은진이 새 PostgreSQL에서 전체 Migration과 Seed 2회를 재현했는지 확인해야 한다.
- 윤승혁이 문의·상담·방문·상태 이력 관계가 확정 State Machine과 일치하는지 검토해야 한다.
- 이동윤이 Evidence·Vector·AI 실행 연결 키와 공개 가능 메타데이터를 검토해야 한다.
- 백업·복구·보존 정책은 실제 운영·시연 환경에서 실행 가능하고 담당자가 지정됐는지 확인해야 한다.
- 공식 제출 문서의 양식·제출 위치·검토 상태를 PM에게 확인해야 한다.

## 3.2 Django·DRF 공통 구조와 실행 환경 구성 — 85%

### 확인된 완료 내용

- Python 버전 파일, Django·DRF·PostgreSQL Driver 요구사항과 Python 3.13 constraints가 저장소에 있다.
- `base`, `local`, `test`, `production` Settings가 분리되어 있고 비밀값·DB·AI URL·CORS·로그 수준을 환경변수로 관리한다.
- `GET /health`와 `/api/v1` URL 경계가 구현되어 있다.
- 성공·오류·페이지네이션·Metadata Wrapper가 OpenAPI 공통 Schema와 연결되어 있다.
- 전역 예외 Handler가 입력·인증·권한·404·409·5xx를 표준 오류 코드로 변환한다.
- `correlation_id`가 요청 Header, 응답 Header, Metadata와 구조화 로그 Context에 연결된다.
- Request Logging은 Query String의 민감값을 기록하지 않도록 작성되어 있고 관련 단위 테스트가 있다.
- `backend/README.md`와 공유 인계서에 bootstrap, 환경변수, PostgreSQL, Migration, Seed, 실행, 테스트, Smoke 절차가 상세히 기록되어 있다.
- 2026-07-29 보고서에 System Check 성공과 전체 Backend `353 passed`, Health·Auth Smoke 성공이 기록되어 있다.

### 완료되지 않은 항목

- 현재 작업 환경에는 `backend/.venv`가 없어 README 절차와 `353 passed`를 독립 재현하지 못했다.
- 시스템 Python은 고정 버전 3.13.13이 아닌 3.13.12이므로 현재 터미널은 공식 실행 환경이 아니다.
- 날짜·시간이 모든 실제 Serializer에서 `+09:00` 포함 ISO 8601로 반환되는지 포괄하는 Runtime 테스트 증거가 부족하다.
- 로그 마스킹은 단위 테스트가 있으나 실제 서버 Logcat·파일·Collector에서 Token·전화번호·주소·고객 원문·AI Prompt가 남지 않는지 수동 검수 증거가 없다.
- `/health`는 의도적으로 Liveness만 검사하므로 DB 연결·Migration 상태는 별도 명령을 실행해야 한다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 공식 bootstrap으로 Python 3.13.13 기반 `backend/.venv`를 재생성하고 환경 fingerprint를 현재 Commit과 맞춘다.
2. `check_environment.py --service backend --full --postgresql`과 Health·Auth Smoke를 같은 Commit에서 실행해 Exit code와 결과를 인계서에 갱신한다.
3. DateTime을 반환하는 Serializer에 한국 시간대 ISO 8601 변환 테스트를 추가한다.
4. 공통 로그 Sanitizer가 Header·Query·Body·예외 Context의 Token·전화번호·주소·원문·Prompt를 제거하는 테스트를 보강한다.
5. `/health`의 Liveness 의미와 DB Readiness 검사 명령을 OpenAPI와 README에서 계속 분리해 표시한다.

### 코드 외 별도 확인

- 다른 팀원이 새 Pull 환경에서 bootstrap→PostgreSQL→Migration→Seed→서버→Smoke 순서를 재현해야 한다.
- 실제 CORS Origin으로 Web 요청이 허용되고 미등록 Origin은 차단되는지 Browser Network에서 확인해야 한다.
- 운영·시연 로그 저장소에서 민감정보 비노출과 `correlation_id` 검색 가능 여부를 확인해야 한다.
- Docker Desktop과 PostgreSQL 16.14 이미지가 팀 환경에서 같은 결과를 내는지 확인해야 한다.

## 3.3 가상 로그인·역할 기반 접근 제어 구현 — 75%

### 확인된 완료 내용

- Demo 로그인, `/me`, Refresh Rotation, 로그아웃과 Refresh Token Blacklist가 구현되어 있다.
- Access Token 60분, Refresh Token 최초 발급 기준 7일과 재발급 시 절대 만료 유지가 코드와 테스트에 반영되어 있다.
- JWT에는 공개 사용자 ID·역할 등 최소 Claim만 넣고 현재 활성 사용자·역할을 DB에서 다시 검사한다.
- `CUSTOMER`, `CONSULTANT`, `TECHNICIAN`, `OPERATOR` 합성 계정 Seed가 있다.
- 역할, 객체 소유자, 기사 배정자, 목록 Queryset 범위를 기본 거부 방식으로 검사하는 Permission 프리미티브가 있다.
- 문의 생성·취소는 인증 고객 역할과 본인 구독·문의 소유권을 Runtime에서 검사하고 타 고객 리소스를 404로 숨긴다.
- 비활성 사용자, 잘못된 Token, Refresh 재사용, 역할 변경과 안전한 `/me` Projection 테스트가 있다.

### 완료되지 않은 항목

- 기사 방문 API의 View와 Permission 파일은 Docstring 골격이므로 실제 미배정 방문 접근 차단을 Runtime으로 확인할 수 없다.
- 상담사·운영자 역할의 실제 목록·상세 Endpoint에서 조회 범위가 구분되는 수직 테스트가 없다.
- 역할 Permission 프리미티브가 존재하는 것과 모든 API가 이를 빠짐없이 사용하는 것은 별도이며, 현재 Runtime은 Auth와 고객 문의 생성·취소에 한정된다.
- 작성자 외 팀원이 역할별 계정으로 401·403·404 경계를 재현한 완료 증거가 없다.
- 인증 OpenAPI 예시는 있으나 Web·Mobile의 실제 Token Rotation·Logout 소비 확인은 완료되지 않았다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 방문 조회·변경 Runtime을 구현할 때 `TECHNICIAN` 역할과 `assigned_technician` 객체 범위를 함께 적용하고 미배정 건은 404로 처리한다.
2. 상담사·운영자용 최소 조회 Endpoint에서 역할별 Queryset 범위를 명시하고 고객·기사 데이터 노출 차이를 테스트한다.
3. 각 Detail API에 역할 Permission과 객체 Permission이 동시에 적용되는지 API 테스트로 고정한다.
4. 고객 IDOR, 기사 미배정, 상담사 역할 부족, 운영자 허용 범위를 실제 API Client로 검증하는 테스트를 추가한다.
5. 역할별 Demo 계정, 로그인·Refresh·Logout·Header·401·403·404 호출 예시를 README에 표로 정리한다.

### 코드 외 별도 확인

- 한예나·양정현이 실제 Backend를 대상으로 로그인·재발급·로그아웃을 수행했는지 확인해야 한다.
- 실제 Token이 Browser Storage·Mobile Log·Backend Log에 남지 않는지 확인해야 한다.
- 상담사와 운영자의 조회 범위가 요구사항·화면 기준과 일치하는지 PM 검토가 필요하다.
- 김은진이 고객 IDOR와 기사 미배정 접근을 별도 계정으로 재현해야 한다.

## 3.4 문의 생성·증상 제출·추가 입력 누적 API 구현 — 40%

### 확인된 완료 내용

- `POST /api/v1/inquiries` 신규 문의 생성 Runtime이 구현되어 있다.
- 인증 고객의 활성 구독 공개 UUID를 조회하고 타 고객 구독은 404로 차단한다.
- 고객 원문을 Trim·필수 검증하고 문의와 대표 증상 1건을 한 Transaction에서 저장한다.
- 내부 정수 PK 대신 `inquiry_id` UUID, 별도 `inquiry_code`, 상태, `state_version`, `allowed_actions`를 응답한다.
- 생성 요청에 `Idempotency-Key`를 요구하고 같은 요청 재생과 다른 Payload 재사용 충돌을 구분한다.
- 문의와 대표 증상, 시작 전이 이력을 한 Transaction에서 생성하는 API 테스트가 있다.
- OpenAPI와 생성 요청·성공·Replay 예시가 실제 Serializer와 대조되어 있다.

### 완료되지 않은 항목

- `PATCH /inquiries/{id}/questionnaire`와 `POST /inquiries/{id}/action-results`는 OpenAPI에만 있고 Runtime Route·View·Service가 없다.
- 대표 증상은 `OneToOne` 단일 값으로 구현되어 지침서의 복수 증상 선택을 처리하지 못한다.
- `FollowupAnswer`, `CustomerActionResult`, 문진 Session·Answer가 실질 Model이 아니므로 보충 설명·문진 답변·AI 추가 질문 답변을 같은 문의에 순서대로 누적할 수 없다.
- 생성 Service는 본인 활성 구독만 확인하며 MVP 지원 제품 여부를 검사하는 `ProductValidationService`를 호출하지 않는다.
- 발생 조건, 표시 문구·오류 코드, 진입 유형이 분리 저장되지 않는다.
- 종료 문의·허용되지 않은 입력 유형 차단과 입력 누적 Transaction Rollback이 Runtime으로 구현되지 않았다.
- `InquiryAiService`가 Docstring 골격이라 AI 실패·Timeout 후 문의와 입력 유지·재조회·재시도 흐름을 검증할 수 없다.
- 문의 상세 조회 API가 없어 AI 실패 후 저장된 문의·입력을 API로 다시 조회하는 완료 기준을 충족하지 못한다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `QuestionnaireSession`, `QuestionnaireAnswer`, `FollowupAnswer`, `CustomerActionResult` Model·Migration을 만들고 문의 FK·순번·작성자·생성 시각을 둔다.
2. `SymptomEntry`를 복수 증상 구조로 변경하거나 별도 대표 증상 필드를 두고 다중 Row 저장을 허용한다.
3. 문진 누적과 자가조치 결과 Endpoint의 Serializer·Service·Repository·View·URL을 구현하고 OpenAPI-only 상태를 Runtime으로 갱신한다.
4. 원문, 구조화 증상, 발생 조건, 표시 코드, 진입 유형을 독립 필드·Row로 저장해 AI 결과가 원문을 덮어쓰지 않게 한다.
5. 문의 생성 전에 `ProductValidationService`를 호출해 소유권·활성 구독·MVP 지원 모델을 AI 호출보다 먼저 차단한다.
6. 종료 문의, 타 고객, 존재하지 않는 문의, 잘못된 입력 유형과 순서 위반을 403/404/409/422 정책에 맞게 처리한다.
7. AI 호출을 입력 저장 Transaction과 분리하고, AI 실패·Timeout에도 입력 Commit이 유지되도록 Outbox 또는 명시적 재시도 상태를 둔다.
8. 문의 생성·상세 조회·복수 증상·자연어 단독·입력 누적·중복 제출·Rollback·AI 실패 테스트와 정상·오류 예시 JSON을 작성한다.

### 코드 외 별도 확인

- 양정현이 문의 생성·문진 누적·자가조치 Request/Response와 오류 예시로 Mobile Mock을 교체할 수 있는지 확인해야 한다.
- 이동윤과 AI 요청 시점, Timeout, 재시도 0회, 실패 저장 경계를 확인해야 한다.
- MVP 지원 제품 목록과 `removed_legacy` 처리 기준을 데이터·PM 기준과 대조해야 한다.
- 실제 PostgreSQL에서 입력 누적 순서, Rollback과 동시 제출 결과를 확인해야 한다.

## 3.5 State Machine 행동별 API와 상태 이력 구현 — 45%

### 확인된 완료 내용

- 승인된 State Machine YAML을 읽는 Loader와 계약 교차 참조 Validator가 있다.
- 순수 State Machine Engine, Guard Evaluator, `AllowedActionResolver`가 구현되어 있고 단위 테스트가 있다.
- 문의 상태 Enum은 승인된 13개 문의 상태와 맞춰져 있다.
- `WorkflowTransitionHistory`, `IdempotencyRecord` Model·Migration·Repository가 있다.
- 문의 생성과 취소에서 이전·다음 상태, 이벤트, 수행자, 시각, `correlation_id`, `idempotency_key`를 기록한다.
- 문의 취소는 Row Lock, `state_version`, 멱등 Key, Transaction을 사용하고 상태 충돌·Key 재사용 충돌을 409로 구분한다.
- 상태 충돌 409에 최신 `current_status`, `current_state_version`, `allowed_actions`를 포함한다.
- 생성·취소 API와 State Engine·Guard·Resolver에 집중 테스트가 있다.

### 완료되지 않은 항목

- 외부 행동 API는 `CANCEL_INQUIRY` 하나뿐이며 `/request-consultation`, `/start-consultation`, `/finalize` 등 지침서의 최소 행동 Endpoint가 없다.
- `SUBMIT_SYMPTOM`, `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `REQUEST_CONSULTATION`의 Runtime Service 연결이 없다.
- 순수 State Machine Engine과 Guard가 범용 Workflow Application Service에 연결되지 않았고 문의 생성·취소 Service가 상태를 개별 구현한다.
- 방문 Snapshot·방문 상태 이력 Model과 문의·방문 동시 전이 Transaction이 없다.
- 상태 이력에 별도의 수행 사유·Payload Snapshot 필드가 없고 취소 사유는 Inquiry에만 저장된다.
- AI 결과를 검증해 State Machine 이벤트 후보로 변환하는 Backend Service가 골격뿐이다.
- 실제 PostgreSQL에서 동시 요청, Row Lock, Rollback, 같은 Key 중복 이력 방지를 검증한 최신 독립 실행 증거가 없다.
- 범용 성공 응답 Schema인 `AllowedAction.yaml`, `StateTransitionResult.yaml`의 속성이 비어 있다.
- 한예나·양정현이 사용할 행동별 성공·권한·순서 오류·409 예시는 취소 흐름 외에는 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 공통 `StateMachineService`를 작성해 계약 Loader→Guard→Engine→Repository→이력 저장을 하나의 Transaction 경계로 연결한다.
2. 3주차 최소 이벤트의 행동별 View·Serializer·Service·URL을 구현하고 모든 상태 변경이 공통 Service만 통과하게 한다.
3. 방문 Snapshot·방문 상태 이력을 구현해 문의와 방문을 함께 바꾸는 이벤트를 하나의 DB Transaction으로 처리한다.
4. Transition History에 승인된 사유 코드·공개 가능한 Payload 요약을 추가하고 내부 비밀값·원문 전체는 기록하지 않는다.
5. AI Adapter가 분석 결과를 상태 직접 변경이 아닌 이벤트 후보로 변환하고 Backend Guard가 최종 결정하게 한다.
6. `AllowedAction.yaml`에 `code`, `label`, `operation_id`, `style`, 확인 필요 여부를 정의하고 `StateTransitionResult.yaml`에 상태·버전·허용 행동을 명시한다.
7. 성공 응답의 행동 객체 배열과 409 응답의 행동 코드 배열을 Schema 설명과 타입 이름으로 분명히 구분한다.
8. PostgreSQL에서 잘못된 역할·순서·Payload, 오래된 Version, 같은 Key 재전송, 다른 Payload Key 재사용, 동시 요청·Rollback을 테스트한다.

### 코드 외 별도 확인

- 윤승혁이 상태·이벤트·Guard·완료·Reopen 정책과 Runtime 행동 매핑을 검토해야 한다.
- 김은진이 실제 PostgreSQL 동시성·멱등성·Rollback을 재현해야 한다.
- 한예나·양정현이 성공 `allowed_actions` 객체와 409 행동 코드 배열을 각각 처리할 수 있는지 확인해야 한다.
- 이동윤이 AI 결과가 DB 상태를 직접 바꾸지 않고 이벤트 후보만 전달하는지 확인해야 한다.

## 3.6 API 계약·테스트 자료·7월 29일 산출물 정리 — 55%

### 확인된 완료 내용

- `contracts/api/**`, `contracts/codes/**`, `contracts/error-codes/**`에 OpenAPI·공통 코드·오류 Registry가 있다.
- OpenAPI 9개 Operation과 Runtime 7개·OpenAPI-only 2개가 별도 상태표로 관리된다.
- Auth·오류·문의 생성·문의 취소의 요청·성공·Replay·409 예시 JSON 22개가 있다.
- OpenAPI Path, Django Route, Serializer, 오류 Registry, JSON 예시를 대조하는 Backend 계약 테스트가 있다.
- 2026-07-29 계약 검증 보고서에는 계약·Auth `94 passed`, 권한·소유권 `31 passed`, 전체 Backend `353 passed`가 기록되어 있다.
- `backend/README.md`, API Runtime 상태표, Backend 인계서, 팀 인계 문서가 구현 범위와 미구현 범위를 명확히 구분한다.
- 역할별 계정과 Seed·환경·API 실행 절차가 상세히 제공되어 있다.

### 완료되지 않은 항목

- OpenAPI-only 2개 문의 누적 API와 취소 외 Workflow 행동 API의 Runtime·정상·오류 예시가 없다.
- `AllowedAction.yaml`, `StateTransitionResult.yaml`이 빈 객체라 Web·Mobile이 범용 상태 성공 응답을 계약만으로 구현할 수 없다.
- `tests/contract/api/**`, `tests/integration/backend-database/**`, `tests/integration/workflow/**`에 김은진과 공동 검증할 테스트가 없다.
- 정상·위험·근거 없음 전체 Fixture를 Backend Runtime에 적재하고 조회하는 검증이 없다.
- `docs/submission/database-storage-design.md`가 없고 데이터 전처리 결과서의 DB 적재 검수 의견도 확인되지 않는다.
- 실제 완료 증거 JSON, PR URL, 비작성자 리뷰, PM `main` 병합과 팀 소비 확인이 없다. 현행 Runtime 상태표도 팀 리뷰 완료를 0으로 기록한다.
- 계약 PR이 구현 PR보다 먼저 병합되었는지 로컬 파일만으로 판정할 수 없다.
- 현재 환경에서 문서에 기록된 `353 passed`를 다시 실행하지 못했다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 빈 Workflow Schema를 실제 Serializer·예시와 일치하게 완성하고 OpenAPI 전체 참조·예시 검증을 추가한다.
2. 문진 누적·자가조치·최소 Workflow 행동 Runtime을 구현할 때 정상·400·401·403·404·409·422·5xx 예시를 함께 작성한다.
3. `tests/contract/api/**`에 OpenAPI·예시·Serializer 계약 테스트, `tests/integration/backend-database/**`에 Migration·Seed·Transaction 테스트, `tests/integration/workflow/**`에 상태 전이·멱등성·409 테스트를 김은진과 협의해 추가한다.
4. 정상·위험·근거 없음·권한·상태 충돌 Fixture를 `data/synthetic/fixtures/**` 기준본과 명시적으로 매핑한다.
5. 공식 `docs/submission/database-storage-design.md`와 데이터 전처리 결과서 DB 적재 검수 의견을 작성한다.
6. 실제 완료 증거 JSON에 Commit SHA·명령·Exit code·PostgreSQL Vendor·Seed 2회·비작성자 검토를 기록한다.
7. PR에 계약 선행 병합, 구현 범위, Mock·미구현·미확정 항목, 재현 명령과 검증 결과를 연결한다.

### 코드 외 별도 확인

- 계약 PR이 구현 PR보다 먼저 병합되었는지 원격 Git 기록으로 확인해야 한다.
- PM의 `main` 병합 SHA와 비작성자 리뷰, Web·Mobile·AI 소비 확인을 받아야 한다.
- 김은진이 같은 Commit에서 Backend 테스트와 PostgreSQL Integration을 재현해야 한다.
- 공식 제출 문서 두 건이 제출 양식·위치·검토 기한을 충족하는지 확인해야 한다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 사항

| 확인 대상 | 필요한 별도 증빙 또는 확인자 |
| --- | --- |
| 현재 Commit의 Backend 전체 테스트 | Python 3.13.13 가상환경에서 실행한 명령·Commit SHA·Exit code·테스트 수 |
| 빈 PostgreSQL Migration | 새 DB 이름·PostgreSQL 버전·Migration 목록·`migrate --check` 결과. 비밀번호·DSN은 제외 |
| Seed 2회 멱등성 | 1·2차 명령, 생성·갱신·중복 건수와 김은진의 재현 결과 |
| 실제 권한 경계 | 역할별 합성 계정으로 수행한 401·403·404 및 고객 IDOR·기사 미배정 결과 |
| PostgreSQL 동시성·멱등성 | 동시에 보낸 요청의 상태 코드, 이력 건수, Row Lock·Rollback 결과 |
| 로그·개인정보 안전 | 실제 서버 로그·Crash·Collector에서 Token·전화번호·주소·고객 원문·Prompt 비노출 검수 |
| Web·Mobile·AI 계약 소비 | 한예나·양정현·이동윤의 DTO·Mock·실제 호출 검토 결과 |
| 계약 선행 병합과 PR 완료 | 원격 PR URL, 계약 PR·구현 PR 병합 순서, 리뷰 코멘트와 병합 SHA |
| 공식 산출물 제출 | `database-storage-design.md`, 데이터 전처리 결과서의 검토자·제출 시각·수신 확인 |
| 추가 업무 착수 순서 | 필수 업무 완료 시점과 제품·케어·AI Client 착수 Issue·Commit의 시간 순서 |

## 5. 완료 기준 및 최종 체크리스트 판정

## 5.1 7월 29일 필수 완료 기준

### 현재 완료로 체크해도 되는 항목

아래 항목은 저장소의 구현과 2026-07-29 실행 보고서를 함께 근거로 완료 체크할 수 있다. 다만 최종 공유 Commit에서는 김은진 또는 비작성자가 같은 명령을 재현해야 한다.

- [x] Seed를 반복 실행해도 예상하지 않은 중복이 발생하지 않는다.
  - Accounts·Products·Subscriptions·Care Seed 2회 성공 기록과 멱등 Command가 있다.
- [x] Backend 서버와 `/health`가 실행된다.
  - Health Runtime·자동 테스트·2026-07-29 Smoke 성공 기록이 있다.
- [x] 공통 성공·오류·페이지네이션·`correlation_id` 구조가 동작한다.
  - 공통 코드와 계약·Runtime 테스트가 있다.
- [x] 가상 로그인과 현재 사용자 조회가 동작한다.
  - Demo 로그인·`/me`·Refresh·Logout Runtime과 테스트가 있다.
- [x] `state_version` 충돌과 `idempotency_key` 중복이 검증된다.
  - 현재 구현 범위인 문의 생성·취소에서 Replay·Key 재사용·상태 충돌 테스트가 있다.
- [x] 상태 전이 이력이 이전·다음 상태·이벤트·수행자와 함께 저장된다.
  - 현재 구현 범위인 `START_INQUIRY`, `CANCEL_INQUIRY`에서 확인된다.
- [x] 역할별 테스트 계정과 Seed·실행 절차가 제공되어 있다.
  - 네 역할 Seed와 README·인계 절차가 있다.
- [x] 7월 29일까지 검토 가능한 PR 또는 공유 파일이 있다.
  - 7월 29일자 Backend 공유 인계서·계약 검증 보고서·팀 인계 파일이 있다. PR 병합·리뷰 완료 여부는 별도다.

### 아직 완료로 체크하면 안 되는 항목

- [ ] ERD·Django Model·Migration·DB 설계 문서의 테이블과 관계가 일치한다.
  - 전체 32개 중 현행 인계 기준 7개만 Runtime 구현됐고 공식 설계 제출 문서가 없다.
- [ ] 빈 PostgreSQL에서 전체 Migration이 성공한다.
  - 현재 구현 Migration의 성공 기록은 있으나 미구현 테이블까지 포함한 전체 구조와 현재 Commit의 독립 재현은 완료되지 않았다.
- [ ] 누락된 문진 세션·문의 입력·상태 이력·근거 연결 구조가 반영되어 있다.
  - 문진·추가 입력·Evidence 구조 다수가 골격뿐이다.
- [ ] 고객·상담사·기사·운영 역할과 객체 단위 권한 검사가 있다.
  - 공통 Permission은 있으나 기사·상담사·운영자 실제 Runtime 수직 검증이 부족하다.
- [ ] 고객 IDOR와 기사 미배정 접근이 차단된다.
  - 고객 문의 소유권은 확인되지만 기사 방문 Runtime이 없다.
- [ ] 신규 문의 생성과 증상 제출이 동작한다.
  - 신규 문의와 대표 증상 1건은 동작하지만 별도 증상 제출·복수 증상은 미구현이다.
- [ ] 같은 문의에 추가 입력·답변이 누적된다.
  - 관련 Model과 두 Runtime Endpoint가 미구현이다.
- [ ] 미지원 제품·필수값 누락·타 고객 요청이 AI 호출 전에 차단된다.
  - 필수 원문과 타 고객은 차단하지만 MVP 지원 제품 검사가 생성 Service에 연결되지 않았다.
- [ ] State Machine 최소 이벤트와 `allowed_actions`가 동작한다.
  - `allowed_actions` 계산은 있으나 최소 이벤트군의 Runtime 연결은 생성·취소 일부뿐이다.
- [ ] 인증·문의·상태 API의 정상·오류 예시가 작성되어 있다.
  - Runtime 7개 예시는 있으나 문의 누적·최소 상태 행동 예시와 범용 Workflow Schema가 미완성이다.
- [ ] 데이터베이스·저장소 설계 문서가 검토 가능한 상태로 작성되어 있다.
  - 공식 제출 경로의 문서가 없다.

## 5.2 7월 30일~31일 최종 정리 기준

### 현재 완료로 체크해도 되는 항목

- [x] OpenAPI·예시 JSON과 실제 Serializer 응답의 차이가 수정되었다. *(현재 Runtime 7개 범위)*
  - 계약·Serializer·예시 정합성 자동 검증과 2026-07-29 `94 passed` 기록이 있다. OpenAPI-only 2개와 후속 Workflow 행동은 이 체크 범위에 포함되지 않는다.
- [x] 미완료 기능과 4주차 진입 조건이 인계 문서에 기록되어 있다.
  - `docs/handoffs/README.md`에 7개 Runtime, 2개 OpenAPI-only, T-005 7/32와 후속 조건이 명시되어 있다.

### 아직 완료로 체크하면 안 되는 항목

- [ ] Web·Mobile·AI 담당자의 계약 검토 의견이 반영되었다.
  - 계약 수정 요청이 남아 있고 소비 검증 완료 증거가 없다.
- [ ] Migration 초기화·재적용·Seed 절차를 다른 팀원이 재현했다.
  - 작성자 실행 기록은 있으나 김은진 등 비작성자의 실제 완료 증거가 없다.
- [ ] 권한·중복 요청·상태 충돌·트랜잭션 실패 테스트가 보강되었다.
  - 현재 생성·취소 범위 테스트는 있으나 실제 PostgreSQL 동시성·Rollback과 전체 역할 수직 검증이 없다.
- [ ] 로그에 토큰·개인정보·고객 원문 전체가 노출되지 않는지 확인했다.
  - 단위 테스트 외 실제 Runtime 로그 검수 증거가 없다.
- [ ] Mock·미구현·미확정 기능이 Issue와 PR에 기록되어 있다.
  - 문서에는 기록되어 있으나 연결된 Issue·PR 증거가 없다.
- [ ] 두 공식 산출물에 실제 테이블·경로·수치·검증 결과가 반영되었다.
  - 공식 DB·저장소 설계 문서와 데이터 전처리 결과서 검수 의견이 없다.
- [ ] 필수 업무와 리뷰 대응 완료 후에만 4장의 추가 업무를 시작했다.
  - 제품·구독·케어와 AI Client 골격이 존재하지만 필수 업무 완료 이후 착수했는지는 코드만으로 확인할 수 없다.

## 6. 우선 처리 순서

1. `AllowedAction.yaml`, `StateTransitionResult.yaml`을 실제 성공·409 응답과 맞춰 Web·Mobile의 계약 차단을 먼저 해소한다.
2. 문의 문진 누적·자가조치 결과 Model과 두 OpenAPI-only Runtime을 구현해 T-022의 동일 문의 입력 누적을 완성한다.
3. `ProductValidationService`와 AI 실패 보존 경계를 문의 생성·입력 Service에 연결한다.
4. 공통 State Machine Service와 최소 행동 Endpoint를 구현하고 생성·취소의 개별 전이를 공통 Engine 경계로 통합한다.
5. 문진·상담·방문·Evidence·Audit·AI 실행 Model과 Migration을 Wave별로 완성한다.
6. 공식 DB·저장소 설계 문서와 데이터 전처리 결과서 검수 의견을 현재 Runtime 수치로 작성한다.
7. Python 3.13.13 환경에서 전체 Backend·PostgreSQL·Seed·Smoke를 재실행하고 김은진의 비작성자 재현, PM 리뷰와 `main` 병합 증거를 남긴다.
