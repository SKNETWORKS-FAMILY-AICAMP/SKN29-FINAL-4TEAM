# P1 1대1 매칭 상담사 고객 연락처 조건부 공개 구현·검증

## 1. 작업 정보

- Backend 작업일: 2026-08-29
- Web 연동일: 2026-08-30
- Web 연동 기준 main: `1e939cc148a919db336844d3707ddae46b8a70dc`
- 작업 Branch: `jiyong`
- 담당: 최지용(Backend·DB, PM·한예나 승인 범위 내 Web 최소 연동)
- 판정: `BACKEND_CONTRACT_RUNTIME_PASS / WEB_LOCAL_PASS / DB_UNCHANGED / AWS_DEPLOYMENT_PENDING`

## 2. 목적

상담사 화면의 기본 개인정보 보호 원칙은 유지하면서, P1 팀 합성 고객 6명과
상담사 6명의 사전 확정된 1:1 조합에서는 담당 상담사가 본인 고객의 전화번호를
문의 상세 화면에서 확인할 수 있게 한다.

단순히 문의에 배정됐다는 이유만으로 원문 번호를 공개하면 잘못된 교차 배정이나
일반 합성 데이터까지 노출될 수 있다. 따라서 로그인 상담사, 문의 배정 상담사,
P1 예약 계약번호가 모두 정확히 일치하는지를 Backend가 최종 판정한다.

## 3. 기존 연결 구조 확인

| 구간 | 기존 동작 | 이번 판정 |
| --- | --- | --- |
| 고객 전화 저장 | `CustomerProfile.phone`에 저장 | 저장값·Schema 변경 없음 |
| 상담사 목록 | 고객 이름·연락처를 마스킹해 최소 Projection 제공 | 그대로 유지 |
| 상담사 상세 | 본인 배정 문의만 조회하고 전화번호는 마스킹 | 조건부 필드만 Additive 추가 |
| 미배정 대기열 | Claim용 최소 정보만 제공 | 그대로 유지 |
| P1 1:1 규칙 | 상담사 ID와 예약 계약 6쌍을 Backend에서 관리 | 원문 공개 판정에 재사용 |
| Web 표시 | 기존 `phone_masked` 사용 | 상세 화면만 `contact_phone ?? phone_masked`로 연동 완료 |

## 4. 공개 계약

대상 Endpoint는 기존 상담사 상세 조회다.

```text
GET /api/v1/inquiries/{inquiry_id}
```

고객 응답은 다음 규칙을 따른다.

```json
{
  "phone": "010-****-0000",
  "phone_masked": "010-****-0000",
  "contact_phone": null
}
```

- `phone`과 `phone_masked`는 항상 마스킹 상태를 유지한다.
- `contact_phone`은 정확한 P1 1:1 매칭에서만 원문 합성 번호다.
- 그 외에는 필드를 생략하지 않고 `null`을 반환한다.

`contact_phone` 공개 조건은 다음 다섯 가지를 모두 만족해야 한다.

1. 로그인 사용자가 `CONSULTANT`다.
2. Inquiry가 로그인 상담사에게 배정돼 있다.
3. Inquiry 배정 역할이 `CONSULTANT`다.
4. Inquiry 구독 계약번호가 P1 예약 계약 6건 중 하나다.
5. 로그인 상담사의 P1 담당 계약과 Inquiry 계약이 정확히 일치한다.

## 5. 변경 내용

| 영역 | 변경 |
| --- | --- |
| P1 Routing | 정확한 예약 상담사·계약 쌍 판정 메서드 추가 |
| Detail Service | 조건을 만족할 때만 `contact_phone` 구성 |
| Serializer | nullable Additive 필드 추가 |
| OpenAPI | Schema·Endpoint 설명·성공 예제 정합화 |
| API 공개 계약 | 조건부 필드와 `null` Fallback 명시 |
| Test | 6개 정확한 쌍, 교차 쌍, 일반 계약, 목록 비노출 검증 |
| Web 상세 DTO | nullable `contact_phone` 응답 필드 추가 |
| Web 상세 Mapper | 원문이 허용된 경우에만 `phoneDisplay`로 전달하고 `null`이면 마스킹 Fallback |
| Web 상세 UI | 상담 상세의 연락처 표시값만 `phoneDisplay`로 교체 |
| Web Test | 담당 고객 원문 우선 표시와 `null` 마스킹 Fallback 검증 |

## 6. 안전 경계

- 목록·검색·Dashboard·미배정 대기열에는 원문 연락처를 추가하지 않았다.
- 다른 상담사 문의는 기존처럼 조회 자체를 404로 숨긴다.
- 잘못 교차 배정된 P1 문의는 상세가 보이더라도 원문 연락처는 `null`이다.
- 일반 계약 Claim 허용 규칙을 원문 공개 권한으로 사용하지 않는다.
- 실제 고객·RDS·AWS 데이터는 조회하거나 변경하지 않았다.
- DB Schema·Migration·Seed·상태 전이는 변경하지 않았다.
- 공용 `contracts/state-machine/**`와 김은진 담당 `data/**`, `.github/**`,
  전역 `tests/**`는 변경하지 않았다. 조건부 공개는 Backend Runtime과
  `contracts/api/**`의 Additive Projection으로만 구성했다.
- Mobile·AI 담당 파일은 수정하지 않았다. Web은 PM과 한예나의 임시 관할 승인을
  받은 뒤 상담 상세 DTO·Mapper·표시 컴포넌트와 직접 관련된 Test만 최소 수정했다.
- 목록·검색·Dashboard·전화 문의 등록 화면의 기존 마스킹 로직은 수정하지 않았다.
- 예제와 테스트에는 합성 번호만 사용했다.

## 7. 검증 결과

| 검증 | 결과 |
| --- | --- |
| 변경 전 표적 기준선 | `32 passed` |
| P1 Routing·상세·계약 표적 | `49 passed` |
| 6쌍·교차·계약 재검증 | `27 passed` |
| Backend API 전체 | `427 passed / 19 skipped` |
| Backend 전체 회귀 | `1656 passed / 45 skipped` |
| 실제 HTTP 표적 | `1 passed / 1 PostgreSQL-only skipped` |
| 계약 Test | `46 passed` |
| State Machine 검증 | PASS (공용 계약 원본 무변경) |
| OpenAPI 검증 | PASS (`55 paths / 60 operations`) |
| API Example 검증 | PASS (`73 examples`) |
| Contract Crosswalk | PASS |
| Django System Check | PASS (`0 issues`) |
| Migration 코드 Drift | `No changes detected` |
| Web Unit Test (Backend 작업 당시) | `307 passed / 4 skipped` |
| Web Lint·TypeCheck·E2E TypeCheck·Build | PASS |
| Web 연락처 표적 Test | `28 passed` |
| Backend 1:1 공개 조건 재검증 | `6 passed` |
| Web Unit Test (Web 연동 후) | `308 passed / 4 skipped` |

`skipped`는 이번 변경으로 생긴 실패가 아니라 기존 PostgreSQL 잠금 전용 또는
외부 AI Socket 실행 조건이 없는 테스트다. Backend 전체 회귀 실패는 0건이다.
Migration Dry-run에서는 기본 DB 연결 시간 초과 경고가 있었지만 모델 변경 검사는
`No changes detected`로 끝났다. 실제 운영 DB Migration 실행이나 데이터 변경은
수행하지 않았다.

## 8. Web 연동 결과

Backend 계약을 다음 Web 경로에 최소 연결했다.

1. 상담사 상세 Remote DTO가 `contact_phone: string | null`을 받는다.
2. Mapper는 `contact_phone ?? phone_masked`로 `phoneDisplay`를 만든다.
3. 상담 상세 화면은 `phoneDisplay`만 표시한다.
4. Mock 상세는 `contact_phone=null`로 유지해 기존 마스킹 화면을 보존한다.
5. 담당 고객 원문 우선 표시와 `null` Fallback을 Unit Test로 검증했다.
6. 목록·검색·Dashboard의 고객명·연락처 마스킹은 변경하지 않았다.

원문 합성 번호는 화면 표시 외 Browser Log에 별도로 기록하지 않는다. 운영
Screenshot·Trace를 수집할 때에는 원문 연락처가 Artifact에 포함될 수 있으므로
정제 또는 접근 제한을 적용해야 한다.

## 9. 완료 기준

- Backend 계약·Runtime·회귀: PASS
- DB Migration·데이터 변경: 없음
- Web 소비 연결·로컬 회귀: PASS
- 목록·검색·Dashboard 마스킹 유지: 소스 변경 없음
- 배포·AWS 실측: PM main 병합·배포 후 정확한 P1 1:1 계정으로 별도 검증
