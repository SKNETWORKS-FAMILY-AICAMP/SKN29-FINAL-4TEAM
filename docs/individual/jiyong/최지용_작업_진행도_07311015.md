# 최지용 3주차 작업 진행도

> 최초 작성: 2026-07-29 16:35 KST
>
> 최종 갱신: 2026-07-31 17:45 KST
>
> 판정 기준: `Daily_Process/20260731/최지용_업무계획표_v0.9.md`
>
> 게시된 `jiyong` SHA: `1a44462ef4095a7fbfe9d795e04e5dc69e2e95e4`
>
> 포함된 `origin/main` SHA: `21e196637bb4bc41ab19248f265967ae0e2da28e`

## 1. 종합 판정

현재 기능 증거 기준 종합 진행도는 **약 79%**다.

WaterBridge의 T-005 물리 테이블 32개와 PostgreSQL 기본 데이터베이스
전환은 작성자 환경에서 기술 검증을 통과했다. 데이터가 있는 Active
MVP 13개만 현재 운용하고, Target-only 19개는 물리 구조를 유지한 채
모두 0행으로 보존한다.

| 필수 업무 | 증거 진행도 | 현재 판정 | 최신 근거 |
| --- | ---: | --- | --- |
| ERD·Model·Migration·DB | 95% | 기술 완료·공식 리뷰 대기 | `READY 32/32`, `waterbridge/public`, Active 13·Target-only 19 |
| Django·DRF 공통 환경 | 97% | 대부분 완료 | SQLite·PostgreSQL·Data·Health/Auth·로그 회귀 PASS |
| 로그인·RBAC | 80% | 부분 완료 | UUID JWT·Auth Runtime 완료, T-017A 설계 READY·리뷰 대기 |
| 문의·증상·입력 누적 | 50% | 부분 완료 | 문의 생성 Runtime 완료, `SUBMIT_SYMPTOM` 계약 Gate 대기 |
| State Machine Action | 62% | 부분 완료 | 취소·이력·멱등·버전 충돌 완료, 상담·방문 Action 미구현 |
| API 계약·테스트·문서 | 88% | 대부분 완료 | 계약·예시·전체 회귀·인계 문서 정합, 외부 소비 E2E 잔여 |

기술 검증 완료와 팀 공식 완료는 구분한다. 비작성자 독립 재현,
T-017A 정책·Migration 리뷰, T-022 OpenAPI·저장 위치 계약과 PM 완료
승인이 남아 있으므로 T-005는
`TECHNICALLY_COMPLETE_REVIEW_PENDING`으로 유지한다.

공식 WBS 상태와 위 기능 증거 진행도는 별도 지표다. 지침서 WBS가
갱신되기 전에는 `T-017A`, `T-022`, `T-023` 등을 임의로 공식 완료
처리하지 않는다.

## 2. 현재 검증 경계

- PostgreSQL Database는 `waterbridge`, Schema는 `public`이다.
- T-005 물리 계약은 `READY 32/32`, 구현 차단 0건이다.
- Active MVP 13개는 총 369행, Target-only 19개는 총 0행이다.
- Backend 전체 회귀는 SQLite `740 passed, 11 skipped`,
  PostgreSQL `751 passed`다.
- Data 단위 테스트는 `67 tests OK`, 대표 E2E는 `17/17`이다.
- Migration 미적용 0·drift 0, Seed 2회차 비의도 신규 생성 0이다.
- 합성 Importer는 기본 DB가 아닌 새 빈 격리 DB에서만 실행·Replay했다.
- Request·Exception 로그 민감정보 비노출은 `14 passed`다.
- T-017 Auth 준비도는 Auth 38건·PostgreSQL·Migration parity PASS다.
- T-022 기존 Create·Cancel·OpenAPI·State·Guard 기준선은 `40 passed`이며,
  `SUBMIT_SYMPTOM` Runtime 검증 결과는 아니다.

## 3. 필수 업무별 평가

### 3.1 ERD·Django Model·Migration과 DB 설계 문서 — 95%

완료된 내용:

- 32개 테이블 논리·물리 계약, ERD, 식별자·상태 이력·멱등성 ADR이 있다.
- 32개 계약 테이블 모두 Django Model로 선언되고 App Registry와 번호 Migration에 연결됐다.
- PostgreSQL 기본 Database를 `waterbridge`, Schema를 `public`으로
  정합화했다.
- 물리 32개를 유지하면서 Active 13개 369행과 Target-only 19개 0행을
  분리 검증했다.
- 완전히 빈 격리 PostgreSQL에서 전체 Migration, 5종 Seed 2회,
  367건 Import·Replay를 검증했다.
- Accounts 내부 정수 PK와 외부 공개 UUID를 분리하고 JWT `sub`를 UUID로 전환했다.

수행 근거: [T-005 DB 계약](../../database/t-005/README.md),
[최지용 문서 허브](README.md),
[DB 저장소 설계](../../submission/database-storage-design.md)

남은 내용과 완료 방법:

- 비작성자가 동일 SHA에서 빈 PostgreSQL·Seed·회귀를 독립 재현하고 Evidence를 남긴다.
- PM·계약 담당자가 3계층 식별자 전환과 불변 물리 계약의 완료 상태를 승인한다.
- 외부 소비자와 PM이 병합 후보 SHA를 검증한 뒤 공식 완료를 결정한다.

### 3.2 Django·DRF 공통 구조와 실행 환경 — 97%

완료된 내용:

- 환경별 설정, 공통 오류·추적 ID·로그, Health, 테스트 설정과 실행 가이드가 있다.
- SQLite 740건과 PostgreSQL 751건 회귀, Django System Check와 Migration Drift 0건이 기록됐다.
- 기존 DB Custom Dump의 실제 Restore를 확인한 뒤 기본 DB를
  `waterbridge`로 전환했다.
- 현재 Request·Exception 로그의 Token·Cookie·Query·Body·Exception
  원문 비노출을 `14 passed`로 검증했다.

수행 근거: [Backend README](../../../backend/README.md),
[최지용 문서 허브](README.md),
[팀 인계 허브](../../handoffs/README.md)

남은 내용과 완료 방법:

- 다른 팀원이 최종 `main`에서 환경 생성→Migration→Seed→전체 회귀를 독립 재현한다.
- T-024의 AI·RAG·Evidence·업무 행위 전체 추적성을 별도 구현한다.

### 3.3 가상 로그인·역할 기반 접근 제어 — 80%

완료된 내용:

- Demo Login·Refresh·Logout·Me Runtime, JWT 정책, 역할 Seed와 권한 테스트가 있다.
- 문의 생성·취소의 고객 소유권과 Public UUID 경계를 검증한다.
- `accounts.User`는 내부 정수 PK와 공개 UUID를 분리하고 Legacy 문자열 JWT Subject를 거부한다.
- T-017A 계정 단일 원장·역할·비활성화·삭제 제한·감사 경계를
  OWNER 설계로 정리했다.

수행 근거: [계정 API](../../../backend/apps/accounts/api/views.py),
[계정 권한](../../../backend/apps/accounts/permissions.py),
[Auth API 테스트](../../../backend/tests/unit/accounts/test_auth_api.py),
[최지용 문서 허브](README.md)

남은 내용과 완료 방법:

- 윤승혁의 정책 리뷰와 김은진의 Migration·QA 리뷰를 받는다.
- T-017B Django Admin과 T-017C Lifecycle·Audit를 구현한다.
- 상담사 배정 문의와 기사 배정 방문에 대한 객체 단위 접근 API·테스트를 실제 Runtime과 연결한다.
- 고객 IDOR, 미배정 기사, 운영자·상담사 교차 접근을 상담·방문 Endpoint에서 검증한다.
- Web·Mobile에서 실제 토큰 만료·Refresh·Logout E2E를 수행한다.

### 3.4 문의 생성·증상 제출·추가 입력 누적 API — 50%

완료된 내용:

- `POST /api/v1/inquiries`가 Public UUID·멱등성·초기 이력과 함께 구현됐다.
- 문의 생성 정상·Replay·오류 계약과 예시가 있다.
- `SUBMIT_SYMPTOM`의 State·권한·버전·멱등성 Guard와 Slice A/B 경계를
  설계했다.

수행 근거: [문의 API View](../../../backend/apps/inquiries/api/views.py),
[문의 Service](../../../backend/apps/inquiries/services/inquiry_service.py),
[T-022 API 테스트](../../../backend/tests/api/test_t022_create_inquiry.py),
[최지용 문서 허브](README.md)

남은 내용과 완료 방법:

- OpenAPI Method·Path·Request/Response와 ADHOC Projection을 먼저 확정한다.
- 이후 `SUBMIT_SYMPTOM: DRAFT → QUESTIONNAIRE_IN_PROGRESS`
  Service·Serializer·Route·409·멱등성 테스트를 Slice A로 구현한다.
- OpenAPI-only인 문진 누적과 조치 결과 API를 Runtime으로 연결한다.
- AI Effect는 durable dispatch 계약을 확정한 뒤 Slice B로 분리한다.

### 3.5 State Machine 행동별 API와 상태 이력 — 62%

완료된 내용:

- 계약 Loader·Validator·State Machine·Guard·Allowed Action Resolver가 있다.
- 문의 취소는 소유권·state_version·Idempotency-Key·Replay·충돌·상태 이력을 처리한다.
- Workflow 이력과 멱등성 Model·Migration이 있다.
- 상태 이력 필드·인덱스·계약명이 활성 State Machine 계약에 맞게 정렬됐고 PostgreSQL 회귀가 기록됐다.

수행 근거: [State Machine Engine](../../../backend/apps/workflow/engine/state_machine.py),
[Allowed Action Resolver](../../../backend/apps/workflow/engine/allowed_action_resolver.py),
[문의 전이 Service](../../../backend/apps/inquiries/services/inquiry_transition_service.py),
[T-023 취소 API 테스트](../../../backend/tests/api/test_t023_cancel_inquiry.py)

남은 내용과 완료 방법:

- 증상 제출 뒤 `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `PRODUCT_VALIDATION_FAILED`를 Backend가 재검증해 전이하는 Service를 구현한다.
- 상담 시작·완료·방문 검토·일정·방문 완료·최종 완료 Action을 작은 Wave로 추가한다.
- PostgreSQL 동시 요청에서 row lock·state_version·Idempotency 충돌을 검증한다.

### 3.6 API 계약·테스트 자료·산출물 — 88%

완료된 내용:

- OpenAPI 9개, Runtime 7개, OpenAPI-only 2개의 경계가 문서화됐다.
- Auth·오류·문의·Workflow 정상/오류/Replay JSON 예시 22개가 있다.
- Backend 실행·DB·API·팀 인계 문서가 통합·정리됐다.
- WaterBridge 전환·32/13/19·PostgreSQL·Seed·Importer·회귀 결과가
  최신 검증 보고서에 기록됐다.

수행 근거: [OpenAPI 계약](../../../contracts/api/openapi.yaml),
[OpenAPI–Runtime Coverage 테스트](../../../backend/tests/api/test_openapi_runtime_coverage.py),
[최지용 문서 허브](README.md),
[팀 인계 허브](../../handoffs/README.md)

남은 내용과 완료 방법:

- 상담·방문 API 계약과 Runtime을 같은 변경 단위로 추가하고 Web·Mobile 소비 결과를 받는다.
- Backend AI Client Placeholder를 실제 AI Runtime 계약에 연결한다.
- 비작성자·외부 소비자·PM 검토 증거를 병합 후보 SHA에 연결한다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 | 담당·협업 |
| --- | --- | --- |
| T-005 독립 재현 | 작성자 DB·Container·Volume 없이 새 PostgreSQL에서 32/32·Seed·Replay·전체 회귀 | 지정 비작성자·최지용 |
| T-005 공식 완료 | 독립 재현 증거와 병합 후보 SHA 승인 | 윤승혁 PM·김은진 |
| T-017A | 계정 정책·Migration·QA 결정 | 윤승혁·김은진·최지용 |
| T-022 | OpenAPI submit 동작·ADHOC Projection·AI dispatch 확정 | 최지용·윤승혁·김은진·이동윤 |
| 권한·소비 | Web·Mobile의 UUID·401·403·409·422·내부 PK 비노출 | 한예나·양정현·최지용 |
| AI 연동 | AI Runtime SHA·Timeout·Fallback·Evidence Schema | 이동윤·최지용·김은진 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 완료

- [x] T-005 32개 물리 계약과 Model·Migration 정합을 검증했다.
- [x] PostgreSQL 미적용 Migration 0·drift 0을 확인했다.
- [x] Seed 2회차 비의도 신규 생성 0을 확인했다.
- [x] 격리 Importer 실행·Replay를 검증했다.
- [x] 기본 DB를 `waterbridge`, Schema를 `public`으로 정합화했다.
- [x] Active 13개 369행과 Target-only 19개 0행을 확인했다.
- [x] SQLite 740건·PostgreSQL 751건·Data 67건을 통과했다.
- [x] Health·Login·Me·Refresh·Replay·Logout Smoke를 통과했다.
- [x] `state_version`·멱등성·상태 이력을 검증했다.
- [x] 현재 로그의 Token·PII·고객 원문 비노출을 검증했다.
- [x] T-017A와 T-022의 구현 전 Gate를 문서화했다.
- [x] 결과를 `jiyong`에 게시하고 원격 SHA를 확인했다.

### 5.2 미완료·대기

- [ ] T-005를 비작성자가 새 PostgreSQL에서 독립 재현한다.
- [ ] PM이 T-005 공식 완료와 `main` 병합을 승인한다.
- [ ] T-017A 정책·Migration·QA 리뷰를 완료한다.
- [ ] 상담사·기사 객체 권한과 고객 IDOR를 Runtime에서 검증한다.
- [ ] `SUBMIT_SYMPTOM` OpenAPI와 ADHOC Projection을 확정한다.
- [ ] `SUBMIT_SYMPTOM` 정상·401/403/404/409/422·Replay·동시성을 구현한다.
- [ ] 추가 답변 누적·중복 질문 방지·AI 실패 입력 보존을 구현한다.
- [ ] 상담·방문·최종 완료 Action을 작은 Wave로 구현한다.
- [ ] T-024 AI·RAG·업무 감사 전체 추적성을 구현한다.
- [ ] Web·Mobile·AI 소비 E2E를 수행한다.

## 6. 다음 작업

| 우선 | 다음 작업 | 주담당 | 협업·리뷰 | 완료 증거 |
| ---: | --- | --- | --- | --- |
| 1 | T-017A 설계 리뷰 | 윤승혁·김은진 | 최지용 | 정책·Migration·QA 결정 |
| 2 | T-022 Slice A 계약 Gate | 최지용 | 윤승혁·김은진 | OpenAPI·ADHOC Projection |
| 3 | `SUBMIT_SYMPTOM` Slice A | 최지용 | PM·Data QA | 정상·권한·409·422·Replay·동시성 |
| 4 | T-005 독립 재현 | 지정 비작성자 | 최지용 지원 | 새 DB 32/32·Seed·Replay·PG 전체 |
| 5 | T-005 공식 완료 | 윤승혁 PM | 최지용·김은진 | 승인 기록·병합된 `main` SHA |
| 6 | T-017B Admin | 최지용 | 윤승혁·김은진 | 권한·CSRF·삭제 차단·회귀 |
| 7 | T-017C Lifecycle·Audit | 최지용 | 김은진 | Token 폐기·동시성·감사 |
| 8 | T-022 Slice B AI Effect | 최지용·이동윤 | PM·Data QA | Commit 이후 1회 호출·stale 차단 |
| 9 | 소비 E2E | 한예나·양정현·이동윤 | 최지용 | UUID·401/403·409/422·Evidence |

## 7. Git 게시 상태

| 항목 | 값 |
| --- | --- |
| Branch | `jiyong` |
| 게시된 원격 HEAD | `1a44462ef4095a7fbfe9d795e04e5dc69e2e95e4` |
| 포함한 `origin/main` | `21e196637bb4bc41ab19248f265967ae0e2da28e` |
| 게시 시 main 대비 | 4 ahead / 0 behind |
| 게시 시 원격 jiyong 대비 | 0 / 0 |
| 게시 시 작업트리 | clean |
| PR #10 | open·draft·unmerged |

| Commit | 메시지 |
| --- | --- |
| `35671c4` | `2026-07-31 \| WaterBridge PostgreSQL 기본 DB 전환` |
| `1a44462` | `2026-07-31 \| WaterBridge 검증 및 Backend 후속 설계 문서화` |

게시 후 로컬에는 문서 이름 정리·통합·삭제 변경과 이 진행도 갱신이
추가됐다. 따라서 **현재 로컬 작업트리는 clean이 아니다.** 이 후속
문서 변경은 위 게시 SHA에 포함되지 않았다.

## 8. 도미노 오류 방지 규칙

1. `waterbridge`는 Database, `public`은 Schema다.
2. Active 13을 위해 별도 Schema나 축소 Migration을 만들지 않는다.
3. Target-only 19를 삭제·Truncate·역Migration하지 않는다.
4. 기본 `waterbridge`에서 Importer와 `--dry-run`을 실행하지 않는다.
5. State·공개 ID·오류 코드를 계약 승인 없이 병렬 변경하지 않는다.
6. `SUBMIT_SYMPTOM`은 OpenAPI·Projection 뒤 Slice A부터 구현한다.
7. DB Transaction 안에서 AI HTTP 호출을 실행하지 않는다.
8. T-017A 설계를 T-017B/C Runtime 완료로 해석하지 않는다.
9. Schema·설계·Runtime·팀 승인 Gate를 구분한다.
10. 팀원은 PM이 공유한 병합 후 `main` SHA에서 소비 검증한다.

## 9. 최신 근거 문서

- [팀 인계 허브](../../handoffs/README.md)
- [T-005 계약](../../database/t-005/README.md)
- [최지용 문서 허브](README.md)
- [Backend README](../../../backend/README.md)
- [OpenAPI](../../../contracts/api/openapi.yaml)
- [DB 저장소 설계](../../submission/database-storage-design.md)
