# 최지용 3주차 작업 진행도

> 최초 작성: 2026-07-29 16:35 KST
>
> 최종 갱신: 2026-07-30 11:34 KST
>
> 기준 Commit: `f4149cf` (`main`)
> 참고: `docs/weekly-task/최지용_3주차_업무_지침서.md`, `docs/api/runtime_implementation_status.md`, `docs/database/t-005/README.md`, `docs/handoffs/README.md`

> 대체된 역사 스냅샷: 이 문서는 2026-07-30 11:34 기준 진행도다.
> 현재 T-005 Runtime·회귀 판정은
> [T-005 32개 테이블 최종 검증 보고서](technical/backend/t005_final_32_table_postgresql_seed_importer_validation_report.md)의
> `READY 32/32`, SQLite 740·PostgreSQL 751 결과를 우선한다.

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 현재 근거 |
|---|---:|---|---|
| 3.1 ERD·Model·Migration·DB 설계 | 65% | 부분 완료 | 설계 계약은 상세하지만 T-005 Runtime은 10/32이며 제출용 DB 문서가 최신 상태와 다름 |
| 3.2 Django·DRF 공통 구조·실행 환경 | 90% | 대부분 완료 | 환경별 설정·공통 응답·Health·재현 가이드와 Backend 397건 통과 기록이 있음 |
| 3.3 가상 로그인·역할 기반 접근 제어 | 80% | 부분 완료 | Auth Runtime 4개와 권한 기반이 있으나 상담·기사 객체 권한의 실제 API 수직 검증은 부족함 |
| 3.4 문의 생성·증상 제출·입력 누적 | 45% | 부분 완료 | 문의 생성 Runtime은 있으나 증상 제출·문진 누적·조치 결과는 미구현 또는 OpenAPI-only임 |
| 3.5 State Machine 행동 API·상태 이력 | 55% | 부분 완료 | 계약 Engine·Guard·이력·멱등성과 취소 API는 있으나 나머지 행동 Runtime이 없음 |
| 3.6 API 계약·테스트·산출물 | 75% | 부분 완료 | OpenAPI 9, Runtime 7, 예시 22, 인계 문서가 있으나 공식 산출물과 팀 실제 소비 검증이 남음 |

종합 완료도는 **약 68%**다. Backend 공통 기반·Auth·문의 생성·취소·합성 Import는 검증 기록이 충분하다. 그러나 P0 흐름을 여는 증상 제출과 상담·방문 Action API, T-005 잔여 22개, AI Client가 남아 있다.

## 2. 현재 검증 경계

- 기록된 전체 Backend 회귀: `397 passed`(SQLite 테스트 설정).
- OpenAPI 9개 중 Django Runtime 7개, OpenAPI-only 2개다.
- T-005 계약 테이블은 32개 중 10개 구현, 22개 미구현으로 `NOT_READY`다.
- 합성 Handoff 367행은 빈 격리 PostgreSQL에서 Import·Replay가 검증됐다.
- 이번 점검 환경에는 `backend/.venv/Scripts/python.exe`가 없어 397건을 독립 재실행하지 못했다. 저장된 실행 기록과 인계 문서를 근거로 판정했다.

## 3. 필수 업무별 평가

### 3.1 ERD·Django Model·Migration과 DB 설계 문서 — 65%

완료된 내용:

- 32개 테이블 논리·물리 계약, ERD, 식별자·상태 이력·멱등성 ADR이 있다.
- 문의·상담·방문·감사·운영·Workflow 등 Runtime Wave가 추가돼 10/32까지 구현됐다.
- 격리 PostgreSQL의 Migration·합성 Import·Replay 증거가 있다.

수행 근거: [T-005 DB 계약](<../../../docs/database/t-005/README.md>), [Django Inquiry Model](<../../../backend/apps/inquiries/models/inquiry.py>), [Inquiry Migration](<../../../backend/apps/inquiries/migrations/0001_initial.py>), [PostgreSQL 적재 검증](<manuals/20260729_postgresql_synthetic_handoff_runtime_verification.md>)

남은 내용과 완료 방법:

- 잔여 22개를 공통 코드·문의·안내·문진 Wave와 AI·Knowledge·Evidence Wave로 나눠 `Model → Migration → Test → PostgreSQL → 문서` 순서로 구현한다.
- `workflow_transition_history`의 T-005 편입 여부와 나머지 운영 보조 테이블 분류를 PM 결정에 맞춰 계약에 반영한다.
- `docs/submission/database-storage-design.md`에 T-005 10/32, 격리 DB 검증, 운영 DB 미검증, Vector 미완료를 정확히 기록한다.

### 3.2 Django·DRF 공통 구조와 실행 환경 — 90%

완료된 내용:

- 환경별 설정, 공통 오류·추적 ID·로그, Health, 테스트 설정과 실행 가이드가 있다.
- 397건 회귀와 Django System Check 통과 기록이 있다.
- PostgreSQL 공유 패키지·Migration·Seed·백업 절차 v1.3이 인계됐다.

수행 근거: [Backend README](<../../../backend/README.md>), [Health API](<../../../backend/common/api/health.py>), [Correlation ID Middleware](<../../../backend/common/middleware/correlation_id.py>), [PostgreSQL 공유 패키지 인계서](<manuals/20260729_최지용_Django_PostgreSQL_공유패키지_인계서_v1.3.md>)

남은 내용과 완료 방법:

- 팀 공용 환경에 재현 가능한 Backend 가상환경을 준비하고 최종 `main`에서 `397 passed`를 한 번 재현한다.
- 로그의 토큰·개인정보·고객 원문 비노출을 별도 로그 샘플로 검수한다.

### 3.3 가상 로그인·역할 기반 접근 제어 — 80%

완료된 내용:

- Demo Login·Refresh·Logout·Me Runtime, JWT 정책, 역할 Seed와 권한 테스트가 있다.
- 문의 생성·취소의 고객 소유권과 Public UUID 경계를 검증한다.

수행 근거: [계정 API](<../../../backend/apps/accounts/api/views.py>), [계정 권한](<../../../backend/apps/accounts/permissions.py>), [문의 권한](<../../../backend/apps/inquiries/permissions.py>), [Auth API 테스트](<../../../backend/tests/unit/accounts/test_auth_api.py>)

남은 내용과 완료 방법:

- 상담사 배정 문의와 기사 배정 방문에 대한 객체 단위 접근 API·테스트를 실제 Runtime과 연결한다.
- 고객 IDOR, 미배정 기사, 운영자·상담사 교차 접근을 상담·방문 Endpoint에서 검증한다.
- Web·Mobile에서 실제 토큰 만료·Refresh·Logout E2E를 수행한다.

### 3.4 문의 생성·증상 제출·추가 입력 누적 API — 45%

완료된 내용:

- `POST /api/v1/inquiries`가 Public UUID·멱등성·초기 이력과 함께 구현됐다.
- 문의 생성 정상·Replay·오류 계약과 예시가 있다.

수행 근거: [문의 API View](<../../../backend/apps/inquiries/api/views.py>), [문의 Service](<../../../backend/apps/inquiries/services/inquiry_service.py>), [문의 생성 Serializer](<../../../backend/apps/inquiries/api/serializers/create_inquiry.py>), [T-022 API 테스트](<../../../backend/tests/api/test_t022_create_inquiry.py>)

남은 내용과 완료 방법:

- 우선 `SUBMIT_SYMPTOM: DRAFT → QUESTIONNAIRE_IN_PROGRESS` Service·Serializer·Route·409·멱등성 테스트를 구현한다.
- OpenAPI-only인 문진 누적과 조치 결과 API를 Runtime으로 연결한다.
- 추가 답변 누적, 중복 질문 방지, AI 실패 시 입력 보존을 통합 테스트한다.

### 3.5 State Machine 행동별 API와 상태 이력 — 55%

완료된 내용:

- 계약 Loader·Validator·State Machine·Guard·Allowed Action Resolver가 있다.
- 문의 취소는 소유권·state_version·Idempotency-Key·Replay·충돌·상태 이력을 처리한다.
- Workflow 이력과 멱등성 Model·Migration이 있다.

수행 근거: [State Machine Engine](<../../../backend/apps/workflow/engine/state_machine.py>), [Allowed Action Resolver](<../../../backend/apps/workflow/engine/allowed_action_resolver.py>), [문의 전이 Service](<../../../backend/apps/inquiries/services/inquiry_transition_service.py>), [T-023 취소 API 테스트](<../../../backend/tests/api/test_t023_cancel_inquiry.py>)

남은 내용과 완료 방법:

- 증상 제출 뒤 `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `PRODUCT_VALIDATION_FAILED`를 Backend가 재검증해 전이하는 Service를 구현한다.
- 상담 시작·완료·방문 검토·일정·방문 완료·최종 완료 Action을 작은 Wave로 추가한다.
- PostgreSQL 동시 요청에서 row lock·state_version·Idempotency 충돌을 검증한다.

### 3.6 API 계약·테스트 자료·산출물 — 75%

완료된 내용:

- OpenAPI 9개, Runtime 7개, OpenAPI-only 2개의 경계가 문서화됐다.
- Auth·오류·문의·Workflow 정상/오류/Replay JSON 예시 22개가 있다.
- 담당자별 인계 문서와 Backend 실행·DB·API 가이드가 상세히 준비됐다.

수행 근거: [OpenAPI 계약](<../../../contracts/api/openapi.yaml>), [OpenAPI–Runtime Coverage 테스트](<../../../backend/tests/api/test_openapi_runtime_coverage.py>), [API 계약 인계 가이드](<technical/backend/api_contract_handover_guide.md>), [팀 인계 문서](<team_handover/20260729_최지용_윤승혁PM_인계및요청사항.md>)

남은 내용과 완료 방법:

- 상담·방문 API 계약과 Runtime을 같은 변경 단위로 추가하고 Web·Mobile 소비 결과를 받는다.
- Backend AI Client Placeholder를 실제 AI Runtime 계약에 연결한다.
- 제출 문서의 오래된 DB 판정과 테스트 수치를 최신화하고 공동 검수한다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 |
|---|---|
| Backend 환경 | 다른 팀원 PC에서 설치·Migration·Seed·397건 재현 |
| PostgreSQL | 격리 DB와 운영/개발 DB 구분, Extension·권한·백업 복원 |
| 보안 | 로그·응답의 Token·PII·고객 원문 노출 여부 |
| API 소비 | Web·Mobile 실제 401·403·409·422 처리 |
| AI 연동 | AI Commit·실행 명령·Timeout·Fallback·Schema 확정 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 현재 완료로 체크해도 되는 항목

- [x] 빈 격리 PostgreSQL에서 현재 구현 Migration과 합성 Import가 성공한 증거가 있다.
- [x] Seed·합성 Import를 반복해도 예상하지 않은 중복이 발생하지 않는다.
- [x] Backend 서버와 `/health` 실행 기록이 있다.
- [x] 공통 성공·오류·페이지네이션·`correlation_id` 구조가 동작한다.
- [x] 가상 로그인과 현재 사용자 조회가 동작한다.
- [x] 문의 생성 Runtime이 동작한다.
- [x] `state_version` 충돌과 `idempotency_key` 중복이 검증된다.
- [x] 상태 전이 이력이 이전·다음 상태·이벤트·수행자와 함께 저장된다.
- [x] 인증·문의 생성·취소의 정상·오류·Replay 예시가 있다.
- [x] 역할별 테스트 계정과 Seed·실행 절차가 제공돼 있다.
- [x] OpenAPI·예시와 현재 Runtime 7개 응답의 경계가 정리됐다.
- [x] 미완료 기능과 다음 Wave가 인계 문서에 기록됐다.

### 5.2 아직 완료로 체크하면 안 되는 항목

- [ ] T-005 32개 테이블 전체의 ERD·Model·Migration이 일치한다.
- [ ] 증상 제출과 추가 답변 누적 Runtime이 동작한다.
- [ ] 상담·기사 객체 권한과 IDOR가 전체 Runtime에서 검증됐다.
- [ ] AI 호출 전 제품·필수값 검증과 AI 결과 전이가 연결됐다.
- [ ] State Machine 대표 14단계 Action API가 실제로 실행된다.
- [ ] Web·Mobile·AI와 동일 Commit E2E가 통과했다.
- [ ] 공식 DB·저장소 설계 문서가 최신 수치로 공동 승인됐다.
- [ ] 최종 `main`에서 다른 팀원이 Backend 환경과 397건을 재현했다.

## 6. 다음 작업

1. PM에게 T-005 계약 밖 테이블 분류와 다음 Action 결정을 받는다.
2. `SUBMIT_SYMPTOM` Runtime을 첫 번째 수직 Slice로 구현한다.
3. AI Runtime 인계를 받아 Backend AI Client와 네 결과 이벤트를 연결한다.
4. T-005 잔여 22개를 두 Wave로 진행한다.
5. 실제 Web·Mobile 소비와 동일 Commit E2E를 수행한다.
