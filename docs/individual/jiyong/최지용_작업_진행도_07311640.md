# 최지용 3주차 작업 진행도

> 최초 작성: 2026-07-29 16:35 KST
>
> 최종 갱신: 2026-07-31 16:40 KST
>
> 기준 Commit: `0ce08d8` (`main`)
> 참고: `docs/weekly-task/최지용_3주차_업무_지침서.md`, `docs/api/runtime_implementation_status.md`, `docs/database/t-005/README.md`, `docs/handoffs/README.md`

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 현재 근거 |
|---|---:|---|---|
| 3.1 ERD·Model·Migration·DB 설계 | 97% | 대부분 완료 | T-005 물리 계약 v1.3과 32/32 Runtime·PostgreSQL 기술 검증이 확정됐고 비작성자·PM 완료 리뷰만 남음 |
| 3.2 Django·DRF 공통 구조·실행 환경 | 97% | 대부분 완료 | 빈 PostgreSQL 전체 Migration·5종 Seed·367건 Import와 Backend 740/751건 회귀 기록이 있음 |
| 3.3 가상 로그인·역할 기반 접근 제어 | 82% | 부분 완료 | 정수 PK·공개 UUID·UUID-only JWT까지 반영됐으나 상담·기사 객체 권한의 실제 API 수직 검증은 부족함 |
| 3.4 문의 생성·증상 제출·입력 누적 | 50% | 부분 완료 | 문진·증상·QA·안내·조치 결과 저장 구조는 구현됐지만 증상 제출과 입력 누적 Runtime API는 미완료임 |
| 3.5 State Machine 행동 API·상태 이력 | 62% | 부분 완료 | 상태 이력 계약·Migration과 취소 API는 보강됐지만 상담·방문을 포함한 나머지 행동 Runtime이 없음 |
| 3.6 API 계약·테스트·산출물 | 90% | 대부분 완료 | T-005 v1.3·작성자 격리 Evidence와 인계 기준이 보강됐으나 실제 AI·Web·Mobile 소비와 공식 산출물 공동 검수가 남음 |

종합 완료도는 **약 80%**다. T-005 물리 계약 v1.3에 32/32 Runtime과 3계층 식별자 기술 완료가 기록됐다. 다만 새 Evidence도 작성자 격리 재현이므로 비작성자 독립 재현·PM 승인 전 공식 완료로 표시하면 안 된다. 증상 제출, 상담·방문 Action API와 실동작 Backend AI Client도 아직 남아 있다.

## 2. 현재 검증 경계

- 최신 기록된 Backend 전체 회귀: SQLite `740 passed, 11 skipped`, PostgreSQL `751 passed`.
- Data 단위 테스트 `67 tests, OK`, 대표 E2E 17/17, 소스 Hash `changed=0`가 기록됐다.
- OpenAPI 9개 중 Django Runtime 7개, OpenAPI-only 2개다.
- T-005 계약 테이블은 Model·App Registry·번호 Migration 기준 `32/32`, Auditor `READY`, 구현 차단 0건이다.
- 빈 PostgreSQL 전체 Migration, 5종 Seed 2회와 합성 Handoff 367행 Import·Replay가 검증됐다.
- pgvector `0.8.6`, `vector(1024)` 저장과 Exact Search가 확인됐으며 ANN Index는 아직 없다.
- 물리 계약 v1.3은 `TECHNICALLY_COMPLETE_REVIEW_PENDING`, `completion_claim_allowed=false`이며 작성자 격리 Evidence도 비작성자 리뷰를 대체하지 않는다.
- 이번 점검 환경에는 `backend/.venv/Scripts/python.exe`와 `pytest`가 없어 740/751건을 독립 재실행하지 못했다. 저장된 실행 결과와 구현·검증 보고서를 근거로 판정했다.

## 3. 필수 업무별 평가

### 3.1 ERD·Django Model·Migration과 DB 설계 문서 — 97%

완료된 내용:

- 32개 테이블 논리·물리 계약, ERD, 식별자·상태 이력·멱등성 ADR이 있다.
- 32개 계약 테이블 모두 Django Model로 선언되고 App Registry와 번호 Migration에 연결됐다.
- 문진 세션·증상 평가·문의 QA·안내·상태 이력·AI 실행·검색·문서·페이지·청크·Vector·근거 연결 구조가 반영됐다.
- 완전히 빈 PostgreSQL에서 전체 Migration, 5종 Seed 2회, 367건 Import·Replay를 검증했다.
- Accounts 내부 정수 PK와 외부 공개 UUID를 분리하고 JWT `sub`를 UUID로 전환했다.

수행 근거: [T-005 DB 계약](<../../../docs/database/t-005/README.md>), [32개 테이블 최종 검증 보고서](<technical/backend/t005_final_32_table_postgresql_seed_importer_validation_report.md>), [DB Schema 인계 가이드](<technical/backend/database_schema_handover_guide.md>), [EvidenceLink Model](<../../../backend/apps/evidence/models/evidence_link.py>)

남은 내용과 완료 방법:

- 비작성자가 동일 SHA에서 빈 PostgreSQL·Seed·회귀를 독립 재현하고 Evidence를 남긴다.
- PM·계약 담당자가 3계층 식별자 전환과 불변 물리 계약의 완료 상태를 승인한다.
- `docs/submission/database-storage-design.md`에 32/32, 실제 PostgreSQL 건수, pgvector·Seed·Importer 경계와 검증 결과를 반영해 공동 검수한다.

### 3.2 Django·DRF 공통 구조와 실행 환경 — 97%

완료된 내용:

- 환경별 설정, 공통 오류·추적 ID·로그, Health, 테스트 설정과 실행 가이드가 있다.
- SQLite 740건과 PostgreSQL 751건 회귀, Django System Check와 Migration Drift 0건이 기록됐다.
- 기본 `watercare` DB를 백업한 뒤 미적용 Migration 24개·Seed·367건을 동기화하고 보존 건수를 확인했다.

수행 근거: [Backend README](<../../../backend/README.md>), [Health API](<../../../backend/common/api/health.py>), [Correlation ID Middleware](<../../../backend/common/middleware/correlation_id.py>), [PostgreSQL 공유 패키지 인계서](<manuals/20260729_최지용_Django_PostgreSQL_공유패키지_인계서_v1.3.md>)

남은 내용과 완료 방법:

- 다른 팀원이 최종 `main`에서 환경 생성→Migration→Seed→전체 회귀를 독립 재현한다.
- 로그의 토큰·개인정보·고객 원문 비노출을 별도 로그 샘플로 검수한다.

### 3.3 가상 로그인·역할 기반 접근 제어 — 80%

완료된 내용:

- Demo Login·Refresh·Logout·Me Runtime, JWT 정책, 역할 Seed와 권한 테스트가 있다.
- 문의 생성·취소의 고객 소유권과 Public UUID 경계를 검증한다.
- `accounts.User`는 내부 정수 PK와 공개 UUID를 분리하고 Legacy 문자열 JWT Subject를 거부한다.

수행 근거: [계정 API](<../../../backend/apps/accounts/api/views.py>), [계정 권한](<../../../backend/apps/accounts/permissions.py>), [문의 권한](<../../../backend/apps/inquiries/permissions.py>), [Auth API 테스트](<../../../backend/tests/unit/accounts/test_auth_api.py>)

남은 내용과 완료 방법:

- 상담사 배정 문의와 기사 배정 방문에 대한 객체 단위 접근 API·테스트를 실제 Runtime과 연결한다.
- 고객 IDOR, 미배정 기사, 운영자·상담사 교차 접근을 상담·방문 Endpoint에서 검증한다.
- Web·Mobile에서 실제 토큰 만료·Refresh·Logout E2E를 수행한다.

### 3.4 문의 생성·증상 제출·추가 입력 누적 API — 50%

완료된 내용:

- `POST /api/v1/inquiries`가 Public UUID·멱등성·초기 이력과 함께 구현됐다.
- 문의 생성 정상·Replay·오류 계약과 예시가 있다.
- QuestionnaireSession·SymptomAssessment·InquiryQA·Guidance·GuidanceItem·CustomerActionResult의 Model·Migration이 구현됐다.

수행 근거: [문의 API View](<../../../backend/apps/inquiries/api/views.py>), [문의 Service](<../../../backend/apps/inquiries/services/inquiry_service.py>), [문의 생성 Serializer](<../../../backend/apps/inquiries/api/serializers/create_inquiry.py>), [T-022 API 테스트](<../../../backend/tests/api/test_t022_create_inquiry.py>)

남은 내용과 완료 방법:

- 우선 `SUBMIT_SYMPTOM: DRAFT → QUESTIONNAIRE_IN_PROGRESS` Service·Serializer·Route·409·멱등성 테스트를 구현한다.
- OpenAPI-only인 문진 누적과 조치 결과 API를 Runtime으로 연결한다.
- 추가 답변 누적, 중복 질문 방지, AI 실패 시 입력 보존을 통합 테스트한다.

### 3.5 State Machine 행동별 API와 상태 이력 — 62%

완료된 내용:

- 계약 Loader·Validator·State Machine·Guard·Allowed Action Resolver가 있다.
- 문의 취소는 소유권·state_version·Idempotency-Key·Replay·충돌·상태 이력을 처리한다.
- Workflow 이력과 멱등성 Model·Migration이 있다.
- 상태 이력 필드·인덱스·계약명이 활성 State Machine 계약에 맞게 정렬됐고 PostgreSQL 회귀가 기록됐다.

수행 근거: [State Machine Engine](<../../../backend/apps/workflow/engine/state_machine.py>), [Allowed Action Resolver](<../../../backend/apps/workflow/engine/allowed_action_resolver.py>), [문의 전이 Service](<../../../backend/apps/inquiries/services/inquiry_transition_service.py>), [T-023 취소 API 테스트](<../../../backend/tests/api/test_t023_cancel_inquiry.py>)

남은 내용과 완료 방법:

- 증상 제출 뒤 `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `PRODUCT_VALIDATION_FAILED`를 Backend가 재검증해 전이하는 Service를 구현한다.
- 상담 시작·완료·방문 검토·일정·방문 완료·최종 완료 Action을 작은 Wave로 추가한다.
- PostgreSQL 동시 요청에서 row lock·state_version·Idempotency 충돌을 검증한다.

### 3.6 API 계약·테스트 자료·산출물 — 90%

완료된 내용:

- OpenAPI 9개, Runtime 7개, OpenAPI-only 2개의 경계가 문서화됐다.
- Auth·오류·문의·Workflow 정상/오류/Replay JSON 예시 22개가 있다.
- 담당자별 인계 문서와 Backend 실행·DB·API 가이드가 상세히 준비됐다.
- T-005 32개 구현 Wave와 PostgreSQL·Seed·Importer·회귀 결과가 최종 검증 보고서에 실제 건수로 기록됐다.

수행 근거: [OpenAPI 계약](<../../../contracts/api/openapi.yaml>), [OpenAPI–Runtime Coverage 테스트](<../../../backend/tests/api/test_openapi_runtime_coverage.py>), [API 계약 인계 가이드](<technical/backend/api_contract_handover_guide.md>), [팀 인계 문서](<team_handover/20260729_최지용_윤승혁PM_인계및요청사항.md>)

남은 내용과 완료 방법:

- 상담·방문 API 계약과 Runtime을 같은 변경 단위로 추가하고 Web·Mobile 소비 결과를 받는다.
- Backend AI Client Placeholder를 실제 AI Runtime 계약에 연결한다.
- 제출 문서의 오래된 DB 판정과 테스트 수치를 최신화하고 공동 검수한다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 |
|---|---|
| Backend 환경 | 다른 팀원 PC에서 설치·Migration·Seed·740/751건 재현 |
| PostgreSQL | 작성자 검증과 독립 검증 구분, Extension·권한·백업·복원·Rollback |
| 보안 | 로그·응답의 Token·PII·고객 원문 노출 여부 |
| API 소비 | Web·Mobile 실제 401·403·409·422 처리 |
| AI 연동 | AI Commit·실행 명령·Timeout·Fallback·Schema 확정 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 7월 29일 필수 완료 기준

- [x] ERD·Django Model·Migration·DB 설계 문서의 테이블과 관계가 일치한다.
- [x] 빈 PostgreSQL에서 전체 Migration이 성공한다.
- [x] Seed를 반복 실행해도 예상하지 않은 중복이 발생하지 않는다.
- [x] 누락된 문진 세션·문의 입력·상태 이력·근거 연결 구조가 반영되어 있다.
- [x] Backend 서버와 `/health`가 실행된다.
- [x] 공통 성공·오류·페이지네이션·`correlation_id` 구조가 동작한다.
- [x] 가상 로그인과 현재 사용자 조회가 동작한다.
- [ ] 고객·상담사·기사·운영 역할과 객체 단위 권한 검사가 있다.
- [ ] 고객 IDOR와 기사 미배정 접근이 차단된다.
- [ ] 신규 문의 생성과 증상 제출이 동작한다.
- [ ] 같은 문의에 추가 입력·답변이 누적된다.
- [ ] 미지원 제품·필수값 누락·타 고객 요청이 AI 호출 전에 차단된다.
- [ ] State Machine 최소 이벤트와 `allowed_actions`가 동작한다.
- [x] `state_version` 충돌과 `idempotency_key` 중복이 검증된다.
- [x] 상태 전이 이력이 이전·다음 상태·이벤트·수행자와 함께 저장된다.
- [x] 인증·문의·상태 API의 정상·오류 예시가 작성되어 있다.
- [x] 역할별 테스트 계정과 Seed·실행 절차가 제공되어 있다.
- [x] 데이터베이스·저장소 설계 문서가 검토 가능한 상태로 작성되어 있다.
- [x] 7월 29일까지 검토 가능한 PR 또는 공유 파일이 있다.

### 5.2 7월 30일~31일 최종 정리 기준

- [ ] Web·Mobile·AI 담당자의 계약 검토 의견이 반영되었다.
- [x] OpenAPI·예시 JSON과 실제 Serializer 응답의 차이가 수정되었다.
- [ ] Migration 초기화·재적용·Seed 절차를 다른 팀원이 재현했다.
- [ ] 권한·중복 요청·상태 충돌·트랜잭션 실패 테스트가 보강되었다.
- [ ] 로그에 토큰·개인정보·고객 원문 전체가 노출되지 않는지 확인했다.
- [ ] Mock·미구현·미확정 기능이 Issue와 PR에 기록되어 있다.
- [ ] 두 공식 산출물에 실제 테이블·경로·수치·검증 결과가 반영되었다.
- [x] 미완료 기능과 4주차 진입 조건이 인계 문서에 기록되어 있다.
- [ ] 필수 업무와 리뷰 대응 완료 후에만 4장의 추가 업무를 시작했다.

## 6. 다음 작업

1. 비작성자에게 T-005 빈 PostgreSQL·Seed·회귀 독립 재현을 요청하고 Evidence를 연결한다.
2. PM·계약 담당자에게 3계층 식별자 전환과 T-005 공식 완료 승인을 요청한다.
3. `SUBMIT_SYMPTOM` Runtime을 첫 번째 수직 Slice로 구현한다.
4. 이동윤 AI Runtime과 Backend AI Client·네 결과 이벤트를 연결한다.
5. 실제 Web·Mobile 소비와 동일 Commit E2E를 수행한다.
