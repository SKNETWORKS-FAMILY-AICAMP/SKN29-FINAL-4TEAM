# 최지용 4주차 작업 진행도

> 작성 일시: **2026-08-07 10:09 KST**  
> 점검 기준 Commit: `a708a049484c30e09995c747faf3877bb3b36bb9` (`main`, `origin/main`)  
> 참고 지침서: [최지용 4주차 업무 지침서](../../weekly-task/최지용_4주차_업무_지침서.md)  
> 관할 기준: [팀원별 관할 영역 v2](<../../planning/md/팀원별 관할 영역 v2.md>)  
> 이전 점검: [최지용 4주차 작업 진행도 08042358](최지용_4주차_작업_진행도_08042358.md)

## 1. 판정 원칙과 점검 범위

- 최지용의 주관할인 `backend/**`, Backend DB Migration·Seed·Runtime API와 부관할인 `contracts/api/**`를 중심으로 확인했다.
- 파일·Model·Migration 존재만으로 완료 처리하지 않고, URL 등록→인증 요청→Service→DB 저장→상태·권한·멱등성·오류 Test가 연결됐는지를 기준으로 판정했다.
- 현재 기본 Python은 3.13.12이며 `pytest`·Django가 없고, 별도 Anaconda Python 3.13.9에는 Django가 없다. 따라서 이번 점검에서 Backend Test·Migration·Seed를 직접 재실행하지 못했다.
- 저장된 작성자 Test 결과는 해당 Commit의 과거 실행 증거로 인정하되, 현재 HEAD의 직접 PASS로 확대하지 않았다.
- 중간 발표 완료와 발표 관련 지원은 PM의 명시적 확인 및 [WaterBridge 발표자료](../../presentation/WaterBridge.pdf)를 반영했다. 다만 문서상 실패한 Backend Smoke·미구현 수직 흐름은 발표가 끝났다는 이유만으로 PASS 처리하지 않았다.

## 2. 종합 판정

| 4주차 필수 업무 | 완료도 | 판정 | 핵심 근거와 남은 Gate |
| --- | ---: | --- | --- |
| 3.1 Backend 기준선·Migration·Runtime 증빙 복구 | 60% | 부분 완료 | 기준 Commit, 회귀 결과, 과거 PostgreSQL·Migration 증거와 Runtime 상태표가 있다. 현재 HEAD 실행 환경 복구, DB Readiness, 전체 Migration·Seed 재실행과 안정 경로 기준선 문서가 남았다. |
| 3.2 제품·구독 최소 API (`T-018`) | 20% | 계약 완료·Runtime 미구현 | 구독 목록·상세 OpenAPI·Schema·Example·계약 Test가 병합됐다. 그러나 URL·View·Serializer·Service는 여전히 설명 문자열뿐이고 Runtime Endpoint가 없다. |
| 3.3 문의 증상 제출·추가 답변·자가조치 누적 (`T-022`) | 60% | 부분 완료 | 문의 생성·증상 제출, 상태 전이, 멱등 Replay·409와 이력 저장은 Runtime이다. 추가 답변·자가조치·AI 구조화 저장은 미구현이다. |
| 3.4 State Machine Event API Runtime (`T-023`) | 45% | 부분 완료 | START·SUBMIT·DRAFT 취소와 계약 기반 Guard·`allowed_actions`가 동작한다. AI 결과 Event·상담 요청 등 발표 필수 Event 전체는 연결되지 않았다. |
| 3.5 Backend↔AI 최소 수직 연동 | 10% | 미완료 | 계약과 협업 인계는 있으나 Backend Client·Mapper·Validator·조율 Service가 골격이고 실제 HTTP·DB 저장 E2E가 없다. |
| 3.6 케어 이력 최소 API (`T-019`) | 5% | 미착수에 가까움 | Model·Migration·Seed·단위 Model Test는 있으나 URL·View·Serializer·Service가 실행 코드가 아니며 API가 등록되지 않았다. |
| 3.7 중간 발표 Backend 지원·발표 후 인계 | 65% | 부분 완료 | 발표 기준 Commit, 기능 분류, 발표자료와 공통 인계는 있다. Backend 실행 환경 차단으로 Smoke가 통과하지 않았고 정적 DB 증빙·전용 5주차 인계가 부족하다. |

7개 절의 단순 평균 완료도는 **약 38%**다. 이전 점검 이후 T-018 기계 계약과 발표·통합 인계가 진전됐지만, 지침서의 핵심인 **제품·구독 Runtime, 입력 누적, Backend↔AI, 케어 API는 구현되지 않았다.**

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 Backend 기준선·Migration·Runtime 검증 증빙 복구 — 60%

완료된 내용:

- 발표 기준 Commit `e95e633d58324579a28bf7858fa8be1555ca1a09`과 현재 기준선·Known Failure·통합 인계 문서가 있다.
- 과거 PostgreSQL 전체 Migration·Seed·합성 Import 검증 보고서와 Data QA Crosswalk가 존재한다.
- T-018 작성자 환경에서 Backend 전체 `798 passed, 13 skipped`, Django System Check 문제 0, `makemigrations --check` 변경 없음이 기록됐다.
- OpenAPI는 23개 Operation을 Runtime 8개와 OpenAPI-only 15개로 분리해 문서화한다.
- 현재 `/health`가 DB Readiness가 아니라 빈 본문의 임시 Liveness라는 제한이 명시돼 있다.

수행 근거:

- [4주차 현재 기준선](../../planning/week4-current-baseline.md)
- [Main 회귀 검증 결과](../../testing/week4-main-regression-result.md)
- [Known Failures](../../testing/week4-known-failures.md)
- [T-018 작성자 수행보고](../../handoffs/t018/20260805_01_T018_Part4_Part5A_작성자수행보고.md)
- [PostgreSQL 통합 검증 보고서](technical/backend/20260731_t005_워터브리지_postgresql_통합_검증_보고서.md)
- [Backend Import Crosswalk](../../../data/config/handoff/backend_import_crosswalk.json)
- [Runtime 구현 상태표](../../api/runtime_implementation_status.md)
- [Health 구현](../../../backend/common/api/health.py)

완료되지 않은 내용:

- 현재 HEAD에서 요구 Python 3.13.13 환경을 만들지 못해 Backend 전체 Test·빈 PostgreSQL Migration·Seed를 재실행하지 못했다.
- 현재 `/health`는 Process 생존 여부만 보는 빈 `200`이며 DB 연결·필수 Migration을 검사하는 Readiness가 없다.
- `docs/testing/results/week4-backend-baseline.md`와 `docs/testing/evidence/backend/**`가 없다.
- Seed와 합성 Import를 현재 환경에서 2회 실행한 Replay·Upsert 결과가 없다.
- Crosswalk가 현재 HEAD의 신규 Backend 기준선 결과와 Hash를 참조하도록 갱신된 증거가 없다.
- 다른 팀원이 현재 README만으로 같은 기준선을 재현했다는 확인이 없다.

완료 방법:

1. Python 3.13.13과 Backend Lock 의존성을 설치한 재현 환경을 만든다.
2. 빈 PostgreSQL에서 `migrate`, `showmigrations`, `makemigrations --check`와 핵심 Table 검사를 실행한다.
3. 역할 Seed와 합성 Import를 두 번 실행해 Replay·Upsert·Row 수를 기록한다.
4. `/health/live`와 `/health/ready`를 분리하고 Readiness 실패 시 안전한 오류 코드와 503을 반환한다.
5. `docs/testing/results/week4-backend-baseline.md`에 Commit·환경·명령·Exit Code·Migration·Seed·Runtime·Test·제한사항을 기록한다.
6. 김은진과 Crosswalk의 증빙 경로·SHA-256을 갱신한다.

코드 외 별도 확인:

- 팀 PostgreSQL DSN·권한·pgvector Extension이 실제 공유됐는지 확인해야 한다.
- 김은진이 새 기준선 경로와 Crosswalk Hash를 Data QA 입력으로 승인했는지 확인해야 한다.
- 다른 팀원이 README 절차로 같은 결과를 재현했는지 실행자·시각·Commit을 받아야 한다.

### 3.2 제품·구독 조회·등록·수정 최소 API 구현 (`T-018`) — 20%

완료된 내용:

- 고객 본인 ACTIVE 구독 목록·상세 조회 2개의 OpenAPI, Schema, 정상·빈 목록·검증 오류 Example이 병합됐다.
- OpenAPI Operation Inventory는 23개 = Runtime 8개 + OpenAPI-only 15개로 갱신됐다.
- T-018 단독 7개, 핵심 계약 33개, 확장 계약 52개와 Backend 전체 798개 작성자 Test 결과가 기록됐다.
- `WPUJAC104DWH`, 소유 ACTIVE 구독, 공개 UUID와 내부·개인정보 비노출 원칙이 계약에 반영됐다.
- 작성자 문서가 Runtime·Migration·DB·Seed 변경 0과 `NOT_IMPLEMENTED` 상태를 명확히 밝힌다.

수행 근거:

- [OpenAPI](../../../contracts/api/openapi.yaml)
- [제품·구독 Path 계약](../../../contracts/api/paths/products.yaml)
- [T-018 계약 Test](../../../backend/tests/api/test_t018_product_subscription_contract.py)
- [T-018 작성자 수행보고](../../handoffs/t018/20260805_01_T018_Part4_Part5A_작성자수행보고.md)
- [T-018 PM 병합 요청](../../handoffs/t018/20260805_02_최지용_to_윤승혁_PM_main_병합요청.md)
- [한예나 계약 소비 회신](../../handoffs/20260804_한예나_T018-D06_Web_회신_v0.1.md)

완료되지 않은 내용:

- [제품 URL](../../../backend/apps/products/api/urls.py), [View](../../../backend/apps/products/api/views.py), [Serializer](../../../backend/apps/products/api/serializers.py), [Service](../../../backend/apps/products/services/product_service.py)는 설명 문자열뿐이다.
- 구독 URL·View·Serializer·Service도 실행 코드가 없다.
- `backend/config/api_urls.py`는 Accounts·Inquiries만 등록하며 제품·구독 Route가 없다.
- 합성 제품 등록·허용 필드 수정, 소유권 QuerySet, 타 고객 404, 중복·종료 구독·잘못된 관리 유형 Runtime Test가 없다.
- 문의가 검증된 MVP 제품·세대 Row를 사용하고 차단 모델을 AI 호출 전에 막는 Service가 완성되지 않았다.
- Web·Mobile에 전달한 정상·오류 JSON이 실제 Runtime 응답으로 검증되지 않았다.

완료 방법:

1. 제품 Catalog와 고객 소유 구독을 분리한 Repository·Service를 구현한다.
2. 본인 구독 목록·상세, 합성 제품 등록과 허용 필드 수정 Serializer·View·URL을 `/api/v1`에 등록한다.
3. QuerySet 단계에서 소유권을 제한하고 타 고객 UUID를 404로 처리한다.
4. `WPUJAC104DWH` D세대만 MVP 문의에 허용하고 IAC425·IAC506·S세대를 업무 오류로 차단한다.
5. 정상·401·403·404·409·422와 중복·종료·비활성 Case를 실제 DB API Test로 작성한다.
6. OpenAPI Example과 Serializer 실응답을 Contract Test로 대조한다.

코드 외 별도 확인:

- 김은진이 모델·세대·매뉴얼 Revision과 차단 범위를 승인했는지 확인해야 한다.
- 한예나·양정현이 구독 목록·상세 DTO를 실제 화면에서 소비했는지 확인해야 한다.
- 합성 환경의 제품 등록 허용 범위가 운영 정책으로 오인되지 않는지 PM 확인이 필요하다.

### 3.3 문의 증상 제출·추가 답변·자가조치 결과 누적 구현 (`T-022`) — 60%

완료된 내용:

- 문의 생성과 실제 Runtime 증상 제출 Route가 등록돼 있다.
- 고객 소유 문의·수정 가능 상태·활성 구독과 증상 Payload Guard를 검증한다.
- `SUBMIT_SYMPTOM` 전이, 상태 Version 증가, 상태 이력과 멱등 원장을 하나의 Transaction에서 저장한다.
- 동일 Key·동일 Payload는 Replay하고 같은 Key·다른 Payload와 stale `state_version`은 409로 처리한다.
- 타 고객 문의는 조회 단계에서 404로 처리한다.
- 작성자 인계서에 SQLite·PostgreSQL 집중·전체 Test 결과가 기록돼 있다.

수행 근거:

- [문의 URL](../../../backend/apps/inquiries/api/urls.py)
- [문의 View](../../../backend/apps/inquiries/api/views.py)
- [문의 생성 Service](../../../backend/apps/inquiries/services/inquiry_service.py)
- [증상 제출·전이 Service](../../../backend/apps/inquiries/services/inquiry_transition_service.py)
- [문의 Repository](../../../backend/apps/inquiries/repositories/inquiry_repository.py)
- [증상 제출 API Test](../../../backend/tests/api/test_t022_submit_symptom.py)
- [T-022 구현·검증 인계서](API/Django_REST_API_문의_증상제출_구현_검증_인계서.md)

완료되지 않은 내용:

- 추가 질문 답변 저장 Endpoint와 누적 조회 Runtime이 없다.
- 자가조치 결과 Serializer·Model은 있으나 등록 Endpoint·Service가 없다.
- 고객 원문과 AI 구조화 결과를 실제 AI 호출 전·후 경계에 따라 분리 저장하는 Service가 없다.
- 차단 모델 자체를 검사하는 제품 검증 Service가 증상 제출 Runtime에 연결되지 않았다.
- `DEMO-INQ-002`의 생성→증상 제출→추가 답변→자가조치 전체 실행 증빙이 없다.
- 현재 HEAD의 PostgreSQL Transaction·동시성 Test 재실행 결과가 없다.

완료 방법:

1. 추가 답변과 자가조치 결과의 Serializer·Service·Repository·URL을 구현한다.
2. 질문 ID·답변·작성자·시각·상태 Version과 조치 코드·결과·해결 여부를 누적 저장한다.
3. 고객 원문, 구조화 결과, AI 응답을 별도 Model·필드에 저장하고 AI 실패 시에도 원문 Transaction을 보존한다.
4. 제품·상태·소유권·필수값을 AI 호출 전에 차단한다.
5. `DEMO-INQ-002` 전체 흐름과 부분 저장 Rollback을 PostgreSQL 통합 Test로 검증한다.

코드 외 별도 확인:

- 고객 원문 조회 범위를 상담사·기사·운영 역할별로 승인해야 한다.
- 양정현·한예나가 추가 질문·자가조치 DTO와 상태 Version 처리 방식을 확인해야 한다.
- 실패 후 입력 보존과 재개 UX가 화면 기획과 일치하는지 PM 검수가 필요하다.

### 3.4 State Machine Event API Runtime 확장 (`T-023`) — 45%

완료된 내용:

- `START_INQUIRY`, `SUBMIT_SYMPTOM`, DRAFT 상태의 `CANCEL_INQUIRY`가 실제 Runtime Service에 연결돼 있다.
- State Machine 계약 Loader·Engine·Guard와 계약 기반 `AllowedActionResolver`가 있다.
- 상태 변경·이력·멱등성 기록을 Transaction으로 묶고 `state_version` 충돌과 Replay·Conflict를 처리한다.
- 응답은 상태·새 Version과 계약에서 계산한 `allowed_actions`를 반환한다.
- 계약 Validator와 Mermaid 생성 Check가 발표 기준에서 통과했다.

수행 근거:

- [State Machine Engine](../../../backend/apps/workflow/engine/state_machine.py)
- [Guard Evaluator](../../../backend/apps/workflow/engine/guard_evaluator.py)
- [Allowed Action Resolver](../../../backend/apps/workflow/engine/allowed_action_resolver.py)
- [상태 이력 Service](../../../backend/apps/workflow/services/transition_history_service.py)
- [Workflow API Test](../../../backend/tests/api/test_workflow_conflict_contract.py)
- [T-023 구현·검증 인계서](API/Django_State_Machine_API_구현_검증_인계서.md)
- [State Machine 계약](../../../contracts/state-machine/README.md)

완료되지 않은 내용:

- `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `PRODUCT_VALIDATION_FAILED`, `REQUEST_CONSULTATION` Runtime Event가 없다.
- 공통 Event API와 역할·담당자별 Guard가 발표 필수 Event 전체에 적용되지 않았다.
- 동시 같은 `state_version` 요청을 실제 PostgreSQL에서 경쟁시켜 하나만 성공하는 현재 HEAD 증거가 없다.
- AI 응답 Schema·제품·근거 상태를 검증한 후 Event를 선택하는 Backend 조율 코드가 없다.
- 구현·미구현 Event 전체 상태표와 Web·Mobile용 상태·행동 Example이 완결되지 않았다.

완료 방법:

1. 발표 필수 AI 결과 Event와 `REQUEST_CONSULTATION`의 Service·URL·Serializer를 구현한다.
2. Event 공통 입력과 역할·소유권·담당자·Payload Guard를 적용한다.
3. 상태·이력·감사·멱등 기록을 같은 Transaction에 묶고 PostgreSQL 동시성 Test를 작성한다.
4. 계약 변경이 필요하면 윤승혁 승인과 `contracts/state-machine/**` 변경을 먼저 또는 동시에 처리한다.
5. 구현·계약 전용·5주차 이관 Event를 Runtime 상태표에 구분한다.

코드 외 별도 확인:

- 윤승혁이 AI 결과별 Event 선택과 `PENDING_CONSULTATION` 정책을 승인해야 한다.
- 한예나·양정현이 `allowed_actions`만으로 화면 버튼을 결정할 수 있는지 확인해야 한다.
- 동시성 Test에 사용할 PostgreSQL 격리 수준과 Lock 정책을 팀 DB 환경에서 합의해야 한다.

### 3.5 Backend↔AI 최소 수직 연동과 결과 저장 — 10%

완료된 내용:

- AI 요청·응답 JSON Schema와 오류·Fallback Example이 있다.
- Backend↔AI 요청 필드, 오류 Mapping, Timeout·재시도와 저장 경계를 협업 문서로 정리했다.
- AI가 Inquiry DB 상태를 직접 바꾸지 않고 Backend가 Event를 선택한다는 경계가 문서와 구조에 유지돼 있다.

수행 근거:

- [AI 계약 README](../../../contracts/ai/README.md)
- [Backend AI 미해결 사항](연동_인계/Backend_AI_API_계약_구현_미해결_사항.md)
- [Backend↔AI 수직연동 협업요청서](../dongyoon/20260804_이동윤_최지용_Backend_AI_수직연동_협업요청서_v0.1.md)
- [공통 통합 인계](../../handoffs/week4-integration-handoff.md)

완료되지 않은 내용:

- [AI Client](../../../backend/integrations/ai/client.py), Request·Response Mapper, Schema Validator와 Retry Policy는 설명 문자열뿐이다.
- [Inquiry AI Service](../../../backend/apps/inquiries/services/inquiry_ai_service.py)도 실행 코드가 없다.
- 증상 제출 후 실제 FastAPI 호출, Schema 검증, AI Run·위험도·안내·근거·오류 저장이 없다.
- Timeout·연결 실패·JSON 오류·Schema 불일치·근거 없음의 Backend Fallback이 없다.
- 내부 Evidence 필드와 고객 공개 필드를 분리하는 실제 응답 Mapping Test가 없다.
- `correlation_id`로 Backend→AI→검색→DB 저장을 추적하는 E2E가 없다.

완료 방법:

1. 계약 Version 1.1.0에 맞춰 HTTP Client, DTO Mapper, Schema Validator, 예외와 Timeout 정책을 구현한다.
2. 고객 입력 Commit 뒤 AI를 호출하고 성공·실패 결과를 AI Run·Evidence 후보·문의 보조 결과에 저장한다.
3. Backend 자동 재시도 0회와 전체 30초 Timeout을 적용하고 AI 실패를 안전한 상담 대기 상태로 변환한다.
4. 고객 응답에서 `source_path`, `chunk_id`, 원문 전체와 내부 점수를 제거한다.
5. 실제·Mock 결과를 구분해 정상·위험·근거 없음·503·504·Schema 오류 E2E를 작성한다.

코드 외 별도 확인:

- 이동윤과 요청·응답 Schema Version, Timeout과 AI 내부 최대 1회 재시도를 공동 승인해야 한다.
- 김은진이 Evidence 후보의 공개 승격 기준과 13번째 정책 차단 Case를 확인해야 한다.
- 윤승혁이 실패 시 상태와 고객 `allowed_actions`를 승인해야 한다.

### 3.6 케어 이력 등록·조회 최소 API 구현 (`T-019`) — 5%

완료된 내용:

- 케어 이력·일정 Model과 Migration, Demo Seed, Repository·Service 파일 골격이 있다.
- 케어 코드·Model·Seed 단위 Test가 존재한다.
- `T-019` 이력과 `T-020` 다음 일정 계산을 분리한 디렉터리 구조가 있다.

수행 근거:

- [케어 이력 Model](../../../backend/apps/care/models/care_history.py)
- [케어 일정 Model](../../../backend/apps/care/models/care_schedule.py)
- [케어 Migration](../../../backend/apps/care/migrations/0001_initial.py)
- [케어 Demo Seed](../../../backend/apps/care/management/commands/seed_demo_care_records.py)
- [케어 Model Test](../../../backend/tests/unit/care/test_models.py)

완료되지 않은 내용:

- [케어 URL](../../../backend/apps/care/api/urls.py), View·Serializer·Service는 설명 문자열만 있고 `/api/v1`에 등록되지 않았다.
- 고객 제품 기준 목록·상세·합성 등록 Runtime이 없다.
- 타 고객 404, 잘못된 날짜·종료 구독·케어 유형과 중복 등록 정책 Test가 없다.
- AI가 최근 케어 이력을 조회할 Service Interface가 없다.
- `T-019`와 `T-020`의 실제 구현 상태를 설명하는 API 인계 문서가 없다.

완료 방법:

1. 케어 유형 계약을 사용한 Serializer·Repository·Service와 목록·상세·등록 URL을 구현한다.
2. 제품·구독·관리일·수행 주체·출처를 연결하고 소유권을 QuerySet에서 제한한다.
3. 미래 날짜·구독 시작 전·종료 구독·잘못된 유형과 중복 등록을 검증한다.
4. AI가 최근 케어 이력을 조회할 읽기 전용 Service Interface를 제공한다.
5. `T-019` 구현과 `T-020` 미구현 경계를 OpenAPI·Runtime 상태표·인계 문서에 반영한다.

코드 외 별도 확인:

- 공식 케어 유형과 날짜 정책을 김은진이 승인했는지 확인해야 한다.
- 상담사·운영자·기사별 조회·등록 범위를 윤승혁과 확인해야 한다.
- 기사 등록은 방문 완료 API가 준비된 뒤로 미루는 데 팀이 동의했는지 확인해야 한다.

### 3.7 8월 6일 중간 발표 Backend 지원·발표 후 인계 — 65%

완료된 내용:

- 중간 발표가 완료됐고 발표자료가 저장소 작업 경로에 있다.
- 발표 기준 Commit과 기능 상태를 `RECORDED_RUNTIME`, `MOCK_UI`, `CONTRACT_ONLY`, `NOT_INCLUDED`로 구분한 공통 인계가 있다.
- 발표자료는 OpenAPI 23개, Runtime 8개, DB 계약 32개 중 13개 반영과 Backend↔AI E2E 미통과를 구분한다.
- Model·Migration·계약 수를 Backend 전체 기능 완료로 표현하지 않는다.
- 발표 후 공통 차단 항목과 담당자별 다음 조치가 문서화돼 있다.

수행 근거:

- [WaterBridge 중간 발표자료](../../presentation/WaterBridge.pdf)
- [중간 발표 피드백](../../presentation/중간_피드백.md)
- [Main 회귀 검증 결과](../../testing/week4-main-regression-result.md)
- [Known Failures](../../testing/week4-known-failures.md)
- [4주차 통합 인계](../../handoffs/week4-integration-handoff.md)
- [T-018 계약 소비 확인 요청](../../handoffs/t018/20260805_04_최지용_to_한예나_main_계약소비_확인요청.md)

완료되지 않은 내용:

- Backend 공식 환경이 Python Patch Version 차이로 중단돼 발표 기준 Commit의 Liveness·Readiness·Migration·Seed Smoke가 통과하지 않았다.
- 가상 로그인→제품 조회→문의→증상→AI→상태 반환 수직 흐름은 제품 Runtime과 AI 연동 부재로 준비되지 않았다.
- 요청·응답·DB Row·상태 이력·`correlation_id`를 한 세트로 묶은 정적 시연 증빙이 없다.
- 다른 팀원이 실행 가능한 중앙 초기화·Seed·Smoke 명령과 3회 리허설 기록이 없다.
- 발표 피드백이 Git 미추적 문서이며 외부 Issue와 직접 연결됐는지 확인되지 않았다.
- Backend 전용 `docs/handoffs/week4-to-week5/backend.md`가 없다.

완료 방법:

1. Python 3.13.13 환경에서 발표 후 Backend Smoke와 Migration·Seed Gate를 재실행한다.
2. 실제·기록·Mock·미구현 기능별 요청·응답·DB Row·상태 이력 증빙을 고정한다.
3. `scripts/testing/**`에 초기화·Seed·Health·대표 흐름 Runner를 만들고 다른 팀원이 재현한다.
4. `docs/handoffs/week4-to-week5/backend.md`에 완료·부분·미구현 Endpoint·Event, 결함, 담당자와 목표일을 기록한다.
5. 발표 피드백을 기능·계약·UI·AI·인프라로 분류해 Issue와 연결한다.

코드 외 별도 확인:

- 발표자료 최종본과 실제 발표본이 같은지 김은진에게 확인해야 한다.
- 발표 당일 Backend 장애 대응 담당·진행 순서와 실제 장애 발생 여부를 확인해야 한다.
- GitHub Issue의 상태·담당자·목표일이 로컬 Known Failure·WBS와 일치하는지 확인해야 한다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 항목

| 확인 대상 | 별도로 확인할 내용 | 확인 주체 |
| --- | --- | --- |
| 팀 DB 기준선 | 실제 DSN·권한·Extension, 빈 DB Migration과 2회 Seed 결과 | 최지용·김은진 |
| Data Crosswalk | 새 Backend 증빙 경로·Hash가 Data QA에서 재검증됐는지 | 김은진 |
| 제품 범위 | MVP 모델·세대·매뉴얼 Revision과 차단 모델 승인 | 김은진·윤승혁 |
| State Event | AI 결과·상담 전환 Event와 역할·소유권 정책 승인 | 윤승혁 |
| Backend↔AI | Schema Version, Timeout·재시도·Fallback 공동 승인 | 이동윤·최지용 |
| 화면 DTO | 구독·질문·상태·Evidence DTO 실제 소비 여부 | 한예나·양정현 |
| 보안 | 운영과 유사한 로그에 Token·원문·Prompt가 남지 않는지 | 최지용·윤승혁 |
| 발표·인계 | 실제 발표 결과, 피드백, 담당자·목표일과 Issue 연결 | 김은진·윤승혁 |

## 5. 완료 기준 및 최종 체크리스트

업무 지침서의 문구와 순서를 유지하고 현재 확인 가능한 근거만 반영했다.

### 5.1 8월 5일 발표 기준 동결 전 필수 완료 기준

- [ ] 빈 PostgreSQL에서 전체 Migration이 성공한다.
- [x] `makemigrations --check` 결과가 변경 없음이다.
- [ ] Seed·합성 Fixture Import가 반복 실행 가능하다.
- [ ] Liveness와 DB Readiness가 분리되어 동작한다.
- [x] Runtime URL과 OpenAPI 구현 상태가 대조되어 있다.
- [ ] 고객 본인 제품·구독 목록·상세 조회가 동작한다.
- [ ] 미지원·차단 제품이 문의와 AI 실행 전에 차단된다.
- [x] 문의 생성 후 증상 제출이 실제 Runtime에서 동작한다.
- [ ] 같은 문의에 추가 답변과 자가조치 결과가 누적된다.
- [ ] 고객 원문과 AI 구조화 결과가 분리 저장된다.
- [x] `SUBMIT_SYMPTOM` 상태 전이가 동작한다.
- [ ] 발표 필수 State Machine Event가 Runtime에 연결되어 있다.
- [ ] `allowed_actions`가 계약과 일치한다.
- [x] `state_version` 동시 충돌이 409로 처리된다.
- [x] `idempotency_key` Replay와 Conflict가 구분된다.
- [ ] 타 고객 제품·구독·문의 접근이 404로 차단된다.
- [x] 역할 부족은 403, 미인증은 401로 구분된다.
- [ ] Backend↔AI 요청·응답 Schema가 일치한다.
- [ ] AI 실패·Timeout·Schema 오류가 안전하게 처리된다.
- [x] AI가 DB 상태를 직접 변경하지 않는다.
- [ ] 대표 `DEMO-INQ-002` 수직 흐름이 반복 재현된다.
- [x] 발표 기준 Commit과 실행 명령이 기록되어 있다.

현재 완료로 체크 가능한 항목은 **9/22개**다.

### 5.2 8월 6일 중간 발표 지원 기준

- [x] 발표자료의 Backend 수치가 실제 코드와 일치한다.
- [x] Model·Migration 수와 Runtime 기능 수가 구분되어 있다.
- [x] 실제 구현·Mock·미구현 기능이 발표자료에서 구분된다.
- [ ] 발표 직전 Liveness·Readiness·Migration·Seed Smoke Test가 통과한다.
- [ ] 가상 로그인→제품 조회→문의 생성→증상 제출→AI 결과→상태 반환 흐름이 준비되어 있다.
- [ ] 실패 대비 요청·응답·DB Row·상태 이력 증빙이 준비되어 있다.
- [ ] 시연 초기화와 Seed 명령을 다른 팀원이 실행할 수 있다.
- [x] 발표 당일 Backend 장애 대응 담당과 순서가 정해져 있다.

현재 완료로 체크 가능한 항목은 **4/8개**다. 발표 자체는 완료됐지만, 실패한 Smoke와 미구현 수직 흐름은 기술적으로 완료 처리하지 않았다.

### 5.3 8월 7일 최종 정리 기준

- [ ] 케어 이력 목록·상세·등록 최소 기능이 동작한다.
- [ ] Web·Mobile·AI 담당자의 계약 피드백이 반영되었다.
- [ ] 발표 후 발견된 회귀가 수정되었거나 Issue로 등록되었다.
- [x] OpenAPI·Serializer·실제 응답 차이가 정리되었다.
- [x] 권한·멱등성·동시성·Transaction Test가 보강되었다.
- [x] 로그에 Token·개인정보·고객 원문 전체·AI Prompt 전체가 노출되지 않는다.
- [x] 완료·부분 완료·미구현 Endpoint와 Event가 문서화되어 있다.
- [x] 5주차 상담·방문·Evidence·AI 실패 처리 진입 조건이 인계되었다.
- [x] 김은진의 Data QA가 참조하는 Backend Runtime 증빙이 유효하다.
- [ ] 필수 업무 완료 후에만 4장의 추가 업무를 시작했다.

현재 완료로 체크 가능한 항목은 **6/10개**다.

전체 체크리스트에서 현재 완료로 체크 가능한 항목은 **19/40개**다.

## 6. 완료를 위한 우선순위

1. **P0 — Backend 실행 환경 복구:** Python 3.13.13 환경에서 전체 Test·빈 DB Migration·2회 Seed를 실행하고 기준선 문서를 작성한다.
2. **P0 — T-018 Runtime:** 제품·구독 목록·상세·등록·수정과 소유권·지원 모델 Guard를 `/api/v1`에 연결한다.
3. **P0 — Backend↔AI 수직 연동:** 실제 HTTP·Schema 검증·AI Run·Evidence·오류 저장과 `correlation_id` E2E를 완성한다.
4. **P0 — T-022 입력 누적:** 추가 답변·자가조치와 고객 원문·AI 구조화 결과 분리 저장을 구현한다.
5. **P1 — State Event 확장:** AI 결과 Event와 `REQUEST_CONSULTATION`, 권한·동시성·`allowed_actions`를 Runtime에 연결한다.
6. **P1 — T-019 케어 API:** 고객 제품별 목록·상세·합성 등록, 날짜·중복·권한 Test와 AI 조회 Interface를 구현한다.
7. **P1 — 발표 후 공식화:** 피드백·차단 결함을 Issue에 연결하고 Backend 전용 5주차 인계와 재현 가능한 Smoke 증빙을 남긴다.
