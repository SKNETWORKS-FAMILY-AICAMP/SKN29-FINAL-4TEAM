# 최지용 4주차 작업 진행도

> 작성 일시: **2026-08-04 23:58 KST**  
> 점검 기준 Commit: `d9052621e2f07a8447f7c1f998875104c031a3c0` (`main`)  
> 참고 지침서: [최지용 4주차 업무 지침서](../../weekly-task/최지용_4주차_업무_지침서.md)  
> 관할 기준: [팀원별 관할 영역 v2](../../planning/md/팀원별%20관할%20영역%20v2.md)

## 1. 종합 판정

| 4주차 필수 업무 | 완료도 | 판정 | 핵심 근거와 남은 Gate |
| --- | ---: | --- | --- |
| 3.1 Backend 기준선·Migration·Runtime 증빙 복구 | 55% | 부분 완료 | 과거 PostgreSQL 전체 회귀와 유효한 Data Crosswalk가 있다. 현재 HEAD의 전체 Migration·Seed 재실행, DB Readiness와 안정 경로 기준선 문서가 없다. |
| 3.2 제품·구독 최소 API (`T-018`) | 5% | 미착수에 가까움 | Model·Seed·계약 Schema는 있으나 제품·구독 URL·View·Serializer·Service가 설명 문자열뿐이고 `/api/v1`에 등록되지 않았다. |
| 3.3 문의 증상 제출·추가 답변·자가조치 누적 (`T-022`) | 60% | 부분 완료 | 문의 생성·증상 제출 Slice A와 State·409·Replay·PostgreSQL 증거가 있다. 추가 답변·자가조치·AI 효과 Slice B는 미구현이다. |
| 3.4 State Machine Event API Runtime (`T-023`) | 45% | 부분 완료 | START·SUBMIT·DRAFT 고객 CANCEL과 계약 Engine·Guard가 동작한다. AI 결과·상담 요청·방문·완료 Event와 동적 `allowed_actions`는 미구현이다. |
| 3.5 Backend↔AI 최소 수직 연동 | 10% | 미완료 | AI 계약과 협업요청은 있으나 Backend Client·Mapper·Validator·조율 Service가 빈 골격이며 HTTP 호출·결과 저장 E2E가 없다. |
| 3.6 케어 이력 최소 API (`T-019`) | 5% | 미착수에 가까움 | Care Model·Migration·Seed는 있으나 URL·View·Service·Repository가 실행 코드가 아니고 API가 등록되지 않았다. |
| 3.7 중간 발표 Backend 지원·발표 후 인계 | 20% | 착수 | Runtime/OpenAPI 수치와 G2 계약 Manifest는 있다. 발표 Commit·Smoke·초기화 Script·정적 증빙·5주차 인계는 없다. |

전체 단순 평균 완료도는 **약 29%**다. 기존 Backend 기반은 강하고 문의 생성·증상 제출·취소의 세 Slice는 실제 Runtime이다. 그러나 4주차 지침서의 중심인 **제품·구독, 추가 입력, AI 결과, 케어 이력과 발표 수직 흐름은 아직 연결되지 않았다.** Model·Migration과 G2 OpenAPI가 많이 존재하더라도 Runtime 완료로 합산하지 않았다.

## 2. 이번 점검에서 직접 확인한 현재 경계

### 2.1 코드와 저장된 증거로 확인된 내용

- `/api/v1` Runtime에는 Accounts와 Inquiries만 등록돼 있다.
- 실제 Inquiry Runtime은 문의 생성, DRAFT 고객 취소, 증상 제출 세 Route다.
- 증상 제출은 State Machine·Guard·행 잠금·멱등 원장·상태 이력을 하나의 Transaction으로 처리한다.
- 동일 Key·동일 요청 Replay, 같은 Key·다른 Hash 409와 stale `state_version` 409 코드·테스트가 있다.
- T-022 작성자 기록에는 SQLite 전체 `778 passed, 13 skipped`, PostgreSQL 전체 `791 passed`, 증상 제출 PostgreSQL 집중 `22 passed`가 있다.
- T-017은 김은진의 독립 QA가 `APPROVED`이며 PostgreSQL Migration·역할 Matrix가 통과했다.
- G2 기계 계약은 OpenAPI 21개, Runtime 8개, OpenAPI-only 13개를 명시하며 신규 11개는 모두 `NOT_IMPLEMENTED`로 구분한다.
- G2 작성자 계약 테스트는 34개, 회귀 103개가 통과했으나 Data·QA 재검증과 Runtime 착수 Gate는 아직 닫혀 있다.
- Data Crosswalk가 참조하는 Backend Runtime 문서와 Hash는 현재 존재하며 Data QA는 현재 HEAD에서 통과했다.
- 현재 `/health`는 빈 `200`을 반환하는 임시 Liveness이고 DB Readiness는 아니다.

수행 근거:

- [Backend API URL](../../../backend/config/api_urls.py)
- [Inquiry URL](../../../backend/apps/inquiries/api/urls.py)
- [Inquiry View](../../../backend/apps/inquiries/api/views.py)
- [Inquiry Service](../../../backend/apps/inquiries/services/inquiry_service.py)
- [Inquiry Transition Service](../../../backend/apps/inquiries/services/inquiry_transition_service.py)
- [T-022 구현·검증 인계서](API/Django_REST_API_문의_증상제출_구현_검증_인계서.md)
- [T-023 구현·검증 인계서](API/Django_State_Machine_API_구현_검증_인계서.md)
- [T-017 완료 증거](../../handoffs/t017_completion_evidence_20260803.json)
- [G2 계약 Manifest](../../handoffs/g2_contract_manifest_20260804.json)
- [Runtime 구현 상태](../../api/runtime_implementation_status.md)
- [Backend Import Crosswalk](../../../data/config/handoff/backend_import_crosswalk.json)
- [Health 구현](../../../backend/common/api/health.py)

### 2.2 이번 점검에서 직접 재실행하지 못한 내용

- 현재 환경은 Python `3.13.12`이고 `pytest`와 `backend/.venv`가 없어 Backend 테스트를 현재 HEAD에서 재실행하지 못했다.
- PostgreSQL `791 passed`는 8월 2일 작성자 환경 기록이며 현재 HEAD의 새 G2 계약까지 포함한 팀 DB 재실행은 아니다.
- G2 Manifest의 `source_baseline_commit`은 현재 HEAD보다 과거 Commit이고 `qa_contract_test_passed=false`다.
- 따라서 저장된 테스트 수치는 유효한 작성자 증거로 참고하되, 현재 `main`의 발표 동결·전체 Runtime 완료로 승계하지 않았다.

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 Backend 기준선·Migration·Runtime 검증 증빙 복구 — 55%

완료된 내용:

- Django Model·Migration과 T-005 32개 계약 Table의 정합성을 검사하는 Script·테스트·문서가 있다.
- T-017 독립 QA에서 PostgreSQL Migration, 역할별 Seed와 인증 Matrix가 통과했다.
- T-022 작성자 환경에서 전체 PostgreSQL 회귀와 실제 동시성 검증 기록이 있다.
- Runtime 8개와 OpenAPI-only 13개를 구분하는 상태 자료와 G2 Crosswalk가 있다.
- 합성 계정·제품·구독·케어 Seed Command가 있고 반복 실행 정책 테스트가 있다.
- Data Crosswalk가 참조하는 Backend Runtime 증빙 경로·Hash가 현재 유효해 Data QA가 통과한다.

수행 근거:

- [PostgreSQL 통합 검증 보고서](데이터베이스/PostgreSQL_통합검증_보고서_20260731.md)
- [Backend Runtime 검증 문서](manuals/20260729_postgresql_synthetic_handoff_runtime_verification.md)
- [Backend 설치·Migration·Seed 가이드](manuals/워터브리지_백엔드_설치_마이그레이션_시드_복구_가이드.md)
- [Runtime 구현 상태](../../api/runtime_implementation_status.md)
- [G2 Crosswalk](../../../contracts/api/g2-operation-crosswalk.yaml)
- [G2 계약 Manifest](../../handoffs/g2_contract_manifest_20260804.json)
- [Data Crosswalk](../../../data/config/handoff/backend_import_crosswalk.json)

완료되지 않은 내용:

- 현재 HEAD에서 빈 PostgreSQL 전체 Migration, `showmigrations`, `makemigrations --check`, 핵심 Table 검증을 다시 실행한 결과가 없다.
- 현재 HEAD에서 Seed·Fixture Import를 2회 실행한 결과와 변경 건수가 없다.
- `/health`는 임시 Liveness만 제공하며 PostgreSQL·필수 Migration을 확인하는 Readiness와 실패 시 503이 없다.
- 지침서가 요구한 `docs/testing/results/week4-backend-baseline.md`와 `docs/testing/evidence/backend/**`가 없다.
- Runtime 문서는 여전히 개인 폴더 경로에 있고 안정적인 공용 증빙 경로가 없다.
- G2는 `SHARED_CANDIDATE`이고 QA 재검증, Runtime 구현, 소비자 연동과 완료 주장 Gate가 모두 열리지 않았다.
- 다른 팀원이 현재 README만으로 최신 HEAD 기준선을 재현했다는 증거가 없다.

완료 방법:

1. Python·Django·DRF·PostgreSQL·pgvector Version을 README와 Lock에 고정한다.
2. 새 PostgreSQL에서 전체 Migration, `makemigrations --check`, Seed·Import 2회를 실행해 명령·환경·Exit code·건수를 기록한다.
3. `/health/live`와 `/health/ready` 또는 승인된 경로를 분리하고 Readiness 실패 시 안전한 503을 반환한다.
4. Runtime URL과 OpenAPI Operation을 자동 대조해 `RUNTIME_IMPLEMENTED`, `OPENAPI_ONLY`, `PLANNED`, `NOT_IMPLEMENTED`를 출력한다.
5. `docs/testing/results/week4-backend-baseline.md`에 현재 HEAD의 Migration·Seed·Endpoint·Test·제한을 정리한다.
6. Backend Runtime 증빙을 공용 안정 경로로 옮긴 뒤 김은진과 Crosswalk 경로·Hash를 갱신하고 Data QA를 재실행한다.

코드 외 별도 확인:

- 김은진이 새 Runtime 증빙·Crosswalk를 승인했는지 확인해야 한다.
- 다른 팀원이 README만으로 빈 DB 재현에 성공했는지 확인해야 한다.
- G2 Data·QA 재검증과 윤승혁의 Runtime 착수 결정을 확인해야 한다.

### 3.2 제품·구독 조회·등록·수정 최소 API 구현 (`T-018`) — 5%

완료된 내용:

- 제품 Catalog, 고객 구독 Model·Migration과 합성 Seed가 있다.
- OpenAPI에는 제품·구독 조회용 Schema·Path 초안이 있다.
- T-018의 소유권·MVP 필터·공개 필드 범위를 제안한 문서가 있다.

수행 근거:

- [Product Model](../../../backend/apps/products/models)
- [Subscription Model](../../../backend/apps/subscriptions/models)
- [제품 Seed](../../../backend/apps/products/management/commands/seed_demo_products.py)
- [구독 Seed](../../../backend/apps/subscriptions/management/commands/seed_demo_subscriptions.py)
- [T-018 계약 제안서](technical/backend/t018_구독_제품_조회_api_계약_제안서.md)

완료되지 않은 내용:

- Product·Subscription URL, View, Serializer, Service, Repository 다수가 설명 문자열뿐이다.
- `backend/config/api_urls.py`에 Product·Subscription Route가 등록되지 않았다.
- 본인 제품·구독 목록·상세·등록·수정 Runtime이 없다.
- 타 고객 404, 역할 403, 미인증 401, 중복·미지원·비활성·관리 유형 오류 테스트가 없다.
- `WPUJAC104DWH`·D세대·Revision을 검증하고 확장·금지 모델을 문의·AI 전에 막는 실행 Service가 없다.
- Inquiry 생성은 소유 활성 구독을 찾지만 T-018의 전체 제품 범위 검증을 수행하지 않는다.
- OpenAPI·Serializer·실제 응답 정합성과 Web·Mobile 인계 예시가 없다.

완료 방법:

1. Product·Subscription Serializer·Repository·Service·Permission을 실제 코드로 구현한다.
2. 본인 목록·상세, 합성 제품 등록, 허용 필드 수정 Route를 `/api/v1`에 등록한다.
3. QuerySet 단계에서 소유권을 제한하고 타 고객은 404, 역할·인증 오류는 403·401로 구분한다.
4. 제품 지원 범위 Service에 MVP 제품·D세대·Revision과 IAC425·IAC506·S세대 차단을 구현한다.
5. Inquiry 생성·AI Mapper가 요청 문자열 대신 검증된 Subscription·Product FK를 사용하도록 연결한다.
6. 정상·401·403·404·409·업무 오류 API 테스트와 실제 응답 예시를 작성해 Web·Mobile에 전달한다.

코드 외 별도 확인:

- 제품 등록·수정 허용 필드와 합성 환경 범위를 김은진·윤승혁이 승인해야 한다.
- 한예나·양정현이 실제 응답 DTO를 소비할 수 있는지 확인해야 한다.
- 운영 Catalog 변경 권한과 고객 제품 등록 정책을 별도로 확인해야 한다.

### 3.3 문의 증상 제출·추가 답변·자가조치 결과 누적 구현 (`T-022`) — 60%

완료된 내용:

- 문의 생성과 실제 `POST /api/v1/inquiries/{id}/submit` Runtime이 있다.
- 고객 소유 DRAFT 문의, 저장된 원문, 활성 구독, 제품 연결, `state_version`과 멱등 키를 검증한다.
- `SUBMIT_SYMPTOM`이 DRAFT에서 QUESTIONNAIRE_IN_PROGRESS로 전이하고 상태 이력을 저장한다.
- 고객 원문은 Inquiry에 보존하고 AI 구조화용 Model과 별도 구조가 있다.
- 동일 Key Replay·다른 Payload 409, stale Version 409와 PostgreSQL 동시 요청을 검증한 작성자 증거가 있다.
- Mobile 실제 단말 기록에 문의 생성 201과 증상 제출 200이 있다.
- Slice A와 AI 효과 Slice B를 문서에서 명확히 구분한다.

수행 근거:

- [Inquiry API](../../../backend/apps/inquiries/api/views.py)
- [증상 제출 Serializer](../../../backend/apps/inquiries/api/serializers/symptom_submission.py)
- [Inquiry Transition Service](../../../backend/apps/inquiries/services/inquiry_transition_service.py)
- [T-022 API 테스트](../../../backend/tests/api/test_t022_submit_symptom.py)
- [T-022 구현·검증 인계서](API/Django_REST_API_문의_증상제출_구현_검증_인계서.md)
- [Mobile 4주차 검증](../../../mobile/docs/week4-mobile-verification.md)

완료되지 않은 내용:

- 현재 Submit Endpoint는 최초 문의에 저장된 원문을 확정할 뿐 새 증상·추가 답변을 받지 않는다.
- 추가 질문 답변 저장과 입력 이력·누적 상태 조회 Runtime이 없다.
- 자가조치 결과 저장 Runtime이 없다.
- Questionnaire·Action Result Model·Migration은 있으나 해당 URL·View·Service가 실행되지 않는다.
- AI 구조화 결과 저장, AI dispatch, stale AI 결과 차단과 실패 재처리 Slice B가 없다.
- 제품 미지원·세대·Revision·금지 모델을 완전하게 차단하는 T-018 Service가 없다.
- `DEMO-INQ-002` 생성→증상 제출→추가 답변→자가조치 결과의 반복 실행 증빙이 없다.
- 작성자 로컬 결과를 비작성자·팀 기준선으로 승인한 기록이 없다.

완료 방법:

1. OpenAPI-only 문진 답변과 Action Result Endpoint를 Serializer·Service·Repository·Route로 구현한다.
2. 질문 ID·답변·작성자·시각·상태 Version을 Append-only로 저장하고 같은 문의의 현재 누적 Projection을 제공한다.
3. 자가조치 코드·결과·해결 여부·상담 필요를 저장하되 고객 피드백만으로 최종 RESOLVED 처리하지 않는다.
4. 각 입력의 멱등·409·Rollback·소유권·취소 상태·필수값 테스트를 추가한다.
5. T-018 제품 검증을 AI 호출 전 Gate로 연결한다.
6. `DEMO-INQ-002` 전체 누적 흐름을 새 DB에서 반복 실행하고 DB Row·상태 이력·응답을 결과서에 기록한다.

코드 외 별도 확인:

- 추가 답변의 Append·수정 정책과 Question ID를 이동윤·윤승혁과 확정해야 한다.
- 고객 원문 조회 범위를 상담사·기사·운영 역할별로 승인받아야 한다.
- Data·QA 또는 다른 비작성자가 PostgreSQL Slice A를 재현했는지 확인해야 한다.

### 3.4 State Machine Event API Runtime 확장 (`T-023`) — 45%

완료된 내용:

- State 계약 Loader·Validator·Engine·Guard·Allowed Action Resolver가 있다.
- `START_INQUIRY`, `SUBMIT_SYMPTOM`, DRAFT 고객 `CANCEL_INQUIRY` Runtime이 있다.
- `SUBMIT_SYMPTOM`은 State Machine·Guard·이력·409·Replay를 실제 사용한다.
- 상태 변경·이력·멱등 원장이 Transaction에 포함된다.
- stale Version과 Submit PostgreSQL 동시성에서 한 버전 승자만 허용하는 증거가 있다.
- 응답에 현재 상태·`state_version`·`allowed_actions`가 포함된다.
- 계약 전체와 현재 Runtime 세 Slice를 문서에서 구분한다.

수행 근거:

- [Workflow Engine](../../../backend/apps/workflow/engine)
- [Allowed Action Resolver](../../../backend/apps/workflow/engine/allowed_action_resolver.py)
- [Guard Evaluator](../../../backend/apps/workflow/engine/guard_evaluator.py)
- [Transition History Service](../../../backend/apps/workflow/services/transition_history_service.py)
- [Idempotency Service](../../../backend/apps/workflow/services/idempotency_service.py)
- [T-023 인계서](API/Django_State_Machine_API_구현_검증_인계서.md)

완료되지 않은 내용:

- AI 결과 Event `SAFE_GUIDANCE_READY`, `DANGER_DETECTED`, `NO_EVIDENCE`, `PRODUCT_VALIDATION_FAILED` Runtime이 없다.
- `REQUEST_CONSULTATION` Runtime이 없다.
- `START_INQUIRY`와 `CANCEL_INQUIRY`는 공통 StateMachine·Guard 경로를 완전히 사용하지 않는다.
- `allowed_actions`는 정적 State·Role 후보이며 Visit·담당자·동적 Guard를 반영하지 않는다.
- 상담·방문·완료·재개 Action과 역할·담당자 권한 Runtime이 없다.
- 상태 이력 조회 API와 SYSTEM Actor 이력 정책이 없다.
- CANCEL·SYSTEM·Inquiry+Visit의 PostgreSQL 동시성·잠금 순서가 검증되지 않았다.
- G2 신규 11개 Action은 계약만 있고 모두 `NOT_IMPLEMENTED`, QA·Runtime Gate도 닫혀 있다.

완료 방법:

1. 4주차 범위인 AI 결과 4종과 `REQUEST_CONSULTATION`의 Action별 Endpoint·Service·Guard를 먼저 구현한다.
2. AI Schema·제품·근거·stale Version 검증 뒤 Backend가 SYSTEM Event를 선택하도록 한다.
3. `START_INQUIRY`·CANCEL을 공통 Engine·Guard 경로로 리팩터링하되 공개 계약 회귀를 유지한다.
4. Allowed Action Resolver가 소유권·담당자·Visit·동적 Guard를 반영하도록 보강한다.
5. 상태 이력 조회와 SYSTEM Actor·업무 사유를 저장할 Repository·Service를 구현한다.
6. Event별 권한·멱등·409·Rollback·동시성 테스트와 Runtime 구현 상태표를 갱신한다.
7. G2 QA 재검증과 PM 승인 후에만 신규 Runtime을 착수·완료 표시한다.

코드 외 별도 확인:

- AI 결과 Event와 상담 전환 정책은 이동윤·윤승혁 승인이 필요하다.
- G2의 Data·QA 재검증과 PM 최종 Gate가 열렸는지 확인해야 한다.
- Web·Mobile이 Backend `allowed_actions`를 그대로 소비하는지 확인해야 한다.

### 3.5 Backend↔AI 최소 수직 연동과 결과 저장 — 10%

완료된 내용:

- AI 계약 1.1.0과 FastAPI `/api/v1/ai/analyze`가 있다.
- Backend에는 Client·Mapper·Validator·Retry Policy 파일 경로와 AI Run·Retrieval·Evidence Model 기반이 있다.
- 이동윤이 요청·응답·Timeout·오류·저장 후보·10개 E2E Case를 협업요청서로 전달할 준비를 했다.
- AI가 DB 상태를 직접 변경하지 않고 Backend가 Event를 선택한다는 책임 경계가 문서화돼 있다.

수행 근거:

- [AI 계약](../../../contracts/ai)
- [Backend AI Client 골격](../../../backend/integrations/ai/client.py)
- [Inquiry AI Service 골격](../../../backend/apps/inquiries/services/inquiry_ai_service.py)
- [Backend AI 미해결 사항](연동_인계/Backend_AI_API_계약_구현_미해결_사항.md)
- [이동윤 Backend↔AI 협업요청서](../dongyoon/20260804_이동윤_최지용_Backend_AI_수직연동_협업요청서_v0.1.md)

완료되지 않은 내용:

- Backend AI Client·Request Mapper·Response Mapper·Schema Validator·Retry Policy가 모두 설명 문자열만 있다.
- `inquiry_ai_service.py`도 실행 코드가 없다.
- 실제 HTTP 호출, 인증·Timeout, JSON 파싱·Schema 검증과 오류 Mapping이 없다.
- AI Run·구조화·안전·근거·안내·실패 Stage 저장 Service와 Transaction 경계가 없다.
- Backend→AI→검색→DB를 같은 `correlation_id`로 추적할 수 없다.
- AI 실패 시 고객 입력 보존·상담 Fallback, stale·중복 결과 차단이 없다.
- Evidence 내부 필드와 공개 DTO 경계는 `PROPOSED`이고 Backend·Web·Data·PM 승인이 끝나지 않았다.
- 이동윤 협업요청서가 `READY_TO_SEND`이며 최지용의 회신이 없다.

완료 방법:

1. 이동윤 요청서에 `ACCEPT / CHANGE_REQUEST / BLOCKED`로 회신하고 호출 시점·Schema·Timeout·저장 Mapping을 확정한다.
2. Backend Client·Mapper·Validator·예외·설정을 계약 1.1.0으로 구현한다.
3. 증상 Transaction Commit 이후 멱등한 AI dispatch를 만들고 Backend 자동 재시도 0회·전체 Timeout 30초를 적용한다.
4. 정상·위험·근거 없음·503·504·Schema 오류를 AI Run과 안전한 상담 상태로 구분 저장한다.
5. stale `state_version`, 중복 `ai_request_id`, Evidence 재검증과 상태 Event 선택을 Transaction Test로 고정한다.
6. 팀 DB·실제 FastAPI에서 10개 공동 E2E를 실행해 요청·응답·DB Row·상태·로그를 기록한다.

코드 외 별도 확인:

- AI 호출 시점·Event·상담 Fallback은 이동윤·윤승혁과 공동 결정해야 한다.
- Evidence 공개 계약은 이동윤·한예나·김은진·윤승혁 승인이 필요하다.
- 팀 DB 최소 권한 DSN과 Secret 전달 경계를 김은진과 확인해야 한다.

### 3.6 케어 이력 등록·조회 최소 API 구현 (`T-019`) — 5%

완료된 내용:

- Care History·Schedule Model·Migration과 합성 Care Seed가 있다.
- Care Type 공통 코드와 Model 단위 테스트가 있다.
- T-019와 다음 예정일 산정 T-020의 개념 경계가 WBS와 지침서에 있다.

수행 근거:

- [Care Model](../../../backend/apps/care/models)
- [Care Migration](../../../backend/apps/care/migrations)
- [Care Seed](../../../backend/apps/care/management/commands/seed_demo_care_records.py)
- [Care 단위 테스트](../../../backend/tests/unit/care)
- [WBS](../../planning/md/WBS.md)

완료되지 않은 내용:

- Care URL·View·Serializer·History Service·Repository가 설명 문자열뿐이다.
- `/api/v1`에 Care Route가 등록되지 않았다.
- 고객 제품별 목록·상세·합성 등록 Runtime이 없다.
- 타 고객 접근, 역할 범위, 미래·구독 전 날짜, 종료 구독, Care Type, 중복 등록 테스트가 없다.
- AI가 최근 Care 이력을 조회할 Service Interface가 없다.
- T-019와 T-020 구현 상태를 구분하는 Runtime 문서와 Web·Mobile·AI 인계 예시가 없다.

완료 방법:

1. Care 목록·상세·등록 Serializer·Repository·Service·Permission과 Route를 구현한다.
2. Customer Product·Subscription·Care Type·관리일·수행자·출처 FK를 검증한다.
3. 고객 본인 범위와 상담사·운영 역할 범위를 적용하고 타 고객은 404로 숨긴다.
4. 날짜·구독 상태·Code·중복 등록·멱등 테스트를 작성한다.
5. AI 요청용 최근 Care History 조회 Interface를 추가하고 T-020 자동 예정일 계산과 분리한다.
6. 실제 응답 예시와 실행 결과를 Web·Mobile·AI 담당자에게 전달한다.

코드 외 별도 확인:

- 케어 등록 수행 주체와 중복 업무 식별자 정책을 김은진·윤승혁과 확정해야 한다.
- 공식 관리 주기 데이터가 없는 상태에서 T-020을 임의 구현하지 않았는지 확인해야 한다.
- AI가 필요한 최근 Care 범위와 개인정보 최소화를 이동윤과 합의해야 한다.

### 3.7 8월 6일 중간 발표 Backend 지원·발표 후 인계 — 20%

완료된 내용:

- OpenAPI 21개·Runtime 8개·OpenAPI-only 13개와 G2 신규 11개 미구현이 Manifest에 기록돼 있다.
- T-022·T-023 문서는 실제 세 Slice, Model 기반, 미구현 AI·상담·방문을 구분한다.
- Backend 인계 문서에는 Migration·Seed·권한·AI 제한과 재현 명령이 다수 정리돼 있다.
- 로그는 요청 Body·Authorization·Cookie·예외 Message를 기록하지 않는 Middleware와 테스트가 있다.

수행 근거:

- [G2 계약 Manifest](../../handoffs/g2_contract_manifest_20260804.json)
- [Runtime 구현 상태](../../api/runtime_implementation_status.md)
- [T-022 인계서](API/Django_REST_API_문의_증상제출_구현_검증_인계서.md)
- [T-023 인계서](API/Django_State_Machine_API_구현_검증_인계서.md)
- [로그 보안 테스트](../../../backend/tests/unit/common/test_request_logging.py)
- [Backend 팀 검토 체크리스트](연동_인계/Backend_팀_검토_인계_체크리스트.md)

완료되지 않은 내용:

- 8월 5일 발표 기준 Commit이 고정되지 않았다.
- `docs/testing/results/week4-backend-smoke.md`, 발표 검수 의견과 `docs/presentation/week4/**`가 없다.
- Liveness·Readiness·Migration·Seed·로그인·제품·문의·AI·상태·DB를 잇는 Smoke Test가 없다.
- 제품 조회와 AI 결과가 없으므로 대표 고객 수직 흐름을 실제로 재현할 수 없다.
- 발표용 초기화·Seed 단일 Script와 실패 대비 요청·응답·DB Row·상태 이력·로그·캡처 묶음이 없다.
- 발표자료 Backend 수치가 실제 코드와 일치하는지 최종 검수한 기록이 없다.
- 발표 당일 Backend 장애 대응 순서와 담당 확인이 없다.
- 발표 후 피드백과 5주차 Backend 인계는 아직 수행 전이다.

완료 방법:

1. 현재 Runtime·Mock·OpenAPI-only 수치를 발표자료와 대조하고 검수 의견을 작성한다.
2. T-018·AI 연동이 완료되지 않으면 시연 단계를 명시적 Mock 또는 Recorded Evidence로 분리한다.
3. `scripts/testing/week4_backend_smoke.py` 또는 동등 Script로 Liveness·Readiness·Migration·Seed·세 Runtime Slice·DB 이력을 점검한다.
4. 대표 Seed 초기화·실행 명령과 정적 요청·응답·DB Row·상태 이력·로그·Fallback을 패키지화한다.
5. `docs/testing/results/week4-backend-smoke.md`에 발표 Commit·환경·결과·제한을 기록한다.
6. 발표 후 `docs/handoffs/week4-to-week5/backend.md`에 Endpoint·Event 상태, 결함, Migration·Seed 주의, AI·상담·방문 진입 조건을 인계한다.

코드 외 별도 확인:

- 김은진 발표자료의 Backend 수치·표현을 최지용이 실제 검수했는지 확인해야 한다.
- 발표 당일 장애 대응·DB 초기화 역할과 순서를 팀이 합의했는지 확인해야 한다.
- 발표 후 질문·결함이 Issue와 5주차 일정으로 연결됐는지 확인해야 한다.

## 4. 코드만으로 완료 여부를 판단할 수 없는 항목

| 별도 확인 항목 | 확인 대상 | 완료 증거 |
| --- | --- | --- |
| 독립 PostgreSQL 재현 | 작성자 외 팀원의 Migration·Seed·회귀 실행 | 환경·명령·Exit code·DB 건수·검수자 |
| Data Crosswalk 승인 | 공용 Runtime 증빙 경로·Hash 수용 여부 | 김은진 QA 결과와 Data CI |
| G2 Runtime Gate | QA 재검증과 신규 Runtime 착수 허용 | 김은진 회신·윤승혁 최종 결정 |
| 제품 정책 | 등록·수정 허용 필드와 MVP 차단 범위 | Data·PM 결정 기록 |
| 추가 답변 정책 | Append·수정·거절·Question ID 기준 | AI·State·Backend 공동 결정 |
| AI 저장·Event | 호출 시점, 오류·stale·Evidence·상담 전환 | 이동윤·윤승혁 승인과 E2E |
| Web·Mobile 소비 | DTO·401/403/404/409·Replay 처리 | 한예나·양정현 검토·Smoke 결과 |
| 발표자료 수치 | Model·Migration·Runtime·OpenAPI-only 구분 | 김은진 자료 Review Log |
| 발표 운영 | 초기화·Fallback·장애 대응 역할 | 리허설 기록과 팀 확인 |
| 5주차 인계 | 미완료 Endpoint·Event의 담당자·목표일 | Issue·WBS·인계 문서 |

## 5. 완료 기준 및 최종 체크리스트

아래 체크는 업무 지침서의 문구를 그대로 기준으로 판정했다. Model·Migration·파일 또는 OpenAPI가 있어도 **실제 URL·Service·DB 결과가 없으면 Runtime 완료로 체크하지 않았다.**

### 5.1 8월 5일 발표 기준 동결 전 필수 완료 기준

- [ ] 빈 PostgreSQL에서 전체 Migration이 성공한다.
- [ ] `makemigrations --check` 결과가 변경 없음이다.
- [ ] Seed·합성 Fixture Import가 반복 실행 가능하다.
- [ ] Liveness와 DB Readiness가 분리되어 동작한다.
- [x] Runtime URL과 OpenAPI 구현 상태가 대조되어 있다.
- [ ] 고객 본인 제품·구독 목록·상세 조회가 동작한다.
- [ ] 미지원·차단 제품이 문의와 AI 실행 전에 차단된다.
- [x] 문의 생성 후 증상 제출이 실제 Runtime에서 동작한다.
- [ ] 같은 문의에 추가 답변과 자가조치 결과가 누적된다.
- [ ] 고객 원문과 AI 구조화 결과가 분리 저장된다.
- [x] `SUBMIT_SYMPTOM` 상태 전이가 동작한다.
- [ ] 발표 필수 State Machine Event가 Runtime에 연결되어 있다.
- [ ] `allowed_actions`가 계약과 일치한다.
- [x] `state_version` 동시 충돌이 409로 처리된다.
- [x] `idempotency_key` Replay와 Conflict가 구분된다.
- [ ] 타 고객 제품·구독·문의 접근이 404로 차단된다.
- [x] 역할 부족은 403, 미인증은 401로 구분된다.
- [ ] Backend↔AI 요청·응답 Schema가 일치한다.
- [ ] AI 실패·Timeout·Schema 오류가 안전하게 처리된다.
- [x] AI가 DB 상태를 직접 변경하지 않는다.
- [ ] 대표 `DEMO-INQ-002` 수직 흐름이 반복 재현된다.
- [ ] 발표 기준 Commit과 실행 명령이 기록되어 있다.

### 5.2 8월 6일 중간 발표 지원 기준

- [ ] 발표자료의 Backend 수치가 실제 코드와 일치한다.
- [ ] Model·Migration 수와 Runtime 기능 수가 구분되어 있다.
- [ ] 실제 구현·Mock·미구현 기능이 발표자료에서 구분된다.
- [ ] 발표 직전 Liveness·Readiness·Migration·Seed Smoke Test가 통과한다.
- [ ] 가상 로그인→제품 조회→문의 생성→증상 제출→AI 결과→상태 반환 흐름이 준비되어 있다.
- [ ] 실패 대비 요청·응답·DB Row·상태 이력 증빙이 준비되어 있다.
- [ ] 시연 초기화와 Seed 명령을 다른 팀원이 실행할 수 있다.
- [ ] 발표 당일 Backend 장애 대응 담당과 순서가 정해져 있다.

### 5.3 8월 7일 최종 정리 기준

- [ ] 케어 이력 목록·상세·등록 최소 기능이 동작한다.
- [ ] Web·Mobile·AI 담당자의 계약 피드백이 반영되었다.
- [ ] 발표 후 발견된 회귀가 수정되었거나 Issue로 등록되었다.
- [x] OpenAPI·Serializer·실제 응답 차이가 정리되었다.
- [x] 권한·멱등성·동시성·Transaction Test가 보강되었다.
- [x] 로그에 Token·개인정보·고객 원문 전체·AI Prompt 전체가 노출되지 않는다.
- [x] 완료·부분 완료·미구현 Endpoint와 Event가 문서화되어 있다.
- [ ] 5주차 상담·방문·Evidence·AI 실패 처리 진입 조건이 인계되었다.
- [x] 김은진의 Data QA가 참조하는 Backend Runtime 증빙이 유효하다.
- [ ] 필수 업무 완료 후에만 4장의 추가 업무를 시작했다.

현재 완료로 체크 가능한 항목은 **12개**다.

1. Runtime URL–OpenAPI 구현 상태 대조
2. 문의 생성 후 증상 제출 Runtime
3. `SUBMIT_SYMPTOM` 상태 전이
4. `state_version` 동시 충돌 409
5. `idempotency_key` Replay·Conflict 구분
6. 401·403 구분
7. AI가 DB 상태를 직접 변경하지 않는 책임 경계
8. OpenAPI·Serializer·실제 응답 차이 정리
9. 권한·멱등성·동시성·Transaction Test 보강
10. 요청·예외 로그 민감정보 비노출
11. 완료·부분 완료·미구현 Endpoint·Event 문서화
12. Data QA가 참조하는 Backend Runtime 증빙 유효성

## 6. 우선순위 제안

1. **P0:** G2 QA 재검증 회신을 받아 Runtime Gate 확정
2. **P0:** T-018 제품·구독 Runtime과 MVP 제품 차단 Service 구현
3. **P0:** 이동윤과 Backend AI Client·저장·SYSTEM Event E2E 구현
4. **P0:** T-022 추가 답변·자가조치 누적과 `DEMO-INQ-002` 재현
5. **발표 전:** 현재 HEAD Migration·Seed·Readiness·Smoke·발표 Commit 증거 고정
6. **발표 후:** T-019 Care Runtime, 상담·방문·Evidence·AI 실패의 5주차 인계 정리

