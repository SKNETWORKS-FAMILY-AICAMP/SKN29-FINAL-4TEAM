# 양정현 3주차 작업 진행도

- 작성 일시: **2026-07-29 16:14:58 +09:00 (Asia/Seoul)**
- 구조 기준 보정 일시: **2026-07-29 16:23:36 +09:00 (Asia/Seoul)**
- 기준 문서: `docs/architecture/프로젝트 디렉토리 구조 v2.md`, `docs/planning/md/팀원별 관할 영역 v2.md`, `docs/weekly-task/양정현_3주차_업무_지침서.md`
- 점검 범위: 양정현 주관할인 `mobile/**`와 관련 공동 검토 문서
- 점검 방식: `mobile/**`를 변경하지 않는 읽기 전용 정적 점검만 수행했다. Gradle Sync·빌드·테스트·Wrapper bootstrap처럼 파일·캐시·산출물을 만들 수 있는 명령은 실행하지 않았다. 지속 변경은 이 진행도 보고서 한 파일뿐이다.
- 판정 원칙: 화면이나 파일이 존재하는 것만으로 완료 처리하지 않고, 지침서 3.1~3.6의 완료 기준과 현재 계약 정합성까지 확인했다. 디렉토리·모듈 구조는 더 최신인 두 v2 문서를 우선 적용하며, 역할 지침서의 단일 `app` 모듈 문구는 폐기된 기준으로 본다. 빌드·에뮬레이터·실제 API·PR·담당자 확인은 별도 확인 대상으로 분리했다.

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 핵심 사유 |
| --- | ---: | --- | --- |
| 3.1 Android 실행 환경과 공통 구조 | 70% | 부분 완료 | 승인된 3모듈 구조와 Compose 설정·README·빌드 스크립트는 있으나 현재 빌드·에뮬레이터 증빙과 일부 문서·applicationId 정합성이 없음 |
| 3.2 고객 최소 화면과 Navigation | 50% | 부분 완료 | 최신 구조 문서에 명시된 화면 배치와 Navigation은 구현됐으나 CUST 계약, Route 식별자, 상태별 Mock, ViewModel 기반 화면 구조가 충족되지 않음 |
| 3.3 ViewModel·StateFlow·직렬화 | 20% | 미완료 | 승인 구조의 AppStateStore와 StateFlow 저장소는 있으나 ViewModel·요청 DTO·Kotlinx Serialization·Mapper가 없으며 확정 상태 계약과 불일치함 |
| 3.4 Backend API와 Mock Repository | 20% | 미완료 | 일부 HttpURLConnection API가 있으나 Retrofit·OkHttp·Repository/Fake·공통 오류·인증 갱신·409 처리 구조가 없음 |
| 3.5 AI 결과·위험도·공식 근거 | 15% | 미완료 | 정적 안내 문구는 있으나 AI/안전 DTO·Mapper·상태별 Mock·Evidence 공개 필드·안전 Fallback 계약이 없음 |
| 3.6 테스트·기술 결정·제출 | 10% | 미완료 | README·마이그레이션 기록 외 테스트 코드·3주차 문서·현재 빌드 결과·PR 증빙이 없음 |

단순 평균 기준 종합 완료도는 **약 31%**이다. `customer-app`·`technician-app`·`core` 3모듈 구조는 최신 구조 문서에 반영된 승인 구조이므로 감점하지 않았다. 다만 3주차 필수 범위인 고객 CUST-01·02·04의 계약 기반 구조, 상태·안전·Evidence 처리, 테스트와 검토 산출물은 완료 기준에 미치지 못한다.

## 2. 주요 확인 결과

### 구현되어 있는 기반

- `customer-app`, `technician-app`, `core` 3개 Gradle 모듈과 Compose 앱 진입점이 있다.
- 이 3모듈 구성과 앱 간 의존 방향은 `프로젝트 디렉토리 구조 v2.md` 및 `팀원별 관할 영역 v2.md`에 명시된 현재 기준과 일치한다.
- 고객 홈·QR·문진·결과·방문 요청·기사 추적 화면과 기사 업무 화면이 존재한다.
- API Base URL과 Kakao Key는 BuildConfig 및 `local.properties.example`로 분리되어 있다.
- `local.properties`, keystore, APK와 build 디렉터리는 Git 제외 규칙이 있고 현재 추적되지 않는다.
- 고객 문의와 위치 상태를 보관하는 일부 StateFlow 저장소가 있다.
- `verify-build.bat`, Wrapper bootstrap, local.properties 설정 스크립트가 있다.

### 현재 완료 판정을 막는 핵심 문제

- 확정 State Machine에 없는 `ERROR_CONFIRMED` 상태와 `ConfirmError`, `RequestVisit`, `CompleteVisit` 자체 이벤트를 사용한다.
- 계약의 `AI_GUIDANCE`, `CONSULTATION_REQUIRED`, `CONSULTATION_IN_PROGRESS`, `VISIT_SCHEDULING`, `REVISIT_REQUIRED`, `REOPENED`가 Mobile Enum에 없다.
- Mobile의 `RequestVisit`는 상담·방문 검토 단계를 건너뛰고 `VISIT_SCHEDULED`로 직접 전환한다.
- Inquiry 모델에 `state_version`, `allowed_actions`가 없고 409 최신 Snapshot·입력 유지 처리가 없다.
- ViewModel, Kotlinx Serialization DTO, Retrofit·OkHttp, 공통 API 오류, 인증 갱신·로그아웃, P0 Fake Repository가 없다.
- 고객·기사 Composable이 API 객체를 직접 호출하는 경로가 있어 지침서의 Repository/ViewModel 분리 원칙과 어긋난다.
- 테스트 경로에는 `.gitkeep`만 있고 실행 가능한 단위·Compose 테스트가 없다.
- `mobile/docs/week3-*.md` 문서가 모두 없다.

## 3. 필수 업무별 상세 평가와 완료 방법

## 3.1 Android 프로젝트 실행 환경과 최소 공통 구조 구성 — 70%

### 확인된 완료 내용

- Gradle 설정, Android Manifest, Compose 앱 진입점, Material Theme와 Navigation 의존성이 있다.
- `com.skn29.watercare` 계열 package와 고객·기사 앱별 namespace가 사용된다.
- `BACKEND_BASE_URL`, Kakao Native Key가 화면 코드가 아닌 BuildConfig에 주입된다.
- `10.0.2.2:8000` 기본 Backend 주소와 Mock 지도 Fallback이 문서화되어 있다.
- Hilt·Room·WorkManager·FCM은 불필요하게 추가되지 않았다.
- 민감한 local.properties·keystore·빌드 산출물이 Git에 추적되지 않는다.
- `verify-build.bat`는 core build와 두 APK의 assembleDebug를 순서대로 실행하도록 작성되어 있다.
- `customer-app`, `technician-app`, `core`의 역할과 `customer-app → core`, `technician-app → core` 의존 방향이 최신 구조 문서와 일치한다.
- 고객 화면·기사 화면·공통 모델의 실제 배치도 `프로젝트 디렉토리 구조 v2.md`에 기술된 현재 구조와 대체로 일치한다.

### 완료되지 않은 항목

- 업무 지침서의 단일 `app` 모듈 요구는 최신 `프로젝트 디렉토리 구조 v2.md`와 `팀원별 관할 영역 v2.md`에 의해 대체된 낡은 기준이다. 현재 3모듈 구조는 미완료 항목이 아니며, 역할 지침서 문구만 최신 구조로 갱신해야 한다.
- `gradle-wrapper.jar`, `local.properties`, Debug APK 산출물이 현재 환경에 없어 Wrapper 빌드와 에뮬레이터 실행 성공을 확인할 수 없다.
- 현재 의존성에 지침서가 요구한 Kotlinx Serialization, Retrofit, OkHttp가 없다.
- `mobile/README.md`의 구조 예시는 `mobile2/`로 적혀 있어 실제 `mobile/` 경로와 다르다.
- README는 고객 앱 `applicationId=com.skn29.watercare`를 유지한다고 적지만 실제 `customer-app/build.gradle.kts`는 `com.skn29.watercare.customer`이다.
- Mock 사용 여부를 전환하는 명시적 BuildConfig가 없고, API·실제 화면·시연 데이터의 전환 기준이 문서화되지 않았다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `docs/weekly-task/양정현_3주차_업무_지침서.md`의 단일 `app` 모듈 문구를 최신 3모듈 기준으로 갱신한다. 코드는 단일 모듈로 되돌릴 필요가 없다.
2. `mobile/README.md`의 `mobile2/`와 applicationId 설명을 실제 설정과 일치하게 수정한다.
3. Wrapper JAR 관리 정책을 확정하고, 저장소에 포함하지 않는다면 bootstrap 명령·필요 네트워크·Gradle 버전과 무결성 확인 방법을 README에 적는다.
4. `BuildConfig`에 `USE_MOCK_API` 등 명시적 전환 값을 추가하고 고객·기사 화면이 같은 설정 원천을 사용하게 한다.
5. 양정현 관할 환경에서 `:core:build`, `:customer-app:assembleDebug`, `:technician-app:assembleDebug`를 실행하고 JDK·SDK·Gradle·Commit·결과를 `mobile/docs/week3-test-result.md`에 기록한다.

### 코드 외 별도 확인

- 역할 지침서가 최신 구조 문서를 참조하도록 갱신되었는지만 문서 리뷰로 확인해야 한다.
- Android Studio Gradle Sync와 고객·기사 APK의 실제 설치·실행을 각각 확인해야 한다.
- Kakao package 등록이 현재 customer applicationId와 일치하는지 확인해야 한다.
- 다른 팀원이 README와 bootstrap 절차로 Wrapper·local.properties를 준비하고 빌드할 수 있는지 확인해야 한다.

## 3.2 고객 최소 화면과 Navigation 흐름 구현 — 50%

### 확인된 완료 내용

- 고객 홈→문진→결과→방문 요청 Navigation 흐름이 Compose 코드에 존재한다.
- 홈 화면에 WPU-JAC104D 제품, 관리 유형, 문진·QR 진입과 진행 방문을 표시한다.
- 문진 화면에 출수량 저하·누수·온도·물맛/냄새·기타 증상, 상세 설명과 안전 확인 입력이 있다.
- 결과 화면에 제품 코드, 증상 요약, 사용 제한 안내와 공식 문서 명칭을 표시한다.
- 미지원 QR 오류 코드도 앱을 종료시키지 않는 Fallback 코드가 있다.
- `Routes.kt`, `WaterCareApp.kt`, `CustomerScreens.kt`, `AppStateStore.kt`의 현재 배치는 최신 구조 문서에 명시된 설계와 일치한다.

### 완료되지 않은 항목

- Route가 `inquiryId`, `questionnaireSessionId`, `entryMode`를 받지 않으며 공개 UUID와 화면용 문의번호를 구분하지 않는다.
- `InquiryDraft.inquiryId` 기본값은 UUID가 아닌 `DEMO-INQ-002`이다.
- CUST-02의 증상은 복수 선택이 아니라 단일 선택이며 기본값과 기본 설명이 미리 입력되어 있다.
- CUST-02 상태가 Composable의 `remember`에 있고 ViewModel에 없어 재구성·프로세스 복원·제출 실패 후 보존을 보장하지 못한다.
- 결과 화면은 CUST-04 계약의 위험도·사용 안내 상태·제한 기능·안전 행동·상담 조건·allowed_actions를 표시하지 않는다.
- 일반·위험·근거 없음 상태별 Mock이 분리되어 있지 않고 대부분의 문진을 방문 필요로 직접 처리한다.
- Evidence 영역에 문서 버전·페이지·구조화 요약·데이터 분류·공식 URL이 없다.
- 화면은 확정 계약 상태 대신 `ERROR_CONFIRMED`와 로컬 상태 전이를 사용한다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 고객 흐름을 `feature/customer/home`, `intake`, `guidance`로 분리하고 화면별 ViewModel·UiState를 작성한다.
2. Navigation 계약에 공개 UUID `inquiryId`, `questionnaireSessionId`, `entryMode`를 추가하고 업무 문의번호와 타입을 분리한다.
3. 문진을 다중 증상·고객 원문·발생 조건·표시 문구로 구성하고, 선택 증상이 없을 때의 필수 원문 검증을 ViewModel에서 수행한다.
4. 정상·위험·근거 없음·AI 실패·네트워크 실패·미지원 제품 Fixture를 만들고 같은 화면을 UiState로 재현한다.
5. CUST-04를 위험도, 사용 제한, 안전 행동, 상담 조건, Evidence, 증상 요약, 금지 행동 순서로 재구성한다.
6. 화면명·필드·행동을 `mobile/docs/week3-api-ai-field-map.md`에서 화면설계서와 대조한다.

### 코드 외 별도 확인

- CUST-01·02·04에 해당하는 현재 화면의 공식 명칭과 화면설계서 일치 여부를 PM이 확인해야 한다.
- 에뮬레이터에서 뒤로가기·재진입·회전·프로세스 재생성 후 입력 유지 여부를 확인해야 한다.
- 제품·관리·고객 표시 정보가 합성 데이터이며 실제 개인정보가 아닌지 확인해야 한다.

## 3.3 ViewModel·StateFlow 상태 관리와 입력 직렬화 — 20%

### 확인된 완료 내용

- `AppStateStore`와 `TrackingRepository`가 일부 상태를 StateFlow로 노출한다.
- 알 수 없는 QR 오류 코드에서 앱이 즉시 종료되지는 않는다.
- 화면과 상태 모델을 `customer-app`과 `core`로 일부 분리했다.

### 완료되지 않은 항목

- 고객 홈·문진·안내 ViewModel과 화면별 UiState가 없다.
- CUST-02 입력·검증·제출 로직이 Composable과 전역 Store에 직접 들어 있다.
- 요청 DTO, Kotlinx Serialization, 서버 Enum Mapper와 직렬화 테스트가 없다.
- 로딩·입력 오류·네트워크 오류·업무 오류·AI 실패·근거 없음 상태가 하나의 안전한 UiState로 정의되어 있지 않다.
- 제출 중 중복 차단, 실패 후 입력 보존, 재시도 상태가 없다.
- 확정 계약에 없는 `ERROR_CONFIRMED`와 자체 상태·이벤트를 사용하고 필수 상태 6개가 빠져 있다.
- 알 수 없는 오류 코드를 안전한 상담 상태로 매핑하지 않고 이름과 방문 필요를 앱에서 추정한다.
- `state_version`, `allowed_actions`, `usage_guidance_status`, 위험도·데이터 분류 모델이 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 화면별 `ViewModel`과 단일 `UiState`를 작성해 입력·검증·제출·재시도·오류 확인을 함수로 분리한다.
2. `SymptomIntakeRequest`, `SymptomTopic`, Mapper를 Kotlinx Serialization로 작성하고 계약 JSON 이름·필수값·UNKNOWN 처리 테스트를 추가한다.
3. Mobile Inquiry Enum을 State Machine v1.0.0 상태만 사용하도록 교체하고 자체 이벤트·로컬 next-state 계산을 제거한다.
4. `state_version`, `allowed_actions`, 공개 UUID, 업무 문의번호를 DTO와 View Model에 추가한다.
5. 알 수 없는 서버 코드는 `UNKNOWN` 또는 `PENDING_CONSULTATION` 안전 UI로 보내고 방문·고장 여부를 모바일이 추정하지 않게 한다.
6. 제출 중 버튼 비활성화와 동일 입력 Snapshot·실패 후 입력 유지 동작을 단위 테스트한다.

### 코드 외 별도 확인

- State Machine v1.0.0의 상태·행동 표시 책임을 윤승혁·최지용과 확인해야 한다.
- CUST-02 요청 필드와 Enum을 최지용의 OpenAPI 예시와 대조해야 한다.
- AI 실패·근거 없음의 안전 Fallback 문구를 이동윤이 확인해야 한다.

## 3.4 백엔드 API와 Mock Repository 연동 — 20%

### 확인된 완료 내용

- Backend Base URL이 BuildConfig로 분리되어 있다.
- 고객·기사 Service Call과 위치 추적에 HttpURLConnection 기반 API 호출 코드가 있다.
- 연결·읽기 Timeout과 일부 Authorization Header가 구현되어 있다.
- 서버가 없을 때 지도 기능 일부는 Demo Fallback을 사용한다.

### 완료되지 않은 항목

- 지침서가 요구한 Retrofit·OkHttp Client, Serialization Converter, Auth·Trace Interceptor가 없다.
- 고객 P0 화면용 Repository 인터페이스와 Fake 구현이 없고 Composable이 ServiceCallApi를 직접 호출한다.
- 공통 `ApiResponse`, `ApiError`, HTTP 성공과 업무 성공 분리, 오류 코드·재시도 가능 여부 Mapper가 없다.
- Access Token 60분·Refresh Token 7일 정책, 401 한 번 갱신·원요청 재시도, 실패 시 Token 제거·로그인 이동과 로그아웃 폐기 API가 없다.
- `correlation_id`, `idempotency_key`, `state_version`, `allowed_actions` 전달·반영이 없다.
- 400·401·403·404·409·5xx·파싱·AI 실패가 사용자 상태로 구분되지 않는다.
- 상태 변경을 승인된 행동 Endpoint로 요청하지 않고 Mobile의 자체 State Machine과 Store가 상태를 직접 바꾼다.
- `api/service-calls/**` 임시 경로가 `Fake`가 아닌 실제 API 객체로 구현되어 있으며 공통 계약과의 연결 문서가 없다.
- 정상·위험·근거 없음·AI 실패·네트워크 실패·미지원 제품의 P0 Mock 데이터가 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `core/network`에 Retrofit·OkHttp Client, 공통 Wrapper·오류 DTO, Auth·Trace Interceptor와 Token Store를 작성한다.
2. 고객 홈·문진·안내 Repository 인터페이스와 `Fake*Repository`, `Remote*Repository`를 분리하고 화면은 ViewModel을 통해 인터페이스만 사용하게 한다.
3. 401 Refresh single-flight 또는 동등 안전 구조, 원요청 1회 재시도, 실패 시 세션 제거·로그인 이동과 로그아웃 폐기를 구현한다.
4. 쓰기 요청에 최신 `state_version`, Idempotency-Key, Correlation ID를 적용하고 409 최신 Snapshot을 UiState에 반영한다.
5. 현재 Service Call·Tracking Endpoint가 승인 계약인지 최지용에게 확인하고, 임시라면 이름·패키지·문서에서 명시적인 provisional/Fake로 분류한다.
6. 오류 코드·재시도 가능 여부를 사용자 문구로 Mapping하고 서버 원문을 그대로 노출하지 않는다.
7. `mobile/docs/week3-open-issues.md`에 실제 API 제공 여부, 임시 Endpoint, Mock 교체 지점과 담당자를 기록한다.

### 코드 외 별도 확인

- Backend `/health`, 인증, 문의, 상담 요청, 방문 API의 실제 URL과 응답을 최지용에게 확인해야 한다.
- 실제 Token이 Logcat·오류 문구·Crash 로그에 남지 않는지 확인해야 한다.
- 에뮬레이터의 `10.0.2.2` HTTP 연결과 네트워크 보안 설정을 실제 서버에서 확인해야 한다.

## 3.5 AI 결과·위험도·공식 근거 표시 — 15%

### 확인된 완료 내용

- 결과 화면에 사용 제한 안내, 공식 문서 명칭과 검증 완료 문구가 있다.
- 누수·전기 위험 시 제품 사용 중지와 상담 우선 문구를 표시한다.
- 내부 `chunk_id`, 내부 경로, 검색 점수와 원문 전체를 현재 정적 화면에 표시하지 않는다.
- 위험 화면에 일반 해결·문의 종료 버튼은 없고 방문기사 요청을 제공한다.

### 완료되지 않은 항목

- `GuidanceResponse`, `UsageGuidanceDto`, `EvidenceCardDto`, `GuidanceMapper`, GuidanceViewModel·UiState가 없다.
- `risk_level`, `NORMAL`, `PARTIAL_STOP`, `TOTAL_STOP`, `PENDING_CONSULTATION`, 제한 기능·안전 행동·상담 조건·금지 행동 모델이 없다.
- 일반·주의·위험·근거 없음·AI 실패 상태별 Mock JSON과 렌더링 경로가 없다.
- 현재 문진 결과 대부분이 방문 필요로 결정되어 정상·자가 해결 흐름과 근거 없음 Fallback을 검증할 수 없다.
- EvidenceCard에 문서 버전·페이지·구조화 요약·데이터 분류·공식 URL이 없다.
- 알 수 없는 오류 코드에서 제품 고장·방문 필요 의미를 앱이 추정한다.
- 이동윤·최지용의 사용 필드·공개 필드 검토 기록이 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. Backend가 검증한 Guidance 응답 DTO와 Mapper를 작성하고 Mobile 내부 안전 상태로 변환한다.
2. 일반·주의·위험·근거 없음·AI 실패 Fixture를 작성해 같은 GuidanceScreen에서 재현한다.
3. 위험·상담 필수 상태에서는 해결·종료 행동을 숨기고 Backend `allowed_actions`만 표시한다.
4. EvidenceCard에 승인된 문서명·버전·페이지·요약·검증 상태·분류·공식 URL만 전달하는 공개 View Model을 작성한다.
5. `chunk_id`, `source_path`, `retrieval_text`, 원문, 내부 URL을 DTO→UI Mapper에서 제거하는 테스트를 추가한다.
6. 근거 없음·미확인 코드는 `PENDING_CONSULTATION`과 상담 Fallback으로 처리하고 임의 자가조치·고장 확정·방문 판정을 제거한다.

### 코드 외 별도 확인

- 위험·금지 행동·상담 Fallback 문구를 이동윤이 검수해야 한다.
- Evidence 페이지·문서 버전·공식 URL의 공개 허용 범위를 이동윤·최지용이 확인해야 한다.
- 위험도가 색상 외 문구·아이콘·접근성 설명으로 구분되는지 에뮬레이터에서 확인해야 한다.

## 3.6 테스트·기술 결정 기록·7월 29일 제출 — 10%

### 확인된 완료 내용

- 프로젝트 README, 모듈별 README와 마이그레이션 보고서가 있다.
- 빌드 순서를 자동화한 `verify-build.bat`가 있다.
- Mobile 구현은 Git에 추적되어 있고 최근 Mobile Commit 기록이 존재한다.

### 완료되지 않은 항목

- `core/src/test`, `customer-app/src/test`, `customer-app/src/androidTest`, 기사 앱 테스트 경로에 `.gitkeep` 외 테스트가 없다.
- 문진 검증·입력 유지·직렬화·Guidance Mapper·근거 없음·인증 갱신·403·404·409 테스트가 없다.
- 고객 홈→문진 이동, 필수 입력 오류, 위험 상담 문구의 Compose·Navigation 테스트가 없다.
- `mobile/docs/week3-mobile-decisions.md`, `week3-api-ai-field-map.md`, `week3-test-result.md`, `week3-open-issues.md`가 없다.
- 현재 Debug 빌드·에뮬레이터 실행·수동 화면 검증 결과와 캡처가 없다.
- 검토용 PR·공유 파일, 리뷰 의견과 7월 30~31일 변경 기록을 확인할 수 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. core에 State/Enum/Mapper 테스트, customer-app에 ViewModel·직렬화·409 테스트를 작성한다.
2. CUST-01→02→04, 입력 오류, danger·no-evidence UI와 내부 Evidence 필드 비노출 Compose 테스트를 추가한다.
3. `mobile/docs/week3-mobile-decisions.md`에 3모듈 채택 여부, Mock 교체, 인증, 테스트 범위와 보류 기술을 기록한다.
4. `mobile/docs/week3-api-ai-field-map.md`에 공개 UUID·문의번호·요청/응답·오류·추적 Header·allowed_actions·Evidence 공개 필드를 매핑한다.
5. `mobile/docs/week3-test-result.md`에 JDK·SDK·Android Studio·에뮬레이터·빌드/테스트 명령·결과·캡처 경로를 기록한다.
6. `mobile/docs/week3-open-issues.md`에 계약 불일치·실제 API 미제공·후속 화면·담당자·기한을 기록한다.
7. 검토용 PR 또는 공유 문서에 WBS·변경 파일·재현 방법·미연동 범위를 연결한다.

### 코드 외 별도 확인

- Gradle Sync·두 APK Debug 빌드·에뮬레이터 실행을 양정현 환경에서 확인해야 한다.
- PR URL·리뷰어·리뷰 반영·병합 상태를 원격 저장소에서 확인해야 한다.
- 김은진이 테스트 재현 방법과 결과를 확인했는지 확인해야 한다.
- 7월 30~31일 계약·리뷰 수정 내역이 실제 변경 기록에 남았는지 확인해야 한다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 사항

| 확인 대상 | 필요한 별도 증빙 또는 확인자 |
| --- | --- |
| 역할 지침서의 구조 기준 갱신 | 최신 두 v2 문서를 참조하는지 문서 리뷰로 확인. 3모듈 구조 자체에 대한 추가 승인은 불필요 |
| Gradle Sync·Debug 빌드·APK 설치 | JDK·SDK·Gradle·Commit이 포함된 실행 로그와 APK 경로 |
| CUST-01·02·04 에뮬레이터 화면 | 화면 캡처·Navigation·회전·재진입 수동 결과 |
| 실제 Backend API와 인증 | 최지용의 Endpoint·OpenAPI·테스트 계정·응답 Fixture |
| AI 안전·근거 공개 필드 | 이동윤의 DTO·문구·Evidence 검수 기록 |
| 개인정보·Token·Log 안전 | 합성 계정 확인, Logcat·Crash·네트워크 로그 점검 |
| PR·자료 전달·리뷰 완료 | 원격 PR URL, 리뷰 코멘트, 수신자 회신 |
| 추가 업무 착수 순서 | Issue·Commit·PR 시간 순서와 PM 확인 |

## 5. 완료 기준 및 최종 체크리스트 판정

## 5.1 7월 29일 필수 완료 기준

### 현재 완료로 체크해도 되는 항목

역할 지침서의 원문 체크리스트 중 구조 항목은 최신 문서와 충돌하므로 문구 그대로 체크하지 않고, 아래 대체 기준으로 **완료 처리해도 된다.** 그 밖의 항목은 실행·계약·문서 조건을 함께 만족하지 못한다.

- [x] `customer-app`, `technician-app`, `core` 3모듈 구조와 `com.skn29.watercare` 계열 패키지 구조가 적용되어 있다. *(최신 v2 구조 문서에 따른 대체 완료 기준)*

### 최신 기준으로 적용 제외하는 원문 항목

- 단일 `app` 모듈과 `com.skn29.watercare` 구조가 적용되어 있다.
  - 단일 모듈 요구는 폐기된 기준이므로 미완료가 아니라 **적용 제외(N/A)**이다. 위의 3모듈 대체 기준은 완료로 판정한다. 단, 고객 앱 `applicationId`가 구조 문서·README의 `com.skn29.watercare`와 실제 설정의 `com.skn29.watercare.customer` 중 무엇이 맞는지는 별도 정합화가 필요하다.

### 아직 완료로 체크하면 안 되는 항목

- [ ] Gradle Sync·Debug 빌드·에뮬레이터 실행이 성공한다.
  - Wrapper JAR·빌드 산출물·실행 결과가 없고 이번 점검에서 빌드하지 않았다.
- [ ] CUST-01·02·04가 표시되고 최소 Navigation이 동작한다.
  - 유사 화면과 Navigation 코드는 있으나 CUST 계약·에뮬레이터 실행을 확인하지 못했다.
- [ ] CUST-02 입력 검증과 실패 후 입력 유지가 동작한다.
  - 기본 nonblank 검증만 있고 실패 흐름·ViewModel 입력 유지가 없다.
- [ ] ViewModel·StateFlow와 Kotlinx Serialization이 적용되어 있다.
  - StateFlow Store 일부만 있고 ViewModel·Serialization이 없다.
- [ ] Retrofit·OkHttp Client와 Repository 교체 지점이 있다.
  - HttpURLConnection 직접 호출이며 P0 Repository/Fake가 없다.
- [ ] 정상·위험·근거 없음·AI 실패·네트워크 실패 Mock이 있다.
  - 상태별 Mock이 없다.
- [ ] 위험 상태에서 일반 해결·종료 버튼이 보이지 않는다.
  - 현재 결과 화면에는 해결 버튼이 없지만 명시적 위험 상태 모델과 상태별 렌더링이 없어 조건을 검증할 수 없다.
- [ ] EvidenceCard에 내부 경로·`chunk_id`·원문 전체가 노출되지 않는다.
  - 정적 화면에는 노출되지 않지만 계약 DTO·Mapper·EvidenceCard 테스트가 없어 안전 경계를 검증할 수 없다.
- [ ] 최소 테스트 또는 수동 검증 결과가 있다.
  - 테스트 파일과 결과 문서가 없다.
- [ ] README, 기술 결정, 필드 매핑, 미해결 사항이 작성되어 있다.
  - README만 있고 필수 3주차 문서 4종이 없다.
- [ ] 7월 29일까지 검토 가능한 PR 또는 공유 파일이 있다.
  - 로컬에서 PR·공유·리뷰 증빙을 확인할 수 없다.

## 5.2 7월 30일~31일 최종 정리 기준

현재 저장소 정적 증거만으로 **완료 체크해도 되는 항목은 없다.**

- [ ] API·AI·State Machine 계약 변경이 DTO와 화면에 반영되었다.
  - 계약 외 상태·이벤트와 필수 상태 누락이 남아 있다.
- [ ] 다른 팀원이 README로 앱을 실행할 수 있다.
  - Wrapper bootstrap·README 경로/applicationId 정합화·실제 빌드 재현 확인이 필요하다.
- [ ] 빌드·Navigation·위험·근거 표시 오류가 수정되었다.
  - 현재 실행 결과와 상태별 검증이 없다.
- [ ] Mock과 실제 API 차이가 문서에 표시되어 있다.
  - 일부 README 설명은 있으나 P0 Mock/실제 API 매핑·임시 Endpoint가 체계적으로 기록되지 않았다.
- [ ] 최지용·이동윤·김은진에게 연동·테스트 자료를 전달했다.
  - 자료와 전달 증빙이 없다.
- [ ] 미완료 화면과 다음 주 작업이 Issue에 기록되어 있다.
  - `week3-open-issues.md` 또는 Issue 연결이 없다.
- [ ] 필수 업무 완료 후에만 4장의 추가 업무를 시작했다.
  - 기사·추적 등 추가 범위가 존재하지만 착수 순서를 코드만으로 판정할 수 없다.

## 6. 우선 처리 순서

1. 승인된 3모듈 구조를 기준으로 역할 지침서와 README를 갱신하고, 고객 앱 applicationId의 문서·설정 불일치를 확정된 값으로 통일한다.
2. Mobile 상태·이벤트를 State Machine v1.0.0과 맞추고 로컬 상태 전이를 제거한다.
3. 고객 CUST-01·02·04를 ViewModel·UiState·공개 UUID·계약 DTO 기반으로 재구성한다.
4. Retrofit·OkHttp·Repository/Fake·인증·공통 오류·state_version·allowed_actions·409 구조를 구현한다.
5. AI 안전·근거 DTO와 상태별 Mock·Evidence 비노출 테스트를 구현한다.
6. 단위·Compose 테스트와 3주차 문서 4종을 작성한 뒤 Gradle·에뮬레이터·PR·협업 검수 증빙을 남긴다.
