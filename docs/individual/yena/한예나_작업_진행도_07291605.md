# 한예나 3주차 작업 진행도

- 작성 일시: **2026-07-29 16:05:29 +09:00 (Asia/Seoul)**
- 요청된 기준 문서: `docs/planning/md/팀원별 관할 영역 v2.md`, `docs/weekly-task/한예나_업무_지침서.md`
- 실제 사용한 업무 지침서: 요청 경로의 파일이 없어 동일 대상인 `docs/weekly-task/한예나_3주차_업무_지침서.md`를 사용했다.
- 점검 범위: 한예나 주관할인 `web/**`와 공동 편집 산출물 `docs/testing/week3-web-review.md`
- 점검 방식: 타인 또는 한예나 관할 산출물을 변경하지 않는 읽기 전용 정적 점검만 수행했다. `npm test`, `npm run build`, Fixture 생성처럼 파일이나 캐시를 갱신할 수 있는 명령은 실행하지 않았다. 따라서 실행 성공 여부는 저장소에 기록된 2026-07-29 결과를 인용하고, 현재 작업 트리에서의 재실행은 별도 확인으로 분류한다.

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 핵심 사유 |
| --- | ---: | --- | --- |
| 3.1 React 실행 환경과 공통 구조 | 85% | 부분 완료 | Vite·Router·Guard·인증 갱신·오류 페이지·README가 있으나 현재 빌드 재실행 증빙과 다른 팀원의 README 재현 확인이 필요함 |
| 3.2 공통 API 계층과 상태·근거 UI | 85% | 부분 완료 | API Wrapper·오류·Request Context·공통 UI·EvidenceCard가 구현됐으나 실제 상담 API 계약과 Runtime 연동이 비어 있음 |
| 3.3 CONS-01 문의 목록 | 90% | 부분 완료 | 공식 Fixture 기반 목록·검색·필터·정렬·URL Query·페이지·상세 이동이 구현됐으나 실제 서버 조회 API와 문서 건수 갱신이 남음 |
| 3.4 CONS-02 문의 상세 | 85% | 부분 완료 | 상세 영역·부분 실패·AI 초안·Evidence·상태 이력이 Mock으로 구현됐으나 실제 상세 API·마스킹·공개 필드 공동 검수가 남음 |
| 3.5 상담 기록 Form과 행동 골격 | 80% | 부분 완료 | Form·allowed_actions·멱등성·409·422 Mock은 있으나 행동별 실제 Endpoint와 확정 DTO가 없음 |
| 3.6 테스트·필드 매핑·검토 산출물 | 80% | 부분 완료 | 테스트·결정·매핑·이슈·공유 문서는 있으나 현재 재실행, PR·리뷰 수신, 공식 산출물 의견 반영을 확인하지 못함 |

단순 평균 기준 종합 완료도는 **약 84%**이다. 3주차가 허용한 Mock 기반 상담사 P0 화면은 대부분 구현되어 있으나, 현재 작업 트리의 실행 재검증과 Backend 계약·실제 API·협업 승인 증빙이 남아 있어 필수 업무 전체를 100% 완료로 판정하지 않는다.

## 2. 확인된 저장소 증거와 제한

### 확인된 증거

- React 19·Vite·TypeScript 프로젝트와 `package-lock.json`이 존재한다.
- `web/docs/week3-test-result.md`와 `docs/testing/week3-web-review.md`에는 2026-07-29 기준 **20개 Test File, 80개 테스트 통과, lint 통과, Production Build 통과**가 기록되어 있다.
- 상담사·운영자 Router, `AuthGuard`, `RoleGuard`, 403·404·전역 오류 페이지가 존재한다.
- 공통 API Wrapper는 JSON, 인증 Header, Timeout, 오류 분류, 401 Refresh single-flight, 원요청 1회 재시도와 세션 제거를 구현한다.
- `CONS-01`, `CONS-02`, 상담 Form, `CONS-03` Mock, `ADMIN-01` Mock과 단위·컴포넌트·통합 테스트가 존재한다.
- `web/docs/`에 기술 결정, 화면–API–DB 매핑, 테스트 결과, 미해결 이슈, 완료 기준 대조표가 모두 존재한다.
- Web Mock은 `data/synthetic/fixtures/inquiries.json`을 직접 읽으며 현재 활성 문의는 **22건**이다.

### 점검 제한

- 현재 빌드·lint·테스트는 재실행하지 않았다. 기존 결과 문서가 현재 HEAD 및 작업 트리와 정확히 같은 상태에서 생성됐는지는 별도 확인해야 한다.
- 실제 Backend 상담 경로와 상담 요청 Schema는 현재 빈 계약이므로 Mock 결과를 실제 API 완료 증빙으로 보지 않는다.
- 브라우저 레이아웃, 콘솔 오류, 접근성, 실제 토큰 만료와 네트워크 동작은 정적 코드만으로 확정할 수 없다.
- PR 존재·리뷰 수신·타 팀원 자료 전달 여부는 로컬 저장소만으로 확정할 수 없다.

## 3. 필수 업무별 상세 평가와 완료 방법

## 3.1 React 웹 실행 환경과 역할별 공통 구조 구성 — 85%

### 확인된 완료 내용

- `package.json`, Vite·TypeScript 설정, `.env.example`, `main.tsx`, App Provider, Router, Layout, Guard와 시스템 오류 페이지가 있다.
- 로그인·상담사·운영자·403·404 경로와 경로 상수가 분리되어 있다.
- 미인증 사용자의 로그인 이동, 원래 경로 복귀, 상담사·운영자 역할 차단을 검증하는 통합 테스트가 있다.
- Access Token은 메모리 세션에 저장되고, 동시 401의 Refresh 단일화·원요청 1회 재시도·실패 시 세션 제거가 구현되어 있다.
- API Base URL과 Mock 인증·역할 설정은 `env.ts`와 `.env.example`에 모여 있다.
- 설치·실행·검증·Mock 전환·현재 연동 상태가 `web/README.md`에 기록되어 있다.

### 완료되지 않은 항목

- 현재 작업 트리에서 개발 서버·lint·test·Production Build가 성공하는지 이번 점검에서는 재실행하지 않았다.
- README의 설치 예시는 `C:\Users\Playdata\Desktop\skn29\final\SKN29-FINAL-4TEAM\web`이라는 개인 절대 경로를 사용해 다른 팀원이 그대로 실행하기 어렵다.
- 실제 Backend 로그인·Refresh rotation·만료·로그아웃·`/me` 브라우저 E2E는 `AUTH-01` 미해결 항목이다.
- `ADMIN-01`은 지침서상 Placeholder면 충분하지만 현재 P1 Mock 구현까지 확대되어 있다. 필수 업무 이후에 착수했는지는 코드만으로 알 수 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `web/README.md`의 개인 절대 경로를 `cd web` 또는 저장소 루트 기준 상대 경로로 바꾸고 Node·npm 버전 확인 명령을 추가한다.
2. 한예나 관할 환경에서 `npm.cmd run lint`, `npm.cmd run test`, `npm.cmd run build`를 실행하고 Commit·Node 버전·실행 시각·결과를 `web/docs/week3-test-result.md`에 갱신한다.
3. Backend Runtime 준비 후 `web/tests/integration/` 또는 별도 E2E 경로에 실제 로그인→Refresh 성공·실패→로그아웃 시나리오를 추가한다.
4. 실제 Runtime 전까지 현재 인증 코드는 Mock/계약 Client라는 점을 README와 이슈 문서에 유지한다.

### 코드 외 별도 확인

- 다른 팀원이 새 환경에서 README만으로 설치·실행했는지 확인해야 한다.
- 브라우저에서 상담사·운영자·403·404 이동과 로그아웃 후 Token 제거를 확인해야 한다.
- 개발 의존성의 high 보안 경고 2건에 대한 영향과 안전한 업그레이드 범위를 팀이 검토해야 한다.

## 3.2 공통 API 계층과 상태·근거 UI 구성 — 85%

### 확인된 완료 내용

- `ApiResponse`, `ApiError`, 페이지 정보, `httpClient`, `requestContext`, 멱등 Operation 코드가 존재한다.
- 400·401·403·404·409·422·5xx·Timeout·Network·Parse Error를 구분하는 코드와 테스트가 있다.
- Authorization, `X-Correlation-ID`, `Idempotency-Key`, 401 재시도 정책이 공통 계층에 구현되어 있다.
- Status·Risk·Priority Badge, DataTable, Pagination, Loading·Empty·Error·Forbidden UI가 존재한다.
- 공개 Evidence View Model은 문서명·버전·페이지·요약·검증 상태·공식 HTTPS URL만 사용하며 `chunk_id`, 내부 문서 ID, 검색 점수, 내부 경로와 원문 전체를 타입에서 배제한다.
- 알 수 없는 상태·위험도 값을 `UNKNOWN`/미확인으로 처리하는 코드와 테스트가 있다.

### 완료되지 않은 항목

- 공통 API 계층은 구현됐지만 상담 목록·상세 화면은 실제 API가 아니라 Mock 데이터를 사용한다.
- `contracts/api/paths/consultations.yaml`과 상담 요청 Schema의 `properties`가 비어 있어 상담 화면 DTO를 확정 계약으로 볼 수 없다.
- `AllowedAction`과 여러 상세 필드는 `web/docs/week3-screen-api-db-map.md`에서 `OPEN`으로 남아 있다.
- 날짜 표시 함수가 `timeZone: "Asia/Seoul"`을 명시한다. 입력이 이미 `+09:00`인 경우 중복 변환 여부가 없는지 경계 시각 Case로 확인할 필요가 있다.
- EvidenceCard 공식 URL·검증 상태·공개 필드에 대해 이동윤의 최종 승인 여부를 코드에서 확인할 수 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 최지용이 확정한 OpenAPI가 준비되면 생성 타입 또는 계약 직접 참조 타입으로 공통 응답·오류·AllowedAction DTO를 갱신한다.
2. `web/src/features/inquiry-queue/api/`와 `web/src/features/inquiry-detail/api/`에 실제 조회 Adapter를 작성하고 Mock/실제 API를 `VITE_USE_MOCK_API`에서 교체한다.
3. `+09:00`, UTC, 날짜 경계 입력을 포함한 시간 표시 테스트를 추가하고 지침서의 “표시 형식만 변경” 원칙과 일치시키도록 Formatter를 정리한다.
4. 이동윤과 합의한 EvidenceCard 공개 필드를 `week3-screen-api-db-map.md`에서 `OPEN`이 아닌 확정 상태로 갱신한다.

### 코드 외 별도 확인

- Backend의 실제 공통 Wrapper와 401·403·404·409·422 응답을 캡처해 Mapper와 대조해야 한다.
- 색상 외 문구·아이콘·스크린리더 설명으로 위험도가 구분되는지 접근성 검수가 필요하다.
- 공개 근거 URL·데이터 분류·검증 상태의 노출 허용 범위를 이동윤이 확인해야 한다.

## 3.3 CONS-01 상담사 문의 목록·검색·필터 구현 — 90%

### 확인된 완료 내용

- 공식 `data/synthetic/fixtures/inquiries.json`을 직접 읽어 Web View Model로 변환한다.
- 검색, 상태·위험도·우선순위·담당자·기간 필터, 정렬, 페이지네이션과 URL Query 보존이 구현되어 있다.
- 로딩·초기 빈 목록·검색 결과 없음·권한 없음·조회 오류가 다른 UI로 분리되어 있다.
- 공개 UUID와 화면용 문의 번호가 타입과 경로 생성 함수에서 구분된다.
- 목록 선택 후 UUID 상세 경로로 이동하고 목록 조건을 유지하는 통합 테스트가 있다.
- 위험도·우선순위를 프론트 점수로 새로 계산하지 않고 공식 Fixture 값을 사용한다.

### 완료되지 않은 항목

- `web/src/features/inquiry-queue/api/getInquiryQueue.ts`와 실제 서버 페이지·정렬 API Adapter가 없다. 현재 검색·필터·페이지는 브라우저 Mock 데이터에 적용된다.
- 지침서가 제시한 `web/tests/fixtures/inquiry-queue/{normal,danger,reopened,empty,error}.json` 경로는 없고 공통 `tests/fixtures/consultation/**`을 사용한다. 동등 Fixture라는 설명을 문서에 명확히 해야 한다.
- README는 “공식 합성 문의 24건”이라고 쓰지만 현재 `data/synthetic/fixtures/inquiries.json`은 활성 22건이므로 문서가 현재 기준과 다르다.
- 실제 Backend의 장기 대기·위험·재개 정렬과 Query 이름은 아직 확인 요청 상태이다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 현재 활성 22건과 원본 시나리오 24건의 차이를 `web/README.md`에 구분해 기록한다.
2. 실제 API 확정 후 `getInquiryQueue.ts`, 요청·응답 타입, API→View Model Mapper와 Query Key를 작성한다.
3. 서버 페이지·정렬·필터가 URL Query와 일치하는 통합 테스트를 Mock Service 또는 테스트 API로 추가한다.
4. 현재 `tests/fixtures/consultation/**`이 목록과 상세의 공통 계약 Fixture라는 점을 `web/docs/week3-test-result.md`에 명시하거나 지침서 경로로 분리한다.

### 코드 외 별도 확인

- 상담사에게 공개되는 고객 표시명과 마스킹 범위를 최지용·김은진이 확인해야 한다.
- 실제 API에서 위험·재개·장기 대기 정렬이 Backend 기준으로 수행되는지 확인해야 한다.
- 브라우저 뒤로가기 후 검색·필터·페이지 조건과 스크롤 위치가 실사용에 적절한지 수동 확인해야 한다.

## 3.4 CONS-02 문의 상세·AI 요약·공식 근거 구현 — 85%

### 확인된 완료 내용

- 상세 화면은 문의 헤더, 고객·제품·관리 이력, 증상·문진, 고객 조치, 사용 안내, AI 초안·상담사 확정, Evidence와 상태 이력을 구분한다.
- 공개 UUID로 상세을 찾고 화면에는 별도 문의 번호를 표시한다.
- 로딩·403·404·지원 불가 제품·AI 실패·근거 없음과 AI/Evidence/이력 부분 실패 Mock이 있다.
- Evidence 부분 실패가 상세 전체를 가리지 않는 통합 테스트가 있다.
- 내부 RAG 필드를 Evidence View Model과 화면에서 배제하는 컴포넌트 테스트가 있다.
- 긴 영역을 분리한 화면 구조와 독립 부분 오류 UI가 구현되어 있다.

### 완료되지 않은 항목

- `web/src/features/inquiry-detail/api/getInquiryDetail.ts`와 실제 상세 API Hook이 없고 현재 화면은 공식 합성 데이터를 변환한 Mock이다.
- 고객·구독·케어·AI 초안·상태 이력의 다수 API 필드가 매핑 문서에서 `OPEN`이다.
- 실제 역할별 개인정보 마스킹과 타 사용자 리소스 404 정책을 Runtime에서 검증하지 않았다.
- AI 초안·상담사 확정본의 저장 원천과 감사 이력 계약이 확정되지 않았다.
- Evidence 공개 필드와 직접 링크 정책의 협업 승인 여부가 확인되지 않았다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 상세 OpenAPI 확정 후 `getInquiryDetail.ts`, 세부 영역 Adapter와 실제 조회 Hook을 작성한다.
2. 기본 상세·Evidence·상태 이력 API를 독립 요청으로 유지할 경우 부분 성공 응답과 재시도 정책을 테스트한다.
3. `week3-screen-api-db-map.md`의 `OPEN` 필드를 실제 API·DB 필드와 연결하고 마스킹·필수·nullable 규칙을 확정한다.
4. 타 사용자 UUID, 미지원 제품, AI 실패, 근거 없음, 부분 실패를 실제 Backend Fixture로 검증하는 통합 테스트를 추가한다.

### 코드 외 별도 확인

- 실제 상담사 권한에서 고객·구독·관리 이력의 노출 범위를 최지용이 확인해야 한다.
- AI 초안과 상담사 확정 내용의 라벨·편집 권한·감사 이력을 이동윤·최지용과 확인해야 한다.
- 긴 원문·다수 Evidence·모바일 폭이 아닌 상담사 PC 최소 해상도에서 레이아웃을 육안 확인해야 한다.

## 3.5 상담 기록 Form과 allowed_actions 기반 행동 골격 — 80%

### 확인된 완료 내용

- 상담 기록, 고객 안내, 상담 결과, 방문 필요 여부와 AI 요약 수정·확정 UI가 있다.
- 필수값 검증과 서버 필드 오류 연결, 실패 후 입력 유지가 구현되어 있다.
- 버튼은 Mock Backend의 `allowed_actions`에 있는 행동만 표시한다.
- 논리 쓰기별 Idempotency-Key, 전송 시도별 Correlation ID, 최신 `stateVersion` 사용 구조가 있다.
- 저장 중 중복 클릭 차단, 성공 후 최신 상세 Snapshot 반영, 403·409·422·Network Mock이 있다.
- 409에서 입력을 유지하고 최신 상태·버전·행동을 반영하며 자동 재전송하지 않는 테스트가 있다.

### 완료되지 않은 항목

- `saveConsultationDraft.ts`, `startConsultation.ts`, `finalizeInquiry.ts` 같은 행동별 실제 API 파일이 없고 `consultationMockApi.ts`만 있다.
- 상담 OpenAPI 경로와 요청 Schema가 비어 있어 현재 요청 DTO·Operation 이름이 임시값이다.
- 실제 Backend 403·409·422 Wrapper가 Web Mapper와 일치하는지 확인하지 못했다.
- 성공 후 최신 상세 조회는 현재 Mock Snapshot 갱신이며 실제 네트워크 재조회가 아니다.
- `allowed_actions` 객체 배열과 충돌 응답의 코드 배열 결합 방식은 계약 확정 후 다시 검증해야 한다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 최지용이 상담 저장·시작·완료·방문 검토 Endpoint와 요청/응답을 확정한 뒤 행동별 API Adapter를 작성한다.
2. Provisional Mock DTO를 OpenAPI 생성 타입 또는 확정 계약 타입으로 교체한다.
3. 실제 409에서 `current_status`, `current_state_version`, `allowed_actions`를 반영하고, 422의 `field_errors`를 Form 항목에 연결하는 통합 테스트를 추가한다.
4. 성공 응답 뒤 실제 상세 재조회와 중복 요청·오래된 state_version 처리 결과를 검증한다.
5. 실제 API 준비 전에는 Mock임을 UI·README·테스트 이름에서 계속 명시한다.

### 코드 외 별도 확인

- 행동별 Endpoint와 이벤트 의미를 최지용·윤승혁이 승인해야 한다.
- 409 이후 자동 재전송 금지, 사용자 확인 문구와 입력 보존 UX를 상담사 관점에서 수동 검수해야 한다.
- Idempotency-Key 수명과 동일 요청 재시도 범위를 Backend 구현과 대조해야 한다.

## 3.6 테스트·필드 매핑·7월 29일 검토용 산출물 정리 — 80%

### 확인된 완료 내용

- `web/tests/**`에 단위·컴포넌트·통합 테스트와 합성 데이터 기반 Fixture가 있다.
- 기존 결과 문서에는 20개 Test File·80개 테스트, lint, Production Build 통과가 기록되어 있다.
- 목록→상세, 위험 필터, URL Query, 빈 상태, 403·404, 409, 인증 Guard, Evidence 비노출과 상담 Form 흐름 테스트가 있다.
- 기술 결정, 화면–API–DB 매핑, 테스트 결과, 미해결 이슈, 완료 대조 문서가 모두 존재한다.
- `docs/testing/week3-web-review.md`에 최지용·김은진·이동윤 대상 검수 요청과 Web 안전 원칙이 정리되어 있다.
- 위 문서와 Web 구현은 Git 추적 상태이다.

### 완료되지 않은 항목

- 기존 80개 테스트·lint·build 결과를 현재 작업 트리에서 재실행하지 않았다.
- 브라우저 수동 검증 항목의 입력·실제 결과·화면 캡처가 개별 증빙으로 연결되어 있지 않다.
- 실제 API 기반 인증·상담·방문 E2E는 미구현이다.
- 현재 브랜치는 `main`이며 PR URL·리뷰 상태는 로컬에서 확인할 수 없다. 다만 지침서의 “PR 또는 공유 파일” 중 공유 문서는 존재한다.
- 화면–API–DB 매핑의 상담 관련 핵심 필드가 `OPEN`이다.
- 두 공식 산출물에 Web 관점 검수 의견이 실제 반영됐는지 확인할 수 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 현재 Commit·Node·npm 버전에서 lint·test·build를 재실행하고 결과를 `week3-test-result.md`에 기록한다.
2. 자동화하지 못한 브라우저 흐름은 Case ID, 입력, 절차, 기대, 실제 결과, 캡처 경로를 표로 남긴다.
3. Backend Runtime 준비 후 실제 로그인→목록→상세→상담 저장→방문 전환 E2E를 추가한다.
4. `week3-screen-api-db-map.md`의 OPEN 항목에 담당자·결정 기한·임시 처리와 확정 계약 링크를 추가한다.
5. PR 또는 공유 검토 기록에 실행 Commit, 미연동 범위, 리뷰어 승인·수정 요청을 연결한다.
6. 공식 전처리 결과서·DB 저장소 설계 문서에 보낸 Web 검수 의견과 반영 결과를 `docs/testing/week3-web-review.md`에 갱신한다.

### 코드 외 별도 확인

- PR 생성·공유·리뷰 수신 여부와 리뷰 반영 완료를 원격 저장소에서 확인해야 한다.
- 최지용·이동윤·김은진이 검수 요청을 실제로 받았고 회신했는지 확인해야 한다.
- 다른 팀원의 독립 실행 성공과 브라우저 수동 결과를 확인해야 한다.
- `CONS-03`·`ADMIN-01` 추가 업무가 필수 업무와 리뷰 대응 후 시작됐는지 Issue·Commit 시간 순서로 확인해야 한다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 사항

| 확인 대상 | 필요한 별도 증빙 또는 확인자 |
| --- | --- |
| 현재 작업 트리의 lint·test·Production Build 성공 | 한예나 관할에서 재실행한 명령, 환경, Commit, 실행 시각과 로그 |
| 다른 팀원의 README 재현 | 별도 PC에서 설치·실행한 결과와 브라우저 URL |
| 실제 인증·상담·방문 API 동작 | 최지용의 테스트 Backend, 실제 응답 Fixture와 E2E 로그 |
| 실제 개인정보 마스킹·타 사용자 404 | 역할별 테스트 계정과 Backend 권한 검증 |
| Evidence 공개 필드·공식 URL 허용 | 이동윤 승인 및 실제 응답 검수 |
| 상태·allowed_actions·409 의미 | 윤승혁·최지용의 계약 승인과 Runtime 응답 |
| PR·리뷰·자료 전달 완료 | 원격 PR URL, 리뷰 코멘트, 수신자 회신 |
| 두 공식 산출물에 Web 의견 반영 | 최종 산출물 변경 이력과 공동 검수 기록 |
| 추가 업무 착수 순서 | Issue·Commit·PR 시간 순서와 PM 확인 |

## 5. 완료 기준 및 최종 체크리스트 판정

## 5.1 7월 29일 필수 완료 기준

### 현재 완료로 체크해도 되는 항목

- [x] 상담사 Router·Layout·Role Guard가 동작한다.
- [x] 401·403·404·전역 오류가 구분되어 있다.
- [x] 공통 API 응답·오류·페이지네이션 타입이 있다.
- [x] 상태·위험도·우선순위·근거 카드 공통 UI가 있다.
- [x] `CONS-01` 목록에서 검색·필터·정렬·페이지네이션을 Mock으로 확인할 수 있다.
- [x] 목록에서 `CONS-02` 상세로 이동할 수 있다.
- [x] `CONS-02`에서 고객·제품·증상·문진·AI 요약·공식 근거·상태 이력이 구분되어 표시된다.
- [x] AI 초안과 상담사 확정 내용이 구분된다.
- [x] 내부 경로·검색 점수·`chunk_id`·원문 전체가 화면에 노출되지 않는 구조와 테스트가 있다.
- [x] 상담 기록 Form과 `allowed_actions` 기반 버튼 골격이 있다.
- [x] 정상·위험·근거 없음·권한 없음·409 충돌 Mock이 있다.
- [x] 최소 테스트 및 수동 검증 결과 문서가 있다.
- [x] 화면–API–DB 매핑과 미확정 사항이 문서화되어 있다.
- [x] 7월 29일 검토용 공유 파일이 있다.

### 별도 재확인 후 체크할 항목

- [ ] `web/` 의존성 설치·개발 서버·TypeScript 검사·Production Build가 성공한다.
  - 기존 2026-07-29 문서에는 통과로 기록되어 있으나 이번 점검에서 현재 상태를 재실행하지 않았으므로, 한예나 관할 환경의 최신 실행 로그를 확인한 뒤 체크한다.

## 5.2 7월 30일~31일 최종 정리 기준

### 현재 완료로 체크해도 되는 항목

- [x] Mock과 실제 API 차이가 문서에 표시되어 있다.
- [x] 미완료 기능과 다음 주 작업이 이슈 문서에 기록되어 있다.

### 아직 완료로 체크하면 안 되는 항목

- [ ] API·AI·State Machine 계약 변경이 TypeScript 타입과 화면에 반영되었다.
  - 상담 Schema와 여러 매핑 항목이 비어 있거나 OPEN이며 상태·위험도 차이도 남아 있다.
- [ ] 다른 팀원이 README로 웹을 실행할 수 있다.
  - 개인 절대 경로를 수정하고 독립 환경 재현 증빙이 필요하다.
- [ ] 목록·상세·상담 입력의 빌드·렌더링 오류가 수정되었다.
  - 기존 결과는 있으나 현재 재실행과 브라우저 증빙이 필요하다.
- [ ] 개인정보 마스킹과 내부 근거 비노출을 다시 확인했다.
  - 내부 근거 비노출 테스트는 있으나 실제 역할별 API와 개인정보 마스킹 Runtime 확인이 필요하다.
- [ ] 최지용·이동윤·김은진에게 API·근거·테스트 자료를 전달했다.
  - 공유 문서는 있으나 실제 전달·수신은 외부 확인이 필요하다.
- [ ] 두 공식 산출물에 웹 관점 검수 의견이 반영되었는지 확인했다.
  - 최종 산출물과 반영 기록 확인이 필요하다.
- [ ] 필수 업무 완료 후에만 4장의 추가 업무를 시작했다.
  - 코드만으로 작업 착수 순서를 판정할 수 없다.

## 6. 우선 처리 순서

1. README의 개인 절대 경로와 합성 문의 24건 표기를 현재 실행 방식·활성 22건 기준으로 수정한다.
2. 한예나 관할 환경에서 lint·test·build를 재실행하고 최신 결과를 기록한다.
3. 상담 목록·상세·저장·완료·방문 API 계약의 OPEN 항목을 최지용·윤승혁과 확정한다.
4. 실제 API Adapter와 401·403·404·409·422 통합 테스트를 추가한다.
5. 개인정보 마스킹·Evidence 공개 필드·브라우저 레이아웃을 담당자와 수동 검수한다.
6. PR·검토 회신·공식 산출물 반영 증빙과 추가 업무 착수 순서를 정리한다.

