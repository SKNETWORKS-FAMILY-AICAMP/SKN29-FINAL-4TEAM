# 한예나 4주차 작업 진행도

> 작성 일시: 2026-08-05 00:06 KST  
> 평가 기준 Commit: `d9052621e2f07a8447f7c1f998875104c031a3c0`  
> 담당 영역: `web/**` 주관할  
> 판정 원칙: Mock 화면·계약 준비·실제 Backend Runtime 연동을 분리하고, 실행 기록·코드·공유 문서가 있는 경우에만 완료로 판정한다.  
> 참고: [팀원별 관할 영역 v2](../../planning/md/팀원별%20관할%20영역%20v2.md), [한예나 4주차 업무 지침서](../../weekly-task/한예나_4주차_업무_지침서.md)

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 핵심 근거 |
|---|---:|---|---|
| 3.1 3주차 Web 기준선 재검증과 공식 Runtime 경로 확정 | 95% | 대부분 완료 | 지원 Node 환경에서 26개 파일·109개 Test, Lint, Build 성공과 다른 팀원 재현이 기록됐고 Runtime 경로가 하나로 정리됨 |
| 3.2 상담사 목록·상세 실제 API Adapter와 Mock 경계 정리 | 75% | 부분 완료 | Repository 경계와 `MOCK_ONLY`·`BACKEND_BLOCKED` 표시는 완료됐으나 Remote Adapter와 Evidence 최종 계약 소비는 없음 |
| 3.3 T-040 상담 결과 저장·AI 초안 수정·409 충돌 실제 연동 | 50% | 절반 완료 | Mock에서 409 입력 유지·멱등성·추적·허용 행동을 검증했으나 실제 상담 저장 HTTP·DB 성공은 없음 |
| 3.4 T-041 방문 전환·기사 선택·방문 일정 저장 실제 연동 | 45% | 절반 미만 | Mock Form·날짜 구분·버튼 제어는 있으나 방문 Runtime API와 DB 저장은 없음 |
| 3.5 Web 계약 정합성·중복 코드·CSS 최소 정리 | 78% | 상당 부분 완료 | Runtime 미사용 Source 20개와 Legacy CSS를 제거했으나 대형 CSS, G2 계약 반영, P1 선행 문제가 남음 |
| 3.6 8월 5일 Web 동결·8월 6일 발표 지원·8월 7일 인계 | 72% | 진행 중 | 발표 Commit·수치·캡처 5장·Fallback GIF·3회 리허설은 준비됐으나 발표자료 공동 검수와 발표 후 최종 인계는 미확인 |

단순 평균 완료도는 **약 69%**다. Web 자체의 Mock 화면, 오류 UX, 테스트와 발표 패키지는 강하다. 반면 4주차 지침서의 핵심인 상담·방문 **실제 API·DB 저장은 0건**이며, 현재 G2 계약도 Runtime·소비자 연동 착수 Gate가 닫혀 있다. 따라서 시연 가능 상태를 실제 서비스 연동 완료로 합산하지 않았다.

## 2. 현재 검증 경계와 중요 변경 사항

- Web 발표 기준 Source는 `8509958`이며 현재 HEAD와 `web/**` 차이가 없다. 따라서 [발표 기준 문서](../../../web/docs/presentation/week4-web-presentation-baseline.md)의 109/109·Lint·Build 결과는 현재 Web Source에 적용할 수 있다.
- 담당자 환경의 검증은 Node `v26.4.0`, npm `11.17.0`에서 성공했다. 이번 평가 환경은 Node `v20.11.0`, npm `10.2.4`여서 Test와 Build가 `node:util.styleText` 미지원으로 시작 전에 실패했다. 이는 README 최소 버전 `20.19.0`보다 낮은 환경 문제이며 Web 코드 실패로 판정하지 않는다.
- [4주차 테스트 결과](../../../web/docs/week4-test-result.md)에는 `npm ci`, Test 26 files·109 cases, Lint, Build 성공과 양정현의 README 교차 실행이 기록돼 있다.
- Web 문서의 `consultations.yaml = {}`, `visits.yaml = {}` 설명은 현재 HEAD 기준으로 낡았다. 최신 [G2 Crosswalk](../../../contracts/api/g2-operation-crosswalk.yaml)에는 계약 Operation이 추가됐지만 `runtime_implementation_start_allowed=false`, `consumer_integration_start_allowed=false`이고 신규 11개가 모두 `NOT_IMPLEMENTED`다.
- 공식 근거 계약 DEC-008 v0.2는 Web 재검토가 `NOT_REVIEWED`다. 현재 Evidence 화면은 내부 필드를 노출하지 않지만 최종 Backend `EvidenceCardDTO`를 실제 소비한 증거는 아니다.
- 발표는 아직 8월 6일 전이므로 발표 당일 확인과 8월 7일 후속 업무는 사전 준비 문서가 있어도 완료로 체크하지 않는다.

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 Web 기준선 재검증과 공식 Runtime 경로 확정 — 95%

수행 내용:

- Node `v26.4.0`, npm `11.17.0`에서 `npm ci`, Test 26 files·109 cases, Lint, TypeScript·Production Build 성공을 기록했다.
- 다른 팀원이 README 명령으로 교차 실행해 같은 성공 결과를 확인했다.
- `src/main.tsx → App → AppProviders → AppRouter`를 공식 진입 경로로 정하고 상담사 목록·상세·방문·운영 Route를 문서화했다.
- Runtime 미사용 과거 Feature 19개 파일과 `legacy/styles.css`를 삭제하고 새 직접 Import를 ESLint로 차단했다.
- 실제 API, Mock, Backend 차단 상태를 Repository와 기능 상태표에서 구분했다.

수행 근거:

- [4주차 Web 기준선](../../../web/docs/week4-web-baseline.md)
- [Runtime Source Map](../../../web/docs/week4-runtime-source-map.md)
- [4주차 테스트 결과](../../../web/docs/week4-test-result.md)
- [App Router](../../../web/src/app/router/AppRouter.tsx)
- [Repository 경계](../../../web/src/features/consultation/repositories/consultantWorkspaceRepository.ts)
- [ESLint 규칙](../../../web/eslint.config.js)

완료되지 못한 항목:

- 이번 평가 환경은 지원 Node 버전보다 낮아 현재 장비에서 Test·Build를 독립 재실행하지 못했다.
- npm high severity 3건은 영향 분석 없이 열린 이슈로 남아 있다.

완료 방법:

1. 발표 장비에 Node `22.22.2` 이상 또는 담당자 검증 버전 `26.4.0`을 설치한다.
2. 현재 HEAD에서 `npm ci`, `npm test -- --run`, `npm run lint`, `npm run build`를 재실행한다.
3. 실행 시각·Node/npm·Commit·26 files·109 cases·Bundle 결과를 테스트 문서에 추가하고 보안 경고는 별도 Issue로 분리한다.

코드 외 별도 확인:

- 발표 장비와 다른 팀원 장비에서 같은 Commit·Node 버전으로 재현되는지 확인해야 한다.
- npm 보안 경고의 실제 배포 영향과 Upgrade 허용 여부는 담당자·PM 합의가 필요하다.

### 3.2 상담사 목록·상세 실제 API Adapter와 Mock 경계 정리 — 75%

수행 내용:

- 화면이 Mock 원천을 직접 Import하지 않고 `consultantWorkspaceRepository.ts` 한곳을 통하도록 정리했다.
- `VITE_USE_MOCK_API=true`는 `MOCK_ONLY`, `false`는 실제 Endpoint를 추측하지 않고 `BACKEND_BLOCKED`로 표시한다.
- 목록·상세의 URL·Query·DTO·오류·`state_version`·`allowed_actions` 구현 순서와 차단 사유를 문서화했다.
- 화면은 AI 초안·상담사 확정본·공식 근거를 구분하며 EvidenceCard에 내부 경로·`chunk_id`·검색 점수를 노출하지 않는 테스트가 있다.

수행 근거:

- [Repository 경계 코드](../../../web/src/features/consultation/repositories/consultantWorkspaceRepository.ts)
- [상담사 상세 화면](../../../web/src/features/consultation/components/ConsultantInquiryDetail.tsx)
- [Evidence Mapper](../../../web/src/features/evidence-viewer/model/evidenceMapper.ts)
- [EvidenceCard 비노출 테스트](../../../web/tests/component/EvidenceCard.test.tsx)
- [Web API 연동 상태표](../../../web/docs/week4-web-api-map.md)
- [실제 API 구현 준비표](../../../web/docs/week4-web-implementation-readiness.md)
- [G2 상담사 조회 계약](../../../contracts/api/paths/inquiries.yaml)

완료되지 못한 항목:

- 실제 목록·상세 HTTP Client, DTO, Mapper, Remote Repository가 없다.
- 최신 G2 계약은 생겼지만 조회 Operation이 `NOT_IMPLEMENTED`이고 소비자 연동 Gate가 닫혀 있다.
- 현재 Mock 생성기는 위험도·상태·담당자·허용 행동 일부를 합성 규칙으로 만들기 때문에, 실제 Backend Projection을 그대로 소비한다는 완료 증거가 아니다.
- Evidence는 자체 `PublicEvidenceSource`를 소비하며 DEC-008 v0.2 Web 재검토가 끝나지 않았다.
- 기존 Web 상태 문서의 “계약 파일이 비어 있다”는 설명을 최신 G2 상태로 갱신하지 않았다.

완료 방법:

1. G2 QA 재검증과 PM의 소비자 연동 착수 승인을 받는다.
2. `web/src/features/consultation/api/`에 목록·상세 DTO와 Client를 추가한다.
3. `mappers/`에서 `snake_case` 응답을 View Model로 바꾸고 위험도·우선순위·담당자·상태·허용 행동을 재계산하지 않는다.
4. `repositories/`에 Mock·Remote 구현을 분리하고 실제 모드에서 실패 시 Mock으로 자동 전환하지 않는다.
5. DEC-008 최종 승인 후 `EvidenceCardDTO` 공개 필드만 Mapper 입력으로 허용하고 Contract Test를 추가한다.
6. API 상태 문서의 빈 계약 설명을 `G2 계약 존재 / Runtime 미구현 / 소비 Gate 닫힘`으로 수정한다.

코드 외 별도 확인:

- 최지용의 Runtime Endpoint·권한·오류 Wrapper와 윤승혁의 소비자 착수 승인이 필요하다.
- 이동윤·김은진·한예나·윤승혁의 Evidence 공개 필드 검토·승인 상태를 확인해야 한다.
- 실제 상담사 계정으로 본인 배정 문의만 조회되고 타 문의는 404로 은닉되는지 확인해야 한다.

### 3.3 T-040 상담 결과 저장·AI 초안 수정·409 충돌 실제 연동 — 50%

수행 내용:

- Mock 상담 저장 요청에 `state_version`, `idempotency_key`, `correlation_id`를 구성한다.
- 409 상태 충돌과 멱등 키 재사용을 구분하고 입력 유지·최신 상태 반영·자동 재전송 금지를 테스트한다.
- AI 초안, 수정 입력, 상담사 확정 내용을 화면에서 구분한다.
- `allowed_actions`에 포함된 상담 행동만 표시하고 403·409·422·Network 오류 Mock을 분리한다.

수행 근거:

- [상담 저장 Hook](../../../web/src/features/consultation/hooks/useSaveConsultation.ts)
- [상담 Mock API](../../../web/src/features/consultation/api/consultationMockApi.ts)
- [Workflow DTO](../../../web/src/features/workflow-action/model/workflowActionDtos.ts)
- [Workflow Mapper](../../../web/src/features/workflow-action/model/workflowActionMapper.ts)
- [상담 Action 컴포넌트 테스트](../../../web/tests/component/ConsultationActionPanel.test.tsx)
- [저장 Hook 단위 테스트](../../../web/tests/unit/useSaveConsultation.test.ts)
- [G2 상담 계약](../../../contracts/api/paths/consultations.yaml)

완료되지 못한 항목:

- 실제 상담 시작·저장·확정·완료 HTTP 요청과 DB 저장 성공이 없다.
- 현재 `consultationMockApi.ts` 결과를 실제 Backend Snapshot처럼 사용할 수 없다.
- 실제 401·403·404·409·422·5xx·Network 응답과 `correlation_id` 로그 추적을 확인하지 않았다.
- 저장 성공 뒤 실제 상세 GET 재조회와 State Machine 이력 반영을 확인하지 않았다.
- 실제 API 성공 Test와 Mock 성공 Test가 분리돼 있지 않다.

완료 방법:

1. G2 Runtime Gate가 열린 뒤 상담 4개 Operation에 맞춘 API Client를 작성한다.
2. 공통 HTTP 계층으로 `Idempotency-Key`, `X-Correlation-ID`, 최신 `state_version`을 전송한다.
3. 성공 Snapshot을 반영한 뒤 상세 API를 재조회하고 `allowed_actions`와 상태를 갱신한다.
4. 실제 409에서 입력을 유지하고 최신 Snapshot 조회 후 사용자가 직접 다시 제출하도록 한다.
5. 실제 API 통합 Test, Mock Test, 계약 Fixture Test를 각각 별도 Suite와 결과표로 기록한다.

코드 외 별도 확인:

- PostgreSQL에서 상담 레코드와 상태 이력이 실제로 저장되는지 최지용과 확인해야 한다.
- AI 초안·상담사 수정본·확정본의 책임 경계와 공개 범위를 이동윤·PM이 승인해야 한다.
- 동일 멱등 키 재요청이 중복 저장되지 않고 같은 결과 또는 명시된 409를 반환하는지 확인해야 한다.

### 3.4 T-041 방문 전환·기사 선택·방문 일정 저장 실제 연동 — 45%

수행 내용:

- 방문 필요 사유, 고객 희망일, 기사, 인계 메모, 안전 메모, 확정일을 입력하는 Mock Form이 있다.
- 희망일과 확정일을 별도 값으로 보관하고 확정일이 희망일보다 빠른 경우를 차단한다.
- `allowed_actions`에 없는 방문 확정 버튼을 숨기는 컴포넌트 테스트가 있다.
- 방문 Runtime 미구현을 `BACKEND_BLOCKED`로 문서화했다.

수행 근거:

- [방문 전환 Form](../../../web/src/features/visit-transition/components/VisitTransitionForm.tsx)
- [Inline 방문 일정](../../../web/src/features/visit-transition/components/InlineVisitScheduler.tsx)
- [방문 Mock](../../../web/src/features/visit-transition/model/visitTransitionMock.ts)
- [방문 검증 Schema](../../../web/src/features/visit-transition/validation/visitTransitionSchema.ts)
- [방문 Form 테스트](../../../web/tests/component/VisitTransitionForm.test.tsx)
- [G2 방문 계약](../../../contracts/api/paths/visits.yaml)

완료되지 못한 항목:

- 기사 후보 조회·방문 요청·일정 저장·확정 실제 Client와 DB 저장이 없다.
- 현재 Web 값은 `desiredAt`·`confirmedAt`의 일시형 Mock이며 G2의 날짜형 `preferred_date`·`confirmed_date` 계약을 실제 소비하지 않는다.
- 기사 미배정 `ASSIGNING`, 일정 조율 `SCHEDULING`, 확정 `CONFIRMED` Snapshot을 실제 응답으로 검증하지 않았다.
- 방문 API의 403·404·409·422·Network 오류 및 새로고침 후 저장 유지가 검증되지 않았다.

완료 방법:

1. G2 방문 Runtime과 합성 기사 Seed가 준비된 뒤 방문 DTO·Mapper·Client·Remote Repository를 작성한다.
2. `preferred_date`와 `confirmed_date`를 날짜 단위로 구분하고 기사·상태별 필수 조건을 Backend 계약과 맞춘다.
3. `visit: null`과 Key 누락을 구분하고 Backend `allowed_actions`만으로 버튼을 표시한다.
4. 방문 요청→기사 선택→일정 저장→확정의 실제 DB 결과와 새로고침 후 복구를 통합 Test로 확인한다.

코드 외 별도 확인:

- 기사 계정·배정 권한·합성 방문 데이터가 발표 DB에 준비됐는지 확인해야 한다.
- 고객 희망일과 기사 확정일의 업무 의미, 시간대, 수정 권한을 PM·Backend와 확인해야 한다.

### 3.5 Web 계약 정합성·중복 코드·CSS 최소 정리 — 78%

수행 내용:

- 공식 Runtime 경로와 Repository 경계를 하나로 정리했다.
- Runtime 미사용 Feature 19개와 `legacy/styles.css` 2,406 lines를 삭제했다.
- 상태 전이 성공·충돌 DTO Mapper와 공통 요청 Context·멱등성 기반을 테스트했다.
- 실제 API·Mock·차단 상태와 열린 이슈를 문서화했다.
- 109개 Test, Lint, Build 성공을 삭제 이후 다시 확인했다.

수행 근거:

- [Runtime Source Map](../../../web/docs/week4-runtime-source-map.md)
- [Web 열린 이슈](../../../web/docs/week4-open-issues.md)
- [Workflow Mapper 테스트](../../../web/tests/unit/workflowActionMapper.test.ts)
- [공통 API 기반 테스트](../../../web/tests/unit/apiFoundation.test.ts)
- [요청 Context 테스트](../../../web/tests/unit/requestContext.test.ts)

완료되지 못한 항목:

- 핵심 CSS가 2,919 lines 등 대형 파일로 남아 있고 중복 정리는 후속으로 보류됐다.
- G2 계약 병합 뒤 Web 상태 문서와 타입을 재대조하지 않았다.
- DEC-008 v0.2 Evidence 계약의 Web 재검토가 없다.
- 실제 API 모드의 DTO·Mapper·오류 Contract Test가 없다.
- 상담사 P0 실제 연동이 끝나기 전에 운영 대시보드 P1 Mock이 이미 구현돼 있어 “P1이 P0를 지연시키지 않았다”는 완료 증거가 없다.

완료 방법:

1. G2 OpenAPI Example을 Web Contract Fixture로 가져와 DTO·Mapper Test를 작성한다.
2. Web API 상태표와 구현 준비표를 최신 G2 상태로 갱신한다.
3. 상담사 P0 Remote 연결을 먼저 끝내고 CSS는 시각 회귀 캡처를 확보한 뒤 Token·공통 Component 단위로 분리한다.
4. 운영 대시보드 추가 작업은 P0 Blocker와 별도 Issue·일정으로 관리한다.

코드 외 별도 확인:

- 최지용의 G2 Runtime·오류 계약과 이동윤의 Evidence 계약에 대해 소비자 검토 회신을 남겨야 한다.
- 주요 브라우저·해상도에서 CSS 정리 전후 시각 회귀와 접근성을 수동 확인해야 한다.

### 3.6 8월 5일 Web 동결·8월 6일 발표 지원·8월 7일 인계 — 72%

수행 내용:

- 발표 Source Commit `8509958`, 실행 명령, Node/npm, 검증 수치와 기능 상태를 기록했다.
- 상담 목록→상세→공식 근거→409 입력 유지→방문 전환 Mock 흐름을 3회 재현했다.
- 상담 목록·상세·근거·409·방문 화면 캡처 5장과 약 12.5초 Fallback GIF를 준비했다.
- 실제 업무 API 0개, `/health` 1개, Mock-only 5개, Backend 차단 4개를 발표 문구로 구분했다.
- 5주차 인계 초안과 열린 이슈 문서가 있다.

수행 근거:

- [발표 기준 문서](../../../web/docs/presentation/week4-web-presentation-baseline.md)
- [발표 체크리스트](../../../web/docs/presentation/week4-web-demo-checklist.md)
- [시연 순서](../../../web/docs/presentation/week4-web-demo-script.md)
- [기능 상태표](../../../web/docs/presentation/week4-web-feature-status.md)
- [발표 수치](../../../web/docs/presentation/week4-web-presentation-metrics.md)
- [발표 캡처·Fallback](../../../web/docs/presentation/captures/20260804/README.md)
- [5주차 인계 초안](../../../web/docs/handoffs/week4-to-week5-web-handoff.md)

완료되지 못한 항목:

- 지침서의 대표 ID `DEMO-INQ-002` 대신 `INQ-20260704-0013`, `INQ-20260703-0008`을 사용했다.
- 김은진이 주도하는 실제 중간 발표자료의 Web 설명을 한예나가 검수했다는 회신이 없다.
- 현재 중앙 저장소에는 4주차 중간 발표 Deck 자체가 확인되지 않는다.
- G2 병합 후 발표 기준 문서의 “계약 파일이 비어 있음” 표현을 갱신하지 않았다.
- 8월 6일 발표 직전 Health·로그인·브라우저 점검, 발표 피드백 기록과 8월 7일 최종 인계는 아직 시점 전이다.

완료 방법:

1. 대표 ID를 지침서와 맞추거나 현재 두 ID를 공식 대표 ID로 변경 승인받아 모든 시연 문서에 일치시킨다.
2. 김은진의 발표자료에서 Web 구현·Mock·미구현 표현과 수치를 검수하고 회신을 남긴다.
3. 발표 직전 지원 Node 환경에서 Test·Lint·Build, `/health`, Demo 로그인, Route Guard, 3회 흐름을 재확인한다.
4. 발표 질문·오류·피드백을 기능·UI·설명·범위로 분류한다.
5. 8월 7일 회귀 실행 후 인계 초안을 최종본으로 바꾸고 담당자별 요청·Commit·5주차 범위를 기록한다.

코드 외 별도 확인:

- 발표 장비의 네트워크·브라우저·Node·Demo 계정과 Fallback 재생을 현장에서 확인해야 한다.
- 김은진 발표자와 윤승혁 PM의 Web 설명·수치·Mock 고지 최종 승인이 필요하다.
- 발표 중 받은 질문과 피드백은 코드에서 확인할 수 없으므로 별도 회의록이 필요하다.

## 4. 코드로 완료 여부를 확인할 수 없는 별도 확인 사항

| 확인 대상 | 별도 확인할 내용 | 필요한 증거 |
|---|---|---|
| 발표 장비 | 지원 Node/npm, `npm ci`, Test·Lint·Build, 브라우저 Console | Commit·버전·명령 원시 로그 |
| Backend Gate | G2 QA 재검증과 Runtime·소비자 연동 착수 승인 | 김은진 QA 회신·윤승혁 결정 기록 |
| 실제 저장 | 상담·방문 쓰기 후 PostgreSQL 레코드·상태 이력·멱등성 | API 요청/응답·DB 조회·재요청 결과 |
| 권한 | 상담사 배정 범위·타 문의 404 은닉·기사 배정 권한 | 역할별 계정 수동/자동 Test |
| Evidence | DEC-008 v0.2 공개 필드·Nullable·링크 정책 승인 | 이동윤·한예나·김은진·PM 검토표 |
| 발표자료 | Web 기능 설명·수치·Mock 표시가 실제 Source와 일치 | 김은진 자료에 대한 한예나 검수 회신 |
| 발표 당일 | Health·로그인·시연·Fallback과 실제 오류 원인 분류 | 발표 직전 체크리스트·운영 기록 |
| 발표 후 | 질문·피드백 분류, 회귀 결과, 5주차 담당자·범위 | 회의록·최종 인계 문서 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 8월 3일 기준선·구조 정리

- [x] 현재 Web Source에서 `npm ci` 성공 기록이 있다.
- [x] Node.js·npm Version이 README와 Lock File 요구사항에 맞게 기록되어 있다.
- [x] TypeScript 검사, Test, Lint, Production Build 결과가 현재 Web Source 기준으로 기록되어 있다.
- [x] Test File 26개·Case 109개가 실제 Source 결과와 일치한다.
- [x] 공식 Runtime 구현 경로가 하나로 정해져 있다.
- [x] 중복·미사용 Source가 삭제됐고 재도입 방지 규칙이 있다.
- [x] 실제 API, Mock, 정적 화면·차단 상태의 경계가 문서화되어 있다.
- [x] 다른 팀원이 README로 Web을 실행해 같은 성공 결과를 확인했다.

### 5.2 8월 4일 실제 API 연동

- [x] 상담사 문의 목록 실제 API 미구현이 `BACKEND_BLOCKED`로 명시되어 있다.
- [x] 문의 상세 실제 API 미구현이 `BACKEND_BLOCKED`로 명시되어 있다.
- [ ] 위험도·우선순위·담당자·상태를 Web에서 계산하지 않는다. 현재 Mock 생성 규칙이 있고 실제 Projection 소비가 없다.
- [ ] 공식 근거는 Backend `EvidenceCardDTO` 공개 필드만 표시한다. 현재 자체 공개 Source이며 DEC-008 v0.2 재검토가 남았다.
- [ ] 상담 결과 저장 API가 실제로 호출된다.
- [ ] `state_version`, `idempotency_key`, `correlation_id`가 실제 HTTP 쓰기 요청에 적용된다. 현재는 Mock·공통 기반만 검증됐다.
- [x] AI 초안과 상담사 확정본이 구분된다.
- [x] 403·404·409·422·Network 오류 표시 구조가 구분된다.
- [x] 409 이후 사용자 입력이 유지된다.
- [ ] 실제 API 성공과 Mock 성공 Test가 분리되어 있다.

### 5.3 8월 5일 발표 기준 Web 동결

- [x] 방문 필요 분기와 방문 일정 저장의 실제 미구현·차단 상태가 확정되어 있다.
- [x] 방문 희망일과 확정일이 Mock 화면·검증에서 구분된다.
- [x] `allowed_actions`에 없는 버튼이 표시되지 않는 테스트가 있다.
- [ ] 발표 대표 문의 `DEMO-INQ-002` 흐름을 반복 재현할 수 있다. 현재 다른 두 ID를 사용한다.
- [x] 발표 기준 Commit과 실행 명령이 기록되어 있다.
- [x] Test·Lint·Build가 발표 기준 Commit에서 통과했다.
- [x] 실제 API와 Mock 범위가 기능 상태표에 구분되어 있다.
- [x] 화면 캡처 5장과 Fallback GIF가 준비되어 있다.
- [ ] 김은진 발표자료의 Web 구현 설명을 검수했다는 증거가 있다.

### 5.4 8월 6일 중간 발표

- [ ] 발표 직전 Backend Health와 Web 실행을 확인했다.
- [ ] Demo 로그인 계정과 역할이 정상이다.
- [ ] 발표 중 실제 API 연동 범위를 과장하지 않았다.
- [ ] Mock Fallback 사용 시 즉시 Mock임을 밝혔다.
- [ ] 오류 발생 시 원인을 Web·Backend·AI·환경으로 구분했다.
- [ ] 발표 중 받은 질문과 피드백을 기록했다.
- [ ] 미완료 기능을 완료된 화면처럼 설명하지 않았다.

### 5.5 8월 7일 발표 후 정리·5주차 인계

- [ ] 발표 피드백을 기능·UI·설명·범위로 분류했다.
- [ ] 발표 이후 변경 Commit에서 Test·Lint·Build를 다시 실행했다.
- [x] 실제 API 연결 완료·Mock-only·차단·미구현 기능을 기능 상태표와 열린 이슈에서 구분했다.
- [x] 중복 Source·CSS 후속 정리 항목을 열린 이슈에 기록했다.
- [ ] Test 실패와 Flaky Case를 발표 후 기준으로 기록했다.
- [ ] 최지용에게 Web에 필요한 미완료 Endpoint와 Payload를 전달했다는 수신 확인이 있다.
- [ ] 이동윤에게 AI 요약·근거·오류 표시 불일치를 전달하고 DEC-008 v0.2를 재검토했다.
- [ ] 김은진에게 최종 Web Test·실행 결과를 전달했다.
- [ ] 윤승혁에게 발표 기준 기능 상태와 5주차 필수 범위를 전달했다.
- [ ] 5주차 Web **최종** 인계 문서가 저장소에 있다. 현재 문서는 발표 후 작성 예정인 초안이다.

현재 체크 가능한 항목은 **22개 / 44개**다. 8월 3일 기준선·Mock 발표 준비는 잘 갖춰졌지만, 실제 상담·방문 Runtime과 8월 6일 발표·8월 7일 사후 정리는 아직 완료로 체크할 수 없다.

## 6. 우선순위별 다음 작업

1. **P0 — 발표 전:** 지원 Node 환경에서 현재 HEAD의 Test·Lint·Build와 시연 흐름을 재확인하고 발표자료 Web 설명을 김은진과 공동 검수한다.
2. **P0 — 계약 정정:** Web 상태 문서의 빈 계약 설명을 최신 G2 `NOT_IMPLEMENTED`·Gate 상태로 갱신하고 대표 시연 ID를 통일한다.
3. **P0 — 소비자 검토:** DEC-008 v0.2 Evidence 공개 계약과 G2 상담·방문 DTO에 Web 검토 회신을 남긴다.
4. **P0 — Gate 이후:** 목록→상세→상담 저장→방문의 Remote Adapter를 순서대로 작성하고 실제 API·DB E2E를 남긴다.
5. **P1 — 발표 후:** 질문·피드백·회귀 결과를 반영해 5주차 인계 초안을 최종본으로 바꾼다.
6. **P2 — P0 이후:** 대형 CSS와 운영 대시보드 Mock 후속 작업을 별도 Issue로 정리한다.
