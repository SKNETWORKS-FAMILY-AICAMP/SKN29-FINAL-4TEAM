# 한예나 4주차 작업 진행도

> 작성 일시: **2026-08-07 10:15 KST**  
> 점검 기준 Commit: `a708a049484c30e09995c747faf3877bb3b36bb9` (`main`, `origin/main`)  
> 참고 지침서: [한예나 4주차 업무 지침서](../../weekly-task/한예나_4주차_업무_지침서.md)  
> 관할 기준: [팀원별 관할 영역 v2](<../../planning/md/팀원별 관할 영역 v2.md>)  
> 이전 점검: [한예나 4주차 작업 진행도 08050006](한예나_4주차_작업_진행도_08050006.md)

## 1. 판정 원칙과 점검 범위

- 한예나의 주관할인 `web/**`와 협업 범위인 `contracts/api/**`, 발표·통합 인계 문서를 확인했다.
- Mock 화면의 동작, 계약 소비 준비와 실제 Backend HTTP·DB 저장 성공을 서로 다른 완료 단계로 판정했다.
- 발표 기준 공통 Commit `e95e633d58324579a28bf7858fa8be1555ca1a09` 이후 현재 HEAD까지 `web/**` 소스 차이가 없다. 따라서 해당 Commit의 Main 회귀 결과를 현재 Web Source의 실행 증거로 인정했다.
- Main 회귀 문서에는 Node 24.14.0에서 Vitest 27개 파일·113개 Test, ESLint, TypeScript, Vite Production Build가 모두 통과했다고 기록돼 있다.
- 현재 점검 장비의 Node 20.11.0·npm 10.2.4는 README 최소 Node 20.19.0보다 낮아 이번 점검에서는 Web 명령을 다시 실행하지 않았다.
- 중간 발표 완료는 PM의 명시적 확인과 [WaterBridge 발표자료](../../presentation/WaterBridge.pdf)를 반영했다. 다만 실제 API·DB 저장이 없는 기능은 발표 완료와 무관하게 `MOCK_ONLY / BACKEND_BLOCKED`로 유지했다.

## 2. 종합 판정

| 4주차 필수 업무 | 완료도 | 판정 | 핵심 근거와 남은 Gate |
| --- | ---: | --- | --- |
| 3.1 Web 기준선 재검증·공식 Runtime 확정 | 100% | 완료 | 현재 Web Source에서 27 files·113 tests, Lint·TypeScript·Build가 통과했고 Runtime·Mock·폐기 경로와 README 재현 절차가 정리됐다. |
| 3.2 상담사 목록·상세 API Adapter와 Mock 경계 | 78% | 부분 완료 | Repository와 Mock Projection 경계, 차단 상태·오류 UI는 명확하다. 상담 목록·상세 Remote Repository와 Backend `EvidenceCardDTO` 실제 소비는 없다. |
| 3.3 `T-040` 상담 저장·AI 초안·409 충돌 | 50% | 절반 완료 | Mock에서 초안 분리, 409 입력 유지·멱등·추적·허용 행동을 검증했다. 실제 상담 저장 HTTP·DB 성공은 없다. |
| 3.4 `T-041` 방문 전환·일정 저장 | 45% | 절반 미만 | Mock 화면·희망일/확정일·버튼 제어와 차단 문서가 있다. 방문 Runtime API와 실제 DB 저장은 없다. |
| 3.5 계약 정합성·중복 코드·CSS 최소 정리 | 70% | 부분 완료 | 과거 Source 삭제와 Mock Snapshot 분리, Test Gate는 좋다. 새 Theme CSS를 포함해 핵심 CSS가 다시 커졌고 Source Map의 CSS 설명도 최신 상태와 다르다. |
| 3.6 Web 동결·발표 지원·5주차 인계 | 80% | 대부분 완료 | 발표 Commit·113 Test·Fallback·캡처·3회 반복과 실제/Mock 구분은 완료됐다. 발표 후 재검증과 최종 인계 문서·Issue 연결은 남았다. |

6개 절의 단순 평균 완료도는 **약 71%**다. Web 자체 품질과 발표 재현성은 높은 편이지만, 4주차 핵심인 **상담·방문 실제 Backend 연결은 0건**이므로 Mock 시연 성공을 서비스 연동 완료로 합산하지 않았다.

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 Web 기준선 재검증과 공식 Runtime 경로 확정 — 100%

완료된 내용:

- 발표 기준 Web Source에서 `npm ci`, Vitest 27 files·113 tests, ESLint, TypeScript·Production Build가 통과했다.
- Main 회귀에서도 Node 24.14.0과 Lock File 기준으로 113개 Test·Lint·Build가 통과했고 현재 HEAD까지 Web Source 변경이 없다.
- README에 최소 Node 20.19 또는 22.12 이상과 실제 검증 Version이 구분돼 있다.
- `src/main.tsx → App → Provider → Router`의 공식 진입 경로와 상담사·운영 화면 Runtime Source가 정리돼 있다.
- 과거 `inquiry-queue/**`, `inquiry-detail/**` 19개 파일과 Legacy CSS 2,406줄을 삭제하고 재도입 방지 ESLint 규칙을 두었다.
- 실제 API, Mock, Backend 차단과 정적 발표 자료가 문서에서 구분된다.
- 양정현의 README 교차 실행 기록이 있다.

수행 근거:

- [4주차 Web 기준선](../../../web/docs/week4-web-baseline.md)
- [Runtime Source Map](../../../web/docs/week4-runtime-source-map.md)
- [4주차 Web Test 결과](../../../web/docs/week4-test-result.md)
- [Main 회귀 검증 결과](../../testing/week4-main-regression-result.md)
- [Web README](../../../web/README.md)
- [App Router](../../../web/src/app/router/AppRouter.tsx)
- [ESLint 설정](../../../web/eslint.config.js)

완료 후 관리할 내용:

- npm High Severity 3건은 강제 Upgrade 없이 별도 영향 분석이 필요하다.
- 현재 점검 장비처럼 최소 Node 미만 환경에서는 실행이 차단되므로 팀 공통 Version 관리가 필요하다.

완료 유지 방법:

1. Node 24.14.0 또는 팀 합의 Version을 `.nvmrc`, Volta 또는 README 중 하나에 계속 고정한다.
2. Web Source 변경 시 같은 Commit에서 `npm ci`, Test·Lint·Build를 다시 실행한다.
3. npm Audit 경고는 Runtime 영향, 수정 Version과 회귀 위험을 확인한 뒤 Issue로 관리한다.

코드 외 별도 확인:

- 다른 팀원 환경에서 같은 Node·npm·Commit으로 계속 재현되는지 확인해야 한다.
- npm 보안 경고의 배포 영향과 Upgrade 시점을 PM·담당자가 승인해야 한다.

### 3.2 상담사 목록·상세 실제 API Adapter와 Mock 경계 정리 — 78%

완료된 내용:

- 상담 목록·상세·운영 화면은 `ConsultantWorkspaceRepository` 하나를 통해 데이터를 받는다.
- `VITE_USE_MOCK_API=false`에서 임의 Endpoint를 호출하지 않고 `BACKEND_BLOCKED`를 반환한다.
- Mock 업무 상태·위험도·우선순위·담당자·대기 시간·허용 행동을 명시적 Snapshot으로 분리해 화면에서 재계산하지 않도록 보완했다.
- `MOCK_ONLY`, `BACKEND_BLOCKED`, 계약 검토와 실제 API 0건이 기능 상태표에 명확히 기록돼 있다.
- Loading·Empty·403·409·422·Network와 AI·Evidence·Timeline 부분 실패를 Mock으로 재현할 수 있다.
- 내부 `chunk_id`, 경로·검색 점수·원문 전체를 Evidence 화면 타입에서 제외한다.
- 목록→상세→목록 복귀 시 검색·필터·페이지 Query가 유지된다.

수행 근거:

- [상담 Repository](../../../web/src/features/consultation/repositories/consultantWorkspaceRepository.ts)
- [Mock Backend Projection](../../../web/src/features/consultation/api/consultantWorkspaceMockProjection.ts)
- [Projection Test](../../../web/tests/unit/consultantWorkspaceMockProjection.test.ts)
- [Repository Test](../../../web/tests/unit/consultantWorkspaceRepository.test.ts)
- [Web API 상태표](../../../web/docs/week4-web-api-map.md)
- [기능 상태표](../../../web/docs/presentation/week4-web-feature-status.md)
- [T-018 Web 계약 소비 회신](../../handoffs/20260804_한예나_T018-D06_Web_회신_v0.1.md)

완료되지 않은 내용:

- `RemoteConsultationRepository`와 상담 목록·상세 실제 DTO·Mapper·Hook이 없다.
- Backend 상담 목록·상세 Runtime Endpoint가 없으므로 Browser 실제 호출 성공이 없다.
- 실제 API Mode에서 위험도·우선순위·담당자·`allowed_actions`를 Backend 값 그대로 소비한 Test가 없다.
- Evidence는 Data Fixture의 공개 필드 View Model이며 Backend `EvidenceCardDTO`를 실제 소비한 결과가 아니다.
- 401·403·404·5xx·부분 실패의 실제 API Test와 Mock UI Test를 짝지어 검증하지 못했다.
- API 상태표는 상담·방문 계약 파일이 비어 있다고 적었지만 최신 계약·Runtime 상태와 다시 대조할 필요가 있다.

완료 방법:

1. 최지용이 제공한 Active 상담 목록·상세 계약에 맞춰 Remote Repository와 DTO·Mapper를 작성한다.
2. 환경 Provider 한 곳에서 Remote·Mock을 선택하고 실제 실패를 자동 Mock 성공으로 숨기지 않는다.
3. Backend 응답의 상태·위험도·우선순위·담당자·대기 시간·허용 행동을 그대로 View Model로 변환한다.
4. Backend `EvidenceCardDTO`의 공개 필드만 받는 Type과 Mapper를 확정한다.
5. 실제 API 계약 Test와 Mock UI Test를 별도 Suite로 작성하고 Browser Network·화면 결과를 기록한다.
6. `week4-web-api-map.md`를 최신 OpenAPI·Runtime Inventory와 다시 맞춘다.

코드 외 별도 확인:

- 최지용이 상담 목록·상세 Endpoint·Query·DTO·오류 계약을 승인·제공했는지 확인해야 한다.
- 이동윤·김은진이 Evidence 공개 필드와 공식 URL 승격 기준을 승인했는지 확인해야 한다.
- 실제 고객 표시명·연락처·주소의 역할별 마스킹 정책을 확인해야 한다.

### 3.3 `T-040` 상담 결과 저장·AI 초안 수정·409 충돌 실제 연동 — 50%

완료된 내용:

- 상담 Form은 AI 초안과 상담사 수정·확정 내용을 구분한다.
- `allowed_actions`에 없는 행동을 숨기고 Mock 행동별 저장·완료 흐름을 재현한다.
- `state_version`, 멱등 키와 Correlation ID를 처리할 공통 기반과 Mock 요청 구조가 있다.
- 오래된 상태 409에서 입력을 유지하고 최신 Version·행동을 반영하며 자동 덮어쓰기·자동 재전송을 하지 않는다.
- 403·409·422·Network 오류 UI와 관련 단위·컴포넌트 Test가 있다.

수행 근거:

- [상담 저장 Hook](../../../web/src/features/consultation/hooks/useSaveConsultation.ts)
- [상담 Mock API](../../../web/src/features/consultation/api/consultationMockApi.ts)
- [상담 Action Panel](../../../web/src/features/consultation/components/ConsultationActionPanel.tsx)
- [상담 저장 Test](../../../web/tests/unit/useSaveConsultation.test.ts)
- [Action Panel Test](../../../web/tests/component/ConsultationActionPanel.test.tsx)
- [Web 계약 Test 계획](../../../web/docs/week4-web-contract-test-plan.md)

완료되지 않은 내용:

- 상담 시작·초안 저장·완료·방문 판단의 실제 API Client가 없다.
- 상담 결과가 실제 Backend DB에 저장된 증거가 없다.
- 실제 HTTP 요청의 `state_version`, `Idempotency-Key`, `X-Correlation-ID`와 Backend 로그 연결이 없다.
- 409 상태 충돌·멱등 Payload 충돌·Replay를 실제 Backend 응답으로 구분하지 못했다.
- AI 원본 초안과 상담사 확정본이 Backend 감사·이력에 분리 보존되는지 확인되지 않았다.
- 실제 API 성공 Test나 재현 가능한 수동 검증 기록이 없다.

완료 방법:

1. Active OpenAPI에 맞춰 상담 시작·초안·완료·방문 판단 Client·DTO·Mapper를 구현한다.
2. 쓰기 동작마다 Version·멱등 키·Correlation ID를 전송하고 응답의 최신 상태·행동을 반영한다.
3. 409 상태 충돌과 멱등 충돌을 별도 오류 코드로 처리하고 입력 보존을 실제 통합 Test로 검증한다.
4. AI 초안·수정본·확정본의 Backend 저장·감사 경계를 확인한다.
5. Browser 요청과 Backend 로그·DB Row를 하나의 `correlation_id`로 연결한 결과서를 남긴다.

코드 외 별도 확인:

- 최지용이 상담 행동별 Endpoint·Payload·응답·오류를 확정했는지 확인해야 한다.
- 윤승혁이 Action Code와 상담 완료·방문 검토 상태 전이를 승인해야 한다.
- 상담 메모의 개인정보·보관·마스킹 정책을 확인해야 한다.

### 3.4 `T-041` 방문 전환·기사 선택·방문 일정 저장 실제 연동 — 45%

완료된 내용:

- 방문 필요 분기와 방문 전환 Route·Form이 있다.
- 고객 희망일·기사·전달사항과 방문 확정일을 다른 필드·Label로 구분한다.
- Mock `allowed_actions`에 따라 방문 행동을 표시하고 없는 행동은 차단한다.
- 409에서 입력 유지·자동 재전송 금지와 날짜·일정 상태 Mapping을 계약 계획으로 정리했다.
- 합성 기사·주소·연락처와 Mock 일정만 사용하며 실제 자동 배정·예약으로 표현하지 않는다.
- 실제 API 부재와 `BACKEND_BLOCKED` 상태가 문서·화면에 표시된다.

수행 근거:

- [방문 전환 Page](../../../web/src/pages/consultant/VisitTransitionPage.tsx)
- [방문 Form](../../../web/src/features/visit-transition/components/VisitTransitionForm.tsx)
- [방문 Schema](../../../web/src/features/visit-transition/validation/visitTransitionSchema.ts)
- [방문 Form Test](../../../web/tests/component/VisitTransitionForm.test.tsx)
- [방문 Schema Test](../../../web/tests/unit/visitTransitionSchema.test.ts)
- [실제 API 구현 준비표](../../../web/docs/week4-web-implementation-readiness.md)

완료되지 않은 내용:

- 방문 요청·기사 후보·기사 선택·일정 수정·확정의 실제 API Client가 없다.
- 방문 요청과 일정이 Backend DB에 저장되지 않는다.
- 성공 후 문의 상태·담당 주체·방문 정보·상태 이력을 실제 응답으로 갱신하지 못한다.
- 실제 403·409·422·Network와 입력 유지 통합 Test가 없다.
- Backend 방문 Runtime·기사 배정 정책이 미확정이다.

완료 방법:

1. 최지용과 방문 요청·기사 후보·희망일·확정일·일정 상태 계약을 확정한다.
2. Client·Remote Repository·DTO·Mapper를 구현하고 Version·멱등 키·Correlation ID를 전송한다.
3. 성공 응답으로 문의·담당자·방문 정보·상태 이력을 갱신한다.
4. 403·409·422·Network·입력 유지와 실제 DB Row를 통합 Test로 검증한다.
5. 자동 기사 배정은 미구현으로 유지하고 합성 기사 사용 범위를 명확히 표시한다.

코드 외 별도 확인:

- 최지용이 방문 API와 기사 후보·일정 상태 계약을 제공했는지 확인해야 한다.
- 윤승혁이 방문 Action·상태 전이와 역할 권한을 승인해야 한다.
- 양정현의 기사 앱 DTO와 같은 희망일·확정일·상태 코드를 사용하는지 확인해야 한다.

### 3.5 Web 계약 정합성·중복 코드·CSS 최소 정리 — 70%

완료된 내용:

- 상담사 공식 Runtime 경로와 Repository 경계가 하나로 정리됐다.
- 미사용 과거 상담 Feature 19개 파일과 Legacy CSS 2,406줄을 삭제했다.
- Mock 업무 Projection을 별도 파일로 분리하고 정의되지 않은 Scenario를 임의 추측하지 않게 했다.
- 상태·위험도·우선순위와 행동 Type, 알 수 없는 Enum Fallback과 내부 Evidence 비노출 Test가 있다.
- 정리·디자인 변경 이후 Main 기준 113개 Test·Lint·Build가 통과했다.

수행 근거:

- [Runtime Source Map](../../../web/docs/week4-runtime-source-map.md)
- [Mock Projection](../../../web/src/features/consultation/api/consultantWorkspaceMockProjection.ts)
- [Workflow Action Mapper](../../../web/src/features/workflow-action/model/workflowActionMapper.ts)
- [EvidenceCard Test](../../../web/tests/component/EvidenceCard.test.tsx)
- [공통 Water Glass Theme](../../../web/src/common/styles/water-glass-theme.css)
- [상담사 Pearl Theme](../../../web/src/pages/consultant/ConsultantInquiryPearlTheme.css)

완료되지 않은 내용:

- 실제 API Mode 자체가 없어 Web이 Backend 업무 값을 재계산하지 않는다는 실제 소비 Test는 없다.
- `allowed_actions`의 공통 Type이 실제 Backend 객체 배열과 최종 계약 검증되지 않았다.
- `ConsultantDashboardPage.css` 2,919줄에 Pearl Theme 1,190줄, 공통 Theme 853줄이 추가돼 CSS 중복·우선순위 위험이 다시 커졌다.
- Runtime Source Map에는 새 Theme CSS의 적용 순서와 범위가 반영되지 않았다.
- 상담사 P0 실제 연동 전 운영 대시보드와 시각 Theme 확장이 진행됐다.
- 발표 후 CSS 회귀 캡처 비교와 중복 Rule 정리 결과가 없다.

완료 방법:

1. 새 Theme CSS를 Runtime Source Map과 정리 기록에 반영하고 Selector 적용 범위를 문서화한다.
2. 중복 Rule·전역 Selector·`!important`를 정적 검사하고 실제 충돌만 최소 수정한다.
3. Backend DTO 기반 공통 Enum·`allowed_actions` Type을 하나로 확정한다.
4. 실제 API Mode에서 상태·위험도·우선순위·담당자·행동을 재계산하지 않는 Test를 추가한다.
5. 상담사 핵심 캡처의 변경 전후 시각 회귀를 기록하고 이후 P1 UI 확장을 제한한다.

코드 외 별도 확인:

- 발표 화면에서 CSS 깨짐·가독성·Keyboard Focus 문제가 없었는지 확인해야 한다.
- 디자인 확장이 상담사 P0 실제 연동 일정을 밀지 않았는지 WBS·작업 이력을 확인해야 한다.
- 대규모 CSS 분리는 발표 후 별도 작업으로 진행할지 한예나·윤승혁이 합의해야 한다.

### 3.6 8월 5일 Web 동결·8월 6일 발표 지원·8월 7일 인계 — 80%

완료된 내용:

- 발표 Source Commit `1d1011d`과 공통 발표 기준 Commit이 기록돼 있다.
- 발표 기준에서 27 files·113 tests, Lint·TypeScript·Build와 대표 `DEMO-INQ-002` 조회가 통과했다.
- 실제 업무 API 0개, `/health` 1개, 인증 Client 4개, Mock-only 5개와 Backend 차단 4개를 발표 문서에 구분했다.
- 상담 목록→상세→공식 근거→409 입력 유지→방문 전환을 3회 반복한 기록이 있다.
- 캡처 5장과 Mock 표시가 있는 Fallback GIF·시연 Script·체크리스트가 준비됐다.
- 중간 발표가 완료됐고 발표자료는 Mock과 실제 연동 범위를 과장하지 않는다.
- 발표 후 피드백 문서가 작성돼 있다.

수행 근거:

- [발표 기준](../../../web/docs/presentation/week4-web-presentation-baseline.md)
- [시연 Script](../../../web/docs/presentation/week4-web-demo-script.md)
- [발표 체크리스트](../../../web/docs/presentation/week4-web-demo-checklist.md)
- [Fallback 계획](../../../web/docs/presentation/week4-web-fallback-plan.md)
- [발표 수치](../../../web/docs/presentation/week4-web-presentation-metrics.md)
- [발표 캡처](../../../web/docs/presentation/captures/20260804/README.md)
- [5주차 인계 초안](../../../web/docs/handoffs/week4-to-week5-web-handoff.md)
- [중간 발표 피드백](../../presentation/중간_피드백.md)

완료되지 않은 내용:

- 발표 후 현재 Commit에서 Test·Lint·Build를 다시 실행한 결과가 없다.
- 5주차 인계 문서가 여전히 “발표 후 최종 작성 예정”인 초안이다.
- 기능 상태표와 열린 이슈도 발표 후 정리가 `예정`이라고 남아 있어 현재 시점과 맞지 않는다.
- 피드백을 Web 기능·UI·설명·범위로 분류하고 담당자·Issue·목표일에 연결하지 않았다.
- 이동윤에게 AI 요약·Evidence·오류 표시 불일치를 전달하고 DEC-008을 재검토한 증거가 없다.
- `WaterBridge.pdf`와 피드백 문서는 현재 Git 미추적 상태이므로 다른 팀원이 Pull로 받을 수 없다.

완료 방법:

1. 지원 Node 환경에서 현재 HEAD의 Test·Lint·Build를 다시 실행하고 최종 결과를 기록한다.
2. `week4-to-week5-web-handoff.md`의 초안 표시를 제거하고 완료·Mock·차단·미구현, 담당자·목표일을 확정한다.
3. 발표 피드백을 Web 기능·UI·설명·범위로 분류해 Issue와 연결한다.
4. 최지용·이동윤·김은진·윤승혁별 인계 내용과 수신 확인을 남긴다.
5. 발표자료·피드백과 최종 인계 문서를 검수 후 Git에 포함한다.

코드 외 별도 확인:

- 발표 당일 실제 사용한 Commit·환경·시연 범위와 장애 여부를 김은진·한예나가 확인해야 한다.
- Mock Fallback 사용 여부와 발표 중 질의응답을 참석자 기록으로 보존해야 한다.
- 담당자별 인계 수신과 5주차 목표일은 문서만으로 확정할 수 없으므로 회신이 필요하다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 항목

| 확인 대상 | 별도로 확인할 내용 | 확인 주체 |
| --- | --- | --- |
| README 재현 | 다른 팀원이 같은 Node·Commit으로 설치·실행 가능한지 | 한예나·양정현 |
| 상담·방문 계약 | Active Endpoint·Payload·오류·상태 Version의 최종 승인 | 최지용·윤승혁 |
| Evidence | 공개 필드·공식 URL·마스킹 범위와 DEC-008 승인 | 이동윤·김은진 |
| 개인정보 | 고객 표시명·주소·연락처와 상담 메모의 역할별 마스킹 | 최지용·김은진 |
| 발표 | 실제 시연 범위·Fallback·오류·질의응답 결과 | 김은진·한예나 |
| CSS·접근성 | 시각적 깨짐, Keyboard·Focus와 화면 가독성 | 한예나·검수자 |
| 5주차 인계 | 담당자별 수신 확인, 목표일과 Issue 상태 | 최지용·이동윤·김은진·윤승혁 |

## 5. 완료 기준 및 최종 체크리스트

업무 지침서의 문구와 순서를 유지하고 현재 확인 가능한 근거만 반영했다.

### 5.1 8월 3일 기준선·구조 정리

- [x] 현재 Commit에서 `npm ci`가 성공한다.
- [x] Node.js·npm Version이 README와 Lock File에 맞게 기록되어 있다.
- [x] TypeScript 검사, Test, Lint, Production Build 결과가 현재 소스 기준으로 기록되어 있다.
- [x] Test File·Case 수가 실제 소스와 일치한다.
- [x] 공식 Runtime 구현 경로가 하나로 정해져 있다.
- [x] 중복·미사용 Source가 삭제되었거나 폐기 예정으로 표시되어 있다.
- [x] 실제 API, Mock, 정적 화면의 경계가 문서화되어 있다.
- [x] 다른 팀원이 README로 Web을 실행할 수 있다.

현재 완료로 체크 가능한 항목은 **8/8개**다.

### 5.2 8월 4일 실제 API 연동

- [x] 상담사 문의 목록 실제 API 또는 명시된 차단 상태가 있다.
- [x] 문의 상세 실제 API 또는 명시된 차단 상태가 있다.
- [x] 위험도·우선순위·담당자·상태를 Web에서 계산하지 않는다.
- [ ] 공식 근거는 Backend `EvidenceCardDTO` 공개 필드만 표시한다.
- [ ] 상담 결과 저장 API가 실제로 호출된다.
- [ ] `state_version`, `idempotency_key`, `correlation_id`가 쓰기 요청에 적용된다.
- [x] AI 초안과 상담사 확정본이 구분된다.
- [x] 403·404·409·422·Network 오류가 구분된다.
- [x] 409 이후 사용자 입력이 유지된다.
- [ ] 실제 API 성공과 Mock 성공 Test가 분리되어 있다.

현재 완료로 체크 가능한 항목은 **6/10개**다. 세 번째 항목은 업무 값을 화면에서 계산하던 로직을 명시적 Mock Backend Snapshot으로 옮긴 코드와 Test를 근거로 완료 처리했다.

### 5.3 8월 5일 발표 기준 Web 동결

- [x] 방문 필요 분기와 방문 일정 저장의 실제 또는 차단 상태가 확정되어 있다.
- [x] 방문 희망일과 확정일이 구분된다.
- [x] `allowed_actions`에 없는 버튼이 표시되지 않는다.
- [x] 발표 대표 문의 `DEMO-INQ-002` 흐름을 반복 재현할 수 있다.
- [x] 발표 기준 Commit과 실행 명령이 기록되어 있다.
- [x] Test·Lint·Build가 발표 기준 Commit에서 통과한다.
- [x] 실제 API와 Mock 범위가 기능 상태표에 구분되어 있다.
- [x] 화면 캡처와 Fallback 영상이 준비되어 있다.
- [x] 발표자료의 Web 구현 설명을 검수했다.

현재 완료로 체크 가능한 항목은 **9/9개**다.

### 5.4 8월 6일 중간 발표

- [x] 발표 직전 Backend Health와 Web 실행을 확인했다.
- [x] Demo 로그인 계정과 역할이 정상이다.
- [x] 발표 중 실제 API 연동 범위를 과장하지 않는다.
- [x] Mock Fallback 사용 시 즉시 Mock임을 밝힌다.
- [x] 오류 발생 시 원인을 Web·Backend·AI·환경으로 구분한다.
- [x] 발표 중 받은 질문과 피드백을 기록한다.
- [x] 미완료 기능을 완료된 화면처럼 설명하지 않는다.

PM의 발표 완료 확인과 발표자료·피드백 기록을 근거로 **7/7개**를 완료 처리했다.

### 5.5 8월 7일 발표 후 정리·5주차 인계

- [ ] 발표 피드백을 기능·UI·설명·범위로 분류했다.
- [ ] 발표 이후 변경 Commit에서 Test·Lint·Build를 다시 실행했다.
- [x] 실제 API 연결 완료·Mock-only·차단·미구현 기능을 구분했다.
- [x] 중복 Source·CSS 후속 정리 항목을 기록했다.
- [x] Test 실패와 Flaky Case를 기록했다.
- [x] 최지용에게 Web에서 필요한 미완료 Endpoint와 Payload를 전달했다.
- [ ] 이동윤에게 AI 요약·근거·오류 표시 불일치를 전달했다.
- [x] 김은진에게 최종 Web Test·실행 결과를 전달했다.
- [x] 윤승혁에게 발표 기준 기능 상태와 5주차 필수 범위를 전달했다.
- [ ] 5주차 Web 인계 문서가 저장소에 남아 있다.

현재 완료로 체크 가능한 항목은 **6/10개**다. 인계 초안은 존재하지만 문서 자체가 발표 후 최종 작성 예정이라고 명시하므로 마지막 항목은 완료 처리하지 않았다.

전체 체크리스트에서 현재 완료로 체크 가능한 항목은 **36/44개**다.

## 6. 완료를 위한 우선순위

1. **P0 — 상담 목록·상세 Remote 연결:** Active Backend 계약을 받아 Remote Repository·DTO·Mapper와 실제 오류 Test를 구현한다.
2. **P0 — 상담 저장 E2E:** 상담 행동별 HTTP, Version·멱등·Correlation, 409 입력 유지와 DB 저장을 검증한다.
3. **P0 — 방문 저장 E2E:** 방문 요청·기사 후보·희망일·확정일과 상태 갱신을 실제 Backend에 연결한다.
4. **P0 — 발표 후 최종화:** 현재 HEAD Test·Lint·Build를 재실행하고 피드백을 Issue·담당자·목표일에 연결한다.
5. **P1 — Evidence 계약:** Backend `EvidenceCardDTO` 공개 필드와 DEC-008을 확인해 실제 Mapper를 작성한다.
6. **P1 — CSS 최소 정리:** 새 Theme를 Source Map에 반영하고 대형 CSS 중복·전역 충돌을 시각 회귀와 함께 줄인다.
7. **P1 — 5주차 인계:** 초안 표시를 제거하고 최지용·이동윤·김은진·윤승혁의 수신 확인을 남긴다.
