# 한예나 5주차 작업 진행도

> 작성 일시: **2026-08-11 11:46 KST**  
> 기준 Commit: `b937c0a91290ab31e935815b28bbb24ffb799409`  
> 기준 문서: [팀원별 관할 영역 v2](../../planning/md/팀원별%20관할%20영역%20v2.md), [한예나 5주차 업무 지침서](../../weekly-task/한예나_5주차_업무_지침서.md)  
> 종합 판정: **진행 중 — Web 읽기 Remote 기반과 로컬 Gate는 완료, 상담·Visit 쓰기 화면 연결과 Public Evidence·실제 Smoke는 남음**

## 0. 판정 기준

- 요청문의 `3. 4주차 필수 업무`는 5주차 지침서의 `3. 5주차 필수 업무`로 해석했다.
- 체크리스트는 현행 지침서의 6장을 판정했다.
- Remote Repository 파일 존재와 실제 화면 Hook 소비·Backend Smoke를 구분했다.

## 1. 필수 업무별 요약

| 절 | 완료도 | 판정 | 핵심 미완료 |
|---|---:|---|---|
| 3.1 Web Entry Gate | 95% | 완료에 근접 | Runtime 대응표 최신화 |
| 3.2 비동기 Remote Repository | 95% | 코드 완료 | 실제 Backend Boundary Smoke |
| 3.3 문의 목록 Remote | 85% | 진행 중 | 실제 로그인·서버 Smoke |
| 3.4 문의 상세 Remote | 85% | 진행 중 | 실제 상세·PII Smoke |
| 3.5 AI·Evidence 소비 | 30% | 차단 | Public Evidence DTO·Backend 응답 |
| 3.6 상담 Action API 전환 | 40% | 부분 구현 | 실제 Hook 연결·409 Remote Test |
| 3.7 Visit UI·API 전환 | 50% | 부분 구현 | 실제 화면 연결·Backend Smoke |
| 3.8 State·Action·오류 Runtime 정합 | 60% | 진행 중 | 쓰기 경로 실제 적용 |

## 2. 3장 필수 업무별 평가

### 2.1 3.1 Web Entry Gate·Runtime 대응표 — 95%

완료된 내용과 근거:

- [Web Entry Gate](../../testing/web/week5-web-entry-gate.md)에 Mock→Remote, 계약 불일치, Blocker·해제 조건이 기록됐다.
- 현재 `main`에서 직접 재실행한 결과 Node v24.14.0, Lint PASS, 32개 파일·137 Test PASS, TypeScript·Production Build PASS다.

미완료 및 완료 방법:

- Entry 문서의 Backend 목록·상세 “공용 반영 전”, 상담·Visit “미구현” 표기를 현재 Runtime 12 Action 상태로 갱신해야 한다.
- Public Evidence가 여전히 빈 계약임을 유지하면서 상담·Visit Write는 `RUNTIME_AVAILABLE / WEB_NOT_WIRED`로 재분류해야 한다.

별도 확인: 실제 Backend URL·상담사 합성 계정·CORS 환경에서 Remote Smoke가 가능한지 확인해야 한다.

### 2.2 3.2 Mock 전용 Repository의 비동기 Remote 전환 — 95%

완료된 내용과 근거:

- [Data Repository](../../../web/src/features/consultation/repositories/consultantWorkspaceDataRepository.ts)에 비동기 Remote·Mock 구현, DTO→ViewModel Mapper, Server Query가 분리돼 있다.
- `VITE_USE_MOCK_API=false`는 실제 Remote를 선택하고 실패를 Mock 성공으로 바꾸지 않는다.
- 공통 HTTP Client·Token Refresh·Request Context를 재사용하며 Repository Boundary Test가 있다.

미완료 및 완료 방법:

- 실제 Backend를 대상으로 목록·상세 요청을 실행해 Boundary Test를 Live Smoke로 보강해야 한다.
- 기존 동기 Mock Repository를 사용하는 잔존 Component를 제거하거나 Demo Mode로 한정해야 한다.

### 2.3 3.3 상담사 문의 목록 실제 API·Server Filter·Pagination — 85%

완료된 내용과 근거:

- Remote 목록 Repository는 `GET /inquiries`와 계약 Query `q`, 상태·위험·우선순위·기간·정렬·페이지를 사용한다.
- 화면 Query Hook은 Loading·Empty·Error·Forbidden·Retry와 서버 `page_info`를 사용하며 관련 Test가 통과한다.

미완료 및 완료 방법:

- 실제 상담사 로그인으로 검색·복수 Filter·Sort·Pagination을 실행하고 Correlation ID와 Backend Query 결과를 대조해야 한다.
- 계약에 없는 상담구분·담당자 Filter가 Remote 요청으로 나가지 않는지 브라우저 Network 증거를 남겨야 한다.

별도 확인: 실제 DB 총건수·상태 집계와 화면 `status_counts`가 일치하는지 확인해야 한다.

### 2.4 3.4 상담사 문의 상세 Remote·Mapper·PII — 85%

완료된 내용과 근거:

- 상세 DTO·Section Mapper·ViewModel이 분리돼 있고 `section_errors`, State History, `state_version`, `allowed_actions`를 처리한다.
- Remote ViewModel은 상담사에게 주소를 노출하지 않고 Mock 필드로 상세를 보충하지 않는다.

미완료 및 완료 방법:

- 실제 `GET /inquiries/{id}`로 정상·403·404·부분 Section 오류를 Smoke해야 한다.
- 실제 응답·화면·Network payload에 주소·내부 ID가 없는지 확인해야 한다.

### 2.5 3.5 AI 안내·상담 요약·Evidence 실제 Backend 소비 — 30%

완료된 내용과 근거:

- Web은 AI FastAPI를 직접 호출하지 않고, Evidence UI는 내부 `chunk_id`·점수·경로·Prompt를 ViewModel에서 제외한다.
- 위험·근거 없음·AI 실패·Fallback Mock UI와 Test가 있다.

미완료 및 완료 방법:

- [Public Evidence 계약](../../../contracts/api/components/schemas/evidence/EvidenceCard.yaml)과 Route가 비어 있으므로 최지용·이동윤 확정 후 DTO·Mapper를 연결해야 한다.
- Backend 실제 AI Summary·Evidence·Timeout·Fallback을 상세 화면에서 구분하고 Live Test를 작성해야 한다.

별도 확인: 실제 응답의 허용 필드와 근거 원문·내부 메타데이터 비노출을 공동 검수해야 한다.

### 2.6 3.6 상담 Start·Summary·Confirm·Complete 실제 API — 40%

완료된 내용과 근거:

- [상담 Write Repository](../../../web/src/features/consultation/repositories/consultationWriteRepository.ts)에 실제 Endpoint·Request Context 구조가 있다.
- Backend에는 상담 Start·Save·Confirm·Complete Runtime이 있다.

미완료 및 완료 방법:

- 현재 [상담 저장 Hook](../../../web/src/features/consultation/hooks/useSaveConsultation.ts)은 Remote Mode에서 `RUNTIME_BLOCKED`를 반환하고 Mock만 호출한다. Remote Write Repository를 Hook에 주입해야 한다.
- Backend `status`, `state_version`, `allowed_actions`만 사용하고 로컬 `nextStatus` 계산을 제거해야 한다.
- 중복 클릭·동일 Idempotency Key Retry·409 Draft 보존·최신 상세 재조회 Test와 Smoke를 추가해야 한다.

### 2.7 3.7 Visit UI 계약·실제 API 전환 — 50%

완료된 내용과 근거:

- [Visit Write Repository](../../../web/src/features/visit-transition/repositories/visitWriteRepository.ts)는 Review·Create·Schedule·Confirm Endpoint와 date-only 변환을 구현한다.
- UI는 `type="date"`를 사용하고 Visit Create에는 기사 ID를 선행 전송하지 않는다.

미완료 및 완료 방법:

- Remote Visit Repository를 실제 상담 화면 Hook에 연결하고 Create 결과의 `visit_id`로 Schedule·Confirm을 이어야 한다.
- 주소 입력·표시를 제거하고 기사 선택은 Schedule 단계에만 허용해야 한다.
- 409·Network 실패 시 날짜·메모 보존과 실제 Backend 저장을 Test·Smoke해야 한다.

### 2.8 3.8 State·allowed_actions·오류 처리 정합화 — 60%

완료된 내용과 근거:

- 공통 HTTP Client, Idempotency Operation Tracker, Correlation Context, Remote 목록·상세의 Backend State 소비 구조가 있다.
- 401·403·404·409·422·5xx·Timeout 오류 타입과 입력 보존 단위 Test가 있다.

미완료 및 완료 방법:

- 상담·Visit 실제 Write에 `state_version`, Idempotency Key, Backend `allowed_actions`를 적용해야 한다.
- 409 후 Draft를 유지하면서 상세를 다시 읽어 최신 State·Action을 반영해야 한다.
- Remote 흐름의 모든 로컬 업무 판단을 제거하고 실제 Contract Smoke를 추가해야 한다.

## 3. 우선 완료 순서

1. Entry Gate Runtime 대응표 최신화
2. 실제 Backend 목록·상세 Remote Smoke
3. 상담 Write Repository를 화면 Hook에 연결
4. Visit Write Repository를 화면 Hook에 연결
5. Public Evidence 확정 후 실제 AI·Evidence 소비

## 4. 코드만으로 확인할 수 없는 사항

- 상담사 합성 계정·실제 Backend URL·CORS 환경 제공
- 실제 DB 건수·권한·PII와 화면 결과의 일치
- Backend AI Summary·Evidence의 사람 검수
- 브라우저 Network·Correlation·Backend 로그 공동 확인

## 5. 완료 기준 및 최종 체크리스트 판정

현재 완료로 체크해도 되는 항목:

- [x] 최신 Commit과 Node·npm Version을 기록했다.
- [x] 전체 Web Test·Lint·TypeScript·Production Build가 통과했다.
- [x] 상담사 P0 Endpoint Runtime·Mock 자동 대체·계약 불일치·Blocker를 조사했다.
- [x] 비동기 Repository Interface와 Remote Consultant Repository가 있다.
- [x] Mock·Remote Mode가 분리되고 Remote 실패를 Mock 성공으로 바꾸지 않는다.
- [x] API DTO와 ViewModel Mapper가 분리돼 있다.
- [x] 공통 HTTP Client·Refresh 경계를 재사용한다.
- [x] Repository Boundary Test가 있다.
- [x] 실제 `GET /inquiries` 코드 경로와 서버 검색·Filter·Sort·Pagination이 있다.
- [x] Web이 전체 결과를 다시 Filter하지 않고 계약에 없는 Query를 보내지 않는다.
- [x] 목록 Loading·Empty·Error 상태가 비동기 요청과 연결된다.
- [x] 실제 `GET /inquiries/{id}` 코드 경로와 Section Mapper가 있다.
- [x] 상세 ViewModel은 주소를 노출하지 않고 Mock 필드로 Remote를 보충하지 않는다.
- [x] State History·`state_version`·`allowed_actions`를 Backend DTO에서 받는다.
- [x] Web이 AI FastAPI를 직접 호출하지 않는다.
- [x] UI 모델에 `chunk_id`, Vector Score, 내부 경로, Prompt를 노출하지 않는다.
- [x] 상담·Visit Remote Write Repository 골격이 있다.
- [x] Visit 입력은 date-only이며 Create 단계에서 기사 ID를 보내지 않는다.
- [x] 전체 Test·Lint·TypeScript·Production Build PASS다.
- [x] 실제 Token·비밀값을 소스에 포함하지 않는 구조다.
- [x] P1 Dashboard가 P0 상담사 흐름과 구분돼 있다.

아직 완료로 체크하면 안 되는 핵심 항목:

- [ ] 상담 목록·상세 Remote Smoke가 실제 Backend에서 PASS했다.
- [ ] 실제 AI Summary·Public Evidence가 화면에 표시된다.
- [ ] 상담 Start·Save·Confirm·Complete Hook이 실제 API를 사용한다.
- [ ] Visit Create·Schedule·Confirm 화면 흐름이 실제 API를 사용한다.
- [ ] Remote 쓰기에서 Backend State·Action·409 복구가 검증됐다.
- [ ] 실제 상담사 대표 Web E2E와 Feature Complete가 PASS했다.
