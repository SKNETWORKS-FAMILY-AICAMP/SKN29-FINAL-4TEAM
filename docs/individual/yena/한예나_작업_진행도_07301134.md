# 한예나 3주차 작업 진행도

> 최초 작성: 2026-07-29 16:05 KST
>
> 최종 갱신: 2026-07-30 11:34 KST
>
> 기준 Commit: `f4149cf` (`main`)
> 참고: `docs/weekly-task/한예나_3주차_업무_지침서.md`, `web/docs/week3-test-result.md`, `web/docs/week3-completion-checklist.md`, `docs/handoffs/README.md`

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 현재 근거 |
|---|---:|---|---|
| 3.1 React 실행 환경·공통 구조 | 95% | 대부분 완료 | Vite·Router·Guard·Auth와 Node 26 환경의 Test/Lint/Build 통과 기록이 있음 |
| 3.2 공통 API 계층·상태·근거 UI | 90% | 대부분 완료 | Auth 실제 Client, 공통 HTTP·401 Refresh·근거 UI가 있으나 상담 API는 Mock임 |
| 3.3 CONS-01 문의 목록 | 95% | 대부분 완료 | 공식 Fixture 기반 목록·검색·필터·정렬·URL·페이지·UUID 상세 이동이 검증됨 |
| 3.4 CONS-02 문의 상세 | 90% | 대부분 완료 | 고객·제품·증상·AI 초안·공식 근거·이력과 오류 상태가 Mock으로 구현됨 |
| 3.5 상담 Form·allowed_actions | 90% | 대부분 완료 | 멱등성·409·422·입력 유지·방문 전환 골격이 있으나 실제 행동 Endpoint가 없음 |
| 3.6 테스트·매핑·검토 산출물 | 90% | 대부분 완료 | 23개 파일 92개 테스트와 Lint·Build 통과 기록, 결정·매핑·이슈 문서가 있음 |

종합 완료도는 **약 92%**다. 지침서가 허용한 Web Mock·교체 구조 범위는 완료에 가깝다. 남은 핵심은 Backend가 아직 제공하지 않는 상담·방문 Runtime을 실제로 소비하는 것과 동일 Commit E2E다.

## 2. 현재 검증 경계

- 공식 기록: Node `26.4.0`, npm `11.17.0`, Vitest 23개 파일·92개 테스트 PASS, Lint·Build PASS.
- `public_id`를 외부 문의 UUID로 읽는 Mapper가 반영돼 과거 인계 허브의 식별자 Blocker는 코드상 해결됐다.
- 이번 점검 환경은 Node `20.11.0`이고 `node_modules`가 없어 vitest·eslint를 재실행하지 못했다. 코드 실패가 아니라 의존성 미설치 상태다.
- 실제 Backend Runtime은 Health·Auth·문의 생성·취소 7개뿐이며 상담 목록·상세·행동 API는 아직 없다.

## 3. 필수 업무별 평가

### 3.1 React 웹 실행 환경과 역할별 공통 구조 — 95%

완료된 내용:

- Vite·TypeScript·Router·Provider·상담사/운영자 Layout과 Auth/Role Guard가 있다.
- 401·403·404·전역 오류를 구분하고 Refresh single-flight와 로그아웃 처리가 있다.
- 깨끗한 저장소 복사본에서 설치·Test·Lint·Build를 재현한 기록이 있다.

수행 근거: [Web README](<../../../web/README.md>), [App Router](<../../../web/src/app/router/AppRouter.tsx>), [Role Guard](<../../../web/src/app/router/guards/RoleGuard.tsx>), [빌드·테스트 결과](<../../../web/docs/week3-test-result.md>)

남은 내용과 완료 방법:

- 팀 표준 Node 버전을 README·환경 파일에 고정하고 현재 PC에서도 `npm ci → test → lint → build`를 재현한다.
- 실제 Backend CORS·Token 만료·Refresh Rotation을 브라우저에서 검증한다.

### 3.2 공통 API 계층과 상태·근거 UI — 90%

완료된 내용:

- 공통 응답·오류·페이지네이션·환경·Correlation·Authorization Client가 있다.
- 상태·위험도·우선순위 Badge, DataTable, Pagination, Feedback, EvidenceCard가 있다.
- 내부 경로·검색 점수·`chunk_id`·원문 전체 비노출 테스트가 있다.

수행 근거: [HTTP Client](<../../../web/src/common/api/httpClient.ts>), [공통 API 응답 타입](<../../../web/src/common/api/apiResponse.ts>), [EvidenceCard](<../../../web/src/features/evidence-viewer/components/EvidenceCard.tsx>), [EvidenceCard 테스트](<../../../web/tests/component/EvidenceCard.test.tsx>)

남은 내용과 완료 방법:

- 상담·방문 OpenAPI가 생기면 Mock API를 실제 Repository/Client로 교체한다.
- Backend 실제 403·409·422 응답을 공통 Mapper와 연결한다.
- AI 상담 요약 계약이 완성되면 TypeScript DTO를 다시 생성·검수한다.

### 3.3 CONS-01 상담사 문의 목록·검색·필터 — 95%

완료된 내용:

- 검색, 상태·위험도·우선순위·담당자·기간 필터, 정렬, URL Query와 페이지네이션이 구현됐다.
- 초기 빈 목록·검색 결과 없음·403·오류가 분리된다.
- 표시 코드가 아닌 `public_id` UUID로 상세 경로를 만든다.

수행 근거: [상담사 문의 목록](<../../../web/src/features/consultation/components/ConsultantQueue.tsx>), [목록 필터 Hook](<../../../web/src/features/inquiry-queue/hooks/useInquiryQueueFilters.ts>), [목록 Mock](<../../../web/src/features/inquiry-queue/model/inquiryQueueMock.ts>), [상담사 화면 통합 테스트](<../../../web/tests/integration/ConsultantDashboardPage.test.tsx>)

남은 내용과 완료 방법:

- 실제 상담 대기 목록 API의 Query·PageInfo·정렬 계약을 확정해 Hook을 교체한다.
- Backend 반환 건수·페이지 경계와 Mock Fixture 결과를 대조한다.

### 3.4 CONS-02 문의 상세·AI 요약·공식 근거 — 90%

완료된 내용:

- 고객·제품·증상·문진·조치·사용 안내·AI 초안/확정본·근거·상태 이력이 구분돼 표시된다.
- 지원 불가·AI 실패·근거 없음·부분 실패 Mock과 직접 URL 접근이 있다.

수행 근거: [문의 상세 화면](<../../../web/src/pages/consultant/InquiryDetailPage.tsx>), [상세 Mapper](<../../../web/src/features/inquiry-detail/model/inquiryDetailMapper.ts>), [AI 요약 Section](<../../../web/src/features/inquiry-detail/components/AiSummarySection.tsx>), [상담 Mock Fixture](<../../../web/tests/fixtures/consultation/normal.json>)

남은 내용과 완료 방법:

- 실제 상세 API와 AI 상담 요약 응답 Schema를 연결한다.
- 실제 개인정보 마스킹과 Evidence 공개 필드를 Backend·Data Owner와 공동 검수한다.

### 3.5 상담 기록 Form과 allowed_actions 기반 행동 — 90%

완료된 내용:

- 상담 입력 Validation, 입력 유지, 중복 클릭 차단, 멱등 키와 stateVersion 처리가 있다.
- Backend `allowed_actions`만으로 버튼을 노출하고 409 후 최신 Snapshot을 반영한다.
- 방문 검토·일정 Form과 Mock 저장·확정 흐름이 있다.

수행 근거: [상담 행동 Panel](<../../../web/src/features/consultation/components/ConsultationActionPanel.tsx>), [상담 저장 Hook](<../../../web/src/features/consultation/hooks/useSaveConsultation.ts>), [Workflow Action Mapper](<../../../web/src/features/workflow-action/model/workflowActionMapper.ts>), [상담 행동 테스트](<../../../web/tests/component/ConsultationActionPanel.test.tsx>)

남은 내용과 완료 방법:

- 상담 저장·완료·방문 검토·일정 Endpoint가 구현되면 실제 호출로 교체한다.
- 서버 Replay·Payload 충돌·권한 오류를 브라우저 E2E로 검증한다.

### 3.6 테스트·필드 매핑·검토 산출물 — 90%

완료된 내용:

- 23개 파일 92개 자동 테스트와 수동 화면 검증 기록이 있다.
- 화면–API–DB 매핑, 기술 결정, 미해결 이슈와 Mock/실제 차이가 문서화됐다.
- UUID·시간대·공식 Fixture·근거 비노출 회귀 테스트가 있다.

수행 근거: [주차 테스트 결과](<../../../web/docs/week3-test-result.md>), [화면–API–DB 매핑](<../../../web/docs/week3-screen-api-db-map.md>), [Prototype Migration](<consultant-prototype-migration.md>), [계약 Fixture 테스트](<../../../web/tests/unit/generatedFixtures.test.ts>)

남은 내용과 완료 방법:

- 지원 Node 환경을 팀 공용 기준으로 고정해 다른 팀원이 다시 실행한다.
- 실제 Backend와 Playwright 등 브라우저 E2E를 추가한다.
- P1 운영 대시보드보다 P0 상담·방문 실제 연동을 우선한다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 |
|---|---|
| 실행 환경 | 지원 Node/npm 버전과 깨끗한 `npm ci` 재현 |
| API | 상담·방문 Endpoint와 실제 403·409·422 응답 |
| 개인정보 | 이름·연락처·주소 마스킹 정책의 Backend/PM 승인 |
| Evidence | 공개 필드와 공식 링크의 Data/AI Owner 승인 |
| UI | 주요 해상도·브라우저 접근성·수동 시연 결과 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 현재 완료로 체크해도 되는 항목

- [x] 의존성 설치·TypeScript 검사·Production Build 성공 기록이 있다.
- [x] 상담사 Router·Layout·Role Guard가 동작한다.
- [x] 401·403·404·전역 오류가 구분돼 있다.
- [x] 공통 API 응답·오류·페이지네이션 타입이 있다.
- [x] 상태·위험도·우선순위·근거 카드 공통 UI가 있다.
- [x] CONS-01 검색·필터·정렬·페이지네이션과 상세 이동이 Mock으로 동작한다.
- [x] CONS-02에 고객·제품·증상·문진·AI·근거·이력이 구분돼 표시된다.
- [x] AI 초안과 상담사 확정 내용이 구분된다.
- [x] 내부 근거 필드 비노출 구조와 테스트가 있다.
- [x] 상담 Form과 `allowed_actions` 기반 행동 골격이 있다.
- [x] 정상·위험·근거 없음·권한 없음·409 Mock이 있다.
- [x] 92개 테스트와 수동 검증 결과가 기록됐다.
- [x] 화면–API–DB 매핑과 Mock/실제 차이가 문서화됐다.
- [x] 다른 팀원이 깨끗한 복사본에서 README 명령을 재현한 기록이 있다.

### 5.2 아직 완료로 체크하면 안 되는 항목

- [ ] 상담 목록·상세·저장·완료·방문을 실제 Backend API로 실행했다.
- [ ] 실제 Token 만료·Refresh·403·409·422 브라우저 E2E가 통과했다.
- [ ] 완성된 AI 상담 요약 Schema가 TypeScript DTO에 반영됐다.
- [ ] Web·Backend·AI·Data가 동일 Commit에서 통합 E2E를 수행했다.
- [ ] 공식 제출 문서에 Web 관점 검수 의견이 최종 반영됐다.

## 6. 다음 작업

1. 팀 표준 Node 환경에서 현재 92개 테스트·Lint·Build를 재현한다.
2. Backend의 다음 상담·방문 Runtime 계약에 맞춰 Mock API를 교체한다.
3. 실제 인증·409·422 브라우저 E2E를 추가한다.
4. P1 대시보드보다 P0 수직 흐름 연동을 우선한다.
