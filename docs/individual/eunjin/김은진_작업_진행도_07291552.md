# 김은진 3주차 작업 진행도

- 작성 일시: **2026-07-29 15:52:35 +09:00 (Asia/Seoul)**
- 기준 문서: `docs/planning/md/팀원별 관할 영역 v2.md`, `docs/weekly-task/김은진_3주차_업무_지침서.md`
- 점검 범위: 김은진 주관할인 `data/**`, `tests/**`, `infra/**`, `.github/workflows/**`, 관련 `scripts/**`와 공동 편집 영역인 `docs/**`, 그리고 협업 산출물인 `ai/evaluation/**`, `contracts/**`, `docs/database/**`
- 판정 원칙: 파일이 있다는 사실만으로 완료 처리하지 않고, 지침서의 완료 기준을 현재 저장소 내용과 재실행 결과로 확인했다. 자동 검증으로 확인할 수 없는 팀 승인·실환경 연동·외부 원본은 별도 확인 대상으로 분리했다.

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 핵심 사유 |
| --- | ---: | --- | --- |
| 3.1 공식 원본·출처 Inventory·모델 계보 | 80% | 부분 완료 | Inventory·계보·Git 제외 정책은 갖춰졌으나 원본이 저장소에 없어 현재 시점의 해시 재검증과 공식 URL 재확인이 필요함 |
| 3.2 전처리·Schema·품질 리포트 | 65% | 부분 완료 | 구조화 데이터·Schema·리포트·재현성 도구가 있으나 현재 QA 재실행이 계약 해시 4건으로 실패하고 저장된 PASS 리포트가 현재 상태를 반영하지 못함 |
| 3.3 RAG 정답 세트·검색 평가 | 25% | 미완료 | 기본 평가 3건과 집계 리포트만 있고, 38쪽·오염 차단·Index 설정·Case별 Top-k 결과가 연결된 공식 평가 세트가 없음 |
| 3.4 합성 업무 데이터·DB Seed Fixture | 65% | 부분 완료 | 원본 시나리오 24건과 활성 projection 22건은 있으나 2건이 결정 대기이고 실제 DB Seed·반복 적재 검증이 없음 |
| 3.5 P0 QA·계약 Fixture·Data CI | 15% | 미완료 | 디렉터리 골격만 있고 P0 기준, Case 매트릭스, 계약 Fixture, 실행 스크립트, Workflow가 사실상 비어 있음 |
| 3.6 공식 산출물 작성·검수·인계 | 0% | 미완료 | `docs/submission/`이 비어 있고 요구된 전처리 결과서와 DB·저장소 설계 제출본이 없음 |

단순 평균 기준 종합 완료도는 **약 42%**이다. 데이터 기준본과 생성 도구는 상당 부분 존재하지만, 현재 실행 가능한 녹색 검증과 공식 제출·검수 증빙이 부족하므로 3주차 필수 업무 전체를 완료로 처리할 수 없다.

## 2. 현재 재실행 결과

### 2.1 실행한 명령

```powershell
python -B -m unittest discover -s data/tools/tests -v
python -B data/tools/pipeline.py qa --verify-rebuild
git -c safe.directory='C:/SKN projects/final' ls-files -- 'data/raw/**'
```

### 2.2 결과

- Data 단위 검증: **51건 중 48건 성공, 3건 실패**
- 실패한 테스트:
  - `test_rag_wrapper_and_unified_cli_are_equivalent`
  - `test_synthetic_wrapper_and_unified_cli_are_equivalent`
  - `test_contract_sources_and_vocabularies_are_current`
- `qa --verify-rebuild`: **종료 코드 1, 오류 4건**
- 오류 원인: `data/config/workflow/service_contract_mapping.json`에 기록된 아래 State Machine 계약 해시가 현재 계약 파일의 해시와 다름
  - `contracts/state-machine/inquiry-states.yaml`
  - `contracts/state-machine/inquiry-events.yaml`
  - `contracts/state-machine/transition-rules.yaml`
  - `contracts/state-machine/allowed-actions.yaml`
- 대표 E2E 데이터 불변식 검증은 **17/17 성공**했고 재생성 파일 차이도 없었다.
- `data/raw/**`에서 Git이 추적하는 파일은 `.gitignore`, `.gitkeep`, `README.md` 등 정책 파일뿐이며 PDF·FAQ 원문은 추적되지 않는다.
- 저장소의 `data/processed/validation/latest_qa_summary.json`은 `PASS`로 남아 있으나 현재 재실행은 실패하므로, 이 리포트는 현재 계약 기준에서는 최신 성공 증빙으로 사용할 수 없다.

## 3. 필수 업무별 상세 평가와 완료 방법

## 3.1 공식 원본·출처 Inventory·모델 계보 재검증 — 80%

### 확인된 완료 내용

- `source_inventory.csv`, `model_document_lineage.csv`, `collection_log.csv`, `error_missing_list.csv`, `dataset_manifest.json`이 존재한다.
- MVP `WPUJAC104DWH`는 WPU-JAC104 D세대·REV.00·44쪽·SHA-256과 연결되어 있다.
- 확장 `WPUIAC425SNW`는 REV.02·52쪽·MVP 제외로 기록되어 있고, IAC506은 원본 미확보·사용 제외로 기록되어 있다.
- 미확인 FAQ를 기본 임베딩 또는 공식 단독 근거로 사용하지 않는 정책이 Manifest와 handoff 정보에 기록되어 있다.
- `data/raw/**`의 원본 비추적 정책이 적용되어 있다.

### 완료되지 않은 항목

- Inventory가 가리키는 원본 경로가 `external_backup/...`이며 저장소 안에는 실제 PDF·FAQ 원문이 없다. 따라서 현재 환경에서는 44쪽·52쪽·파일 크기·SHA-256을 다시 계산할 수 없다.
- WPUJAC104DWH 서비스센터 검색과 직접 다운로드 URL은 `OPEN_NON_BLOCKING` 실패 상태이다.
- 지침서가 제시한 `scripts/data/build_source_inventory.py`, `build_model_lineage.py`, `check_model_scope.py`, `check_legacy_exclusion.py`와 같은 독립 실행 명령은 없고 기능이 `data/tools/**`에 통합되어 있다. 동등 기능과 사용법을 명확히 연결한 문서가 필요하다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `scripts/data/verify_source_inventory.py` 또는 동등한 명령을 작성해 접근 가능한 외부 원본의 파일 크기·페이지 수·SHA-256을 `source_inventory.csv`와 비교하고 불일치 시 종료 코드 1을 반환한다.
2. `data/README.md`에 외부 원본을 어디에 마운트하고 어떤 명령으로 Inventory를 재검증하는지, `data/tools/pipeline.py inventory`와의 관계를 명시한다.
3. 공식 URL 확인 결과와 확인 일시를 `collection_log.csv`에 갱신하고, 계속 실패하면 `error_missing_list.csv`에 담당자·재확인 예정일을 추가한다.
4. 확인이 끝난 뒤 `dataset_manifest.json`과 `data/catalog/CHANGELOG.md`를 같은 데이터 버전으로 재생성한다.

### 코드 외 별도 확인

- 외부 백업의 PDF가 공식 배포본과 동일한지 사람이 표지·모델명·REV를 다시 확인해야 한다.
- 공식 URL 실패를 비차단으로 유지할지, 원본 재수집이 필수인지 PM 승인이 필요하다.
- 외부 백업의 보존 위치·접근 권한·복구 책임자가 정해졌는지 확인해야 한다.

## 3.2 전처리 파이프라인·Schema·품질 리포트 확정 — 65%

### 확인된 완료 내용

- MVP 매뉴얼 44쪽, FAQ 119건, FAQ 후보 20건, MVP RAG 청크 7건, Evidence 9건과 관련 JSON Schema가 존재한다.
- Schema·ID/FK·상태·근거·대표 E2E·재현성 검사가 `data/tools/**`와 단위 테스트로 구현되어 있다.
- 대표 E2E 17개 검사는 성공했고 생성 결과의 byte 재현성 차이는 없었다.
- MVP·확장·제외 정책과 실제 집계값은 Manifest·README·QA 문서에 기록되어 있다.

### 완료되지 않은 항목

- 현재 `qa --verify-rebuild`가 계약 해시 4건 때문에 실패한다. 저장된 Schema·Integrity·Quality 리포트의 `PASS`와 현재 실행 결과가 불일치한다.
- 현재 리포트는 계약 오류가 Schema·Integrity·Quality 상태 전체에 전파되는 구조라 데이터 자체 오류와 외부 계약 드리프트를 구분하기 어렵다.
- 원본이 없으므로 원본→페이지 추출 전체를 다른 팀원이 저장소만으로 재생성할 수 없다.
- 공식 `docs/submission/data-preprocessing-result.md`가 없어 Manifest·품질 리포트·평가 결과의 수치 교차 검증이 끝나지 않았다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 윤승혁의 확정 계약을 확인한 후 `data/config/workflow/service_contract_mapping.json`의 계약 해시와 결정 상태를 갱신한다. 의미 변경이 있으면 단순 해시 교체가 아니라 synthetic 설정과 Expected 파일도 함께 수정한다.
2. `python -B -m unittest discover -s data/tools/tests -v`와 `python -B data/tools/pipeline.py qa --verify-rebuild`가 모두 성공하도록 수정한 뒤, 5개 상세 리포트와 `latest_qa_summary.json`을 같은 실행에서 재생성한다.
3. 가능하면 데이터 Schema 오류와 계약 소스 드리프트를 리포트 항목으로 분리해, 어느 검사가 실제로 실패했는지 명확히 표시한다.
4. `data/README.md`에 원본이 없는 상태에서 가능한 재현 범위와 외부 원본이 필요한 전체 재생성 범위를 구분한다.
5. `docs/submission/data-preprocessing-result.md`에 실제 건수, 제외 사유, 오류 0건의 재실행 증빙, 명령, Commit을 기록한다.

### 코드 외 별도 확인

- 페이지 번호가 PDF 물리 페이지인지 매뉴얼 인쇄 페이지인지 38쪽 대표 근거를 사람이 원문과 대조해야 한다.
- FAQ 후보의 `conditional`·`unverified` 판정이 업무 의미상 적절한지 데이터 담당자와 AI 담당자가 공동 검수해야 한다.

## 3.3 RAG 정답 세트·검색 평가·오염 차단 검증 — 25%

### 확인된 완료 내용

- AI 평가 데이터 3건과 `latest_eval_report.json`의 Recall@5·MRR 집계가 존재한다.
- 합성 Expected에는 `SYN-JAC104-002`, 공식 38쪽, 금지 Evidence 참조가 들어 있고 대표 E2E 불변식 검사는 성공한다.
- RAG handoff Manifest는 인덱싱 대상 7개 청크와 FAQ·IAC425·IAC506 제외 정책을 기록한다.

### 완료되지 않은 항목

- `ai/evaluation/datasets/retrieval/rag_eval_dataset.json`과 `ai/evaluation/datasets/rag_eval_dataset.json`이 서로 다른 chunk ID를 사용한다.
- 평가 레코드가 `eval_id`, 질의, 모델, expected chunk 정도만 포함하며 지침서의 `scenario_id`, 문서 ID, 페이지, 금지 문서·모델, Top-k, 통과 조건이 없다.
- 저출수 Case가 실제 REV.00 38쪽을 Top-k에서 반환했다는 Case별 결과가 없다.
- JAC104 S세대, IAC425, IAC506, 미검증 FAQ, 존재하지 않는 모델의 전용 부정 평가 세트가 없다.
- `latest_eval_report.json`은 전체 3건의 평균만 있어 Embedding 모델·버전, Index Manifest, 필터, 순위, 실패 원인과 담당자를 확인할 수 없다.
- 권장 문서 `docs/testing/week3-rag-evaluation-criteria.md`가 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 이동윤과 협의해 `ai/evaluation/datasets/retrieval/jac104_retrieval_cases.jsonl`을 단일 기준본으로 만들고 실제 RAG chunk ID, 문서 ID, 38쪽과 연결한다.
2. `jac104_negative_scope_cases.jsonl`에 S세대·IAC425·IAC506·미검증 FAQ·없는 모델·정책 차단 문서 Case를 작성한다.
3. AI 평가 Runner가 Case별 검색 순위, Top-k, 필터, Embedding 모델, Index 버전, 판정과 실패 원인을 JSON으로 저장하도록 확장한다.
4. `docs/testing/week3-rag-evaluation-criteria.md`에 Recall/Hit 기준, 오염 0건 기준, 근거 없음 Fallback, 수동 검토 조건을 작성한다.
5. 중복된 두 평가 데이터 경로의 책임을 정리하고 한쪽은 제거하거나 호환용으로 명시한다.

### 코드 외 별도 확인

- 실제 pgvector/Vector Store에 최신 7개 청크가 인덱싱되었는지 이동윤의 실행 로그와 Index Manifest가 필요하다.
- `BAAI/bge-m3` 등 실제 Embedding 설정과 Top-k 정책을 AI 담당자가 확인해야 한다.
- 공식 매뉴얼 38쪽이 저출수 정답이라는 원문 육안 검수가 필요하다.

## 3.4 합성 업무 데이터·DB Seed Fixture·기대 결과 완성 — 65%

### 확인된 완료 내용

- 원본 시나리오 24건, 역할별 사용자 16건, 고객·제품·구독·문의·상담·방문·상태 이력·감사 이력이 존재한다.
- 활성 projection 22건은 Schema·ID/FK·상태·Evidence·개인정보 패턴·대표 E2E 불변식 검사를 거친다.
- `SYN-JAC104-002`, `DEMO-INQ-002`, 공식 38쪽 연결과 14단계 대표 흐름이 현재 데이터에서 일치한다.
- DB·RAG·QA·API Mock 소비자별 handoff Manifest와 DB import crosswalk가 존재한다.

### 완료되지 않은 항목

- `SYN-JAC104-012`, `SYN-JAC104-016`은 재개 정책 결정 대기로 projection에서 제외되어 Expected와 Fixture가 22건뿐이다. `reopened_inquiry.json`도 0건이다.
- `products.json`에는 활성 MVP 제품 1건만 있어 확장 제품을 Fixture 안에서 명시적으로 구분한다는 지침을 완전히 만족하지 않는다.
- `service_contract_mapping.json`은 `PARTIAL_BLOCKED_DECISIONS`이고 계약 해시도 현재 계약과 다르다.
- handoff 상태가 `READY_FOR_FIELD_MAPPING`/`SERVICE_MAPPING_PENDING`이며 실제 DB 적재 성공을 뜻하는 `DB_VERIFIED`가 아니다.
- `scripts/database/seed_demo_data.py`, `verify_demo_data.py`가 없고 반복 Seed 시 중복이 증가하지 않는지 실제 DB에서 검증한 기록도 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 윤승혁이 확정한 문의 재개 정책을 `service_contract_mapping.json`과 scenario 설정에 반영하고, 24건 모두를 투영할지 차단 2건을 공식 제외 기준으로 승인받는다.
2. 정책 확정 후 synthetic Fixture·Expected·subset·Manifest·QA 리포트를 다시 생성한다.
3. 확장 제품을 seed 비활성 레코드로 유지할지 완전히 분리할지 결정하고 `products.json`과 Schema/README에 동일하게 반영한다.
4. 최지용 주관으로 `scripts/database/seed_demo_data.py`와 `verify_demo_data.py`를 작성하고 자연키·Upsert·초기화·반복 적재 검사를 구현한다. 김은진은 Fixture·crosswalk·검증 기준을 제공한다.
5. 실제 DB 적재 결과를 `docs/testing/results/`에 환경, Migration 버전, 명령, 전후 건수, 중복 0건과 함께 기록한다.

### 코드 외 별도 확인

- 최신 ERD·Migration의 필드·Enum·시간대·nullable·unique 제약과 Fixture가 일치하는지 최지용 확인이 필요하다.
- Web·Mobile·AI가 실제로 같은 ID와 Expected를 소비하는지 각 담당자의 실행 증빙이 필요하다.
- 합성 표시명·전화번호·주소가 실제 개인정보가 아닌지 사람이 최종 샘플 검수해야 한다.

## 3.5 P0 QA 기준·계약 테스트 Fixture·Data CI 최소 골격 구성 — 15%

### 확인된 완료 내용

- `tests/**`, `.github/workflows/**`, `scripts/testing/**`의 디렉터리 구조와 보고서 보관 경로는 생성되어 있다.
- `data/tools/tests/**`에는 데이터·계약 해시·대표 E2E·재현성 검사가 있으며, 오류가 있을 때 명령이 종료 코드 1을 반환한다.
- 계약 불일치와 담당자별 조치 요청은 `docs/testing/week3-contract-alignment-review.md`와 `docs/handoffs/week3-contract-alignment-action-requests.md`에 정리되어 있다.

### 완료되지 않은 항목

- `tests/**`는 `.gitkeep` 외 실제 테스트·Fixture·`conftest.py`·`README.md`가 없다.
- `.github/workflows/data-ci.yml`, `contracts-ci.yml`이 없고 Workflow 디렉터리도 `.gitkeep`뿐이다.
- `scripts/testing/run_contract_tests.py`, `collect_test_reports.py`가 없다.
- `docs/testing/week3-p0-acceptance-criteria.md`, `test-case-matrix.md`가 없다.
- 정상·오류·권한·근거 없음·모델 불일치·400~5xx·Timeout·파싱 실패 Fixture가 공통 `tests/fixtures/**`에 준비되어 있지 않다.
- 현재 Data 단위 검증도 3건 실패하므로 녹색 기준선이 아니다.
- `tests/reports/**`는 비어 있고 승인 요약 위치인 `docs/testing/results/**`는 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. `docs/testing/week3-p0-acceptance-criteria.md`에 기능·AI·RAG·안전·State Machine·권한·멱등성·Evidence 비노출 기준을 작성한다.
2. `docs/testing/test-case-matrix.md`에 Case ID, 요구사항, 역할, Fixture, 단계, 기대 HTTP/업무 코드, 상태·allowed_actions, Evidence·안전, 자동/수동, 담당자, 증빙 경로를 기록한다.
3. `tests/fixtures/api|ai|errors|mocks/**`에 최소 정상·오류·권한·근거 없음·모델 불일치 Fixture를 작성하고 실제 계약 파일을 읽는 테스트를 추가한다.
4. `scripts/testing/run_contract_tests.py`와 `collect_test_reports.py`를 구현해 미구현 Case를 `blocked`/`not_implemented`/`fixture_ready`로 보존한다.
5. `.github/workflows/data-ci.yml`을 먼저 작성해 로컬과 같은 Data 명령을 실행하고, 정상 데이터 성공·의도적 오류 실패를 검증한다. 계약 명령이 확정되면 `contracts-ci.yml`을 추가한다.
6. `tests/README.md`, `tests/config/local.yaml`, `docs/testing/results/README.md`에 원시 결과와 사람 승인 요약의 저장 규칙을 작성한다.

### 코드 외 별도 확인

- 각 서비스 담당자가 제출할 실행 명령·정상/오류 결과·Fixture·제한 사항·PR/Commit 형식을 수락했는지 확인해야 한다.
- GitHub Actions가 실제 원격 저장소에서 실행되는지, Artifact와 Log에 원본·비밀값이 노출되지 않는지 확인해야 한다.
- 아직 구현되지 않은 Backend/API Case의 `blocked` 판정을 해당 담당자와 합의해야 한다.

## 3.6 7월 29일 공식 산출물 작성·공동 검수·인계 — 0%

### 확인된 완료 내용

- 전처리 결과서에 사용할 Inventory·Manifest·품질 집계와 DB 설계에 참고할 `docs/database/**` 자료는 존재한다.
- 데이터 변경 이력과 계약 불일치 검토 문서는 일부 작성되어 있다.

### 완료되지 않은 항목

- `docs/submission/`이 비어 있다.
- `[데이터 전처리] 데이터 전처리 결과서.docx`와 `[데이터 수집 및 저장] 데이터베이스_저장소 설계 문서.docx` 템플릿 또는 완료본이 저장소에 없다.
- `data-preprocessing-result.md`, `database-storage-design.md`, 실제 건수·검색 평가 결과표, 산출물 검수 기록, 다음 주 인계·미해결 Issue 목록이 없다.
- RAG 실제 검색 결과와 DB 실제 Seed 검증이 없으므로 공식 문서의 핵심 표를 확정할 수 없다.
- 팀 검토본 공유와 윤승혁 검수 완료 증빙을 코드에서 확인할 수 없다.

### 완료를 위해 작성·수정할 문서 또는 코드

1. 제공 템플릿을 확보한 뒤 `docs/submission/data-preprocessing-result.md`를 기준 원본으로 작성한다. 실제 44쪽·FAQ 119건·후보 20건·RAG 7건·source scenario 24건·active projection 22건과 제외 이유를 구분한다.
2. 최지용과 공동으로 `docs/submission/database-storage-design.md`를 작성해 관계형 DB·Vector Store·파일 저장소 책임, 연결 키, Seed/Fixture, 품질, 보안, 백업·복구를 실제 구조와 맞춘다.
3. QA가 다시 PASS한 리포트, Case별 RAG 결과, DB Seed 검증 결과의 링크를 두 문서에 연결한다.
4. 템플릿 예시 수치·다른 기수·다른 팀·예시 테이블을 제거하고 실제 프로젝트명·기수·팀원·제출일·GitHub 경로를 넣는다.
5. `docs/submission/CHANGELOG.md` 또는 검수 기록에 PM 의견, 수정 내역, 미해결 항목과 담당자·기한을 기록한 뒤 DOCX 완료본을 생성한다.

### 코드 외 별도 확인

- 제공 템플릿의 최신본과 제출 형식, 실제 제출 경로를 확인해야 한다.
- 작성 팀원·기수·팀명·GitHub 경로·제출일을 PM이 확인해야 한다.
- 문서 내 수치와 실제 실행 결과를 이동윤·최지용·윤승혁이 공동 검수하고 검토본 전달 여부를 확인해야 한다.

## 4. 코드만으로 완료 여부를 판정할 수 없는 사항

| 확인 대상 | 필요한 별도 증빙 또는 확인자 |
| --- | --- |
| 원본 PDF·FAQ의 공식성, 표지, REV, 페이지, 현재 해시 | 외부 백업 원본, 해시 재실행 로그, 김은진 육안 검수 |
| 공식 URL 접근 실패의 수용 여부 | 김은진 기록과 윤승혁 승인 |
| 실제 Vector Store 인덱싱·Top-k 검색·필터 적용 | 이동윤의 실행 로그, Index Manifest, Case별 결과 |
| 최신 Migration에 대한 실제 Seed 적재·재적재 | 최지용의 DB 실행 로그, 전후 건수, 중복·FK 결과 |
| Web·Mobile이 공통 Fixture를 실제 소비하는지 | 한예나·양정현의 화면/테스트 실행 증빙 |
| 실제 개인정보·운영 비밀값 미포함 | 샘플 육안 검수와 CI Secret/PII 점검 결과 |
| PR·공유 산출물·팀 검토·PM 승인 | PR/Commit URL, 리뷰 코멘트, 승인 기록 |
| 다른 팀원의 README 재현 성공 | 별도 환경에서 실행한 명령·환경·Commit·결과 |
| 4장 추가 업무가 필수 업무 이후에만 시작됐는지 | Issue/PR 시간 순서와 PM 확인 |

## 5. 최종 체크리스트에서 완료로 체크해도 되는 항목

현재 저장소와 재실행 결과만으로 안전하게 완료 체크할 수 있는 항목은 아래 4개이다.

- [x] **MVP·확장·제외 모델의 범위와 모델 계보가 확정되어 있다.**
  - 근거: `source_inventory.csv`, `model_document_lineage.csv`, RAG handoff 제외 정책
- [x] **원본 PDF·FAQ 원문이 Git 정책에 맞게 관리된다.**
  - 근거: Git 추적 대상에 원본이 없고 `.gitignore`·정책 파일만 존재
- [x] **역할별 사용자·제품·구독·케어·문의 Seed Fixture가 준비되어 있다.**
  - 단, 실제 DB 적재 완료를 뜻하지는 않으며 2개 문의 시나리오는 projection 차단 상태임
- [x] **Data 검증 명령 또는 최소 CI 골격이 실행 가능하다.**
  - 현재 CI Workflow는 없지만 로컬 검증 명령은 실행되고 오류 시 종료 코드 1을 반환함

### 아직 완료로 체크하면 안 되는 5.1 항목

- [ ] 공식 원본의 버전·페이지 수·SHA-256 해시가 검증되어 있다.
  - 과거 Inventory 기록은 있으나 현재 환경에서 원본 재검증이 불가능함
- [ ] 전처리 JSON·JSONL·CSV가 Schema 검증을 통과한다.
  - 개별 Schema 검사는 대부분 통과하지만 전체 현재 QA가 계약 해시 오류로 실패함
- [ ] 문서→페이지→청크→근거 참조 무결성이 확인되었다.
  - 대표 불변식은 통과했으나 현재 전체 QA가 실패함
- [ ] 중복·결측·인코딩·페이지·모델 범위 품질 결과가 실제 건수로 기록되었다.
  - 저장 리포트와 현재 재실행 결과가 불일치함
- [ ] 대표 저출수 검색 평가 Case와 공식 38쪽 정답이 준비되어 있다.
  - Expected는 있으나 실제 Case별 Top-k 검색 결과와 38쪽 연결 증빙이 없음
- [ ] JAC104 S세대·IAC425·IAC506·미검증 FAQ 혼입 차단 Case가 있다.
  - 정책은 있으나 전용 부정 평가 세트와 실행 결과가 없음
- [ ] 합성 문의 24건과 상태·안전·근거·인계 기대 결과가 연결되어 있다.
  - 원본 24건 중 2건이 차단되어 Expected는 22건임
- [ ] Synthetic Fixture의 ID·FK·Schema·개인정보 검증이 완료되었다.
  - 활성 22건 검사는 강하지만 현재 전체 QA 실패와 차단 2건이 남음
- [ ] P0 테스트 기준과 요구사항–테스트 Case 매트릭스가 작성되어 있다.
  - 두 문서 모두 없음
- [ ] 데이터 전처리 결과서가 실제 프로젝트 데이터와 수치로 작성되어 있다.
  - 제출 문서 없음
- [ ] 데이터베이스·저장소 설계 문서의 데이터·Seed·품질·보안 항목이 작성되어 있다.
  - 참고 DB 문서는 있으나 요구된 공식 제출 문서 없음
- [ ] 7월 29일까지 검토 가능한 PR 또는 공유 산출물이 있다.
  - 저장소만으로 PR·공유·검토 여부를 확인할 수 없음

### 5.2 항목 판정

현재 저장소와 실행 증거만으로 5.2의 어떤 항목도 완료로 확정 체크하지 않는다. 특히 실제 RAG 결과 반영, 최신 Migration Seed 검증, Web·Mobile 공통 Fixture 소비, 계약 변경 재반영, Issue 분리, 타 팀원 재현, 원시/승인 결과 분리, 공식 템플릿 정리, PM 변경 기록은 각각 구현 또는 외부 협업 증빙이 더 필요하다.

## 6. 우선 처리 순서

1. 확정 State Machine 계약의 의미를 확인하고 Data 계약 해시 4건과 Crosswalk 결정 상태를 갱신한다.
2. Data 단위 테스트 51건과 `qa --verify-rebuild`를 모두 PASS로 만든 뒤 리포트·Manifest를 재생성한다.
3. 차단된 합성 시나리오 2건의 정책을 확정하고 실제 DB Seed 적재·반복 실행을 검증한다.
4. 단일 RAG 평가 세트, 부정 Case, Case별 Top-k·Index 결과를 작성한다.
5. P0 인수 기준·Case 매트릭스·공통 Fixture·Data/Contract 실행 스크립트와 CI를 작성한다.
6. 재검증 결과를 근거로 공식 전처리 결과서와 DB·저장소 설계 문서를 작성하고 팀 검수를 받는다.

