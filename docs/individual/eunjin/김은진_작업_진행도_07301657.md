# 김은진 3주차 작업 진행도

> 최초 작성: 2026-07-29 15:52 KST
>
> 최종 갱신: 2026-07-30 16:57 KST
>
> 작업 기준 Commit: `2b998ef` (`main`)
> RAG 실행 증거 병합 Commit: `4adc84d`
> 참고: `docs/weekly-task/김은진_3주차_업무_지침서.md`, `docs/individual/eunjin/20260730_데이터_Owner_Review.md`, `docs/handoffs/README.md`

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 현재 근거 |
|---|---:|---|---|
| 3.1 공식 원본·Inventory·모델 계보 | 90% | 대부분 완료 | MVP·확장·제외 모델, 문서 계보, 원본 Git 비추적 정책과 해시 검증 도구가 존재함 |
| 3.2 전처리·Schema·품질 리포트 | 95% | 대부분 완료 | Owner Review에서 Source Hash, Data 61건, QA 2회, Manifest 154개와 drift 0을 검증함 |
| 3.3 RAG 정답 세트·검색 평가 | 90% | 대부분 완료 | JAC104D 7건 적재와 12/12 pgvector 평가 증거를 확보했고 v2 평가 포맷·누수 순위 개선이 남음 |
| 3.4 합성 데이터·DB Seed Fixture | 90% | 대부분 완료 | 원본 24건, 활성 22건, 합성 Handoff 367 Source의 격리 PostgreSQL Import·Replay 증거가 있음 |
| 3.5 P0 QA·계약 Fixture·Data CI | 95% | 대부분 완료 | P0 인수 기준·Case Matrix·Fixture·61개 테스트·QA E2E 17/17·결정성 검증이 존재함 |
| 3.6 공식 산출물·공동 검수·인계 | 90% | 대부분 완료 | 제출 문서의 DB·RAG 판정을 최신화했고 팀 공동 검수·최종 승인만 남음 |

종합 완료도는 **약 92%**다. Data 원본·전처리·합성 Fixture·QA와
JAC104D MVP RAG 실행 증거는 완료에 가깝다. 남은 핵심은 지침서 3.3
v2 평가 포맷, 누수 검색 순위 개선, 검증 상태 공통 코드, 차단 시나리오
2건의 PM 결정과 팀 최종 승인이다.

## 2. 현재 검증 결과

- Owner Review 공식 기록: Data `61/61 PASS`, QA 2회 `PASS`, E2E `17/17`, Manifest drift 0.
- 이번 변경 재검증: Python 3.13.13에서 Data 61/61, Source Hash
  `PASS`, QA 2회 연속 `PASS`, 오류·경고 0건, E2E 17/17,
  생성물 drift 0이다.
- 이전 번들 환경 점검의 59/61은 Git dubious ownership과 한글 Git
  오류 출력의 UTF-8 해석 문제였으며, 현재 공식 재검증 수치로
  재사용하지 않는다.
- AI 실행 증거: PostgreSQL 16.14·pgvector 0.8.6, 승인 청크 7건,
  검색 평가 12/12 PASS, Recall@5 `1.0`, MRR `0.8857142857142858`.
- Crosswalk·Schema·Validator·Fixture Mapping에는 Owner Review 과정의 의미 변경이 없었다.
- `DB_FULL_VERIFIED`는 합성 Handoff 367행의 빈 격리 PostgreSQL Import 범위이며 T-005 32개 전체 구현을 뜻하지 않는다.
- RAG 실행 증거 해시는 CI 재현이 가능하도록 정규화됐고 관련 계약 테스트가 보강됐다. 다만 요청된 13번째 동일 모델 정책 차단 Case는 아직 실행 결과에 없다.

## 3. 필수 업무별 평가

### 3.1 공식 원본·출처 Inventory·모델 계보 — 90%

완료된 내용:

- WPUJAC104DWH와 확장·제외 모델의 경계, D세대 문서 계보와 공식 근거 정책이 데이터 계약에 반영됐다.
- 원본 PDF·FAQ는 Git에 직접 포함하지 않고 Inventory·Hash로 추적하는 구조가 있다.
- Source Hash의 LF 정규화, BOM 제거, 잘못된 UTF-8 차단이 검증됐다.

수행 근거: [Data README](<../../../data/README.md>), [Source Inventory](<../../../data/processed/metadata/source_inventory.csv>), [모델–문서 계보](<../../../data/processed/metadata/model_document_lineage.csv>)

남은 내용과 완료 방법:

- 저장소 밖 원본 보존본의 URL·버전·페이지 수를 최종 제출 직전에 다시 확인하고 Inventory 검증 결과를 제출 문서에 기록한다.
- 외부 원본의 실제 보존 위치·접근 권한·라이선스는 코드만으로 확인할 수 없으므로 담당자가 별도 확인한다.

### 3.2 전처리 파이프라인·Schema·품질 리포트 — 95%

완료된 내용:

- Schema·ID/FK·업무 규칙·무결성·재현성·Manifest 검증 파이프라인이 있다.
- Owner Review 기준 Data 61건, QA 오류·경고 0, 2회 재생성 diff 0이다.
- README 원본 템플릿과 최종 Manifest를 공식 `finalize` 절차로 재생성했다.

수행 근거: [Data Pipeline](<../../../data/tools/pipeline.py>), [최신 QA 요약](<../../../data/processed/validation/latest_qa_summary.json>), [최종 Dataset Manifest](<../../../data/processed/metadata/final_dataset_manifest.json>), [Data Owner Review](<20260730_데이터_Owner_Review.md>)

남은 내용과 완료 방법:

- Git 소유권·인코딩에 의존하는 두 테스트가 일반 팀원 환경에서도
  안정적으로 실패 원인을 표시하도록 subprocess 오류 디코딩 보강을
  별도 후속으로 유지한다.
- `docs/submission/data-preprocessing-result.md`의 과거 `55/55`와
  DB·RAG 대기 문구를 61/61과 최신 실행 범위로 갱신했다.

### 3.3 RAG 정답 세트·검색 평가·오염 차단 — 90%

완료된 내용:

- 대표 저출수의 공식 설명서 38쪽 정답, 양성 7건과 범위 밖 부정 Case 5건이 준비됐다.
- S세대·IAC425·IAC506·미검증 FAQ의 혼입 차단 기준이 계약과 평가 데이터에 있다.
- 이동윤이 승인 청크 7건을 실제 PostgreSQL/pgvector에 적재하고
  12개 Case를 실행해 12/12 PASS와 금지 hit 0을 기록했다.
- 실행 모델 revision, Index version, Chunk Set SHA와 Query별 Top-5가
  Result·Index Manifest에 기록됐다.

수행 근거: [RAG 평가 Case](<../../../data/config/rag/jac104_retrieval_cases.json>), [RAG 평가 기준](<../../../docs/testing/week3-rag-evaluation-criteria.md>), [pgvector 실행 결과](<../../../ai/evaluation/reports/pgvector_verification.json>)

남은 내용과 완료 방법:

- 지침서 3.3의 동일 모델 정책 차단 Case를 13번째 평가 Case로
  추가하고 이동윤이 재실행한다.
- Case별 적용 Filter·Page Hit·`PASS/FAIL/MANUAL_REVIEW`·실패 원인
  구조를 v2 계약으로 합의한다.
- 누수 기대 청크 5위는 원문을 변경하지 않고 주제 Filter·재순위화·
  검색 보조 메타데이터 순서로 개선한다.
- `TEXT_AND_VISUAL_VERIFIED → official_verified` 공통 코드는
  윤승혁과 확정한다.

### 3.4 합성 업무 데이터·DB Seed Fixture·기대 결과 — 90%

완료된 내용:

- 합성 원본 24건을 보존하고 계약에 맞는 22건을 활성 projection과 DB Handoff 후보로 제공한다.
- 12종 Fixture, 367 Source, 반복 Import 시 생성·수정 0의 격리 PostgreSQL 증거가 있다.
- 정수 PK를 직접 주입하지 않고 `public_id`·업무키로 실제 FK를 찾는 정책이 검증됐다.

수행 근거: [Synthetic README](<../../../data/synthetic/README.md>), [문의 Fixture](<../../../data/synthetic/fixtures/inquiries.json>), [Backend Import Crosswalk](<../../../data/config/handoff/backend_import_crosswalk.json>), [PostgreSQL 적재 검증](<../../../docs/individual/jiyong/manuals/20260729_postgresql_synthetic_handoff_runtime_verification.md>)

남은 내용과 완료 방법:

- `SYN-JAC104-012`, `016`은 terminal 문의 동일 ID 재개 대신 새 관련 문의를 만드는 정책을 PM이 승인한 뒤 재설계한다.
- 운영 DB 적재와 합성 격리 DB 적재를 분리해 문서화한다.
- T-005 잔여 20개 테이블이 구현되면 Fixture와 DB 설계 문서를 다시 정합화한다.

### 3.5 P0 QA·계약 Fixture·Data CI — 95%

완료된 내용:

- P0 인수 기준, Case Matrix, 대표 E2E, 상태·권한·멱등성·Evidence 비노출 기준이 있다.
- Owner Review에서 Data 61건과 QA E2E 17/17을 재현했다.
- Hash·Manifest·Crosswalk의 결정성과 경로 안전성을 검증한다.

수행 근거: [P0 인수 기준](<../../../docs/testing/week3-p0-acceptance-criteria.md>), [테스트 Case Matrix](<../../../docs/testing/test-case-matrix.md>), [대표 E2E 테스트](<../../../data/tools/tests/test_representative_e2e.py>), [QA 보고서 테스트](<../../../data/tools/tests/test_qa_reports.py>)

남은 내용과 완료 방법:

- 최상위 `tests/contract`, `tests/integration`, `tests/e2e`에는 실제 공통 테스트가 부족하므로 Backend·AI·Web·Mobile 동일 Commit E2E를 추가한다.
- CI 실행 로그와 실패 시 담당자·기한을 팀 인계 허브나 Issue에 연결한다.

### 3.6 공식 산출물 작성·공동 검수·인계 — 90%

완료된 내용:

- `docs/submission/data-preprocessing-result.md`, `database-storage-design.md`가 존재한다.
- Data Owner Review와 팀 공용 인계 허브에 검증 범위·한계·후속 담당이 기록됐다.
- 전처리 결과서와 DB·저장소 설계서의 과거 DB·RAG pending 판정을
  격리 PostgreSQL 367 Source와 pgvector 7건·12/12 증거로 갱신했다.

수행 근거: [데이터 전처리 결과서](<../../../docs/submission/data-preprocessing-result.md>), [DB·저장소 설계서](<../../../docs/submission/database-storage-design.md>), [Data Owner Review](<20260730_데이터_Owner_Review.md>), [팀 인계 허브](<../../../docs/handoffs/README.md>)

남은 내용과 완료 방법:

- 이번 변경의 Data 테스트·QA 재실행 수치를 제출 문서에 확정한다.
- 최지용·이동윤·윤승혁의 공동 검수와 제출 승인 기록을 남긴다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 |
|---|---|
| 외부 원본 | 보존 위치, 접근 권한, 공식 URL, 라이선스 |
| PostgreSQL | 격리 DB 이름·버전·Extension, 운영 DB와의 분리 |
| RAG | 실제 모델 Revision, Index 생성 시각, Query별 Top-5 원시 결과 |
| 산출물 | PM·Backend·AI 공동 검수와 최종 제출 승인 |
| Git/CI | 일반 팀원 계정에서 61개 테스트와 QA 재현 여부 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 7월 29일 필수 완료 기준

- [x] MVP·확장·제외 모델의 범위와 모델 계보가 확정되어 있다.
- [x] 공식 원본의 버전·페이지 수·SHA-256 해시가 검증되어 있다.
- [x] 원본 PDF·FAQ 원문이 Git 정책에 맞게 관리된다.
- [x] 전처리 JSON·JSONL·CSV가 Schema 검증을 통과한다.
- [x] 문서→페이지→청크→근거 참조 무결성이 확인되었다.
- [x] 중복·결측·인코딩·페이지·모델 범위 품질 결과가 실제 건수로 기록되었다.
- [x] 대표 저출수 검색 평가 Case와 공식 38쪽 정답이 준비되어 있다.
- [x] JAC104 S세대·IAC425·IAC506·미검증 FAQ 혼입 차단 Case가 있다.
- [x] 역할별 사용자·제품·구독·케어·문의 Seed Fixture가 준비되어 있다.
- [x] 합성 문의 24건과 상태·안전·근거·인계 기대 결과가 연결되어 있다.
- [x] Synthetic Fixture의 ID·FK·Schema·개인정보 검증이 완료되었다.
- [x] P0 테스트 기준과 요구사항–테스트 Case 매트릭스가 작성되어 있다.
- [x] Data 검증 명령 또는 최소 CI 골격이 실행 가능하다.
- [x] 데이터 전처리 결과서가 실제 프로젝트 데이터와 수치로 작성되어 있다.
- [x] 데이터베이스·저장소 설계 문서의 데이터·Seed·품질·보안 항목이 작성되어 있다.
- [x] 7월 29일까지 검토 가능한 PR 또는 공유 산출물이 있다.

### 5.2 7월 30일~31일 최종 정리 기준

- [x] 이동윤의 실제 인덱싱·검색 결과가 평가표와 전처리 결과서에 반영되었다.
- [ ] 최지용의 최신 Migration·ERD에 맞게 Seed Fixture와 DB 설계 문서가 수정되었다.
- [ ] Web·Mobile Mock이 공통 Fixture·표준 코드·비노출 기준과 일치한다.
- [x] 데이터·AI·API·State Machine 계약 변경이 Expected 파일과 테스트 기준에 반영되었다.
- [ ] 검증 실패·차단 결함·후속 개선 사항이 Issue로 분리되어 있다.
- [x] 다른 팀원이 README와 실행 명령으로 데이터 검증을 재현할 수 있다.
- [x] 테스트 원시 결과와 사람이 승인한 요약 결과가 구분되어 있다.
- [x] 공식 산출물의 템플릿 예시·더미 수치·다른 팀 정보가 제거되었다.
- [x] PM 검토 의견과 수정 내역이 변경 기록에 남아 있다.
- [ ] 필수 업무와 리뷰 대응이 끝난 뒤에만 4장의 추가 업무를 시작했다.

## 6. 다음 작업

1. 현재 12개 RAG 실행 증거의 Data 계약·Manifest·QA를 확정한다.
2. 지침서 3.3 v2 Case·결과 포맷을 이동윤과 합의하고 13개를 재실행한다.
3. 검증 상태 공통 코드와 T-012 조건부 완료 후보를 PM에게 인계한다.
4. PM의 012·016 신규 관련 문의 정책을 별도 후속으로 반영한다.
5. T-005 다음 Wave와 동일 Commit 통합 QA에 맞춰 Fixture·문서를 재검증한다.
