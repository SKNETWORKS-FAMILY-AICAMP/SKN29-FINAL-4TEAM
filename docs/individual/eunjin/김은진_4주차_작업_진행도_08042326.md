# 김은진 4주차 작업 진행도

> 작성 일시: **2026-08-04 23:26 KST**  
> 점검 기준 Commit: `d9052621e2f07a8447f7c1f998875104c031a3c0` (`main`)  
> 참고 지침서: [김은진 4주차 업무 지침서](../../weekly-task/김은진_4주차_업무_지침서.md)  
> 관할 기준: [팀원별 관할 영역 v2](../../planning/md/팀원별%20관할%20영역%20v2.md)

## 1. 종합 판정

| 4주차 필수 업무 | 완료도 | 판정 | 핵심 근거와 남은 Gate |
| --- | ---: | --- | --- |
| 3.1 병합 회귀 수정·Data QA 기준선 복구 | 85% | 대부분 완료 | 현재 HEAD에서 Data 67/67, QA·Finalize·Drift 검증이 통과했다. 회귀 원인·해결 기록과 CI Trigger 보완 문서는 남았다. |
| 3.2 Vector DB·RAG 평가 기준선 재검증 | 65% | 부분 완료 | 격리 pgvector 12/12, Recall@5 1.0, MRR 0.8857, 금지 Hit 0 증거가 있다. 팀 DB 재실행과 13번째 후검증 차단 Case가 미완료다. |
| 3.3 4주차 공식 산출물 작성·검수 | 60% | 부분 완료 | 통합 적용 결과서와 발표 기술자료는 있으나 지침서가 요구한 제출 패키지, 재현 Artifact, PM·Data 승인 기록이 없다. |
| 3.4 4주차 기능·계약·데이터 QA | 55% | 부분 완료 | T-017 QA 승인과 영역별 Backend·Web·Mobile 증거가 있다. 김은진 주관 통합 계획·상태 Matrix·결과서와 최상위 공통 테스트는 없다. |
| 3.5 중간 발표자료 제작·리허설·발표 | 20% | 착수 | AI 기술자료와 Web·Mobile 대체 자료는 준비됐지만, 발표 원본·PDF·대본·Q&A·증거 Index·PM 피드백·전체 리허설 기록이 저장소에 없다. |
| 3.6 발표 후 회귀 검증·5주차 인계 | 0% | 미착수 | 발표 이후 수행 업무이므로 8월 4일 현재 미착수는 일정상 정상이다. |

전체 단순 평균 완료도는 **약 48%**다. 발표 후 업무인 3.6을 제외한 현재 진행 대상 3.1~3.5의 평균은 **약 57%**다. Data 기준선 자체는 발표에 사용할 수준으로 복구됐지만, **발표 패키지와 팀 DB RAG·통합 QA의 승인 증거가 부족하므로 4주차 전체 완료로 판정할 수 없다.**

## 2. 이번 점검에서 직접 확인한 결과

### 2.1 Data 기준선

현재 HEAD에서 다음을 직접 실행했다.

```powershell
python scripts/data/refresh_source_hashes.py --check
python -B -m unittest discover -s data/tools/tests -v
python -B data/tools/pipeline.py qa --verify-rebuild
python -B data/tools/pipeline.py finalize
git status --short
```

| 검사 | 결과 |
| --- | --- |
| Python | `3.13.12` |
| Source Hash | `PASS`, 변경 0 |
| Data 단위 테스트 | `67/67 PASS`, 25.557초 |
| QA | `PASS`, 48파일·740레코드, 오류 0·경고 0 |
| 대표 Data E2E | `17/17 PASS` |
| 재생성·Canonical Drift | 변경 0, `PASS` |
| Finalize | `PASS`, Manifest 154개 확인 |
| 실행 후 작업 트리 | 변경 없음 |

최초 테스트 실행은 현재 샌드박스 계정의 Git `dubious ownership`과 한글 Git 오류 출력 디코딩 때문에 2건이 오류로 끝났다. 저장소 설정을 변경하지 않고 실행 프로세스에만 `safe.directory`를 전달해 다시 실행한 결과 67건 모두 통과했다. 따라서 최초 65/67은 코드 결함 수치로 사용하지 않는다.

수행 근거: [Data Pipeline](../../../data/tools/pipeline.py), [Data 단위 테스트](../../../data/tools/tests), [최신 QA 요약](../../../data/processed/validation/latest_qa_summary.json), [Data 상태 QA](../../../data/processed/validation/DATA_STATUS_QA.md)

### 2.2 다른 영역의 검증 한계

- 현재 환경에는 `backend/.venv`와 `ai/.venv`가 없고 시스템 Python에는 `pytest`가 없어 Backend·AI 테스트를 이번 점검에서 재실행하지 못했다.
- Web 테스트는 현재 환경의 Node `v20.11.0`에서 Vite/Rolldown이 요구하는 `node:util.styleText`가 없어 시작 단계에서 실패했다. Web 담당 문서에는 Node `v26.4.0`에서 109/109·Lint·Build 성공이 기록되어 있으므로 코드 실패로 단정하지 않되, 발표 환경의 Node 버전을 별도로 확인해야 한다.
- Mobile은 담당자 문서에 실제 문의 생성·증상 제출과 APK 빌드 성공이 기록되어 있으나 이번 환경에서 Gradle·실기기 검증을 다시 실행하지 않았다.

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 병합 회귀 수정·Data QA 기준선 복구 — 85%

완료된 내용:

- 누락되었던 Backend Runtime 문서가 현재 경로에 존재하고 Crosswalk가 해당 문서와 Backend Model·Importer Hash를 검증한다.
- Crosswalk는 `DB_FULL_VERIFIED`이며 Data 계약 테스트가 모든 참조 경로·Hash를 통과했다.
- 현재 HEAD에서 Data 67/67, `qa --verify-rebuild`, `finalize`, Drift 0을 직접 확인했다.
- QA 결과는 오류·경고 0, 48파일·740레코드, 대표 E2E 17/17이다.
- Data CI는 전체 Git 이력 Checkout, State Machine 검증, 단위 테스트, 결정적 QA, Drift 거부 단계를 갖는다.

수행 근거:

- [Backend Import Crosswalk](../../../data/config/handoff/backend_import_crosswalk.json)
- [Backend Runtime 검증 문서](../jiyong/manuals/20260729_postgresql_synthetic_handoff_runtime_verification.md)
- [Data CI](../../../.github/workflows/data-ci.yml)
- [최신 QA 요약](../../../data/processed/validation/latest_qa_summary.json)
- [QA 리포트 테스트](../../../data/tools/tests/test_qa_reports.py)

완료되지 않은 내용:

- `backend_runtime_document_missing`이 발생한 원인, 복구 방식, 재발 방지 조치를 설명하는 별도 결과서나 Issue가 없다.
- 지침서 산출물인 `docs/testing/results/week4-data-qa-baseline.md`가 없다.
- Data CI Trigger는 `data/**`, State Machine, Data·Contract Script만 감지한다. Crosswalk가 직접 읽는 Backend Model·Runtime 문서와 `ai/evaluation/reports/**` 변경은 현재 Trigger에 포함되지 않는다.
- 저장된 QA의 고정 `generated_at`은 7월 29일이고 `source_commit`은 최신 전체 HEAD가 아니라 관련 계약 기준 Commit이다. 이번 8월 4일 재실행 정보는 별도 승인 결과서로 남아 있지 않다.

완료 방법:

1. `docs/testing/results/week4-data-qa-baseline.md`를 작성해 최초 회귀 증상, 누락 원인, 복구 Commit, 현재 실행 환경·명령·결과·HEAD를 기록한다.
2. `.github/workflows/data-ci.yml`의 `paths`에 Crosswalk가 실제 읽는 Backend 경로, Runtime 증빙 경로, AI 평가 결과 경로를 최소 범위로 추가한다.
3. CI에서 Data 테스트와 QA를 실행하고 원격 Run URL·Commit·결과를 기준선 문서에 연결한다.

코드 외 별도 확인:

- GitHub Actions가 실제 PR에서 Backend Runtime 문서 또는 AI 평가 결과의 파괴적 변경을 감지하는지 확인해야 한다.
- 회귀 원인·복구 방식이 김은진·최지용 사이에 합의됐는지 확인해야 한다.

### 3.2 Vector DB·RAG 평가 기준선 재검증 — 65%

완료된 내용:

- `BAAI/bge-m3`, 고정 Revision, 1024차원, L2 정규화, Cosine Exact Search, Top-k 5 설정이 문서와 Manifest에 있다.
- 승인 청크 7건과 평가 Case 12건의 Dataset·Chunk·Index Hash가 기록되어 있다.
- 격리 PostgreSQL 16.14·pgvector 0.8.6 실행에서 12/12 PASS, 양성 Recall@5 `1.0`, MRR `0.8857`, 금지 Hit 0이 기록되어 있다.
- 양성 7건과 S세대·IAC425·IAC506·미검증 FAQ·미확인 제품 차단 5건이 Data 계약 테스트를 통과한다.
- 실제 pgvector 이력, 팀 DB 대기, Vector Store 미설정 오프라인 결과가 서로 구분되어 있다.

수행 근거:

- [RAG 평가 Case](../../../data/config/rag/jac104_retrieval_cases.json)
- [pgvector 검증 결과](../../../ai/evaluation/reports/pgvector_verification.json)
- [공식 MVP 후보 기준선](../../../ai/evaluation/reports/official_mvp_baseline_20260803.json)
- [Index Manifest](../../../ai/configs/index_manifest.json)
- [AI·RAG 중간발표 기술자료](../../submission/20260803_AI_RAG_중간발표_기술자료.md)

완료되지 않은 내용:

- 결과는 팀 공용 DB가 아니라 격리 DB의 과거 이력이다. 현재 팀 DB Migration·승인 청크 UPSERT·검색 재실행은 완료되지 않았다.
- 동일 제품·D세대이지만 문서 정책상 사용 불가한 후보를 검색 후 제거하는 13번째 Case가 없다.
- 13번째 Case의 원천·Fixture·차단 사유·기대값에 대한 김은진의 Data Owner 회신이 없다.
- 공식 후보 기준선은 `CANDIDATE_REQUIRES_TEAM_DB_RERUN_AND_COMMIT`이며 내부 `source_commit`과 현재 HEAD가 다르다.
- 지침서 산출물 `docs/testing/results/week4-vector-retrieval-evaluation.md`가 없다.

완료 방법:

1. [13번째 차단·팀 DB 협업 요청서](../dongyoon/20260804_이동윤_김은진_13번째정책차단_팀DB_RAG검증_협업요청서_v0.1.md)의 결정표에 `APPROVE`, `CHANGE_REQUEST`, `BLOCKED` 중 하나로 회신한다.
2. 승인할 수 있는 정확 제품·세대의 부적격 자료가 없다면 데이터를 조작하지 말고 `BLOCKED`와 그 이유를 기록한다.
3. 승인 시 `data/config/rag/jac104_retrieval_cases.json`과 Schema·Hash를 갱신하고, 이동윤이 Runner와 결과 JSON을 13건 기준으로 수정한다.
4. 최지용의 정식 pgvector Migration이 적용된 팀 DB에서 승인 청크 UPSERT 2회, 13개 검색, 금지 Hit, Fixture Rollback을 공동 실행한다.
5. 결과를 `docs/testing/results/week4-vector-retrieval-evaluation.md`에 환경·Commit·Dataset Hash·Case별 순위·실패 원인과 함께 기록한다.

코드 외 별도 확인:

- 팀 DB의 PostgreSQL·pgvector 버전, 최소 권한 계정, 실제 Migration 적용 여부를 최지용과 확인해야 한다.
- 격리 DB 증거를 중간 발표에서 팀 DB 완료로 표현하지 않는지 김은진·윤승혁이 검수해야 한다.

### 3.3 4주차 공식 산출물 작성·검수 — 60%

완료된 내용:

- 사전학습 모델 적용, Vector DB 구성, 평가 결과, Graph DB 미적용 사유를 한 문서에 정리한 결과서가 있다.
- 직접 학습·파인튜닝을 수행하지 않았고 가중치를 Git에 넣지 않는다는 사실이 명확하다.
- 모델 ID·Revision·차원·Framework, Dataset·Chunk Hash, 실행 명령, 제한 사항이 문서화되어 있다.
- 제출 문서 CHANGELOG에 8월 3일 추가 내역이 기록되어 있다.

수행 근거:

- [AI 사전학습 모델·Vector DB 적용 결과서](../../submission/AI_사전학습모델_VectorDB_적용결과서.md)
- [AI·RAG 중간발표 기술자료](../../submission/20260803_AI_RAG_중간발표_기술자료.md)
- [제출 문서 변경 이력](../../submission/CHANGELOG.md)
- [AI Requirements Lock](../../../ai/requirements.lock)

완료되지 않은 내용:

- 지침서가 지정한 `docs/submission/week4/**` 제출 묶음과 `artifacts/week4/model-application/**` 재현 패키지가 없다.
- `model-manifest.json`, 설정 예시, 실행 예시, 체크섬 목록을 하나의 제출 패키지로 찾을 수 없다.
- 현재 결과서는 `ISOLATED_VERIFIED_TEAM_DB_PENDING` 상태이며 팀 DB 결과와 13번째 Case가 반영되지 않았다.
- 기술자료의 `data_qa_review`, `pm_scope_review`, `backend_integration_status`, `presentation_commit`이 모두 `TBD`다.
- 이동윤의 기술 작성은 확인되지만 김은진 Data QA 승인과 윤승혁의 논리·제출 범위 검수 기록이 없다.

완료 방법:

1. 기존 통합 결과서를 유지할 경우 `docs/submission/week4/README.md`에 교육과정의 세 가지 요구 산출물과 실제 파일의 대응표를 작성한다.
2. 모델 Manifest·Revision·환경·설정·실행 명령·결과 Hash를 `artifacts/week4/model-application/`에 재현 패키지로 정리한다. Weight와 Cache는 제외한다.
3. 팀 DB 재실행 후 결과서의 상태·수치·기준 Commit·Hash를 갱신한다.
4. 김은진 Data QA, 이동윤 기술, 윤승혁 PM 검수 결과를 문서 상단 또는 별도 승인 기록에 남긴다.

코드 외 별도 확인:

- 교육기관이 “학습 모델 파일”과 “GraphDB 구축”을 문자 그대로 필수 요구하는지 확인해야 한다.
- `bge-m3` 라이선스와 모델 Weight 재배포 가능 여부를 확인해야 한다.
- 공식 제출 위치·파일 형식·제출 승인자를 확인해야 한다.

### 3.4 4주차 기능·계약·데이터 QA — 55%

완료된 내용:

- 김은진 명의의 T-017 인증·RBAC 독립 QA 승인 기록이 있으며 Matrix 4건, Accounts 70건, Inquiry RBAC 24건과 PostgreSQL Migration PASS가 기록되어 있다.
- Backend에는 API·권한·DB·State Machine·G2 계약 테스트가 구현되어 있다.
- Web은 담당자 환경에서 109/109, Lint·Build 성공과 Mock·Backend 차단 상태를 구분해 기록했다.
- Mobile은 실제 인증·문의 생성·증상 제출, APK 빌드와 남은 409·Replay 검증을 구분해 기록했다.
- AI 기술자료는 계약·독립 Runtime 완료와 Backend 저장 E2E 대기를 구분한다.

수행 근거:

- [T-017 QA 승인 증거](../../handoffs/t017_completion_evidence_20260803.json)
- [Backend Runtime 상태](../../api/runtime_implementation_status.md)
- [Web 4주차 테스트 결과](../../../web/docs/week4-test-result.md)
- [Web 기능 상태표](../../../web/docs/presentation/week4-web-feature-status.md)
- [Mobile 4주차 검증 결과](../../../mobile/docs/week4-mobile-verification.md)
- [Mobile 구현 상태표](../../../mobile/docs/week4-presentation/2026-08-04_모바일_구현_상태표.md)

완료되지 않은 내용:

- 지침서가 요구한 `week4-feature-test-plan.md`, `week4-feature-test-result.md`, `week4-implementation-status.md`가 없다.
- 최상위 `tests/contract/**`, `tests/integration/**`, `tests/e2e/**`, `tests/smoke/**`는 대부분 `.gitkeep`뿐이며 팀 공통 실행 가능한 테스트가 없다.
- G2 기계 계약은 김은진 재검증 요청 상태이며 완료 회신이 없다.
- Backend 전체 최신 HEAD와 AI 95개 테스트를 이번 환경에서 재현하지 못했다.
- Web은 Node 20 환경에서 실행되지 않아 발표 장비의 Node 버전 호환 확인이 필요하다.
- 고객→Backend→AI→상담사→방문 전체 흐름은 아직 연결되지 않았고 Web 상담·방문은 Mock, Mobile AI 안내는 Fixture다.

완료 방법:

1. `docs/testing/plans/week4-feature-test-plan.md`에 발표 기능, 담당 Commit, 정상·오류·권한 Case와 실행자를 정리한다.
2. `docs/testing/matrices/week4-implementation-status.md`에 실제 환경 검증·로컬·Mock·골격·미구현의 다섯 상태를 영역별로 통합한다.
3. `docs/testing/results/week4-feature-test-result.md`에 각 담당자 원시 결과, 김은진의 독립 확인 범위, 재현하지 못한 환경을 분리한다.
4. 최상위 `tests/**` 또는 `scripts/testing/**`에 최소 계약·Smoke Runner를 작성해 같은 Commit의 Backend·AI·Web·Mobile 결과를 모은다.
5. G2 재검증 회신과 대표 시연 흐름의 연결·차단 지점을 결과서에 반영한다.

코드 외 별도 확인:

- Web Node 26 환경의 109/109·Build 결과를 발표 장비에서 다시 확인해야 한다.
- Mobile 실제 단말의 Replay·409·응답 본문과 발표 APK Hash를 확인해야 한다.
- Backend PostgreSQL 전체 결과와 AI 95개 결과를 의존성이 설치된 팀 환경에서 비작성자가 재현해야 한다.

### 3.5 8월 6일 중간 발표자료 제작·리허설·발표 — 20%

완료된 내용:

- AI·RAG 발표 기술자료에 시스템 역할, 공식 발표 후보 수치, 시나리오와 과장 금지 문구가 정리되어 있다.
- Web과 Mobile 담당자가 기능 상태표, 시연 절차, 캡처·Fallback 자료를 준비했다.
- 4주차 데일리 스크럼에 중간발표 준비 중이라는 상태가 기록되어 있다.

수행 근거:

- [AI·RAG 중간발표 기술자료](../../submission/20260803_AI_RAG_중간발표_기술자료.md)
- [Web 발표 자료 디렉토리](../../../web/docs/presentation)
- [Mobile 발표 자료 디렉토리](../../../mobile/docs/week4-presentation)
- [4주차 데일리 스크럼](../../daily-scrum/4주차_데일리스크럼.md)

완료되지 않은 내용:

- `docs/presentation/midterm/` 디렉토리와 중간 발표 원본·PDF가 없다.
- 발표 대본, Demo Script, Demo Checklist, Q&A, Evidence Index가 없다.
- 발표 기준 Commit과 기능·수치·화면의 연결표가 없다.
- 윤승혁의 1차·최종 피드백 기록이 없다.
- 전체 팀 리허설, 발표 시간, 시연 전환 순서, 실패 대체 자료 통합 확인이 없다.
- 발표는 아직 시행되지 않았다.

완료 방법:

1. `docs/presentation/midterm/`에 발표 원본·PDF·대본·Demo Script·Checklist·Q&A·Evidence Index를 작성한다.
2. Web·Mobile·AI 담당 자료를 하나의 상태 Matrix로 통합하고 실제·Mock·골격·미구현을 슬라이드마다 표시한다.
3. 모든 수치에 결과 파일·Commit·담당자를 연결하고 `presentation_commit`을 고정한다.
4. 윤승혁의 두 차례 피드백과 반영 결과를 기록한다.
5. 전체 팀 리허설을 수행하고 발표 장비에서 실제 시연·캡처·영상 대체 순서를 확인한다.

코드 외 별도 확인:

- 발표자료가 저장소 밖 Canva·Google Slides·PPT에서 작성 중인지 김은진에게 확인해야 한다.
- 윤승혁의 피드백과 팀 리허설이 구두로만 진행됐는지 확인해야 한다.
- 발표 시간·장비·네트워크·영상 재생·발표 권한을 확인해야 한다.

### 3.6 발표 후 회귀 검증·5주차 인계 — 0%

현재 상태:

- 8월 6일 발표 이후 수행할 업무로, 8월 4일 현재 미착수는 일정상 정상이다.
- `week4-final-qa-summary.md`와 `docs/handoff/week5/**` 또는 `docs/handoffs/week5/**`가 없다.

완료 방법:

1. 발표 이후 최신 `main`에서 3.1의 Data 검증을 다시 실행한다.
2. `docs/testing/results/week4-final-qa-summary.md`에 발표 기준선 이후 변경 영향과 회귀 결과를 기록한다.
3. 5주차 AI·Backend·Web·Mobile 인계 문서와 미해결 Issue 목록을 작성한다.
4. 발표 피드백을 즉시 수정·5주차 필수·후속·범위 제외로 분류한다.
5. 공식 산출물 최종 버전·승인 상태와 공개 제출 패키지 제외 파일 점검 결과를 기록한다.

코드 외 별도 확인:

- 발표 중 질문과 피드백, 답변 보류 항목을 발표자가 별도로 수집해야 한다.
- 내부 검사용 ZIP과 공개 제출 패키지의 포함 범위를 제출 담당자와 확인해야 한다.

## 4. 코드만으로 완료 여부를 확인할 수 없는 사항

| 확인 대상 | 별도 확인할 내용 | 확인 주체 |
| --- | --- | --- |
| 발표자료 | 저장소 밖 편집본 존재 여부, 최종 원본·PDF, 발표 권한 | 김은진 |
| PM 피드백 | 윤승혁의 1차·최종 피드백과 실제 반영 여부 | 김은진·윤승혁 |
| 리허설 | 전체 팀 참석, 발표 시간, 전환 순서, 실패 대체 시연 | 전 팀 |
| 팀 DB RAG | 정식 Migration, 최소 권한 DSN, 13건 실행, Rollback 잔존 0 | 최지용·이동윤·김은진 |
| 제출 요구 | ML/DL Weight·GraphDB의 문자 그대로의 제출 필요 여부 | 윤승혁·교육기관 |
| 모델 배포 | `bge-m3` Weight 재배포 가능 여부와 라이선스 | 이동윤·김은진 |
| 원격 CI | Data CI의 실제 GitHub Actions 성공과 Trigger 동작 | 김은진 |
| 발표 환경 | Node·JDK·Python·DB·네트워크·APK·영상 재생 | 각 담당자·김은진 |
| 개인정보·비밀값 | 화면·로그·PPT·제출 ZIP의 최종 육안 검수 | 김은진·각 Owner |

## 5. 완료 기준 및 최종 체크리스트 재판정

### 5.1 8월 5일 발표 기준선 필수 완료 기준

- [ ] `backend_runtime_document_missing`의 원인과 해결 방식이 기록되어 있다.
- [x] Crosswalk의 모든 참조 경로와 Hash가 실제 파일과 일치한다.
- [x] Data 테스트 전체가 발표 기준 Commit 후보에서 통과한다.
- [x] `qa --verify-rebuild`가 성공한다.
- [x] `finalize`가 QA Gate를 우회하지 않고 성공한다.
- [x] 생성 결과 Drift가 없거나 의도된 변경 사유가 기록되어 있다.
- [ ] Data CI가 실제 의존 파일 변경을 감지하도록 보완되어 있다.
- [x] pgvector 적재·검색 실행 명령과 환경이 기록되어 있다.
- [x] Retrieval 평가 Case와 결과 Case ID가 일치한다.
- [x] Recall@5·MRR·금지 문서·금지 모델 Hit 결과가 계산되어 있다.
- [x] 실제 pgvector 결과와 Mock·미연결 결과가 구분되어 있다.
- [x] 모델 ID·Revision·임베딩 차원·검색 설정이 문서화되어 있다.
- [x] 자체 ML/DL 학습 미수행 사유가 공식 산출물에 명시되어 있다.
- [ ] 적용 모델·설정·재현 패키지가 준비되어 있다.
- [x] Vector DB 구축 결과서가 실제 격리 검증 구조·수치와 팀 DB 대기 상태를 구분해 작성되어 있다.
- [x] Graph DB 미적용 근거가 작성되어 있다.
- [ ] 발표에 포함할 기능의 실제 동작·Mock·골격·미구현 상태가 팀 공통 자료로 구분되어 있다.
- [ ] 발표자료의 기능·수치·화면이 발표 기준 Commit과 일치한다.
- [ ] 윤승혁의 1차·최종 피드백이 반영되어 있다.
- [ ] 전체 팀 리허설과 시연 실패 대체 자료가 준비되어 있다.

현재 완료로 체크 가능한 항목은 **13/20개**다. 단, Data 테스트·QA·Finalize의 기준 Commit은 현재 점검 Commit이며 발표 직전 추가 병합이 발생하면 다시 실행해야 한다.

### 5.2 8월 6일 발표 완료 기준

- [ ] 발표자료 원본·PDF·대본의 최종본이 구분되어 있다.
- [ ] 문제·사용자·P0 범위·아키텍처·구축 결과·향후 계획이 연결되어 있다.
- [ ] 직접 학습하지 않은 ML/DL 모델을 발표에서 학습 완료 모델로 표현하지 않았다.
- [ ] Vector DB 검색 흐름과 평가 결과를 발표에서 이해하기 쉽게 설명했다.
- [ ] 현재 실제로 동작하는 기능과 미구현 기능을 발표에서 구분했다.
- [ ] 시연 또는 대체 증빙이 발표 흐름을 끊지 않고 제시되었다.
- [ ] 질문에 근거 없는 추정 답변을 하지 않았다.
- [ ] 확인이 필요한 질문과 피드백을 기록했다.

발표 전이므로 현재 완료로 체크할 항목은 없다. 기존 기술자료의 표현이 올바르더라도 **실제 발표 완료 기준**을 대신하지 않는다.

### 5.3 8월 7일 최종 정리 기준

- [ ] 최신 병합 Commit에서 Data QA를 다시 실행했다.
- [ ] 발표 이후 변경된 데이터·평가·문서의 영향이 기록되어 있다.
- [ ] 발표 피드백이 즉시 수정·5주차·후속·제외로 분류되어 있다.
- [ ] 이동윤에게 승인 평가 Dataset·결과·Hash·실패 Case를 인계했다.
- [ ] 최지용에게 Seed·Crosswalk·Runtime 증빙·Test 결과를 인계했다.
- [ ] 한예나·양정현에게 화면 Test용 상태·Fixture·공개 필드를 전달했다.
- [ ] 윤승혁에게 완료·부분 완료·미구현·차단 현황을 전달했다.
- [ ] 공식 산출물의 최종 경로·버전·승인 상태가 기록되어 있다.
- [ ] 실제 비밀값·원본·Cache·개인정보가 Git과 공개 제출물에서 제외되어 있다.
- [ ] 내부 검사용 전체 소스 ZIP과 공개 제출 패키지의 포함 기준이 구분되어 있다.
- [ ] 5주차 시작 기준 Commit 또는 승인 문서가 고정되어 있다.

발표 후 수행 항목이므로 현재 완료로 체크할 항목은 없다.

## 6. 발표 전 우선순위

1. 김은진이 중간 발표 원본·PDF·대본·Q&A·Evidence Index를 저장하고 윤승혁 피드백을 받는다.
2. 13번째 RAG Case 요청에 Data Owner 판정을 회신하고, 팀 DB 검증을 발표 전 수행할지 발표 후 Gate로 명시할지 결정한다.
3. `week4-data-qa-baseline.md`와 팀 공통 기능 상태 Matrix를 작성해 현재 67/67·QA·Finalize 결과와 Web·Mobile·Backend·AI 상태를 한 기준으로 묶는다.
4. 발표 기준 Commit을 고정한 뒤 Data QA와 각 영역 Smoke Test를 재실행한다.
5. 실제 시연·Mock·격리 DB·미구현을 발표자료에서 시각적으로 구분하고 전체 팀 리허설을 수행한다.

