# 양정현 4주차 작업 진행도

> 작성 일시: **2026-08-04 23:34 KST**  
> 점검 기준 Commit: `d9052621e2f07a8447f7c1f998875104c031a3c0` (`main`)  
> 참고 지침서: [양정현 4주차 업무 지침서](../../weekly-task/양정현_4주차_업무_지침서.md)  
> 관할 기준: [팀원별 관할 영역 v2](../../planning/md/팀원별%20관할%20영역%20v2.md)

## 1. 종합 판정

| 4주차 필수 업무 | 완료도 | 판정 | 핵심 근거와 남은 Gate |
| --- | ---: | --- | --- |
| 3.1 모바일 기준선 재검증·계약 정합성 복구 | 75% | 부분 완료 | 계약 Action·오류·State 처리는 크게 보강됐고 작성자 환경의 Build·실기기 기록이 있다. 최신 병합본 재빌드, Wrapper 종료 코드, 과거 문서 참조와 발표 Commit 고정이 남았다. |
| 3.2 고객 홈·제품·구독 실제 API 연동 | 35% | 미완료 | Remote·Fake Mode와 데이터 출처 표시는 구현됐다. 그러나 Remote 모드도 홈 제품·구독은 `FakeCustomerCareRepository`에서 읽는다. |
| 3.3 문의 생성·증상 제출·상태 충돌 실제 연동 | 75% | 대부분 완료 | 실제 문의 생성·증상 제출 HTTP 성공과 재시도 구조가 있다. 실제 Replay·409·응답 본문·전체 오류 Case 검증은 남았다. |
| 3.4 고객 안내·위험·근거·상담 Fallback 연동 | 45% | 부분 완료 | 안전 UI·Evidence 비노출·Action 필터는 구현됐다. 실제 Backend 안내·Evidence·상담 Endpoint는 없고 Remote에서도 Fixture를 사용한다. |
| 3.5 중간 발표용 모바일 기준선 준비·지원 | 45% | 부분 완료 | 상태표·제한사항·Smoke Script·수동 검증 도구가 있다. 최신 APK·Hash·캡처·영상·기준 Commit·발표자 전달 및 리허설 증거가 없다. |
| 3.6 발표 후 회귀·기사 앱 골격 | 5% | 미착수 | 기존 정적 기사 화면과 미연결 ViewModel만 있다. 발표 후 업무이므로 8월 4일 현재 미착수는 일정상 정상이다. |

전체 단순 평균 완료도는 **약 47%**다. 발표 후 업무인 3.6을 제외한 현재 진행 대상 3.1~3.5의 평균은 **약 55%**다. 실제 문의 접수 수직 흐름은 상당히 진전됐지만, 지침서의 핵심 목표인 **실제 고객 홈과 실제 AI 안내**, 그리고 **최신 병합본의 발표 재현 증거**가 아직 없다.

## 2. 이번 점검에서 확인한 실행·코드 경계

### 2.1 작성자 기록으로 확인되는 결과

[4주차 모바일 검증 결과](../../../mobile/docs/week4-mobile-verification.md)에는 다음 결과가 기록되어 있다.

- JDK 17·Gradle 9.5.0
- Core·Customer 단위 테스트 PASS
- Customer·Technician Debug APK PASS
- `BUILD SUCCESSFUL in 37s`
- 실제 단말에서 `/health`, Demo Login, `/me`, 문의 생성 `201`, 증상 제출 `200` 확인
- Compose 실기기 테스트 2건 PASS

다만 이 문서의 구현 기준 Commit은 `5692124`이고, 이후 `fa2f903`, `c48c2d1`, `6d6b227`, `def5803`, `b9a7e2d`, `24c136f`, `7d200e7`, `da6fe43`에서 Repository·UiState·Action·Guidance·Navigation·발표 Script가 변경됐다. 따라서 위 결과를 현재 `d905262` 전체의 최종 Build 결과로 그대로 승계할 수는 없다.

### 2.2 현재 환경의 직접 재현 결과

현재 `main`에서 다음 작업을 시도했다.

```powershell
mobile\gradlew.bat `
  :core:test `
  :customer-app:testDebugUnitTest `
  :customer-app:assembleDebug `
  :technician-app:assembleDebug `
  --no-daemon
```

| 항목 | 현재 환경 결과 |
| --- | --- |
| Java | `22.0.1` — 프로젝트 권장 JDK 17과 다름 |
| `mobile/local.properties` | 없음 |
| Gradle Distribution | 캐시에 없어 다운로드 시도 |
| 다운로드 | 현재 제한 환경의 네트워크 차단으로 실패 |
| 테스트·APK Build | 시작하지 못함 |
| 현재 APK 파일 | 작업 트리에 없음 |

이번 실패는 Android 코드 Compile 실패가 아니라 **실행 환경·네트워크 선행 조건 실패**다. 다만 `gradlew.bat`은 Wrapper Java 프로세스가 예외로 종료됐는데도 Batch 전체 종료 코드가 0으로 반환됐다. `verify-build.bat`은 이 값을 성공으로 오인할 수 있으므로 재현 도구 결함으로 별도 처리해야 한다.

## 3. 4주차 필수 업무별 평가

### 3.1 3주차 모바일 기준선 재검증과 계약 정합성 복구 — 75%

완료된 내용:

- `customer-app`, `technician-app`, `core` 3모듈과 Gradle Wrapper가 존재한다.
- `AllowedAction` 공통 모델, 409 Snapshot Parser, 오류별 UiState와 입력 `SavedStateHandle`이 구현됐다.
- 계약에 없는 `MARK_RESOLVED`, `CLOSE_INQUIRY`, `CONFIRM_GUIDANCE` 등은 고객 UI에서 제거하거나 안전 필터로 배제한다.
- Mobile이 다음 상태를 임의 계산하지 않고 Backend의 `state_version`과 `allowed_actions`를 보관한다.
- 4주차 전용 Build·설치·Backend Health·APK Hash 검증 Script와 수동 증거 수집 Script가 있다.
- 작성자 환경에서는 단위 테스트, 두 APK, Compose 실기기 2건이 통과했다.

수행 근거:

- [Mobile README](../../../mobile/README.md)
- [공통 문의 모델](../../../mobile/core/src/main/java/com/skn29/watercare/core/model/InquiryModels.kt)
- [409 Parser](../../../mobile/core/src/main/java/com/skn29/watercare/core/network/NetworkFactory.kt)
- [409 Parser 테스트](../../../mobile/core/src/test/java/com/skn29/watercare/core/network/NetworkConflictParserTest.kt)
- [모바일 Smoke Script](../../../mobile/scripts/week4-mobile-smoke-test.ps1)
- [4주차 모바일 검증 결과](../../../mobile/docs/week4-mobile-verification.md)

완료되지 않은 내용:

- 최신 `main`의 전체 단위 테스트·두 APK·Compose UI Test 결과가 없다.
- `mobile/gradlew.bat`이 Java/Wrapper 실패 종료 코드를 전달하지 않아 명령이 거짓 성공할 수 있다.
- `verify-build.bat`은 APK 존재와 Test Report를 확인하지 않으므로 Wrapper 실패를 놓칠 수 있다.
- `week3-completion-checklist.md`와 `REBUILD_PROGRESS.md`는 여전히 존재하지 않는 `START_WEEK3_BACKEND.cmd`, `FINALIZE_WEEK3.cmd`, `INSTALL_WEEK3_APPS.cmd`를 실행하도록 안내한다.
- 4주차 Smoke Script는 브랜치를 `jeonghyun`으로 하드코딩해 병합된 `main` 기준 검증에 바로 사용할 수 없다.
- 발표 기준 Commit 또는 Tag와 최종 실행 환경이 고정되지 않았다.

완료 방법:

1. `gradlew.bat`에서 Java 명령 직후 `%ERRORLEVEL%`을 저장하고 `endlocal & exit /b %EXIT_CODE%` 형태로 실제 종료 코드를 전달한다.
2. `verify-build.bat`에서 각 Task 종료 코드, Customer·Technician APK 존재, Test Report 존재를 확인한다.
3. 과거 문서의 존재하지 않는 Script 참조를 현재 `week4-mobile-smoke-test.ps1` 또는 실제 수동 명령으로 교체한다.
4. Smoke Script가 `main`과 발표 Commit을 허용하도록 `-ExpectedBranch` 또는 `-ExpectedCommit` 인자를 추가한다.
5. JDK 17·Android SDK 37·유효한 `local.properties` 환경에서 최신 HEAD의 단위 테스트·두 APK·Compose UI Test를 다시 실행한다.
6. 결과 문서에 40자리 Commit, JDK·SDK·Gradle, 단말·Emulator, APK SHA-256을 기록한다.

코드 외 별도 확인:

- 발표 장비와 다른 팀원 PC에 JDK 17·SDK 37·Gradle Cache가 준비되어 있는지 확인해야 한다.
- 실기기와 Emulator 각각에서 최신 APK 설치·실행이 되는지 확인해야 한다.

### 3.2 고객 홈·제품·구독 실제 API 연동 — 35%

완료된 내용:

- `REMOTE`와 `FAKE` Mode가 BuildConfig·Runtime Config에서 명시적으로 분리된다.
- 기본 Mode는 `REMOTE`이고 유효한 Demo 구독 UUID가 없으면 실제 문의 진입을 차단한다.
- 화면 상단에 `Remote 문의 실제 API·홈/안내 Fixture`, `Demo Mock`, `오프라인 미리보기` 등 데이터 출처가 표시된다.
- Remote 요청 실패를 Fake 성공으로 자동 전환하지 않는다.
- 실제 Demo Login·`/me`와 문의 입력용 구독 Public UUID 사용 기록이 있다.

수행 근거:

- [Runtime Config](../../../mobile/core/src/main/java/com/skn29/watercare/core/config/CustomerCareRuntimeConfig.kt)
- [Core Repository 조립](../../../mobile/core/src/main/java/com/skn29/watercare/core/WaterCareCore.kt)
- [고객 Home ViewModel](../../../mobile/customer-app/src/main/java/com/skn29/watercare/customer/feature/customer/home/CustomerHomeViewModel.kt)
- [고객 홈 데이터 Mode 가이드](../../../mobile/docs/week4-presentation/2026-08-04_고객_홈_데이터_모드_적용_가이드.md)

완료되지 않은 내용:

- `WaterCareApi`에 제품·구독·케어 조회 Endpoint가 없다.
- Remote 모드에서도 `RemoteIntakeCustomerCareRepository.getHome()`은 `fallbackRepository.getHome()`을 호출한다.
- 고객 홈의 제품·구독·최근 케어·진행 문의는 실제 Backend가 아니라 합성 Fixture다.
- 미지원 모델과 타 고객 제품을 실제 Backend 응답으로 차단하는 통합 검증이 없다.
- Token Refresh 최종 실패, 실제 403·404 Home 오류의 화면 검증이 없다.

완료 방법:

1. 최지용이 제품·구독·케어 조회 Runtime 계약을 확정하기 전에는 임의 Endpoint를 만들지 않는다.
2. 계약 확정 후 `WaterCareApi`, DTO, `RemoteCustomerHomeRepository`를 구현하고 Remote 모드의 `getHome()`에서 Fake 위임을 제거한다.
3. 제품·구독 Public UUID와 MVP 지원 여부를 Backend 응답 그대로 Domain 모델로 Mapping한다.
4. 빈 제품·빈 케어, 미지원 모델, 401·403·404·네트워크 오류 테스트를 추가한다.
5. 실제 Demo 고객으로 로그인해 화면 식별자와 Backend 응답을 대조한 실행 결과를 남긴다.

코드 외 별도 확인:

- 제품·구독·케어 Endpoint의 실제 Runtime 준비 여부와 발표 전 제공 가능 여부를 최지용에게 확인해야 한다.
- Demo 고객의 활성 구독 UUID는 Git·문서에 남기지 않고 Secret 경로로 전달되는지 확인해야 한다.

### 3.3 문의 생성·증상 제출·상태 충돌 실제 연동 — 75%

완료된 내용:

- `POST /api/v1/inquiries`와 `POST /api/v1/inquiries/{id}/submit`이 실제 고객 흐름에 연결됐다.
- 문의 생성 응답의 `inquiry_id`, `inquiry_code`, `state_version`을 증상 제출과 결과 화면에서 사용한다.
- 생성·제출에 서로 다른 Idempotency Key를 사용하고 제출 실패 시 새 문의를 만들지 않고 같은 문의·Key로 재시도한다.
- 입력 Snapshot과 `SavedStateHandle`을 사용하며 실패·409 후 고객 입력을 보존한다.
- 400·422, 401, 403, 404, 409, 5xx, Network 오류를 서로 다른 UiState로 Mapping한다.
- 409 응답의 최신 상태·버전·허용 행동을 Parsing하고 내부 Action은 화면에서 제외한다.
- 상담 API가 없을 때 빈 Callback으로 성공 처리하지 않고 `준비 중` 안내를 표시한다.
- 작성자 실제 단말 기록에서 문의 생성 `201`, 증상 제출 `200`이 확인됐다.

수행 근거:

- [Backend API Interface](../../../mobile/core/src/main/java/com/skn29/watercare/core/network/WaterCareApi.kt)
- [Remote 문의 Repository](../../../mobile/core/src/main/java/com/skn29/watercare/core/repository/Repositories.kt)
- [Remote Intake Repository](../../../mobile/core/src/main/java/com/skn29/watercare/core/repository/RemoteIntakeCustomerCareRepository.kt)
- [Intake ViewModel](../../../mobile/customer-app/src/main/java/com/skn29/watercare/customer/feature/customer/intake/SymptomIntakeViewModel.kt)
- [Remote Intake 테스트](../../../mobile/core/src/test/java/com/skn29/watercare/core/repository/RemoteIntakeCustomerCareRepositoryTest.kt)
- [실연동 검증 결과](../../../mobile/docs/week4-mobile-verification.md)

완료되지 않은 내용:

- 실제 증상 제출 응답 본문의 `state`, `state_version`, `allowed_actions`, `idempotent_replay`를 캡처해 대조하지 않았다.
- 동일 Key·동일 Body Replay와 동일 Key·다른 Body 409를 실제 Runtime에서 실행하지 않았다.
- 실제 400·401·403·404·409·5xx·네트워크 오류를 단말 화면까지 검증하지 않았다.
- 409 이후 최신 상태 새로고침·허용 Action 이동의 전체 동선이 완료되지 않았다.
- `RemoteInquiryRepository.cancel()`은 호출할 때마다 Idempotency Key를 새로 생성해 동일 사용자 동작 재시도 Key 유지가 보장되지 않는다.
- Process 종료 이후 진행 중 Idempotency 작업 복원 정책이 없다.
- 최신 `da6fe43`의 접수 정보·Navigation 변경을 포함한 전체 회귀 결과가 없다.

완료 방법:

1. 수동 증거 Script로 실제 Replay·Conflict·오류 Case를 실행하고 요청·마스킹 응답·화면 캡처를 저장한다.
2. 409 시 최신 문의 조회 또는 계약된 허용 Action으로 이동하는 UI 흐름을 구현한다.
3. 취소 Repository가 외부에서 전달받은 Idempotency Key를 사용하도록 수정하고 재시도 중 같은 Key를 유지한다.
4. `PendingIntakeOperation`의 Process 종료 복원 필요성을 결정하고 필요한 값은 `SavedStateHandle` 또는 영속 저장소로 이동한다.
5. 최신 HEAD에서 단위·Compose UI·실단말 Smoke를 다시 실행한다.

코드 외 별도 확인:

- Backend가 Replay·Conflict를 현재 계약대로 반환하는지 최지용과 공동 검증해야 한다.
- 실제 Demo 구독 UUID와 생성된 문의가 합성·격리 데이터인지 확인해야 한다.

### 3.4 고객 안내·위험도·공식 근거·상담 Fallback 연동 — 45%

완료된 내용:

- CUST-04는 현재 행동·위험도·사용 제한·안전 행동·상담 조건·공식 근거·금지 행동을 구분해 표시한다.
- 위험·근거 없음·상담 필수 상태에서 해결·종료 Action을 제거한다.
- `REQUEST_CONSULTATION` 등 표시 가능한 Backend Action만 필터링한다.
- 내부 경로·`chunk_id`·검색 점수·원문 전체를 Domain·화면에 노출하지 않는다.
- 정상·위험·근거 없음·AI 실패·네트워크 실패 Fixture와 단위·Compose UI 테스트가 있다.
- 상담 Endpoint가 없을 때 `상담 요청 준비 중`으로 표시하고 실제 성공처럼 처리하지 않는다.

수행 근거:

- [Guidance Screen](../../../mobile/customer-app/src/main/java/com/skn29/watercare/customer/feature/customer/guidance/GuidanceScreen.kt)
- [Guidance ViewModel](../../../mobile/customer-app/src/main/java/com/skn29/watercare/customer/feature/customer/guidance/GuidanceViewModel.kt)
- [Guidance Mapper](../../../mobile/core/src/main/java/com/skn29/watercare/core/model/CareModels.kt)
- [Guidance Mapper 테스트](../../../mobile/core/src/test/java/com/skn29/watercare/core/model/CareModelsTest.kt)
- [모바일 제한사항](../../../mobile/docs/week4-presentation/2026-08-04_모바일_제한사항.md)

완료되지 않은 내용:

- Remote 모드에서도 `getGuidance()`가 `FakeCustomerCareRepository`를 호출한다.
- 실제 Backend 고객용 AI 결과·Evidence Endpoint가 `WaterCareApi`에 없다.
- 실제 정상·위험·근거 없음·Timeout·미지원 제품 응답의 화면 검증이 없다.
- AI Timeout·검색 오류·상담 전환을 실제 Backend 상태와 연결하지 않았다.
- 상담 요청 Endpoint와 성공 후 최신 상태 갱신이 없다.
- 이동윤·윤승혁의 고객 문구·공개 필드 최종 검수 기록이 없다.

완료 방법:

1. 최지용·이동윤이 고객용 Guidance·Evidence 계약을 확정하면 실제 API DTO와 `RemoteGuidanceRepository`를 구현한다.
2. Backend가 공개한 필드만 Domain에 Mapping하고 내부 AI 응답을 Mobile DTO에 추가하지 않는다.
3. 실제 Timeout·근거 없음·위험·미지원 제품 Fixture 또는 통합 환경으로 화면을 검증한다.
4. 상담 Endpoint가 제공되면 `REQUEST_CONSULTATION`만 실제 호출하고 응답 상태·버전·Action을 갱신한다.
5. Endpoint가 발표 전 제공되지 않으면 CUST-04를 `Demo Fixture`로 명확히 발표하고 완료로 표현하지 않는다.

코드 외 별도 확인:

- Backend AI Adapter·Evidence 저장과 고객 공개 DTO의 준비 여부를 최지용·이동윤에게 확인해야 한다.
- 위험 안내 문구와 공식 근거 공개 범위를 이동윤·김은진·윤승혁에게 검수받아야 한다.

### 3.5 8월 6일 중간 발표용 모바일 시연 기준선 준비·지원 — 45%

완료된 내용:

- 실제 API·Fixture·미구현을 구분한 모바일 구현 상태표와 제한사항 문서가 있다.
- Build·Backend Health·APK Hash·설치·실행을 검사하는 Smoke Script가 있다.
- 실제 단말의 단계별 캡처·로그·결과 문서를 만드는 수동 증거 Script가 있다.
- 고객 문의 접수 정보 표시 가이드와 발표 Smoke Checklist가 있다.
- 작성자 환경에서 실제 인증·문의 생성·증상 제출을 검증했다.

수행 근거:

- [모바일 구현 상태표](../../../mobile/docs/week4-presentation/2026-08-04_모바일_구현_상태표.md)
- [모바일 Smoke 가이드](../../../mobile/docs/week4-presentation/2026-08-04_모바일_Smoke_Test_가이드.md)
- [모바일 Smoke Checklist](../../../mobile/docs/week4-presentation/2026-08-04_모바일_Smoke_Test_체크리스트.md)
- [실연동 수동 검증 가이드](../../../mobile/docs/week4-presentation/2026-08-04_고객_실연동_수동검증_가이드.md)
- [모바일 제한사항](../../../mobile/docs/week4-presentation/2026-08-04_모바일_제한사항.md)

완료되지 않은 내용:

- 최신 HEAD에서 생성한 Customer·Technician APK와 APK SHA-256이 없다.
- 발표 단말과 Emulator 설치 완료 기록이 없다.
- Mobile 발표 기준 Commit 또는 Tag가 고정되지 않았다.
- 모바일 화면 캡처와 1~2분 Fallback 영상이 저장소에 없다.
- Smoke Checklist가 체크되지 않았고 실제 생성 결과 파일도 없다.
- 대표 시나리오의 반복 재현 결과와 Backend 장애 전환 순서가 없다.
- 김은진에게 전달한 자료와 발표자료 반영 확인, 윤승혁의 피드백 기록이 없다.
- 고객 홈과 AI 안내가 Fixture이므로 지침서가 제시한 전체 실제 동선은 재현할 수 없다.

완료 방법:

1. 발표용 JDK 17·SDK·Backend 환경에서 최신 HEAD의 Smoke Script를 실행한다.
2. Customer·Technician APK와 SHA-256, 설치 단말·Emulator 정보를 결과 문서에 남긴다.
3. 로그인→Fixture 홈→실제 문의 생성·제출→Fixture 안내의 정확한 경계를 시연 대본에 표시한다.
4. 로그인·홈·입력·접수·안내 화면 캡처와 짧은 Fallback 영상을 생성한다.
5. 발표 기준 Commit을 고정하고 김은진에게 구현 상태표·Endpoint 수·테스트 수·제한사항을 전달한다.
6. 윤승혁 피드백과 전체 리허설 결과를 체크리스트에 반영한다.

코드 외 별도 확인:

- APK나 영상이 Git 밖 공유 폴더에 존재하는지 양정현에게 확인해야 한다.
- 발표 단말의 USB 디버깅·Wi-Fi·방화벽·Backend Base URL·`adb reverse`를 확인해야 한다.
- 김은진 발표자료가 Mobile을 전체 실연동으로 표현하지 않는지 확인해야 한다.

### 3.6 발표 후 회귀 수정과 방문기사 목록·사전 점검 리포트 골격 — 5%

현재 상태:

- 기존 `TechnicianApp`과 `TechnicianViewModel`은 존재한다.
- `TechnicianApp`은 Composable 내부 `remember`와 직접 Coroutine으로 로그인하고, `TechnicianViewModel`을 사용하지 않는다.
- 일정·고객·주소·작업 정보는 정적 상수이고 방문 목록·상세 Route·Repository가 없다.
- 기사 화면의 `지도·OCR·위치 추적` 실제 기능은 추가하지 않았다.
- 발표 후 수행 업무이므로 8월 4일 현재 미착수는 일정상 정상이다.

수행 근거:

- [기사 App](../../../mobile/technician-app/src/main/java/com/skn29/watercare/technician/TechnicianApp.kt)
- [미연결 Technician ViewModel](../../../mobile/technician-app/src/main/java/com/skn29/watercare/technician/TechnicianViewModel.kt)

완료 방법:

1. 발표 후 고객 앱 차단 결함과 회귀를 먼저 수정하고 Smoke Test를 재실행한다.
2. `TechnicianViewModel`을 실제 화면에 연결하고 UI 상태를 StateFlow로 이동한다.
3. 방문 목록·상세 Navigation, DTO, Repository Interface와 명시적 Fixture를 작성한다.
4. 기사에게 배정된 방문만 표시하는 권한·404 계약과 개인정보 마스킹을 문서화한다.
5. 5주차 실제 방문 API·AI 사전 점검 의존성을 Issue와 인계 문서에 남긴다.

코드 외 별도 확인:

- 발표 피드백으로 기사 화면 범위가 바뀌는지 윤승혁에게 확인해야 한다.
- Backend의 기사 방문 목록·상세·권한 Endpoint 일정은 최지용에게 확인해야 한다.

## 4. 코드만으로 완료 여부를 확인할 수 없는 사항

| 확인 대상 | 별도 확인할 내용 | 확인 주체 |
| --- | --- | --- |
| Android 실행 환경 | JDK 17, SDK 37, Gradle Distribution·의존성 Cache | 양정현·김은진 |
| 최신 Build | 현재 발표 Commit의 Core·Customer·Technician Test와 APK | 양정현·비작성 검토자 |
| 실기기·Emulator | APK 설치, Compose UI, 화면 전환, 재실행 | 양정현 |
| 실제 Backend | Base URL, Demo 계정·구독, Replay·409·오류 응답 | 최지용·양정현 |
| 실제 AI 안내 | 고객 공개 DTO, 위험·근거 없음·Timeout·Evidence | 이동윤·최지용·양정현 |
| 발표 자료 전달 | 캡처·영상·APK·수치가 김은진 자료에 반영됐는지 | 김은진·양정현 |
| PM 피드백 | 실제·Fixture 표현과 시연 순서 승인 | 윤승혁 |
| 비밀정보 | Token·Demo UUID·Base URL·고객정보가 APK·Log·영상에 남지 않는지 | 양정현·김은진 |

## 5. 완료 기준 및 최종 체크리스트 재판정

### 5.1 8월 5일 발표 기준 기능 동결 체크리스트

- [ ] Core·Customer 단위 테스트와 Customer Debug APK 빌드가 최신 발표 기준 Commit에서 성공한다.
- [ ] 고객 최소 Compose UI Test가 최신 발표 기준 Commit에서 통과한다.
- [ ] README와 검증 문서의 명령·파일명이 실제 저장소와 모두 일치한다.
- [x] 계약에 없는 Mobile Action이 고객 화면에서 제거되었다.
- [x] 구현된 Endpoint 범위의 Mobile DTO·상태·오류·`allowed_actions`가 최신 계약과 일치한다.
- [x] Demo 로그인과 Access Token을 사용한 `/me` 호출이 동작한다.
- [ ] 고객 홈이 실제 Backend 제품·구독을 조회한다.
- [ ] Fake 고객 홈이 발표 기본 경로에서 제거되었다.
- [x] CUST-02가 실제 문의 생성과 증상 제출 API를 호출한다.
- [x] `idempotency_key`와 `state_version`이 요청·응답 코드에 반영된다.
- [x] 400·401·403·404·409·5xx·네트워크 실패가 서로 다른 UiState로 구분된다.
- [ ] 실제 409 충돌 후 고객 입력과 최신 상태 유지가 Runtime에서 검증되었다.
- [ ] CUST-04가 실제 Backend의 고객용 AI 결과와 Evidence를 표시한다.
- [x] 위험·근거 없음·Timeout에서 잘못된 해결·종료 버튼이 보이지 않는다.
- [x] EvidenceCard에 내부 경로·`chunk_id`·검색 원문 전체가 노출되지 않는다.
- [x] 상담 요청 Button이 빈 Callback으로 남지 않고 명시적 준비 중 상태를 표시한다.
- [x] 발표 기본 Remote Mode와 비상 Fake Mode가 명확히 구분된다.
- [ ] 발표 기준 Commit 또는 Tag가 기록되어 있다.

현재 완료로 체크 가능한 항목은 **10/18개**다. 작성자 환경의 이전 Build·Compose 결과는 최신 병합본 기준이 아니므로 첫 두 항목을 완료 처리하지 않았다.

### 5.2 8월 6일 중간 발표 지원 체크리스트

- [ ] 발표 단말과 Emulator에 고객 앱 APK가 설치되어 있다.
- [ ] Backend Base URL과 네트워크 연결을 발표 환경에서 확인했다.
- [ ] 대표 Scenario가 초기 상태에서 반복 재현된다.
- [ ] 로그인→고객 홈→증상 입력→안내 화면의 동선이 확정되어 있다.
- [ ] 화면 캡처와 짧은 Fallback 영상이 준비되어 있다.
- [ ] 실제 API와 Mock·Fallback 범위를 발표자가 구분해 설명할 수 있다.
- [ ] 김은진 발표자료의 모바일 구현 수치·화면·테스트 수가 실제 코드와 일치한다.
- [ ] 윤승혁의 시연 순서·표현 피드백이 반영되었다.
- [ ] 발표 당일 새 Dependency·SDK·Base URL 변경을 하지 않는다.
- [ ] 장애 발생 시 APK·서버·계정·네트워크 원인을 빠르게 구분할 수 있다.

발표 환경·발표자 전달·리허설을 코드만으로 확인할 수 없고 저장소에 최종 실행 증거가 없으므로 현재 완료로 체크할 항목은 없다.

### 5.3 8월 7일 최종 정리 기준

- [ ] 발표 중 발견된 고객 앱 차단 결함이 수정되었다.
- [ ] 수정 후 고객 앱 Smoke Test를 다시 실행했다.
- [ ] 발표 피드백과 미해결 항목이 Issue에 기록되어 있다.
- [ ] `TechnicianViewModel`이 실제 기사 앱 화면에 연결되었다.
- [ ] 기사 방문 목록·사전 점검 리포트 최소 화면이 존재한다.
- [x] 기사 앱에 지도·OCR·위치 추적·방문 완료 기능을 무리하게 포함하지 않았다.
- [ ] Backend·AI에 필요한 기사 API·DTO·권한 의존성이 정리되어 있다.
- [ ] 5주차에 진행할 고객 추가 질문·기사 상세·실제 통합 범위가 명확하다.
- [ ] 4주차 모바일 실행·테스트·발표 결과가 최종 문서화되어 있다.

현재 완료로 체크 가능한 항목은 **1/9개**다. 나머지는 발표 이후 수행하거나 확인할 업무다.

## 6. 발표 전 우선순위

1. `gradlew.bat` 종료 코드 전달 오류를 수정하고 최신 `main`의 Test·두 APK를 JDK 17 환경에서 재실행한다.
2. 발표 기준 Commit, APK SHA-256, 단말·Emulator 설치 결과와 Smoke Test 원시 결과를 고정한다.
3. 실제 문의 생성·제출의 응답 본문, Replay, 409 Conflict를 단말에서 확인한다.
4. 고객 홈과 AI 안내가 Fixture라는 경계를 캡처·대본·김은진 발표자료에 명확히 표시한다.
5. 모바일 화면 캡처와 Fallback 영상을 준비하고 윤승혁 피드백·전체 리허설을 반영한다.

