# 이동윤 3주차 작업 진행도

> 최초 작성: 2026-07-29 16:55 KST
>
> 최종 갱신: 2026-07-31 16:40 KST
>
> 기준 Commit: `0ce08d8` (`main`)
>
> 참고: `docs/weekly-task/이동윤_3주차_업무_지침서.md`, `ai/README.md`, `contracts/ai/**`, `docs/individual/dongyoon/walkthrough.md`, `docs/individual/dongyoon/20260730_이동윤_최지용_AI_pgvector_인계보고서.md`

## 1. 종합 판정

| 필수 업무 | 완료도 | 판정 | 현재 근거 |
|---|---:|---|---|
| 3.1 Backend↔AI Schema·Pydantic | 100% | 완료 | AI 계약 1.1.0의 3종 Schema·Pydantic·Negative Parity와 공개 필드가 검증됨 |
| 3.2 안전 규칙·출력 가드레일 | 98% | 대부분 완료 | 위험·근거 없음 분기와 금지 Guard, 안전 평가 4/4 및 허용 필드 로그 비노출 검증이 있음 |
| 3.3 FastAPI·Backend 연동 최소 API | 98% | 대부분 완료 | Python 3.13.13 Lock·71개 회귀·HTTP Smoke·Timeout·동시 실행 상한·구조화 로그가 검증됐으나 Backend Client E2E는 남음 |
| 3.4 bge-m3·pgvector 검색·필터 | 100% | 완료 | 실제 격리 PostgreSQL에서 bge-m3 적재·UPSERT·Top-5·SQL 필터·차원 거부를 검증함 |
| 3.5 단일 RAG·LangGraph 오케스트레이터 | 97% | 대부분 완료 | 단계별 협력 취소·동시 실행 상한·위험/근거 없음 분기와 동일 Schema가 동작하며 Backend 상태 E2E만 남음 |
| 3.6 평가·계약 테스트·산출물 인계 | 90% | 대부분 완료 | 12/12 pgvector 실증과 Schema Matrix는 유효하나 13번째 Case와 오프라인 latest 평가의 실패 수치 분리가 남음 |

종합 완료도는 **약 97%**다. 71개 단위 테스트와 실제 pgvector 12/12 증거는 유지된다. 다만 체크인된 `latest_eval_report.json`은 DB 미연결을 명시하며 RAG 0·안전 50%이므로 성공 증거로 사용할 수 없다. 남은 핵심은 Backend AI Client, 정식 `ChunkEmbedding` 저장 구조 통합, Web·Mobile DTO 검수, 13번째 정책 차단 Case와 PR 리뷰다.

## 2. 현재 실행·검증 결과

- AI 계약: `1.1.0`
- Python: `3.13.13`
- 기록된 AI 단위 회귀: `71 passed, 3 warnings`; 독립 환경 `pip check` PASS
- FastAPI Smoke: 실제 Uvicorn에서 `GET /health → 200`, Mock 분석 요청 성공
- PostgreSQL: `16.14`, pgvector `0.8.6`
- Embedding: `BAAI/bge-m3`, Revision `5617a9f61b028005a4858fdac845db406aefb181`, 1024차원
- 적재 결과: 승인 청크 7건, 재적재 후에도 7건 유지
- 실제 검색 평가: 12/12 PASS
- 양성 평균 Recall@5: `1.0`
- 양성 7건 pgvector 평균 MRR: `0.8857142857142858`
- 정책 차단을 포함한 12건 종합 MRR: `0.9333`
- 금지 문서·모델 혼입: 0건
- SQL Filter 검증 Fixture 누출: 0건
- 잘못된 3차원 Vector: `vector(1024)` DB에서 거부
- 실제 pgvector 통합 테스트: `1 passed`
- 구조화 로그에서 고객 원문·Prompt·Secret을 제외하고 시작·완료 `correlation_id` 일치를 테스트함
- 현재 점검 환경에는 `ai/.venv`와 `pytest`가 없어 71건을 재실행하지 못했고, 병합된 실행 기록과 보고서를 근거로 판정함
- 최신 오프라인 평가 파일: `vector_store_not_configured`, RAG 0, 안전 준수 50%. 실제 pgvector 성공 보고서와 실행 조건이 다른 실패/비구성 결과로 분리함

이 결과는 별도 격리 컨테이너의 실제 pgvector 검증이다. 팀 공용 Backend DB Migration·Backup·Restore 완료를 의미하지는 않는다.

## 3. 필수 업무별 평가

### 3.1 Backend↔AI JSON Schema와 Pydantic 모델 — 100%

완료된 내용:

- 증상 분석·상담 요약·기사 리포트 요청과 응답 JSON Schema가 실제 필드와 필수 조건을 포함한다.
- `inquiry_id`, `correlation_id`, `ai_request_id`, `state_version`을 요청·응답 최상위에서 전달·Echo한다.
- Pydantic 모델, 정상·위험·근거 없음·검증 실패·Timeout 예시가 계약 1.1.0과 연결됐다.
- 공개 응답에서 내부 Processing Trace를 제외하고 역할별 결과 필드를 고정했다.
- JSON Schema와 Pydantic의 Negative Parity 및 다중 페이지 `page_refs` 보존을 자동 검증했다.
- 16개 AI JSON Schema 전체에 Runtime 모델·정상 Payload·추가 필드 거부 Matrix를 적용하고 중첩 경계 반례를 검증했다.

수행 근거: [AI 계약 README](<../../../contracts/ai/README.md>), [증상 분석 응답 Schema](<../../../contracts/ai/responses/SymptomAnalysisResponse.schema.json>), [상담 요약 Pydantic 모델](<../../../ai/app/schemas/consultation_summary.py>), [기사 리포트 Pydantic 모델](<../../../ai/app/schemas/technician_report.py>)

남은 내용과 완료 방법:

- 최지용의 Backend AI Client DTO가 계약 1.1.0을 실제로 소비하는 계약 테스트를 추가한다.
- 한예나·양정현의 TypeScript·Kotlin DTO와 공개 필드를 교차 검토한다.

### 3.2 명시적 안전 규칙과 출력 가드레일 — 95%

완료된 내용:

- 누수, 전기·감전·화재, 온수·화상, 냉온수·출수량 이상 규칙이 설정 파일에 명시됐다.
- 위험도에 따라 `TOTAL_STOP`, `PARTIAL_STOP`, 상담 우선순위와 제한 기능을 반환한다.
- 공식 근거가 없으면 임의 자가조치를 차단하고 `PENDING_CONSULTATION`으로 전환한다.
- 위험과 근거 없음 입력이 일반 안내 경로와 다른 LangGraph 분기를 사용한다.
- 실제 종합 평가에서 안전 Case 4/4, 준수율 100%가 기록됐다.

수행 근거: [안전 규칙 설정](<../../../ai/configs/safety_rules.yaml>), [금지 행동 Guard](<../../../ai/app/safety/prohibited_action_guard.py>), [근거 없음 정책](<../../../ai/app/safety/no_evidence_policy.py>), [안전 테스트](<../../../ai/tests/unit/test_safety_classifier.py>)

남은 내용과 완료 방법:

- 김은진의 안전 기대 결과와 전체 규칙 ID·문구를 사람이 교차 검토한다.
- 실제 응답과 로그에 금지 문구·개인정보·비밀값이 없는지 독립 검수한다.

### 3.3 FastAPI 실행 환경과 Backend 연동 API — 95%

완료된 내용:

- `pyproject.toml`에 필요한 의존성과 정확한 버전이 고정됐다.
- `/health`, `/api/v1/ai/health`, `/api/v1/ai/analyze?mode=mock|local`가 구현됐다.
- 실제 Uvicorn에서 Health와 Local 분석 API가 HTTP 200을 반환했다.
- Header와 Body의 `correlation_id` 일치, 오류 Echo, 30초 Timeout과 내부 최대 1회 정책이 구현됐다.
- `requirements.lock`과 범용 실행 절차, 협력적 Worker 취소, DB 하위 Timeout, 허용 필드 구조화 로그가 추가됐다.
- Timeout 뒤 Torch Thread 종료까지 Slot을 유지하는 동시 실행 상한 기본 2개와 운영 INFO Logger가 적용됐다.

수행 근거: [잠금 의존성](<../../../ai/requirements.lock>), [FastAPI Main](<../../../ai/app/main.py>), [구조화 로그](<../../../ai/app/interfaces/http/structured_logging.py>), [반송 보완 회신](<20260730_이동윤_최지용_10_1_반송보완_회신.md>)

남은 내용과 완료 방법:

- 최지용 Backend Client로 정상·Validation·Timeout·서비스 장애 응답을 호출한다.
- 새로운 독립 가상환경에서 설치→서버→71개 테스트를 다른 팀원이 재현한다.

### 3.4 bge-m3 임베딩·pgvector 검색과 메타데이터 필터 — 100%

완료된 내용:

- 실제 `BAAI/bge-m3` Revision을 고정해 1024차원 임베딩을 생성했다.
- 격리 PostgreSQL에서 vector Extension, Table, UPSERT와 Cosine Exact Search `<=>`를 실행했다.
- 제품 코드·D세대·공식 검증·고객 안내 허용 조건을 Vector 순위 계산 전에 SQL로 제한한다.
- 동일 청크 재적재의 멱등성, Hash·차원, 금지 Fixture 누출과 잘못된 차원 거부를 검증했다.
- 대표 저출수 공식 38쪽을 포함한 7개 양성 Case 정답이 모두 Top-5에 포함됐다.
- S세대·IAC425·IAC506·미검증 FAQ 등 부정 Case가 검색 결과에 혼입되지 않았다.
- 검색 결과 반환 뒤에도 모델·세대·공식 검증·허용 정책과 Document Hash·Embedding Revision·Index Version·Chunk Set Hash를 재검증한다.

수행 근거: [pgvector 검증 Script](<../../../ai/scripts/verify_pgvector_runtime.py>), [Vector Store](<../../../ai/app/integrations/vector_store/vector_store.py>), [pgvector 통합 테스트](<../../../ai/tests/integration/test_pgvector_runtime.py>), [검증 결과 JSON](<../../../ai/evaluation/reports/pgvector_verification.json>)

코드 외 별도 확인:

- 최지용이 구현한 `ChunkEmbedding`·`RetrievalRun`·`RetrievalHit`·`EvidenceLink` Migration과 AI 검색 저장 경로를 통합하고 권한·Backup·Rollback을 검증해야 한다.

### 3.5 단일 RAG 기준선과 LangGraph 오케스트레이터 — 95%

완료된 내용:

- 구조화→누락 확인→안전→검색→생성→검증 Stage와 LangGraph 분기가 있다.
- 위험 입력은 일반 자가조치 경로를 차단하고, 근거 없음은 상담 전환으로 종료한다.
- Mock·Local 모두 계약 1.1.0의 응답 식별자와 결과 구조를 사용한다.
- DB나 모델이 없을 때 하드코딩 검색으로 대체하지 않고 근거 없음 정책을 사용한다.
- LangGraph 각 단계가 취소 신호를 확인해 30초 HTTP Timeout 뒤 백그라운드 작업이 계속되지 않도록 했다.
- Local 분석 동시 실행 상한을 적용해 Timeout 작업이 무제한 누적되지 않도록 했다.

수행 근거: [단일 RAG Pipeline](<../../../ai/app/orchestration/pipelines/single_rag_pipeline.py>), [선택형 Pipeline](<../../../ai/app/orchestration/pipelines/selective_pipeline.py>), [Pipeline Router](<../../../ai/app/orchestration/pipeline_router.py>), [Pipeline 테스트](<../../../ai/tests/unit/test_pipeline.py>)

남은 내용과 완료 방법:

- Backend 상태 전이와 연결한 실제 수직 E2E를 추가한다.
- 상담 요약·기사 리포트를 Web·Mobile 화면까지 전달하는 역할별 흐름을 검증한다.

### 3.6 검색 평가·계약 테스트·산출물 인계 — 90%

완료된 내용:

- 실제 pgvector 12개 평가 Case와 Query별 Top-5·점수·정답·필터 결과가 저장됐다.
- Recall@5·MRR·금지 문서 Hit·차원·SQL 필터 결과가 JSON 보고서에 기록됐다.
- 이동윤→최지용 AI 계약·Runtime·pgvector 인계보고서에 실행 명령과 후속 Gate가 있다.
- Mock·Stub·실제 구현과 격리 실증·팀 공용 DB 미반영 범위가 구분됐다.
- 김은진에게 RAG·AI 데이터 적합성 판단 보고서를 전달했고 2차 검토의 보완 요청을 받았다.
- 검색 평가 입력을 `jac104_retrieval_cases.json` SSOT에서 직접 읽도록 정리하고 2차 반송 보완 내용을 문서화했다.
- DB 미연결 실행에서 가짜 성공 수치를 만들지 않고 `vector_store_not_configured`를 기록하도록 투명화했다.

수행 근거: [pgvector 검증 결과](<../../../ai/evaluation/reports/pgvector_verification.json>), [Data 적합성 판단보고서](<20260730_이동윤_김은진_RAG_AI_데이터_적합성_판단보고서.md>), [2차 재검증 요청](<../eunjin/team_handover/20260730_김은진_이동윤_RAG_3_3_v2_재검증요청.md>), [반송 보완 회신](<20260730_이동윤_최지용_10_1_반송보완_회신.md>)

남은 내용과 완료 방법:

- 김은진이 요청한 동일 모델 정책 차단 13번째 Case와 v2 결과 포맷을 반영해 재실행한다.
- 성공 pgvector 보고서와 오프라인 `latest` 실패 보고서의 명명·승인 용도를 분리한다.
- 한예나·양정현이 AI DTO와 표시 필드를 검토한다.
- 실제 결과를 데이터 전처리 결과서와 DB·저장소 설계 문서에 반영한다.
- PR에 실행·테스트 방법과 Reviewer를 연결한다.

## 4. 코드 외 별도 확인 사항

| 확인 대상 | 별도 확인 내용 |
|---|---|
| Backend 연동 | AI Client 정상·오류·Timeout·상태 전이 E2E |
| Data 검수 | 검색 정답·안전 기대 결과와 청크 Hash 승인 |
| Frontend 검수 | Web·Mobile 공개 DTO·화면 필드·Fallback 일치 |
| 보안 | 응답·로그의 내부 경로·원문 전체·Secret·개인정보 비노출 |
| 공용 DB | Backend `ChunkEmbedding`·검색 이력 Migration과 AI Vector Store 통합, 권한·Backup·Restore·Rollback |
| PR | Commit SHA, 실행 명령, 결과와 Reviewer 기록 |

## 5. 완료 기준 및 최종 체크리스트

### 5.1 7월 29일 필수 완료 기준

- [x] Backend↔︎AI 요청·응답 JSON Schema와 Pydantic 모델이 확정되었다.
- [x] `risk_level`과 4개 `usage_guidance_status` 값이 공통 코드와 일치한다.
- [x] 정상·위험·근거 없음·검증 실패 예시 JSON이 준비되었다.
- [x] 누수·전기·화상·온수 위험 규칙과 금지 행동·표현 Guard가 구현되었다.
- [x] FastAPI 서버가 실행되고 `/health`가 정상 응답한다.
- [x] 분석 API가 Mock 또는 최소 실행 모드로 계약 응답을 반환한다.
- [x] `bge-m3` 임베딩과 pgvector 인덱싱·검색 모듈이 실행된다.
- [x] 모델·세대·문서 검증·허용 정책 필터가 적용된다.
- [x] 대표 저출수 질의에서 공식 사용설명서 38페이지가 합의된 Top-k에 포함된다.
- [x] S세대·IAC425·IAC506·미검증 FAQ 혼입 검사가 수행되었다.
- [x] 단일 RAG 기준선과 LangGraph 최소 골격이 같은 Schema로 실행된다.
- [x] 위험과 근거 없음 입력이 일반 안내 경로와 다르게 처리된다.
- [x] 데이터 전처리 결과서와 DB·저장소 설계 문서에 필요한 자료를 담당자에게 전달했다.
- [x] 실행 명령·설정·데이터·인덱스 버전·테스트 결과가 문서화되어 있다.
- [x] Mock·Stub·실제 구현이 명확히 구분되어 있다.

### 5.2 7월 30일~31일 최종 정리 기준

- [ ] 최지용의 Backend Client 또는 수동 호출로 AI 정상·오류 응답을 확인했다.
- [x] 김은진과 검색 정답·안전 기대 결과를 교차 검증했다.
- [ ] 한예나·양정현의 DTO·화면 필드와 AI 예시 응답의 불일치를 수정했다.
- [x] 계약 테스트·검색 테스트·안전 테스트의 실패 원인을 처리하거나 Issue로 남겼다.
- [x] `correlation_id`로 AI 요청·단계·응답을 추적할 수 있다.
- [x] 내부 경로·원문 전체·비밀값·개인정보가 응답과 로그에 노출되지 않는다.
- [x] 공식 산출물의 템플릿 문구·가짜 수치가 실제 프로젝트 정보로 교체되었다.
- [x] 4주차 필수 구현, 조기 완료 작업, 미해결 의존성을 구분해 인계했다.
- [ ] 관련 PR에 실행·테스트 방법과 검토자를 기록했다.

## 6. 다음 작업

1. 최지용 Backend AI Client와 정상·오류·Timeout E2E를 수행한다.
2. Backend의 `ChunkEmbedding`·검색 이력·Evidence 구조와 AI 검색 저장 경로를 통합한다.
3. 김은진의 2차 요청에 따라 13번째 정책 차단 Case와 v2 결과 포맷을 재실행한다.
4. 한예나·양정현 DTO와 계약 1.1.0 공개 필드를 맞춘다.
5. 관련 PR에 실행·테스트 방법과 Reviewer를 연결한다.
