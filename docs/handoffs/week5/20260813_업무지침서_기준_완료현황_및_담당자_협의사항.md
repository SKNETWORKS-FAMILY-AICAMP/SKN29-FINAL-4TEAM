# 2026-08-13 업무지침서 기준 완료 현황 및 담당자 협의사항

> **최신 재점검 알림:** 아래 P0 PASS는 실행 내용이 `1cd1905`로 보존된 격리 Runtime의 실제 결과입니다. 현재 최신 `6ae659f`에서는 Backend 실행환경과 Guidance·상담 상세 Projection 차이로 동일 흐름을 재실행하지 못했으므로, 최신 SHA 기준 최종 PASS 선언에는 Backend 공동 Smoke가 필요합니다.

> 공유 대상: PM, Backend·DB, AI, Mobile, Web 담당자  
> 기준 SHA: `6ae659f12c02c4abc72cb6b2645e1669c76d571d`  
> P0 목표: **Mobile 실제 AI 안내 → 상담 요청 → 동일 `inquiry_id` Web 조회 → 상담 시작·기록·완료 → Mobile 최종 상태 확인**

## 0. PM 최종 결정

| 결정 항목 | 확정 내용 |
|---|---|
| P0 정상 종점 | `COMPLETION_PENDING` 승인 |
| `RESOLVED` Runtime | 빠른 E2E 구현을 위해 P0에서 제외하고 P1 유지 |
| P0 100% 완료 기준 | PostgreSQL 운영환경 Gate와 물리 Android 기기 Gate까지 모두 통과한 시점 |
| Dashboard·Evidence·AI 실험 | E2E 완료를 우선하고 다음 주부터 재개 |

## 1. 요약

- 로컬 격리 환경 기준 핵심 P0 흐름은 **완료(PASS)** 했습니다.
- 동일 문의 `96d76459-cffe-43f9-b927-67a8aedf1fc7`가 처음부터 끝까지 연결됐습니다.
- 최종 상태는 계약상 정상 상태인 `COMPLETION_PENDING`, v8입니다.
- `COMPLETION_PENDING`은 PM이 이번 P0의 정상 종점으로 승인했습니다.
- 팀 PostgreSQL과 물리 Android 기기 검증은 **P0 100% 완료를 위한 필수 운영환경 Gate**로 남아 있습니다.
- 따라서 현재 상태는 핵심 기능 흐름 PASS이지만, PM 기준 P0 100% 완료 상태는 아닙니다.
- Dashboard·Evidence·AI 실험은 E2E 완료를 우선해 보류하고 다음 주부터 재개합니다.

## 2. 완료된 작업

### Cross-client E2E

- Mobile에서 실제 AI Guidance를 조회했습니다.
- AI가 위험도 `danger`, 사용 안내 `TOTAL_STOP`을 반환했습니다.
- Mobile에서 실제 상담 요청을 생성했습니다.
- 같은 `inquiry_id`가 Web 상담사 목록과 상세에 표시되는 것을 확인했습니다.
- Web에서 상담 시작 → 상담 기록 저장 → 요약 확정 → 상담 완료를 수행했습니다.
- Mobile에서 동일 문의의 최종 `COMPLETION_PENDING`, v8을 재조회했습니다.

### Web

- 실제 Backend Remote 모드로 목록·상세·상담 Action을 연결했습니다.
- Backend의 `state_version`, `allowed_actions`를 기준으로 화면과 Action을 갱신했습니다.
- 새로고침·재조회 후 서버에 저장된 상담 기록이 복구되는 것을 확인했습니다.
- 실제 AI 안내·제한 기능을 상담사 상세 화면에 표시했습니다.
- Public Evidence Runtime이 없을 때 가짜 데이터를 만들지 않고 안전 안내를 표시했습니다.
- Remote 실패가 Mock/Fake 성공으로 바뀌지 않도록 유지했습니다.

### Backend·AI·검증

- 상담 요청 후 미배정 합성 문의가 상담사 Queue에 나타나고, 상담 시작 시 원자적으로 담당자가 선점되도록 검증했습니다.
- 선점 후 다른 상담사의 목록·상세·Action 접근이 차단되는 것을 검증했습니다.
- 위험 안전 규칙과 Evidence 검증 조건을 만족할 때만 위험 상태가 전이되도록 확인했습니다.
- 실제 Web 쓰기 요청 4단계가 모두 HTTP 200이며 Backend correlation 로그와 일치함을 확인했습니다.
- Web 검증 결과: 34개 파일, 155개 테스트, lint, typecheck, production build 모두 PASS입니다.

## 3. 일부 완료된 작업

| 항목 | 완료된 범위 | 남은 범위 |
|---|---|---|
| 최종 E2E 환경 | 격리 SQLite + Android 16(API 36) Emulator에서 PASS | 팀 PostgreSQL + 물리 Android 기기 재검증 |
| 상담사 동시 선점 | 기능·회귀 테스트와 SQLite E2E 확인 | PostgreSQL 실제 row-lock 동시성 확인 |
| Mobile 최종 상태 확인 | 연속 화면 흐름과 명시적 동일 ID 조회 PASS | 홈 복귀·앱 재시작 후 동일 문의 재진입 경로 |
| 상담 요청 중복 방지 | 같은 앱 프로세스 안에서 Idempotency Key 재사용 | 앱 프로세스 재시작 후에도 Key를 유지하는 영속 저장 |
| 최신 환경 재실행 | 실제 G4 완료 로그·DB·테스트 증거 확보 | 로컬 Backend Python 가상환경 복구 후 최신 SHA 재기동 |

## 4. 미진행 작업

### P0 100% 완료 전에 반드시 진행할 작업

- 팀 PostgreSQL 환경에서 전체 동일 Inquiry 공동 Smoke
- PostgreSQL 실제 row-lock 기반 상담사 동시 선점 검증
- 물리 Android 기기에서 네트워크·실행·최종 상태 확인

### P1/P2 또는 다음 주로 분리한 작업

다음 항목은 누락이 아니라 PM 결정에 따라 현재 E2E 완료 이후로 미룬 작업입니다.

- `COMPLETION_PENDING` 이후 `RESOLVED`로 전환하는 Runtime — P1 유지
- Public Evidence 상세 화면과 신규 UI
- Dashboard·AI 검수 UI 고도화 — 다음 주 재개
- Queue·자동 배정·Claim 전용 Web UI
- GPT 모델·임베딩 모델·청킹·Retriever 비교 실험 — 다음 주 재개
- Mobile 홈 복귀·앱 재시작 후 진행 문의 재진입 기능
- Mobile Idempotency Key 영속 저장

## 5. 100% 완료를 위해 담당자별 협의가 필요한 내용

### PM

- `COMPLETION_PENDING`을 P0 정상 종점으로 승인했습니다.
- `RESOLVED` Runtime은 P1 유지로 확정했습니다.
- P0 100% 완료 기준은 PostgreSQL·물리 Android 운영환경 Gate 통과까지로 확정했습니다.
- Dashboard·Evidence·AI 실험은 다음 주 재개로 확정했습니다.
- 남은 협의는 PostgreSQL·물리기기 공동 Smoke 일정과 참여자 확정입니다.

### Backend·DB 담당자

- 팀 PostgreSQL 환경과 최종 Backend SHA·Base URL·Seed를 공유해 주세요.
- 두 상담사가 동시에 상담 시작을 누르는 상황에서 row-lock과 단일 담당자 선점을 공동 검증해야 합니다.
- `RESOLVED` Runtime은 이번 P0에서 제외하고 P1로 유지합니다.
- 로컬 Backend Python 3.13 가상환경 복구 방법 또는 팀 표준 실행환경 공유가 필요합니다.

### Mobile 담당자

- 물리 Android 기기에서 로그인 → AI 안내 → 상담 요청 → 최종 상태 확인을 공동 재검증해야 합니다.
- 홈 복귀·앱 재시작 후 동일 진행 문의를 다시 여는 기능을 P1로 진행할지 협의가 필요합니다.
- 앱 재시작 후에도 Idempotency Key를 유지하는 기능을 P2로 진행할지 협의가 필요합니다.

### AI 담당자

- 현재 실제 AI Guidance 기준선을 E2E 완료 기준으로 계속 유지할지 확인이 필요합니다.
- Public Evidence Runtime을 제공할 경우 공개 가능한 근거 범위와 내부 Chunk·Score 비노출 원칙을 합의해야 합니다.
- 모델·임베딩·청킹·Retriever 비교 실험은 다음 주부터 재개합니다.

### Web 담당자

- Backend 운영환경이 준비되면 동일 Inquiry의 목록·상세·상담 완료·새로고침을 한 차례 더 확인해야 합니다.
- 최종 Backend 계약과 Web DTO의 필드명·null 처리·`allowed_actions`를 다시 대조해야 합니다.
- Public Evidence Runtime 제공 전까지 현재 안전 Fallback을 유지해야 합니다.

### 전체 팀 공동 확인

1. 실제 실행 직전 Mobile·Web·Backend·AI의 통합 SHA를 고정합니다.
2. 동일한 `inquiry_id` 하나로 전체 흐름을 다시 실행합니다.
3. 각 단계의 `state_version`, `allowed_actions`, `correlation_id`를 기록합니다.
4. PostgreSQL DB 상태와 Web·Mobile 최종 화면을 함께 대조합니다.
5. 실패 시 Mock/Fake로 대체하지 않고 실제 차단 원인을 기록합니다.

## 6. 최종 판정

| 기준 | 판정 |
|---|---|
| 업무지침서의 로컬 Cross-client P0 흐름 | **PASS** |
| Web G4 동일 Inquiry 처리 | **PASS** |
| P0 정상 종점 `COMPLETION_PENDING` | **PM 승인 완료** |
| 운영 PostgreSQL 검증 | **미진행 — P0 100% 필수 Gate** |
| 물리 Android 기기 검증 | **미진행 — P0 100% 필수 Gate** |
| PM 기준 P0 100% 완료 | **미완료 — 두 운영환경 Gate 통과 필요** |

현재 핵심 서비스 흐름과 P0 종점은 확정됐습니다. 남은 최우선 작업은 새로운 기능 추가가 아니라 **PostgreSQL과 물리 Android 환경에서 같은 흐름을 공동 검증해 PM 기준 P0 100%를 달성하는 것**입니다. Dashboard·Evidence·AI 실험은 해당 E2E 완료를 방해하지 않도록 다음 주부터 재개합니다.
