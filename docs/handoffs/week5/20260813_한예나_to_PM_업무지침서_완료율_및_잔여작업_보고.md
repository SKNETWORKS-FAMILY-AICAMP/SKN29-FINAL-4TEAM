# 한예나 → PM: 2026-08-13 업무지침서 완료율 및 잔여 작업 보고

> 작성일: 2026-08-13 KST  
> 현재 통합 SHA: `6ae659f12c02c4abc72cb6b2645e1669c76d571d`  
> 현재 `yena` = `origin/yena` = `origin/main`  
> P0 목표: **Mobile 실제 AI 안내 → 상담 요청 → 동일 `inquiry_id` Web 조회 → 상담 시작·기록·완료 → Mobile 최종 상태 확인**

## 1. 결론

업무지침서의 P0 흐름은 통합 작업 SHA `1cd1905`에서 실제로 한 차례 끝까지 완주했습니다. 그러나 현재 최신 `main`인 `6ae659f`에서는 Backend·Mobile 관련 코드와 계약이 변경됐고, Backend 실행환경도 기동되지 않아 동일 흐름을 다시 검증하지 못했습니다.

따라서 현재 판정은 다음과 같습니다.

- 과거 실제 통합 실행: **P0 5단계 모두 PASS**
- 현재 최신 SHA Web 자동검증: **100% PASS**
- 현재 최신 SHA Browser Fail-closed 확인: **PASS** — Backend 미기동 시 문의 목록 오류 표시, 가짜 목록 0건
- 현재 최신 SHA 전체 Remote 공동 Smoke: **BLOCKED / 미실행**
- 관리용 전체 진행률: **약 80%**
  - 검증 단계 5개 중 4개 완료
  - 마지막 1개는 최신 팀 Backend·PostgreSQL 환경의 공동 Smoke
- 현재 최신 SHA 기준 최종 100% 완료 선언: **불가**

이 80%는 기능별 단순 평균이 아니라, `최신 SHA 고정 → Web 검증 → 계약 대조 → 기존 실제 E2E 증거 확인 → 최신 Runtime 공동 Smoke`의 5단계 관리 기준입니다.

## 2. 단계별 완료 현황

| 단계 | 상태 | 결과 |
|---|---:|---|
| 1. 최신 통합 SHA 고정 | 완료 | `yena`, `origin/yena`, `origin/main` 모두 `6ae659f` |
| 2. 현재 Web 품질 검증 | 완료 | 34 files / 155 tests, lint, typecheck, build PASS |
| 3. Web↔Backend 계약·경로 정적 대조 | 완료 | 상담 Action 4개 Method·Path 일치 |
| 4. 동일 Inquiry 실제 E2E 증거 확보 | 완료 | `1cd1905` Runtime에서 동일 ID v4→v8 실제 처리 |
| 5. 최신 SHA·팀 PostgreSQL 공동 Smoke | 차단 | Backend 미기동·가상환경 손상·현재 계약 차이 |

## 3. 완료된 작업

- Mobile에서 실제 AI 위험 안내를 받은 문의를 생성했습니다.
- Mobile에서 상담 요청을 실행했습니다.
- 동일한 `inquiry_id`를 Web 상담사 목록·상세에서 확인했습니다.
- Web에서 상담 시작 → 기록 저장 → 요약 확정 → 상담 완료를 실행했습니다.
- 최종 상태 `COMPLETION_PENDING`, v8을 확인했습니다.
- Mobile에서 같은 문의의 최종 상태를 재조회했습니다.
- Remote 실패가 Mock/Fake 성공으로 대체되지 않도록 했습니다.
- Public Evidence가 없을 때 가짜 근거를 만들지 않았습니다.
- Web 전체 테스트·lint·typecheck·build를 현재 SHA에서 다시 통과했습니다.
- Web과 Backend의 상담 Action 4개 경로가 일치함을 확인했습니다.
- 실제 Browser에서 Backend 연결 실패가 가짜 성공이나 Mock 목록으로 바뀌지 않고 오류로 표시되는 것을 확인했습니다.

## 4. 현재 최신 SHA에서 일부 완료된 작업

| 항목 | 확인된 부분 | 미확인·문제 부분 |
|---|---|---|
| Web 상담 Action | Method·Path·요청 DTO·최신 `state_version` 사용 확인 | 최신 Backend 실서버 요청 미실행 |
| 상담 기록 지속성 | 과거 실제 Runtime에서 새로고침 복구 PASS | 현재 Backend 상세가 `consultation: null`로 고정되어 최신 SHA 보장 불가 |
| AI 제한 기능 표시 | Web은 필드 표시 가능 | 현재 Backend가 `restricted_functions: []`로 고정 |
| Mobile 실제 Guidance | 과거 통합 Runtime에서 PASS | 현재 계약·Backend에 고객 Guidance 조회 Route 없음 |
| 상담사 권한 경계 | 과거 Runtime·회귀 테스트에서 확인 | 최신 PostgreSQL 실제 다중 상담사 Smoke 미실행 |

## 5. 미진행 또는 차단된 작업

- 최신 `6ae659f` Backend를 기동한 Browser Remote 공동 Smoke
- 팀 PostgreSQL에서 동일 Inquiry 처리
- PostgreSQL row-lock 기반 상담사 동시 선점 검증
- 물리 Android 기기의 전체 흐름 재검증
- 현재 Backend에서 저장된 상담 기록의 상세 재조회 검증
- 현재 Backend에서 실제 AI 제한 기능 표시 검증
- 현재 계약 기준 Mobile 실제 Guidance 조회 검증

PM 지침에 따라 의도적으로 미진행한 항목:

- Dashboard·AI 검수 UI 고도화
- Public Evidence 신규 화면
- GPT·임베딩·청킹·Retriever 비교 실험
- Queue·자동 배정 전용 Web UI
- `COMPLETION_PENDING` 이후 `RESOLVED` Runtime

## 6. 최신 공동 Smoke가 막힌 직접 원인

1. Backend `127.0.0.1:8000` Listener가 없습니다.
2. `backend/.venv`가 삭제된 Python 3.13 경로를 가리켜 실행되지 않습니다.
3. 다른 Python Runtime에 기존 패키지를 연결하면 `psycopg`/`libpq` Binary 호환 오류가 발생합니다.
4. 현재 Backend 상담 상세 Projection은 `consultation`을 항상 `null`로 반환합니다.
5. 현재 Backend 상담 상세는 `restricted_functions`를 항상 빈 배열로 반환합니다.
6. 현재 계약·Backend에는 고객 소유 범위의 실제 Guidance 조회 Route가 없습니다.

## 7. 100% 완료를 위해 필요한 결정과 협의

### PM 결정

- 최신 SHA 공동 Smoke까지 완료해야 P0 최종 PASS로 선언하는지 확인이 필요합니다.
- 현재 사라진 고객 Guidance Route를 P0로 복구할지 결정이 필요합니다.
- 상담 기록 재조회 Projection을 P0 완료 조건으로 유지할지 확인이 필요합니다.
- `COMPLETION_PENDING`을 상담 완료의 정상 최종 상태로 계속 인정할지 확인이 필요합니다.

### Backend·DB 담당자 협의

- 실행 가능한 Backend 환경, Base URL, 최종 SHA, PostgreSQL Seed를 전달받아야 합니다.
- 고객 Guidance 조회 Route 제공 여부를 확정해야 합니다.
- 상담사 상세에서 실제 `consultation`과 `restricted_functions`를 반환하도록 할지 합의해야 합니다.
- 두 상담사의 동시 선점·404 경계를 PostgreSQL에서 공동 검증해야 합니다.

### Mobile 담당자 협의

- 현재 계약에서 실제 Guidance를 어떤 Route로 받을지 확정해야 합니다.
- Backend 준비 후 물리기기 또는 Emulator에서 상담 요청과 최종 상태를 다시 확인해야 합니다.

### Web 담당 후속

- Backend 환경을 전달받는 즉시 로그인·목록·상세·4개 상담 Action·새로고침을 재실행합니다.
- 각 요청의 `state_version`, `allowed_actions`, `correlation_id`를 Backend 로그와 대조합니다.
- 실패 시 Mock으로 전환하지 않고 실제 오류를 기록합니다.

## 8. PM 확인 요청

현재 Web에서 단독으로 가능한 작업은 모두 완료했습니다. 아래 세 가지를 PM 기준으로 확정해 주시면 마지막 공동 Smoke를 진행할 수 있습니다.

1. 고객 Guidance 조회 Runtime을 P0에 복구할지
2. 상담 상세의 실제 상담 기록·제한 기능 Projection을 P0에 포함할지
3. Backend·PostgreSQL 최신 환경 공동 Smoke 일정을 언제로 잡을지
