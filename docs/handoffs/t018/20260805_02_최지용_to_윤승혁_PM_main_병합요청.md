# 최지용 → 윤승혁 PM: T-018 R1 main 병합 요청

## 요청 요약

이번 요청은 `T018-D01~D06` 재결정 요청이 아닙니다. PM이 이미 승인한 T-018 R1 기계 계약을 팀 기준선에 게시하기 위한 `jiyong → main` 병합 요청입니다.

| 항목 | 값 |
|---|---|
| 병합 원본 | `origin/jiyong` |
| 병합 대상 | `main` |
| 최신 main 기준선 | `3d7b45a19b3a42ff03eea0642db5a6adeb660a13` |
| 계약 코드 Commit | `2c508585a93a29645fb9f6081f96c51141f44530` |
| 계약 범위 | 고객 본인 ACTIVE 구독 목록·상세 GET 2개 |
| Runtime·Migration·DB·Seed 변경 | `0` |

Push 후 최종 원격 `jiyong` HEAD는 최지용이 전달 메시지에서 별도로 공유합니다.

## 병합 대상 내용

- 활성 `WPUJAC104DWH` 모델의 본인 소유 ACTIVE 구독만 조회
- 최근 관리일은 `Asia/Seoul` 업무일 기준
- Eligibility는 T-022, `allowed_actions`는 T-023 후속 계약
- OpenAPI `0.7.0`, Operation Inventory `23/8/15`
- Schema·Example·Contract Test 포함
- T-018 두 Operation의 Runtime 상태는 계속 `NOT_IMPLEMENTED`

## 검증 증거

| 검증 | 결과 |
|---|---|
| T-018 단독 | `7 passed` |
| 핵심 계약 | `33 passed` |
| 확장 계약 | `52 passed` |
| API 전체 | `131 passed, 2 skipped` |
| Backend 전체 | `798 passed, 13 skipped` |
| Django·Migration | 문제 0, 변경 없음 |

## PM 요청사항

1. `origin/jiyong`의 최종 Push SHA와 위 계약 Commit 포함 여부를 확인해 주세요.
2. 이상이 없으면 `jiyong → main`을 병합해 주세요.
3. 아래 형식으로 병합 결과를 회신해 주세요.
4. 병합 후 김은진·한예나에게 최신 main Pull 가능 여부를 안내해 주세요.

```text
T018_R1_MAIN_MERGE=MERGED | HOLD
merged_main_commit=<병합 후 main SHA>
merge_commit=<merge commit 또는 squash commit SHA>
runtime_implementation_start_allowed=false
note=<필요 시>
```

## 경계와 Rollback

- PM 병합 후 상태는 `MAIN_MERGED_REVIEW_PENDING`입니다. 팀원 재검증 전에는 최종 팀 계약 통과가 아닙니다.
- 병합만으로 Backend Runtime·DB·Migration·Web 연결 작업을 시작하지 않습니다.
- 문제 발생 시 DB 역적용은 필요하지 않으며, PM이 해당 Merge/Squash Commit을 Revert하는 방식으로 되돌릴 수 있습니다.
