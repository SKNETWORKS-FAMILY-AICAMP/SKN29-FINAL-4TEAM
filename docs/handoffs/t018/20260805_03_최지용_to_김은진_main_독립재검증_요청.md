# 최지용 → 김은진: T-018 R1 main 독립 재검증 요청

## 시작 조건

```text
start_condition=PM_MERGED_TO_MAIN
qa_contract_test_passed=false
contract_test_passed=false
```

PM이 `jiyong → main` 병합 완료와 main SHA를 회신한 뒤 시작해 주세요. 임시 브랜치나 과거 계약 Commit이 아니라 병합 후 최신 main이 검토 기준입니다.

## 검토 준비

```powershell
git switch main
git pull --ff-only origin main
git rev-parse HEAD
```

회신에는 반드시 `reviewed_main_commit=<확인한 SHA>`를 기록해 주세요.

## 확인할 계약

- `GET /api/v1/me/subscriptions`
- `GET /api/v1/me/subscriptions/{subscription_id}`
- 본인 소유·활성 `WPUJAC104DWH`·`ACTIVE_ONLY`
- 최근 관리일 `Asia/Seoul` 업무일 규칙
- T-022 Eligibility / T-023 `allowed_actions` 책임 분리
- OpenAPI `0.7.0`, Inventory `23/8/15`
- T-018 Runtime은 `NOT_IMPLEMENTED`
- Runtime·Migration·DB·Seed 변경 0

## 권장 검증 명령

저장소의 `backend` 폴더에서 실행해 주세요.

```powershell
$env:PYTHONDONTWRITEBYTECODE = '1'
.\.venv\Scripts\python.exe -m pytest -q tests/api/test_t018_product_subscription_contract.py
.\.venv\Scripts\python.exe -m pytest -q tests/api/test_g2_machine_contract.py tests/api/test_openapi_runtime_coverage.py tests/api/test_runtime_examples_contract.py tests/api/test_openapi_common_contract.py tests/api/test_t018_product_subscription_contract.py
.\.venv\Scripts\python.exe -m pytest -q tests/api
.\.venv\Scripts\python.exe manage.py check --settings=config.settings.test
.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run --settings=config.settings.test
```

가능하면 Backend 전체 `pytest -q`도 실행해 주세요. 작성자 기준 결과는 T-018 7건, 핵심 33건, API 전체 131건 통과입니다.

## 회신 형식

```text
T018_R1_QA_REVIEW=PASS | FAIL | HOLD
reviewed_main_commit=<PM 병합 후 main SHA>
commands=<실행 명령>
exit_codes=<명령별 Exit Code>
test_result=<passed/skipped/failed>
inventory_23_8_15=PASS | FAIL
runtime_not_implemented=PASS | FAIL
migration_drift=NONE | FOUND
mismatches=<없으면 NONE>
qa_contract_test_passed=true | false
```

FAIL/HOLD이면 코드 수정보다 먼저 불일치 파일·항목·재현 명령을 알려 주세요. 이 요청은 검증 요청이며 Data·DB·Runtime 변경 요청이 아닙니다.
