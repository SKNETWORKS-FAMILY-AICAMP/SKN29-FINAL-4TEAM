# 최종 프로젝트 데일리 스크럼 7주차 요약 보고서 (8/24 ~ )

* 4팀 대주제 : 다중 에이전트 서빙 기반 고객 피드백 분석 및 맞춤형 응대 자동화 시스템

## 8월 24일 (월) - 기능 보완·통합 검증 및 운영 배포 준비

* **진행 업무**
  * Backend는 회원가입 기능과 계정 연결 API 계약을 정리하고, 상담사 Web 연동에 필요한 Backend 기능을 보완함
  * Mobile은 주요 고객 앱 기능을 대상으로 테스트를 진행하고 전반적인 정상 동작을 확인함
  * PM·AI는 지금까지 구현된 기능과 기술스택 전반을 다시 점검하면서 Harness·HITL·Consultation Handoff 등 주요 AI Runtime의 실제 동작과 검증 증거를 확인함
  * Web은 상담사 화면 디자인과 메뉴 표시를 개선하고, 공지사항 상세 조회·문의 상세·상담 처리 흐름을 최신 Backend 계약에 맞게 정리함
  * AI·RAG는 AI 계약·평가 Gate를 강화하고 Retriever·Provider Runtime 증거의 무결성을 보강하면서 Neo4j 도입과 배포 방식도 함께 검토함
  * QA·DevOps는 EC2·RDS·PostgreSQL·pgvector 운영 환경과 Migration·권한 상태를 점검하고, AI Evidence Fixture 생성과 실제 Container 배포를 준비함
  * 전체적으로 신규 기능 구현보다는 기존 구현을 최신 `main` 기준으로 정합화하고, 실제 Runtime·CI·배포 단계에서 동작하는지를 확인하는 검증 작업에 집중함

* **담당자별 주요 작업 내용**
  * **윤승혁 (PM·AI)**
    * 프로젝트 전체 구현 현황과 기술스택 적용 상태를 다시 점검하고 기능별 검증·테스트를 진행함
    * Harness·HITL·Consultation Handoff 담당 영역의 표적 테스트를 실행하여 H01~H08 Case를 검증하고 `15 passed`를 확인함
    * 동일 최종 Commit의 GitHub Actions `AI Backend Socket E2E` 성공 Run을 확인하여 실제 FastAPI ↔ Django HTTP 통신, Backend Handoff 수신, PostgreSQL 저장, Consultation 연결과 상담사 Projection까지 검증함
    * H01~H09를 모두 PASS로 정리하고 Runtime 증거 JSON과 SHA-256을 생성하여 담당 영역의 `OWNER_EVIDENCE_REQUIRED` 검증을 완료함
    * Windows 로컬 회귀에서 MCP stdio-context 관련 1건의 기존 실패가 존재함을 별도 기록하고, 동일 Commit의 Linux CI 전체 AI Unit 성공 결과와 분리하여 증거화함

  * **최지용 (Backend·DB)**
    * 회원가입 기능을 보완하고 계정 연결 API의 계약과 연동 구조를 정리함
    * 고객 계정과 기존 계약을 연결하기 위한 API 흐름을 최신 Backend 기준에 맞게 정합화함
    * 상담사 Web 연동에 필요한 Backend 기능을 추가로 보완하여 Web에서 최신 상태와 데이터를 사용할 수 있도록 연결 범위를 정리함

  * **한예나 (Web Frontend)**
    * 상담사 화면의 디자인과 메뉴 선택 표시를 개선함
    * 공지사항 선택 시 상세 내용을 바로 조회할 수 있도록 Backend API와 연결함
    * 문의 상세와 상담 처리를 별도 페이지가 아닌 하나의 화면에서 이어서 처리하도록 화면 흐름을 정리함
    * 제거된 `전체 기록 보기` 기능과 관련된 테스트를 함께 정리함
    * 상담사 배정 상태가 최신 Backend 방식과 일치하도록 수정함
    * 최신 `main`을 개인 브랜치에 반영하고 Web 테스트 `252 passed, 4 excluded`를 확인함
    * Lint·TypeCheck·Build를 모두 통과하고 최종 변경사항을 `yena` 브랜치에 Push함
    * Playwright 테스트 코드를 최신 Backend 방식에 맞게 수정했으나, 새 격리 DB와 배정 전 신규 문의 데이터가 아직 준비되지 않아 실제 Browser E2E 실행은 보류함
    * 기존 DB와 보관 중인 테스트 자료는 Reset하지 않고 안전하게 유지함
    * 최종 작업 Commit은 `873cb775`임

  * **이동윤 (AI·RAG)**
    * AI 계약과 평가 Gate를 강화하여 Runtime 검증 기준을 보완함
    * Retriever·Provider Runtime 증거의 무결성과 재현성을 높이기 위한 검증 작업을 진행함
    * Neo4j 도입 가능성과 현재 시스템에 적용할 범위를 검토함
    * AI Runtime과 운영 배포 구조를 함께 고려하여 배포 방식을 검토함

  * **양정현 (Mobile Frontend)**
    * Mobile 주요 기능을 대상으로 테스트를 진행함
    * 고객 앱의 주요 화면과 기능 흐름이 전반적으로 정상 동작하는 것을 확인함
    * 기존 Mobile 구현의 안정성을 확인하며 후속 통합 검증을 위한 상태를 점검함

  * **김은진 (QA·Data·DevOps)**
    * EC2의 Docker·Nginx·AWS CLI·RDS TLS 환경을 점검함
    * 운영 RDS의 백업 및 Snapshot 상태를 검증함
    * 운영 DB 기준을 PostgreSQL `16.14`, pgvector `0.8.2`로 확정함
    * 승인 Migration 6개를 적용·검증하고 `evidence.0013` 적용과 `visits.0005` HOLD 상태를 유지함
    * DB 역할과 신규 Table·View 권한을 다시 정합화함
    * 배포 자동화 정적 테스트 `12/12 PASS`를 확인함
    * EC2에서 AI Evidence Fixture 53건 생성을 진행 중임
    * 이후 Evidence Import, `53/53` 검증과 `web`·`backend`·`ai`·`trace-store` 4개 Container의 실제 배포를 진행할 예정임

## 8월 25일 (화) - 핵심 상담 흐름 확인 및 운영 배포·CI 개선

* **진행 업무**
  * 고객이 문의를 등록하면 AI가 내용을 분석하고, 고객의 상담 요청을 상담사가 확인하여 응대하는 전체 흐름을 실제로 연결하고 사람이 직접 확인함
  * 상담 종료 후 고객이 상담 내용을 다시 확인하는 기능은 아직 확인되지 않아 추가 구현 여부를 점검하기로 함
  * Mobile은 개발용 앱과 AWS용 앱의 연결 설정을 분리하고 회원가입 인증 화면을 개선함
  * Web은 상담사 화면 배치를 수정하면서 Backend·Mobile의 실제 데이터를 연결하고 불필요한 표시 정보를 정리함
  * Backend는 상담 연결 과정에서 사용하는 G2~G4 단계의 검사 기준을 수정함
  * AI는 Neo4j 코드 보강, 문서 분할 방식 비교 실험과 MCP 연결 상태 확인을 진행함
  * QA·Data·DevOps는 3모델 데이터 조회와 AI 읽기 권한을 검증하고, 실제 배포 과정에서 발견된 AWS·저장소·인증서 설정 오류를 수정함
  * GitHub 검사는 변경된 영역에 필요한 테스트만 실행하고 Backend 테스트를 세 그룹으로 나누어 동시에 실행하도록 개선함
  * 자동 배포는 `main`에 코드가 반영될 때마다 실행하는 방식에서 정식 버전 태그가 등록됐을 때만 실행하는 방식으로 변경함

* **담당자별 주요 작업 내용**
  * **윤승혁 (PM·AI)**
    * 고객 문의 등록 → AI 분석 → 상담 연결 요청 → 상담사 문의 확인·응대까지 이어지는 핵심 E2E 흐름을 직접 확인함
    * 상담이 끝난 뒤 고객이 상담 내용을 다시 확인하는 기능은 아직 확인되지 않은 상태로 정리함
    * 앞으로 화면 사용 과정에서 불편한 부분을 적극적으로 점검하고, 여러 UI·UX 방식을 비교하는 실험을 진행할 예정임

  * **최지용 (Backend·DB)**
    * 고객 문의와 AI 분석 결과가 상담사에게 전달되는 G2~G4 단계의 검사 기준을 수정 중임
    * 각 단계에서 데이터가 정상적으로 저장되고 다음 단계로 넘어가는지 확인할 수 있도록 Backend Gate를 보완하고 있음

  * **한예나 (Web Frontend)**
    * 상담사 Web 화면의 전체 배치와 표시 방식을 수정함
    * Backend와 Mobile에서 전달되는 실제 데이터를 Web 화면에 연결하는 작업을 진행함
    * 화면에 표시되는 코드와 내부 정보 중 상담사에게 필요하지 않은 내용을 제거할 예정임

  * **이동윤 (AI·RAG)**
    * Neo4j 데이터 연결 관계를 더 안전하게 검사할 수 있도록 관련 코드를 보강함
    * 문서를 어떤 크기로 나누는 것이 AI 검색에 더 적합한지 비교하는 청킹 실험을 진행함
    * AI와 검색 도구를 연결하는 MCP가 정상적으로 연결되는지 상태를 확인함

  * **양정현 (Mobile Frontend)**
    * 휴대폰이 USB 케이블 없이도 서버에 연결될 수 있도록 개발용 APK와 AWS용 APK의 설정을 분리함
    * AWS용 앱은 `https://waterbridge.site/`의 Backend만 사용하도록 연결 기준을 정리함
    * 회원가입 인증번호가 틀려도 다시 입력할 수 있고, 이메일이 오지 않으면 인증번호를 재전송할 수 있도록 화면을 개선 중임

  * **김은진 (QA·Data·DevOps)**
    * 정수기 3모델의 AI 안내 데이터 53건이 정상적으로 조회되는지 확인함
    * AI가 허용된 View만 읽고 원본 Table 조회나 데이터 수정은 할 수 없도록 권한을 검증함
    * `evidence.0014` Migration을 적용하고 `visits.0005`는 P1 HOLD 상태로 미적용 유지함
    * 실제 배포 과정에서 AWS 권한, S3 저장소 설정, TLS·RDS 인증서와 ECR 인증 오류를 찾아 수정함
    * Neo4j JSONL 데이터 연결 관계를 자동 검사하는 QA Workflow와 검증 도구를 구성함
    * 변경된 영역에 필요한 검사만 실행하도록 Backend CI 변경 판정 기능을 구현함
    * 약 1,500개의 Backend 테스트를 세 그룹으로 나누어 동시에 실행하도록 CI 구조를 개선함
    * 자동 배포는 정식 버전 태그에서만 실행하고, 배포 전에 Backend·Web·AI·Data 검사를 통과하도록 방식을 변경함
    * 기존 전체 회귀 `1,503 passed, 41 skipped`와 병렬 실행 결과가 일치하는지 확인함
    * 로컬 측정 기준 Backend 테스트 시간을 약 43% 줄였으며, Contract·Data·CI 정적 검증을 통과함
    * CI 개선 Commit `d9d3af3f`를 `eunjin` 브랜치에 Push하고 `main` 병합까지 완료함

## 8월 26일 (수) - AI 안전 검토·HITL 구현 및 운영 배포·화면 개선

* **진행 업무**
  * AI 답변의 위험도에 따라 바로 안내할지, 사람의 검토를 기다릴지, 상담으로 연결할지를 구분하는 응답 흐름을 보완함
  * Backend에 AI 답변 검토 원장과 승인·수정·거절 기능을 구현하고, 같은 요청이 중복 처리되지 않도록 동시성·재요청 보호 기능을 추가함
  * 승인된 Safety·HITL DB 변경사항을 AWS RDS에 적용하고, 운영 DB의 Table·Migration·AI 검색 데이터 상태를 함께 점검함
  * AI·RAG는 Safety Rule 우선순위와 검토 경계를 정리하고, 평가용 정답지 재작성·청킹 실험·멀티 에이전트 고도화를 진행함
  * Web은 상담사 화면의 사용성을 개선하고 실제 Backend·격리 DB를 이용한 로그인과 고객 검색 연결 상태를 확인함
  * Mobile은 화면의 무반응 요소와 중복 동작을 정리하고, OTP·문진·케어관리 흐름을 개선함
  * QA·Data·DevOps는 AI 검색 데이터 검증, CI·AWS 배포 절차 강화, Backend `v0.1.3` 운영 배포와 운영 DB 점검을 진행함
  * PM은 Harness 로직을 개선하고 최종 발표용 PPT 초안을 작성함
  * 코드·DB·운영 배포 단계의 진전은 확인했으며, HITL 전체 흐름의 실제 연동과 Web·Mobile 소비 화면, 독립 QA는 후속 검증이 필요함

* **담당자별 주요 작업 내용**
  * **윤승혁 (PM·AI)**
    * AI 답변을 평가하는 Harness 로직을 개선함
    * 최종 프로젝트 발표용 PPT 초안을 작성 중임

  * **최지용 (Backend·DB)**
    * AI 답변을 일반·주의·위험으로 구분하여 안전한 답변은 안내하고, 주의 답변은 사람의 검토를 기다리며, 위험 답변은 상담으로 연결하는 Routing·`PRE_SEND` 흐름을 구현함
    * 온수·히터 관련 위험 상황에서 일부 기능만 제한할지 전체 동작을 멈출지 구분하고, 제품 적용 범위와 부정 표현을 잘못 판단하지 않도록 Safety Rule을 보완함
    * AI 답변의 승인·수정·거절 이력을 남기는 HumanReview 원장과 목록·상세·결정 기능을 구현함
    * 여러 검토자가 동시에 처리하거나 같은 요청이 다시 들어와도 결과가 중복 반영되지 않도록 행 잠금·멱등성·재처리 보호 로직을 추가함
    * 승인된 DB Migration 2건을 AWS RDS에 적용하여 승인 대상 `98/98` 적용을 확인하고, 보류 대상인 `visits.0005`는 미적용 상태로 유지함
    * 운영 RDS를 읽기 전용으로 점검하여 Table 68개, Migration 98개, View 1개와 AI 검색 데이터 53건의 상태를 확인함
    * Web 팀원이 개인 PC에서 실제 Backend와 격리 DB를 재현할 수 있도록 Docker 기반 연결 가이드를 정리함. 새 PC에서의 최종 재현 확인은 남아 있음
    * Web의 전화 문의 연동 요청을 검토했으며, 저장 항목과 공개 범위는 Backend·PM 정책 확정 후 연결하기로 함
    * 오늘 작성한 Backend·DB 변경 8건은 최신 `main`에 모두 반영되었고, `jiyong` 브랜치에만 남아 있는 추가 Commit은 없음
    * Backend·AI 회귀 테스트는 통과했지만, AI의 영속 Checkpoint·재개 처리와 Web·Mobile 연동, 독립 QA까지 포함한 HITL 전체 완료 판정은 아직 남아 있음

  * **한예나 (Web Frontend)**
    * 상담사 화면의 디자인과 사용 편의성을 개선함
    * 실제 Backend와 격리 DB 환경에서 로그인과 고객 검색이 연결되는 상태를 확인함

  * **이동윤 (AI·RAG)**
    * AI 응답 Routing과 `PRE_SEND` 검토 경계가 Safety Rule 우선순위와 맞도록 정합화함
    * AI 검색·응답 평가에 사용할 정답지를 다시 만들고 문서 청킹 비교 실험을 진행함
    * 여러 AI 역할이 협업하는 멀티 에이전트 구조를 고도화하고 있음

  * **양정현 (Mobile Frontend)**
    * 반응하지 않는 아이콘과 중복 CTA를 정리하고, 잘리는 문구와 케어 등록 확인창을 수정함
    * OTP 안내와 재입력 흐름을 더 이해하기 쉽게 개선함
    * 문진 시작 전에 선택한 항목이 실제 문진 작성 과정에 반영되도록 연결함
    * 케어관리 현황을 한눈에 확인할 수 있는 화면을 개발 중임

  * **김은진 (QA·Data·DevOps)**
    * 정수기 3개 모델의 AI 검색 데이터 132건을 정리하고 전체 검증을 완료함
    * CI 오류를 더 빠르게 확인할 수 있도록 점검 방식을 개선하고, AWS 배포 권한과 검증 절차를 강화함
    * Backend Container 이미지에서 필수 계약 파일이 누락된 문제를 해결하고 `v0.1.3` 운영 배포를 완료함
    * 운영 DB를 점검하여 AI 검색 데이터 53건과 서비스별 데이터 상태를 확인함
    * 프로젝트 프론트 랜딩페이지를 디자인 중임
    * 팀원이 RDS에 안전하게 연결할 수 있도록 연결 가이드를 작성 중임
