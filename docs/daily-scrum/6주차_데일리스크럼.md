# 최종 프로젝트 데일리 스크럼 6주차 요약 보고서 (8/18 ~ 8/21)

* 4팀 대주제 : 다중 에이전트 서빙 기반 고객 피드백 분석 및 맞춤형 응대 자동화 시스템

## 8월 18일 (화) - 6주차 사용자 화면 보완 및 공용 RDS·AI Runtime 검증 준비

* **진행 업무**
  * Web은 상담사 문의 화면의 날짜 정보와 분류별 선택 기능을 보완하고, 목록·상세 화면의 정보 가시성을 개선함
  * Mobile은 고객이 작성하거나 제출한 문의 답변이 화면 전환 뒤 사라지지 않도록 상태·이동 흐름을 보완하고, 로그인 만료 상황의 동작을 검증함
  * Backend·DB는 인증·권한 Migration을 재현 가능한 절차로 정리하면서 RDS 적용을 준비하고, Web·Mobile·AI가 함께 계약을 검증할 경로를 마련함
  * AI·RAG는 3개 역할 기반 Multi-Agent 후보 Runtime의 1차 구현과 실제 연동 비교를 진행하고, pgvector 접속 장애 시 비밀값이 오류·로그에 노출되지 않도록 방어 경계를 보강함
  * QA·DevOps는 RDS 환경 구성과 후속 업무 인계를 진행하고, EC2 배포를 GitHub Actions CI/CD로 자동화하기 위한 후속 작업을 계획함
  * PM은 현재 Backend의 요청 처리·상태 전이·AI 연동 흐름을 팀원들에게 다시 공유하고 6주차 담당자별 업무 범위를 정리함

* **담당자별 주요 작업 내용**
  * **윤승혁 (PM)**
    * 고객 Mobile 문의부터 Backend 상태 저장·AI 처리·상담사 Web 연결까지의 전체 Backend 중심 흐름을 팀원들에게 리마인드함
    * 6주차 담당자별 업무 지침을 정리하고, 구현·실제 Runtime 검증·운영 적용 상태를 구분해 보고하도록 기준을 공유함
  * **최지용 (Backend·DB)**
    * 인증·권한을 포함한 Migration을 동일한 순서로 재현할 수 있도록 실행 흐름과 검증 경계를 정리함
    * 보류 중인 `visits.0005`를 제외하고 승인된 Migration만 적용할 수 있는 Allowlist 경로를 구현·격리 검증함
    * RDS 환경 적용을 준비하면서 Web·Mobile·AI가 같은 계약과 데이터 기준으로 검증할 수 있는 공동 경로를 마련 중임
  * **한예나 (Web Frontend)**
    * 상담사 대시보드와 전체 문의 화면에서 날짜 정보와 문의 분류별 선택·필터 기능을 보완함
    * 문의 목록·상세의 글자, 배지, 상태 정보와 주요 행동 영역이 더 잘 보이도록 화면 가시성과 레이아웃을 개선함
    * 최근 관리일 표시와 존재하지 않는 리소스의 404 처리에 대한 테스트를 보강함
  * **이동윤 (AI·RAG)**
    * 증상 분석·근거 분석·케어 판단의 3개 역할과 Supervisor Handoff를 갖는 Multi-Agent 후보 Runtime을 1차 구현함
    * 후보 Runtime을 실제 OpenAI·Readonly pgvector·Backend 경로에서 검증했으나, 기본 Runtime은 계속 `single_rag`로 유지하고 운영 전환은 승인 전까지 보류함
    * pgvector 연결·권한·검색 오류에서 DSN·Driver 원문 등 비밀값이 응답, 로그, 예외 연쇄에 노출되지 않도록 보호 오류 경계를 보강함
  * **양정현 (Mobile Frontend)**
    * 고객 문의의 추가 답변과 작성 중 입력이 오류·충돌 또는 화면 이동 뒤 사라지지 않도록 상태 보존과 재시도 흐름을 보완함
    * 문의 화면에서 안내 화면으로 이동한 뒤 뒤로 가기 동작이 꼬이지 않도록 Navigation Back Stack을 정리함
    * 로그인 만료 시 사용자 안내와 후속 동작이 정상적으로 처리되는지 관련 테스트로 검증함
  * **김은진 (QA·DevOps)**
    * 공용 RDS 환경 세팅과 팀원이 이어서 실행할 수 있는 작업 인계 절차를 진행 중임
    * RDS 연결·권한·Migration·복구 검증 범위를 정리하고, 환경 구성만으로 전체 E2E 완료로 판정하지 않도록 기준을 유지함
    * EC2 인스턴스 배포를 GitHub Actions CI/CD로 자동화하는 작업은 후속 예정 범위로 분리함

## 8월 19일 (수) - 3모델 RAG 확장과 상담 Runtime·반복 E2E 기반 정비

* **진행 업무**
  * 기존 한 개 제품 중심의 흐름을 세 가지 정수기 판매코드까지 확장하기 위해 공식 데이터, RAG 검색 후보, 모델별 평가 Case와 활성화 경계를 정리함
  * Backend·DB는 3제품 Fixture를 읽으면서도 현재 승인된 제품만 선택 적재하도록 Importer를 보완하고, 공식 케어 주기와 상담사 Dashboard API를 `main`에 반영함
  * 상담 완료 문의를 Reset하거나 재사용하지 않고 실행마다 새로운 상담 문의를 만드는 Web E2E Fixture와 최신 Migration Allowlist를 구현·검증 중임
  * AI 흐름은 정확 판매코드를 Vector 검색 전에 검사하고 다른 모델의 근거가 섞이지 않도록 RAG 확장, Capability Gate, Harness·HITL·Handoff Runtime을 보강함
  * Web은 상담원 화면 기능·레이아웃 개선을 마치고, 실행별 신규 Fixture를 사용하는 Playwright 상담 E2E 범위와 역할 분담을 확정함
  * Mobile은 고객 앱 Android 테스트·Physical/Remote Smoke와 UI 캡처를 진행하고, MCP SDK 기반 Server와 `health_check` Tool을 구현·검증함
  * QA·DevOps는 3모델 데이터·RAG와 Backend PostgreSQL 독립 QA를 완료하고, EC2·SSM·ECR·OIDC 및 4개 Container 배포 구조를 점검함

* **담당자별 주요 작업 내용**
  * **윤승혁 (PM·AI)**
    * Harness를 실제 AI Pipeline 뒤에 연결하여 결과 검사, 검색·답변 생성 재시도와 상담 전환이 이어지도록 Runtime 흐름을 정리함
    * 검색 재시도 1회와 답변 생성 재시도 1회를 적용하고, 두 단계 뒤에도 해결되지 않으면 상담으로 전환하도록 규칙을 반영함
    * IAC425 질문에 IAC606 근거가 섞이는 것과 같은 교차 제품 Evidence를 차단하고, 위험 증상은 근거가 부족해도 재검색하지 않고 즉시 `TOTAL_STOP` 안내 후 상담으로 연결하도록 오류 흐름을 수정함
    * HITL·Handoff와 3제품 Runtime 처리 범위를 점검하고, IAC425·IAC606을 실제 RAG Runtime에 열기 위한 설정·검증 항목을 이동윤에게 인계함
    * Harness Runtime 통합 `7 passed`, Harness·HITL·Handoff `25 passed`, 기존 Pipeline 회귀 `25 passed`를 확인함
    * 세 모델을 Public API·Backend Runtime 사용 범위로 정하되 신규 모델 Care 일정은 Rule 승인 전까지 비활성으로 유지하고, Backend·AI·Evidence E2E 통과 후 최종 활성 완료로 판정하도록 기준을 확정함
  * **최지용 (Backend·DB)**
    * `WPUJAC104DWH`의 승인된 4개월 대표 케어 주기를 Runtime에 반영하고, 제품·관리 방식·케어 유형이 정확히 일치할 때만 다음 케어일을 계산하도록 Fail-closed 경계를 구현함
    * 물리 Product Fixture 3건을 유효한 입력으로 읽되 기존 `db-smoke`·`db-full`에는 승인된 JAC104 한 건만 선택 적재하도록 합성데이터 Importer를 수정·검증함
    * 상담사 Dashboard 합성 Seed·Projection·API와 `operations.0002` Migration을 구현하여 `main`에 반영함
    * 최신 `operations.0002`를 Migration Allowlist에 포함하면서 `visits.0005` HOLD를 유지하는 정합화와, 고유 `run_id`마다 새 상담 문의를 생성하는 Web G4 E2E Fixture를 로컬에서 구현·표적 검증함
    * T-021 `CARE_PRECHECK`는 기존 Model·Migration·상태 계약을 확인하고, 문의 없이 시작·임시 저장·제출한 뒤 필요한 경우 새 Inquiry에 연결하는 API 수직 Slice를 설계·착수함
  * **한예나 (Web Frontend)**
    * 팀원 피드백을 반영하여 상담원 화면 기능과 레이아웃 개선을 완료함
    * Playwright E2E에 필요한 Backend·QA 준비사항을 확인하고, Backend에 최신 가이드와 실행별 신규 테스트 문의 생성 기능을 요청함
    * Backend는 신규 테스트 문의와 Fixture JSON, Web은 Playwright 자동화, QA는 CI와 결과 보관 기준을 담당하도록 역할을 확정함
    * Playwright 범위를 로그인→상담 시작→내용 저장→요약 확정→상담 완료→새로고침 후 기록 확인과 404·409 오류 검증으로 정리함
    * Screenshot·Trace·Video를 저장하고 초기 실행은 `workers=1`, `retries=0`으로 순차 처리하도록 기준을 정함
  * **이동윤 (AI·RAG)**
    * 기존 1개 모델 RAG를 `WPUJAC104DWH`, `WPUIAC425SNW`, `WPUIAC606SNW` 3개 모델로 확장하고, 정확 판매코드로 모델을 구분하는 검색 구조를 구현함
    * 검색 전 Capability 검증을 추가하여 모델별 조작부 차이를 확인하고, 다른 모델 근거가 섞이거나 잘못 fallback하는 경우를 차단함
    * 3모델 평가 Case 50건을 모두 통과하고 교차 모델 검색 결과가 발생하지 않는지 확인함
    * Backend·Public API의 3모델 계약과 Evidence 연결이 준비될 때까지 신규 모델의 실제 서비스 Runtime 적용은 보류함
  * **양정현 (Mobile Frontend)**
    * 고객 앱 Android 테스트를 보완하고 Physical·Remote Smoke Test와 UI 캡처 작업을 진행함
    * Python 3.13 가상환경을 확인하고 MCP SDK `2.0.0`을 설치하여 관련 의존성을 반영함
    * `MCPServer` 기본 서버와 `health_check` Tool을 구현함
    * MCP Inspector 실행까지 완료했으며, 다음 단계로 `search_official_evidence` Tool과 Retriever 연동을 준비함
  * **김은진 (QA·DevOps)**
    * 정수기 3모델 데이터를 전처리하여 Parent 15건, Child 53건, Evidence Group 43건과 평가 Case 50건의 RAG 인계 자료를 완성함
    * Windows·Linux 간 줄바꿈 차이로 발생한 생성 파일과 Manifest 해시 불일치를 해결함
    * T-020 공식 4개월 케어 주기의 PostgreSQL 독립 QA 71건을 모두 통과시키고 `APPROVE`로 판정함
    * Backend Importer PostgreSQL 독립 QA에서 신규 제품이 승인 전에 DB로 잘못 적재되지 않는지 확인하고, Backend 전체 회귀 `1,302 passed`를 검증함
    * 3모델 RAG 후속 평가 `50/50`과 교차 모델 검색 0건을 확인하되, 실제 Runtime 활성은 Backend·API 계약 확장 전까지 보류함
    * EC2·SSM 상태와 ECR·OIDC 준비 여부를 점검하고 `web`, `backend`, `ai`, `trace-store` 4개 Container 배포 구조를 확정함

## 8월 20일 (목) - 3모델 RAG 데이터 정합화와 Backend·MCP·Handoff·Web E2E 통합 검증

- **진행 업무**
  - 3모델 RAG 데이터에서 `p.36`, `p.41`과 같은 페이지 표기가 문장 분리 과정에서 잘못 처리되는 문제를 수정하고, 영향을 받은 RAG Child 7건과 관련 산출물을 다시 생성하여 데이터 정합성을 보완함
  - 신규 모델 `WPUIAC425SNW`, `WPUIAC606SNW`의 고객·문의 데이터를 확장하기 위해 기존 고객 12명은 유지하고 합성 고객 6명을 추가하여 총 18명, 신규 문의 8건을 추가하여 전체 문의 30건으로 확장하는 데이터안을 설계함
  - 격리 PostgreSQL 환경에서 3모델 데이터의 적재 여부, 제품별 분포와 E2E 흐름을 검증하고, 기존 팀 DB에서 `WPUJAC104DWH` 문서 제목의 공백 불일치를 발견하여 Backend 담당자에게 정합화 필요사항을 전달함
  - Backend는 신규 2개 모델의 격리 E2E Fixture Runtime을 구현하고, AI 상담 전환 결과를 저장하여 Web 상담사가 다시 조회할 수 있는 Handoff 저장·조회 구조를 구축함
  - AI·MCP가 Backend DB를 직접 조회하지 않고 문의·제품 정본을 안전하게 읽을 수 있도록 읽기 전용 내부 Context API를 구현하고 인증·오류 처리 계약을 정리함
  - AI·Harness는 MVP와 3모델 검증 환경을 분리하는 Runtime Profile을 보완하고, MCP stdio Retrieval과 Harness의 실패·Timeout·HITL·Handoff 처리 경로를 연결함
  - AI Handoff를 FastAPI BackgroundTask에서 Backend 내부 API로 실제 전달하도록 연결하고 PII 차단, 멱등성, bounded retry와 OpenTelemetry 추적을 적용함
  - MCP는 기존 공식 Evidence 검색에 제품 Context·문의 Context 조회 Tool을 추가하고, 다른 제품의 Evidence가 섞이지 않는지와 Python 직접 호출·MCP stdio 호출 결과가 동일한지를 검증함
  - Web은 Backend 실제 API 계약에 맞게 상담사 화면의 임시·Fake 데이터를 정리하고, 격리 PostgreSQL 및 실행별 신규 문의를 사용하는 Playwright 실제 Browser E2E를 수행함
  - Django → FastAPI → Harness → Backend Handoff 저장 → 상담 요청 → 상담사 Detail API까지 실제 Socket 기반 수직 E2E를 검증함
  - 3모델 데이터와 Runtime 코드는 준비 범위를 확대했으나 공식 `index_manifest_3model.json`, readonly View 입력 및 최종 Runtime 승인 전까지 신규 2개 모델의 Public Runtime은 HOLD 상태를 유지함

- **담당자별 주요 작업 내용**

  - **윤승혁 (PM·AI)**
    - 3모델 RAG와 Harness를 함께 검증할 수 있는 공동 E2E Gate를 구성하고 MVP Runtime과 3모델 검증 Runtime의 승인 범위를 정합화함
    - MCP stdio Retrieval을 Harness의 실제 실패·Timeout 처리 경로와 연결하여 Tool 실패 시 재시도 또는 상담 전환으로 이어지는 Reliability 흐름을 구성함
    - Harness·HITL·Handoff에 OpenTelemetry 계측을 추가하고 SDK → OTLP/HTTP → 실제 Collector까지 Trace가 전달되는 것을 확인함
    - AI가 생성한 `Consultation Handoff`를 FastAPI BackgroundTask에서 Backend 내부 API로 실제 전달하도록 구현하고 PII 차단·멱등성·bounded retry를 적용함
    - Django → FastAPI → Harness → Backend Handoff 저장 → 상담 요청 → 상담사 Detail API까지 이어지는 실제 Socket 기반 수직 E2E를 완주함
    - 3모델 Runtime 구조는 준비됐지만 공식 3모델 Index와 readonly View 입력 및 최종 승인 전까지 Public Runtime은 HOLD 상태를 유지함

  - **최지용 (Backend·DB)**
    - 신규 정수기 `WPUIAC425SNW`, `WPUIAC606SNW`를 대상으로 실제 검증할 수 있는 격리 E2E Fixture Runtime을 구현함
    - AI가 상담 전환을 요청했을 때 Handoff 정보를 Backend에 저장하고 Web 상담사가 다시 조회할 수 있도록 Model·Migration·권한·API·Service·Test를 구현함
    - AI와 MCP가 Backend 정본을 안전하게 조회할 수 있도록 `GET /api/v1/internal/ai/inquiries/{inquiry_id}/context` 읽기 전용 내부 Context API를 구현함
    - Context API에서 문의 ID, 상태 버전, 제품 판매코드, 제품군, 지원 기능, 고객 문의·증상과 이전 문진 답변을 제공하도록 계약을 구성함
    - 내부 API에 `X-AI-Handoff-Token`, `X-Correlation-ID` 검증을 적용하고 잘못된 인증·없는 문의·잘못된 요청을 각각 403·404·422로 구분하도록 오류 경계를 정리함
    - 기존 Backend·State Machine·Web G4·AI Retrieval 회귀를 별도 환경에서 확인하고 AI·Web·Mobile 담당자에게 실제 Runtime 후속 검증 범위를 인계함
    - 3모델 데이터 검증 과정에서 발견된 기존 JAC104 문서 제목 공백 정합성 문제를 전달받아 Backend 데이터·View 기준의 후속 확인 대상으로 정리함

  - **한예나 (Web Frontend)**
    - Backend 실제 응답에 맞게 상담사 대시보드 API 연결 주소를 수정하고 로그인 사용자의 이름이 `/me` API의 실제 사용자 정보로 표시되도록 변경함
    - 강제로 표시하던 사용자 이름과 고정 사번을 제거하고, 아직 Backend API가 없는 기능은 Fake 데이터로 표시하지 않고 숨김·비활성화·빈 화면으로 처리함
    - 문의 카테고리 필터, 관리자 통계, 상담사 알림, 기사 배정 등 임시 기능을 정리하고 최신 `main` 내용을 개인 브랜치에 반영함
    - Web Test 193건과 Type 검사, 코드 검사, Build를 모두 통과함
    - 공용 DB나 이미 완료된 문의를 재사용하지 않고 별도 테스트 PostgreSQL과 실행별 새로운 `run_id`·문의 데이터를 생성하는 Playwright 환경을 구성함
    - 실제 Browser에서 상담 시작 → 내용 저장 → 요약 확정 → 상담 완료 → 새로고침 및 새 창에서 내용 복구까지 자동으로 검증함
    - 다른 상담사의 문의 또는 존재하지 않는 문의 접근 시 `404`, 오래된 화면 상태로 저장 시 `409`가 발생하며 작성 중이던 내용은 유지되는지 확인함
    - E2E 과정에서 발견한 화면 선택 오류 2건을 수정하고 Playwright 최종 `1 passed, 0 failed`를 확인함
    - 성공 화면 Screenshot과 실행 Trace를 저장하고 검증 후 임시 Backend와 테스트 PostgreSQL을 종료함

  - **이동윤 (AI·RAG)**
    - MVP 단일 모델 환경과 3모델 검증 환경을 안전하게 선택할 수 있도록 Runtime Profile을 구성하고 승인 상태와 Runtime 활성 범위를 보완함
    - Backend에서 전달된 정확 판매코드를 기준으로 MCP 공식 Evidence 검색과 Product Match 검증이 이어지도록 Retrieval Pipeline을 연결함
    - `WPUJAC104DWH`, `WPUIAC425SNW`, `WPUIAC606SNW`의 제품 Identity를 유지하면서 다른 모델의 Evidence가 최종 검색 결과에 섞이지 않도록 검증 경계를 유지함
    - MCP 검색 결과를 기존 AI Pipeline에서 사용할 수 있도록 MCP Retrieval과 Harness 실행 경로를 연결하고 실패·Timeout 상황을 Harness의 후속 처리로 전달하도록 구성함
    - 3모델 Runtime 코드와 검증 환경은 준비했지만 공식 Index·Backend·readonly View의 활성 조건이 모두 충족되기 전까지 신규 2개 모델의 실제 Public Runtime 적용은 HOLD함

  - **양정현 (Mobile·MCP/A2A)**
    - 기존 `search_official_evidence` MCP Tool에 이어 제품 정보를 조회하는 `lookup_product_context`와 고객 문의 정보를 조회하는 `get_inquiry_context` Tool을 구현함
    - 제품 Context에서 정확 판매코드, 제품군, 지원 기능과 Runtime 승인 상태를 구분하여 다른 제품의 정보를 잘못 사용하는 것을 방지하도록 구성함
    - 검색 후보에 다른 제품의 매뉴얼이 포함되더라도 최종 결과에서는 요청 제품과 다른 Evidence가 제거되는 Wrong-model Evidence 차단 테스트를 추가함
    - 기존 Python 함수를 직접 호출한 검색 결과와 실제 MCP stdio를 통한 검색 결과를 비교하여 동일한 결과가 반환되는 것을 검증함
    - 최신 `main`을 개인 브랜치에 병합하면서 발생한 MCP Server와 테스트 충돌을 해결하고 기존 Context Tool과 최신 Retrieval·Harness 변경사항을 함께 유지함
    - MCP Server를 Python 파일로 직접 실행하면서 상대 Import가 실패하는 문제를 확인하고 `python -m app.integrations.mcp.server` Module 실행 방식으로 수정함
    - 최신 `requirements.lock` 기준으로 Python 3.13 가상환경을 동기화하고 `mcp==2.0.0`을 재확인함
    - MCP Unit Test `21 passed`, 실제 MCP stdio Integration Test `2 passed, 1 skipped`를 확인하고 코드 공백·문법 검사까지 통과함
    - 최신 main 병합 및 MCP stdio 호환 작업을 `13e6716` 커밋으로 정리함
    - Backend에서 구현된 내부 Context API 계약을 확인하고, 다음 단계로 Backend Context API → MCP `get_inquiry_context` → 제품별 `search_official_evidence`까지 실제 HTTP 연결을 진행하도록 후속 범위를 정리함
    - A2A Safety Agent PoC에 필요한 SDK 환경은 준비했으며 Backend Context 실제 연결 이후 Agent 간 호출 구현을 이어갈 예정임

  - **김은진 (QA·Data·DevOps)**
    - 3모델 RAG 문장 처리 과정에서 `p.36`, `p.41`과 같은 페이지 번호가 문장 단위로 잘못 분리되는 문제를 확인하고 문장 분절 로직을 수정함
    - 수정 영향 범위에 포함된 RAG Child 7건과 관련 RAG·Evidence·Manifest 산출물을 다시 생성하여 데이터 정합성을 확인함
    - 신규 모델 `WPUIAC425SNW`, `WPUIAC606SNW`를 실제 E2E에서 활용할 수 있도록 고객·문의 데이터 확장안을 작성함
    - 기존 고객 12명은 유지하면서 신규 합성 고객 6명을 추가하여 총 18명으로 구성하고, 신규 문의 8건을 추가하여 전체 고객 문의를 30건으로 확장하는 안을 설계함
    - 신규 모델별 정상·오류·상담 전환 등 후속 E2E에 사용할 수 있도록 고객·문의 데이터의 제품별 분포와 활용 방향을 정리함
    - 격리 PostgreSQL 환경에서 3모델 데이터를 적재하고 모델별 데이터 분포와 E2E 연결 가능 여부를 독립적으로 검증함
    - 검증 과정에서 기존 팀 DB의 JAC104 공식 문서 제목에 공백 불일치가 존재하는 것을 발견하고 Backend 담당자에게 데이터 정합성 확인을 요청함
    - 기존 데이터를 직접 수정하기보다 Backend 정본과 RAG 데이터 사이의 차이를 먼저 공유하여 팀 데이터 계약을 기준으로 후속 수정하도록 경계를 유지함

