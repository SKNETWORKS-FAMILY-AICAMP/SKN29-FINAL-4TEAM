# Web Playwright G4 테스트 결과 보고서

> 작성일: 2026-08-19  
> Web 커밋: `aa366e2`  
> Backend 기준: `49ca9ba`  
> 최종 판정: **부분 성공 — 새 테스트 문의를 받은 뒤 최종 확인 필요**

## 1. 테스트 목적

상담사가 실제 화면에서 문의를 확인하고 상담을 처리하는 과정을 자동으로 검사했다.
실제 고객 정보가 아닌 격리된 합성 테스트 데이터만 사용했다.

## 2. 확인 결과

| 확인 항목 | 결과 | 쉬운 설명 |
|---|---|---|
| Backend 연결 | PASS | 테스트 서버가 정상적으로 응답했다. |
| 로그인 | PASS | 테스트용 상담사 계정으로 로그인했다. |
| 신규 문의 목록·상세 | PASS | 전달받은 문의가 목록과 상세 화면에 표시됐다. |
| 타 상담사 문의 보호 | PASS | 목록에서는 보이지 않고, 상세·처리 요청은 모두 `404`였다. |
| 없는 문의 처리 | PASS | 없는 문의도 `404`로 안전하게 처리됐다. |
| 상담 시작 | PASS | 상담 시작 요청이 정상 처리됐다. |
| 상담 내용 저장 | PASS | 상담 내용 저장 요청이 정상 처리됐다. |
| 오래된 상태값 오류 | PASS | 다른 변경과 충돌하면 `409` 오류가 발생하는 것을 확인했다. |
| 409 이후 입력 유지 | 재확인 필요 | 최신 정보를 다시 불러오는 동안 입력칸 확인 시점이 맞지 않아 테스트가 멈췄다. |
| 상담 완료 | 미실행 | 위 단계에서 중단되어 확인하지 못했다. |
| 새로고침·새 탭 기록 유지 | 미실행 | 상담 완료 후 확인해야 하므로 아직 실행하지 못했다. |

## 3. 기본 Web 검사

- 전체 Web 테스트: `195 passed`
- 보완 후 관련 테스트: `8 passed`
- TypeScript: PASS
- E2E TypeScript: PASS
- ESLint: PASS
- Production Build: PASS

## 4. 중단 이유와 조치

테스트 문의는 현재 `CONSULTATION_IN_PROGRESS / state_version=4` 상태다.
이미 사용한 문의를 처음 상태로 되돌리거나 다시 사용하면 안 되므로 재실행하지 않았다.

Web에서는 `409` 발생 후 최신 상세 조회가 끝날 때까지 기다린 다음 입력 유지 여부를
확인하도록 보완했다. 실패 결과물의 Screenshot·Trace·Video는 Token과 개인정보가
남지 않도록 마스킹·정제했다.

## 5. 다음 작업

1. Backend 담당자에게 새로운 `run_id`의 Fixture JSON을 받는다.
2. 새 문의로 전체 Playwright 테스트를 한 번 실행한다.
3. `상담 완료 → COMPLETION_PENDING → 새로고침 → 새 탭 기록 유지`를 확인한다.

새 Fixture로 마지막 단계까지 통과해야 **최종 PASS**로 보고할 수 있다.
