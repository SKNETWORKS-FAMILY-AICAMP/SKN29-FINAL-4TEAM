# 과업별 AI 모델 및 실행 프로필 설정

tasks:
  symptom_structuring:
    provider: "openai"
    model_name: "gpt-4o-mini"
    temperature: 0.0
    max_tokens: 500

  safety_and_guidance:
    provider: "rule_engine_first"  # 명시적 규칙·근거 허용 판정 후 안내 문구만 LLM 생성
    grounded_guidance:
      provider: "openai"
      model_name: "gpt-4.1-mini"
      temperature: 0.0
      max_tokens: 500

  consultation_summary:
    provider: "openai"
    model_name: "gpt-4o-mini"
    temperature: 0.2
    max_tokens: 800

  technician_report:
    provider: "openai"
    model_name: "gpt-4o-mini"
    temperature: 0.2
    max_tokens: 800
