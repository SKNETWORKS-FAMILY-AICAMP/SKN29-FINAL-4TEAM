services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: watercare
      POSTGRES_USER: watercare
      POSTGRES_PASSWORD: watercare
    ports:
      - "5432:5432"
    volumes:
      - watercare_postgres:/var/lib/postgresql/data
volumes:
  watercare_postgres:
