description: HTTP 403 공통 오류 응답
headers:
  X-Correlation-ID:
    $ref: ../headers/CorrelationId.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/common/ApiResponse.yaml
    examples:
      forbidden:
        externalValue: ../../examples/errors/forbidden.json
