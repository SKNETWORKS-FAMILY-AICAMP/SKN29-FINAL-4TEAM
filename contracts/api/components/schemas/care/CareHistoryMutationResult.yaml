title: CareHistoryMutationResult
description: T-019 케어 이력 등록 결과와 멱등 Replay 여부
x-contract-status: CONFIRMED
allOf:
  - $ref: ./CareHistoryItem.yaml
  - type: object
    required: [idempotent_replay]
    properties:
      idempotent_replay:
        type: boolean
