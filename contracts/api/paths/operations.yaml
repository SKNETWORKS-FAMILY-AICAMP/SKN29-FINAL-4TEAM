/consultant/dashboard:
  get:
    operationId: getConsultantDashboard
    summary: 상담사 합성 Dashboard 조회
    description: |
      인증 상담사에게 로컬 E2E용 합성 공지·직원·방문기사·담당 문의
      projection을 반환한다. 응답의 technicians는 활성 합성 TECHNICIAN
      계정만 포함하며, technicians[].user_id는 방문 일정 저장 요청의
      synthetic_technician_id로 사용할 수 있다. 실제 기사 배정·예약
      시스템과는 연동하지 않는다. Query parameter는 허용하지 않는다.
    x-contract-status: CONFIRMED
    x-runtime-status: IMPLEMENTED
    x-requirements: [FR-026, CR-005]
    x-permission-scope: CONSULTANT_SYNTHETIC_DASHBOARD
    security:
      - BearerAuth: []
    parameters:
      - $ref: ../components/parameters/CorrelationId.yaml
    responses:
      "200":
        description: 담당 합성 문의와 합성 Dashboard reference data
        headers:
          X-Correlation-ID:
            $ref: ../components/headers/CorrelationId.yaml
        content:
          application/json:
            schema:
              allOf:
                - $ref: ../components/schemas/common/ApiResponse.yaml
                - type: object
                  properties:
                    data:
                      $ref: ../components/schemas/operations/ConsultantDashboardData.yaml
      "401":
        $ref: ../components/responses/Unauthorized.yaml
      "403":
        $ref: ../components/responses/Forbidden.yaml
      "422":
        $ref: ../components/responses/UnprocessableEntity.yaml
      "500":
        $ref: ../components/responses/InternalServerError.yaml

/consultant/notices/{notice_id}:
  get:
    operationId: getConsultantDashboardNotice
    summary: 상담사 합성 공지 상세 조회
    description: |
      Dashboard에 공개된 합성 공지 한 건을 notice_id로 조회한다.
      미게시·미래 게시·존재하지 않는 공지는 모두 404로 숨긴다.
      Query parameter는 허용하지 않는다.
    x-contract-status: CONFIRMED
    x-runtime-status: IMPLEMENTED
    x-requirements: [FR-026, CR-005]
    x-permission-scope: CONSULTANT_SYNTHETIC_DASHBOARD_NOTICE
    security:
      - BearerAuth: []
    parameters:
      - $ref: ../components/parameters/CorrelationId.yaml
      - name: notice_id
        in: path
        required: true
        description: 합성 공지 공개 UUID
        schema:
          type: string
          format: uuid
    responses:
      "200":
        description: 게시 중인 합성 공지 상세
        headers:
          X-Correlation-ID:
            $ref: ../components/headers/CorrelationId.yaml
        content:
          application/json:
            schema:
              allOf:
                - $ref: ../components/schemas/common/ApiResponse.yaml
                - type: object
                  properties:
                    data:
                      $ref: ../components/schemas/operations/ConsultantDashboardData.yaml#/$defs/notice
      "401":
        $ref: ../components/responses/Unauthorized.yaml
      "403":
        $ref: ../components/responses/Forbidden.yaml
      "404":
        $ref: ../components/responses/NotFound.yaml
      "422":
        $ref: ../components/responses/UnprocessableEntity.yaml
      "500":
        $ref: ../components/responses/InternalServerError.yaml
